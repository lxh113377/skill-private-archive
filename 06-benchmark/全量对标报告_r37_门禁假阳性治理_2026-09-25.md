# 全量对标报告 r37 — 第十三维「门禁可信度（假阳性治理）」

日期：2026-09-25（第 37 轮）｜触发：同一指令第七次逐字下达 ⇒ 按 behavior_core #23 **整跑一轮**（非核验、非跳过）
本轮主轴：**把「门禁自己会错」做成可测维度**，并清偿本项目连续 7 轮 `savepoint` 被拒的真正根因。

---

## 1. 对比总览表

| # | 维度 | 我方（本轮实测后） | 对标项目（对应我方哪一段） | 档位 |
|---|---|---|---|---|
| 1 | 功能模块覆盖范围 | 范围裁定→扫描→精读→审计→计划→执行→门禁→台账→CI，共 9 段 | `pre-commit`（钩子编排 + 自动修复 + 缓存）；`anthropics/skills`（技能面） | 我方覆盖段数不落下风（详见 `comparison.md` §1–13） |
| 2 | 技术架构与实现方式 | 判据=单源表 `GATES` + 三态（PASS/FAIL/UNVERIFIED）+ 台账 | `pre-commit` 的 exit-code 契约（0/1/2 = pass/fail/error）与 `--hook-stage` 分相 | 同构；我方多出 UNVERIFIED |
| 3 | 性能表现 | 8 门合计 ~2.1s（均值 ~265ms/门，`run_gates.py` 自带计时行实测） | 对手无同类可测件 | 领先性 ❌未实测（无可比物） |
| 4 | 可扩展性 | 新门 = 表里加一条 dict，聚合/台账/CI 自动继承 | `pre-commit` 插件式 repo hook | 相当 |
| **5** | **维护状态 → 本轮聚焦** | **噪声门禁连续 7 轮红，全部为假阳性，且工具建议是破坏性的（迁 `_trash`）** | `anthropics/skills` run 分布 25% success / **75% failure** 停摆 6 周无人报警；`superpowers` 近 50 run **82% failure**；`spec-kit` 50% run 停在 `action_required` | **平手偏我方**：对手是「红了没人管」，我方是「红是错的」——两种失效同源：**判据失效无人裁决** |
| 6 | 文档完善程度 | `noise_lint.py` 内注释含根因/验证/反例；版本行 + 版本历史同步至 **V3.54.1** | `pre-commit` docs 有「hook 失败如何 debug」专章 | 相当 |
| 7 | 适用场景 | 单机多会话共享受管根（八端 junction）——**判据误报会阻断全员**，比开源仓库更敏感 | 对手面向公开贡献流，误报只挡 PR | 我方风险面更窄更高 |

---

## 2. 逐项差距说明（本轮新发现）

- **D21 · 假阳性没有任何判据**。我方有 8 道门，但**没有一道回答「这道门自己是不是在报错」**。
  实测证据：`handoff.py noise` 报的 5 条 VIOL 中，
  ① `焚诀\CONTRIBUTING.md` / `LICENSE.md` / `SECURITY.md` —— `git ls-files` 逐条实测 **三件全部 tracked**，
  由归属方提交 `114b5b0 docs(7-J): 根部社区件回正`；工具却判违规并建议 **迁往 `_trash`**（= 搬走已入库治理件）。
  ② `.git/push-failure.log`（GM 与 焚诀 各一份）—— 由**仓内工具 `git_autopush.py` 自己写**的运行态日志，
  位置在 `.git` 内，`.gitignore` 天然管不到 ⇒ **只要发生过一次推送失败，本门永久红**（结构性自锁）。
- **D22 · 白名单逐文件登记 = 每次新增都要挡一次全员**。R282 当天为 `feedback-user-command-supreme.md`
  逐名登记，同日第二件 `feedback-edit-long-line-ledger.md` 复现同因才收编为模式；
  今日第三件 `reference-github-pages-blocked.md` **又挡一次** —— 放行面只覆盖 `feedback-` 前缀，
  而 auto-memory 的类型前缀有 4 种（user/feedback/project/reference）。
- **D23 · 「改数据凑判据」的诱惑就在旁边**。让 savepoint 变绿最快的三条路（迁 `_trash` / 删 `.log` /
  往 GM 根再逐名登记一条）全都是错的，且前两条属破坏性。R263 在本轮是唯一有用的护栏。
- **D24 · 自家硬编码「当前值」会陈旧**（顺手实测）：`run_gates.py` 的 `covers` 串写死
  「实测 10 份 / 8 pattern」，实跑已变成 **11 份 / 9 pattern** —— 违反本仓自己的约定
  （记忆里写当前值必须附取值命令）。

### 反例（判据没有放松，桩内实测）
放行「Git 已跟踪根部文件」后仍然红：未跟踪随手件 `scratch.md`、
已跟踪但命名即散落的 `notes_old`、已跟踪**目录** `docs_dir`、非仓库根的任意项（`git_tracked` 异常返回 False）、
二级扫描 `scripts/handoff.py.bak_20260815`。放行面**不外溢**：同一 `reference-*.md` 在焚诀根仍判 VIOL。

---

## 3. 改进建议清单（含优先级）

| 级别 | 编号 | 建议 | 本轮状态 |
|---|---|---|---|
| **高** | **H-1** | 给噪声门建「假阳性裁决」机制：strict allowlist 判定前先看 **Git 是否跟踪**；已跟踪 = 归属方有意决策，不该建议搬走 | ✅ 已落地（`git_tracked()`，三重收窄） |
| **高** | **H-2** | 二级深扫排除 `.git` 运行态目录（不可 gitignore 的位置 ⇒ 不能靠豁免条款解） | ✅ 已落地（`name == ".git"` 跳过，一级照判） |
| **高** | **H-3′** | auto-memory 根件放行面从 `feedback-*` 扩到实测存在的 `feedback-*`/`reference-*`，**不预先放行无实物的前缀** | ✅ 已落地（R283 续，桩 t12–t14） |
| **高** | **H-4** | 把这套反假阳性判据**注册成常驻门**，红了就说明有人在无分类情况下放行了 | ✅ 已落地（`noise_tracked_stub` 入 `GATES`，门数 7→8） |
| 中 | M-1 | `--on-write` 挂进 `rule_editor` 写前链 —— **先量近 20 次真实编辑的误报率**，绝不默认 fail-closed（r25 用户否决过拦任务的闸门） | ⏳ 下轮首推 |
| 中 | M-2 | 契约不变式 `set(baseline.metrics) == set(ratchet_gate.METRIC_NAMES)` + 「少一项必红」夹具 | ⏳ 待办（挂账 3 轮） |
| 中 | M-3 | 对手侧同尺对照：给 `anthropics/skills` / `superpowers` 的 CI 红**逐条分类**（真缺陷 vs 假阳性），才能宣称本维度领先 | ❌ 未实测 ⇒ 本维度结论上限只到「已量化」 |
| 低 | L-4 | `rule_conflict_scan.DEFAULT_FILES` 死条目 `D:\global_memory\AGENTS.md` | ⏳ 归属方待办 |
| 低 | L-5 | `run_gates.py` `covers` 串去掉硬编码计数，改指取值命令 | ✅ 本轮顺手落地 |

---

## 4. 可操作实施路径（本轮实际执行序，全部带复现命令）

1. **先取证再定性**（不许凭报错文本判"散落"）：
   `git -C C:\Users\37533\Desktop\workspace\焚诀 ls-files --error-unmatch CONTRIBUTING.md` → rc=0（tracked）
   `git -C 焚诀 log --oneline -- CONTRIBUTING.md LICENSE.md SECURITY.md` → `114b5b0`（有意提交）
   `grep -rl "push-failure.log" D:\global_skills\A-project-handoff\scripts` → `git_autopush.py`（工具自写）
2. **TDD 先写桩并看红**：`python 05-exec/r37_noise_tracked_stub.py` → 修前 **8 项 FAIL**（含 `t1` 输出
   `violation 根部意外项（不在核心 allowlist）`，直接证伪「这是散落物」）。
3. **改判据不改数据**：`rule_editor.py replace --file A-project-handoff/scripts/noise_lint.py --patch …`
   （自动备份 `A-project-handoff.scripts.noise_lint.py.20260925_1000*.bak`，`--dry-run` 先验锚点唯一）。
4. **复验三层**：层a 桩 **16/16**（含 5 条保牙反例）→ 层b 真机
   `python D:\global_skills\A-project-handoff\scripts\handoff.py noise` → **`[GATE:noise-pass]`**
   （global_skills 168/0、global_memory 67/0、archive 6/0、焚诀 41/0）→ 层c
   `handoff.py savepoint` → **`🎯 本次对话已安全落盘`**（**AC-OBS-05 连红 7 轮后首次转绿**）。
5. **常驻化**：新门入 `GATES`（`portable: False` + `not_portable_reason` 写清需真实受管根与 temp git 仓），
   本仓 `run_gates.py` 复跑 → `[GATES:PASS] PASS=8`。
6. **文档同步**：受管根版本行 3.54.0 → **3.54.1** + `references/version-history.md` 补 R283 条目（含验证与修桩留痕）。

### 本轮两次「自己写的桩是错的」（R220 抽验抓回，必须留痕）
- 首版把 `scratch.md` 写在 `git add -A` **之前** ⇒ 「未跟踪」断言其实全程跟踪着，t2/t6/t10 是**假通过**；
- 首版以为 `_bak` 可在文件名中间命中 ⇒ `foo_bak.txt` 实际不匹配（`STRAY_NAME` **行尾锚定**），t3 也是假反例。
⇒ 修桩后重跑才拿到「修前 8 红 / 修后 16 绿」的真实对照。**判据的桩自己也要有反例，否则它只是在复读实现。**

---

## 5. 结论与不学清单

- 本维度我方**不宣称领先**（M-3 未做）。可宣称的是：本项目现已具备「**发现判据错 → 取证 → 改判据 → 桩锁死边界**」
  这条闭环，并且第一次把它用在了**解除自己的阻塞**上，而不是为了让红灯消失。
- **不学清单 X-9（本轮立）**：不得为了「让门禁变绿」而迁走 / 删除 / 改名任何 **Git 已跟踪**的根部件。
  变绿的正确路径只有两条 —— 归属方登记清单，或判据承认「已跟踪 = 有意决策」。
- 下一步：M-1（写时冲突检查先量误报率）→ M-2（棘轮与基线契约的集合相等不变式）→ M-3（对手侧同尺分类）。
