# 全量对标报告 r44 — W-12/W-13 落地：让「裁决清单」本身成为判据

日期：2026-09-25（第 44 轮）｜触发：同一指令第十四次逐字下达 ⇒ 按 #23 整跑一轮；延续 W-5 **不开新维度**。
本轮只干一件事：把 r42、r43 连续两轮点名却未落地的 **W-12（裁决清单完整性）** 与 **W-13（标题锚点定位）** 落地，并立刻拿真实数据跑。

---

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖范围 | 6 态分类 + 完整性判据 + 锚点定位；仍缺 REPEAT 独立指标（W-14） | `github/spec-kit` label 驱动流转（27 workflow） | 覆盖面对齐，自动化面落后 |
| 2 | 技术架构与实现方式 | `adjudication_gap()` 纯函数 + 上轮证据件互查，last-wins 语义与本仓台账统一 | `pre-commit` 单配置源 | 相当 |
| 3 | 性能表现 | 账龄尺 ~2.9s（含 15 条 `git log -S` 复核 + 上轮清单回读）；10 道门 ~9s | 无可比物 | ❌未跨机实测 |
| 4 | 可扩展性 | 新增语义 = 1 函数 + 1 组夹具（t40–t46 七例，39/39 绿） | — | 良好 |
| **5** | **维护状态（本轮聚焦）** | **OVERDUE 16 → 2 → 1**；`DECIDED 43 / DEFERRED 1 / REPEAT 0`；余 1 条真实未做 ⇒ **棘轮如实留红** | `anthropics/skills` 1290 / `superpowers` 401 / `spec-kit` 287 / `agent-skills` 118 / `pre-commit` 25 open；仅 spec-kit 有到期自动化且**到期即关闭** | 到期与延期显形面领先 5 家；**本轮起「清偿率」首次可量化（14/15）** |
| 6 | 文档完善程度 | 每条裁决带取值命令；D56–D59 逐条留证 | 对手 issue 模板 | 相当 |
| 7 | 适用场景 | 锚点定位专为共享卷并发重写设计（r43 D55 的直接后果） | 对手无此约束 | 我方独有风险，已闭环为判据 |

---

## 2. 逐项差距说明（全部实测）

- **D56 · 完整性判据首跑就打我脸**：`状态 INCOMPLETE ｜ 上轮 OVERDUE 15 → 本轮仍未处理 15`。
  根因是我 r44 的次序倒了 —— 先写判据代码、还没回头还 r43 留下的债，`W-12` 却已"完成"。
  若没有这条判据，我会带着一个"实现完了"的判据和**没动的账面**结束本轮。
- **D57 · 「只裁第一行就宣称完成」被夹具堵死**：t46 用重复登记的同一标题（主壳 + 分卷各一份）断言 missing 必须 = 2；
  t41 断言多命中时调用方不得默认取第一行。
- **D58 · 已到期挂账不得冒充已裁**：t45 断言「最后一次标记是 r43 挂账、本轮 r44」⇒ 仍 missing；
  t43 断言未来挂账（r46）不算 missing 但会进 DEFERRED/REPEAT 列。
- **D59 · 「降级」是新的免检通道**：本轮 11 条靠降级把 OVERDUE 压掉；若降级项没有独立指标，就再没人追它们
  ⇒ 立 **W-14**（REPEAT 第 9 指标）与 **X-18**。
- **D60 · 我自己的测试会互相矛盾**：首版 t44/t45 与 t43 对「已到期挂账是否算 missing」三者不一致。
  按 TDD 正解是**先定语义再实现**，而不是把断言改到能过 —— 四条判定规则最终写进函数注释与本报告。

### 反例（判据没有放松）
t42：已勾选 `- [x]` 条目不参与定位（禁往别人已闭环的条目上写裁决）；
t45：旧挂账到期不豁免；t46：多命中逐行计；`coverage_check` 在历史证据件不足 2 份时返回
`UNVERIFIED` 并打印 `[ADJUDICATION:UNVERIFIED]`，**不得当作「已裁完」**（R247）。

---

## 3. 改进建议清单（含优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| 高 | W-13 | 待办按**标题锚点**定位（`find_item_line`） | ✅ 落地（t40/t41/t42/t46） |
| 高 | W-12 | **裁决清单完整性**判据 + main 接线（`adjudication_gap`/`coverage_check`） | ✅ 落地（t43/t44/t45） |
| 高 | W-14 | 给 `REPEAT` 立棘轮**第 9 指标**（防"降级"变新免检通道） | ⏳ 下轮首推 |
| 中 | W-15 | 完整性结果写进趋势台账行 + 契约取值域（让 CI 看到斜率） | ⏳ 待办 |
| 中 | W-1 | 12 条 UNDATED 定年 | ⏳ 挂账 r47（到期即按 REPEAT 强制升级） |
| 低 | L-6 | 编号命名空间收敛（X/M/H/W 已三次撞车） | ⏳ P2 看守 |

---

## 4. 可操作实施路径（本轮实际序，全部带复现命令）

1. **RED**：`python 05-exec/r39_debt_ledger_fixtures.py` → `AttributeError: module has no attribute 'find_item_line'`
   （红在正确位置，非环境错）。
2. **语义先行**：发现自写 t44/t45 与 t43 矛盾 ⇒ 先定四条 missing 判定规则（写入函数 docstring），再实现。
3. **GREEN**：Edit 工具落 `find_item_line` / `adjudication_gap` / `coverage_check` + `main` 接线 → **39/39**。
4. **立刻跑真实数据**：`python 05-exec/r38_debt_aging.py --json 06-benchmark/debt_aging_r44_2026-09-25.json`
   → 首跑 `INCOMPLETE 15` → 逐条真裁决（11 降级看守 + 2 执行完毕 + 1 首次延期 r47）+ 带证据补勾
   r40 已交付却漏勾的 W-4 ⇒ `OVERDUE 16 → 2 → 1`、`DECIDED 43`、`REPEAT 0`。
5. **诚实性检查**：`python 05-exec/ratchet_gate.py` → `[RATCHET:FAIL] overdue=1 > 基线 0` **保留**；
   全程未使用 `--raise-baseline`，也未把未做项标成完成。

---

## 5. 结论与不学清单

- 本轮把「还了多少债」从**我记得**变成**机器算**；并且第一次由判据替我承认「代码写完但债没还」。
- **X-18（本轮立）**：判据/工具写完不当场跑真实数据，就不得宣布有效 —— 本轮 W-12 首跑即 INCOMPLETE 为实证。
- **X-17 沿用**（r43 立）：REPEAT 项禁止再挂新到期日，必须三选一（执行 / 降级看守带指标 / 作废带证据）。
- 下一步：W-14（REPEAT 指标）→ W-15（完整性进契约）→ W-1（12 条定年，r47 到期）。
