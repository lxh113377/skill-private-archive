# 全量对标报告 r45 — W-14 反降级免检 + W-9 真因归位（我按行号写标记，写到别处了）

日期：2026-09-25（第 45 轮）｜触发：同一指令第十五次逐字下达 ⇒ 按 #23 整跑一轮；W-5 约束下不开新维度。
本轮两件：**给「降级」加看守**（W-14），以及**把 r40 起挂了 5 轮的 W-9 鬼影项查到真因**。

---

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖范围 | 债务 6 态 + 9 指标看守（overdue / deferred / **repeat** 三态独立） | `spec-kit` 27 workflow 标签流转 | 覆盖面对齐；**降级/反复改期度量为我方独有** |
| 2 | 技术架构与实现方式 | 三态共用 `_debt_class_state` 单一自洽门；台账行新增 `coverage_*` 三列 | `pre-commit` 单配置源 | 相当 |
| 3 | 性能表现 | 账龄尺 ~3.1s；10 门 ~9.4s（新增 6 例夹具） | 无可比物 | ❌未跨机实测 |
| 4 | 可扩展性 | 新指标 = 1 函数 + 1 处 `METRIC_NAMES` + 1 处 `COMPUTE`，由 t50–t54 锁死 | — | 良好 |
| **5** | **维护状态（聚焦）** | `OVERDUE 3 → 0`、`DECIDED 46`、`REPEAT 0`；W-12 完整性**首报 OK** | `anthropics/skills` 1290、`superpowers` 401、`spec-kit` 287、`agent-skills` 118、`pre-commit` 25 open | 五家均无「反复改期 / 降级」度量 ⇒ 此面本轮起领先 |
| 6 | 文档完善程度 | W-15 只做一半并写明**为什么不进契约**（会让历史行全体违规且不可回写） | 对手 changelog | 相当（差异在自我交代） |
| 7 | 适用场景 | 锚点定位缺失的后果被实测抓出：**标记写到别的行**（W-9 真因） | 对手无并发共享卷问题 | 我方独有，已立 W-16 |

---

## 2. 逐项差距说明（全部实测）

- **D59（承接 r44）· 降级是新免检通道**：r44 那轮 OVERDUE 16→1 里有 11 条是「降级为长期看守」。
  降级是合法终局裁决，但**没有独立指标时，全降级比真还债更便宜** ⇒ 立 `repeat_debt_items`（第 9 指标，读 `by_class.REPEAT`，
  与 overdue/deferred 同一自洽门：脏证据一律 None）。
- **D61 · 恒真断言会复发**：本轮新写的 t53 又出现 `or True`（与 r39 t18 同族）。删除后 45/45 才有意义
  —— 说明「写完测试要扫一遍是否恒真」必须成为固定动作，而不是靠一次觉悟。
- **D62 · W-9 不是鬼影，是我写错了地方**：该 H-3″ 行实测**裁决标记数 = 0**，
  即 r40/r41/r43 三轮「给它打标记」都没打在这行上 —— 因为脚本**按行号索引写入**，而 07 在 savepoint 拆卷 / 并发重写下行号会漂。
  W-13 已提供 `find_item_line()`，但我在临时脚本里没用它 ⇒ 立 **W-16**（写记忆标记必须走锚点函数，命中数 != 1 即拒写）。
- **D63 · 契约不能天真加必填列**：把 `coverage_*` 加进 `row_required` 会让 r38–r44 已入库的台账行全体违规，
  而行是 append-only 不可回写 ⇒ 与 r42 D36「代际豁免」同族。本轮**只做台账列、不做契约列**，并立 **W-17**（按 `ts` 分代必填）。
- **D64 · 到期追讨第 3 次真响**：轮号推进使 r41 写的 3 条 `挂账至 r44` 集体到期，机制稳定工作（无需人工想起）。

---

## 3. 改进建议清单（含优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| 高 | W-14 | `repeat_debt_items` 第 9 指标（防降级变免检） | ✅ 落地，t50–t54 锁死 |
| 高 | W-16 | 写记忆标记禁行号、必须用 `find_item_line`（命中 != 1 拒写） | ⏳ 约定已立，下轮把临时脚本模式固化为工具 |
| 中 | W-15 | 完整性结果进趋势台账 | ✅ 三列已进台账；契约列未做（见 W-17） |
| 中 | W-17 | 契约按 `ts` 分代必填，使新列可强制而不回溯误杀历史行 | ⏳ 待办 |
| 中 | W-1 | 12 条 UNDATED 定年（挂账 r47） | ⏳ 到期即 REPEAT 升级 |
| 低 | L-6 | 编号命名空间收敛 | ⏳ P2 看守 |

---

## 4. 可操作实施路径（本轮实际序）

1. 先查 W-9：`grep -rn "H-3″（r36 补记" memory/07-next-steps*.md` → 该行裁决标记数 = 0 ⇒ 真因是写入位置错（D62）。
2. **RED**：`python 05-exec/r39_debt_ledger_fixtures.py` → t50–t55 六例红（`repeat_debt_items` 不存在 / 台账无 coverage 列）。
3. 自查发现 t53 是恒真断言 → 改写为真实三态断言 + 补脏证据前置写入。
4. **GREEN**：`ratchet_gate.py` 加第 9 指标；`r38_debt_aging.py::append_ledger` 加 `coverage_*` 三列 → **45/45**。
5. 基线核定：`repeat_debt_items=0`（实测现值，未削减）；`overdue=0` 维持。
6. 到期项按 X-17 处置：3 条降级看守并写明外部条件 ⇒ `OVERDUE 3 → 0`，`[ADJUDICATION] OK ｜ 上轮 1 条已处理 0 缺`。
7. 复跑：`[CONTRACT:PASS]`、`[CTRL:CLEAN]`、`[GATE:evolution-pass]`、夹具 45/45；`[GATES]` 见 §5。

---

## 5. 结论与不学清单

- 本轮把「账面好看」的最后一道退路（批量降级）也纳入只降不升的看守面；并且用一条挂了 5 轮的旧账，
  实测证明**工具存在 ≠ 我被工具约束**（我在临时脚本里绕过了自己的 `find_item_line`）。
- **X-19（本轮立）**：禁止用行号索引写共享记忆卷的标记；只用锚点函数，命中数 != 1 即拒写。
- 诚实边界：`[GATES]` 本轮最后一次跑除 `gate_run_freshness` 外全绿（该门如实转述上一次的红，再跑一次即清），
  契约未纳入 coverage 列一事已写进本报告与 W-17，**未记为「已完成」**。
