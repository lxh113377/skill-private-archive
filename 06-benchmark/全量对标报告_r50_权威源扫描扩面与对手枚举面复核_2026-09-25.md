# 全量对标报告 r50 — 两件事：把"输入面自证"推到自己的判据上（发现 16 件权威分卷从未进面），以及用同一把尺复核**对手取证本身**的枚举面（推翻 r31/r32 的口径）

日期：2026-09-25 ｜ 轮次：第 20 轮（同一指令第二十次逐字触发 ⇒ 整跑一轮，不降级，X-3）
本轮定位：**不开新维度**（W-5 约束）。增量 = 第 13 维（判据可信度）+ 第 8/9 维（CI 面）的**口径校正**。

---

## 1. 对比总览表（14 维；只更新本轮有实测变化的行）

| # | 维度 | 对手实况（本轮四路取值，见证据件） | 我方 r50 实况 | 差距判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖 | 五家为 skill/模板集合，无自校验层 | 14 维 + 13 道常驻门 + 五面自证 | 领先（不同赛道） |
| 2 | 技术架构 | 静态 md + 少量脚本 | 判据/证据/台账/契约四层，判据纯函数化 | 领先 |
| 3 | 性能 | 无度量 | 注入面棘轮只降不升 | 平（对手不做） |
| 4 | 可扩展性 | 加文件即扩展 | 加判据须同时交夹具（R-ENUM） | 有意收紧 |
| 5 | 维护状态 | spec-kit 活跃；superpowers/anthropics **无自有 CI**（见 D-80） | 每轮 commit+annotated tag+push | 领先 |
| 6 | 文档完善度 | spec-kit 把 **8 份 .md 放在 `.github/workflows/` 里**（实测差集，D-82） | 严格 allowlist + `_trash` 迁移制 | **领先（头部仓库亦有目录卫生敞口）** |
| 7 | 适用场景 | 给人用的 skill 包 | agent 自证工作流 | 不同赛道 |
| 8 | CI 存在性 | 校正后：spec-kit **18**（原报 27）、AAS **2**（原报 5）、pre-commit **2**、superpowers **0**、anthropics **0** | 本机 13 门，无 CI | 落后（有意，X-1） |
| 9 | CI 有效性 | **r32 结论需限定**：所测 superpowers 82%/anthropics 75% failure 属平台 Copilot 件，非仓库测试 CI | freshness 门 + 台账 origin 分域 | 领先（且我方口径已修） |
| 10 | 发布与治理 | 满配 5/5 仅 spec-kit、AAS | tag 每轮、无 LICENSE（X-2） | 差距在册 |
| 11 | 可移植性 | 天然可移植 | 13 门中 11 门 portable | 落后（在册） |
| 12 | 引用可解析性 | 未测 | 死输入不变式 + 26/26 存在 | 领先 |
| 13 | 判据输入面自证 | 零家自证；pre-commit 把面定义在**别的仓库**的复用 workflow 里（D-81） | 五面（账龄尺）+ 覆盖面（冲突判据）+ 读取面对账（棘轮） | 由"部分自证"扩到"逐判据自证" |
| 14 | 债务到期治理 | 仅 spec-kit 有 `stale.yml`（结论仍成立，它是仓库自有件） | 到期追讨本轮再抓 1 条（W-17） | 领先机制 |

## 2. 逐项差距说明（全部带取值命令）

**D-79（P0，第 13 维）：手抄清单当分母 ⇒ 冲突判据对 16 件权威分卷全盲。**
`05-exec/rule_conflict_scan.py` 的 `DEFAULT_FILES` 手抄 10 件；实测 `D:/global_memory/core/*.md` 有 18 件带可配对正负规则 —— 其中 `behavior_core_rules_p1..p8`（含其分卷）正是 22 锚点规则的**正文所在**。
扩面后实测：扫描面 10→**26 件**、极性规则 116→**188**、自述冲突 6→**11**、互斥候选 6→**21**。
⇒ 此前所有 `[CONFLICT:CLEAN]` 类绿灯只是**在 38% 的面上算出的结论**。这是 r49 的 X-24（面内自洽≠全面）在第二个判据上的实证。
→ 落地 `discover_authority_candidates()`（按形状发现：同目录 `*.md` 且 pos≥1 且 neg≥1）+ `face_authority_coverage()`（漏扫即点名）+ `effective_files()`（手抄 ∪ 发现），并加**对照例 c6b**「只用手抄清单时同一断言必须判红」防扩面空转。

**D-80（P0，第 8/9 维）：对手取证自身的枚举面搞错了，我引用了平台件当仓库 CI。**
两条独立途径对账同一件事，结论互相矛盾：`/actions/workflows` 报 superpowers 2 个 workflow，而 `/contents/.github/workflows` 返回 **404（目录不存在）**；按 `.path` 前缀分组后看清 —— 那 2 个都是 `dynamic/*`（Copilot 平台件）。五家实测：spec-kit 27=18 仓库内+9 dynamic、AAS 5=2+3、pre-commit 3=2+1、superpowers 0+2、anthropics 0+2。
⇒ **r31/r32 的"CI 存在性/有效性"结论有口径错**（数值不是造假，是取错了面）：所谓"anthropics 停摆 6 周无人报警、superpowers 82% failure"描述的是 GitHub 平台在其仓库里跑的 Copilot 任务，**不是该仓库自己的测试 CI**；真实结论应是"这两个头部 skill 仓**根本没有自有 CI**"。
→ 证据件 `06-benchmark/opponents_workflow_face_r50_2026-09-25.json` 逐仓给四路取值 + 差额归因；r31/r32 的历史条目不删，只在 `comparison.md` 加校正注（R241）。r38 关于 spec-kit `stale.yml` 的结论**复核后仍成立**（它是仓库自有件），只是总数应为 18。

**D-81（P1，第 13 维）：判据的取值面定义在别的仓库里。**
pre-commit 的 2 个仓库内 workflow 都是 `uses: asottile/workflows/.github/workflows/tox.yml@v1.8.1` —— 它的输入面（跑哪些测试、取哪些文件）在**第三方仓库的可复用工作流**里。
→ 我方同族形态：`run_gates.py` 里有 2 门读 `D:\global_skills` 的桩。已用 `covers` + `not_portable_reason` 字段把"面在哪、换机怎么办"写死在注册表里（本轮补 `authority_face_fixtures` 也走同一声明），未再新增无声明的外部面。

**D-82（P2，第 6 维）：头部仓库也有目录卫生敞口。**
spec-kit 磁盘 26 件 vs 注册 18 件，差额 8 件全部是躺在 `.github/workflows/` 里的 `.md`（`RELEASE-PROCESS.md`、`bug-{assess,fix,test}.md`、`add-community-{bundle,extension,preset}.md`、`feature-assess.md`）。
→ 不据此改任何东西（不是我的根），但**反向印证**我方 `noise_lint` strict allowlist + "只迁 `_trash` 不删"的价值：这类散落靠约定挡不住，得靠判据。

**D-83（P0·转裁定，第 14 维）：扩面暴露的权威源内部冲突。**
21 组互斥候选里的典型：`core/behavior_core_appendix.md:22`「必须含下一步建议」vs `core/behavior_core_rules_p3.md:13`「禁以状态报告/下一步建议形式结束回合」；另有三处版本计数口径互斥（V41/22 ↔ `core/MEMORY.md` V37/17 ↔ appendix V36/18，规则文本自己承认"未修"）。
→ 本项目**不改受管根规则正文**（红线 + R241 + r25 否决），登记为 **W-27** 转归属会话/用户裁定；裁定前 21 组候选按已知敞口看守，**不得为让扫描变绿而删规则或缩回面**。

## 3. 改进建议清单（含优先级）与本轮处置

| 编号 | 优先级 | 建议 | 本轮处置 |
|---|---|---|---|
| W-25 | P0 | 输入面自证推广到其余判据 | **执行完毕**：冲突判据覆盖面 + 棘轮读取面对账；契约扫描的 pattern 零命中 r33 已有 ⇒ 只补防回退断言 c6/k6，不重复建 |
| W-26 | P1 | 混合行尾根因收口 | **执行完毕（限定）**：`git show r43..r49` 实测仓库内全纯 LF ⇒ 成因是写卷未传 `newline=""`（非并行会话）；06 加**归因校正注**，写入侧约定立 **X-25** |
| W-27 | P0·转裁定 | 扩面暴露的 21 组互斥候选 + 11 条自述冲突 | 已登记，不改受管根正文（红线） |
| **W-28** | **P0（下轮首推）** | 把「枚举源双路对账」做成判据：凡取证件引用外部/对手数字，必须带 `retrieval`（≥2 条独立取值命令）+ 差额归因，缺即由 `baseline_contract_scan` 判红 | 未动（本轮产能给了自家判据扩面与对手口径复核） |
| **W-29** | P1 | 复核其余历史对手数字（open issues 计数、星标、r38 的 287/1290/401/118/25）是否也取自错面 | 未动，登记在册 |
| W-17 | P1 | 台账 coverage 三列分代必填 | **执行完毕**：r48 已落，本轮把 `row_required_from` 扩为多代列表以容纳第 2 代 `face_*` 列（到期追讨抓出我漏裁此条，账龄 5 轮） |
| W-21 | P1 | unowned 升第 10 指标 | 维持 r49 的**作废**裁定（恒 0 + 可刷） |

## 4. 可操作实施路径（r51 直接照做）

```bash
export PYTHONPYCACHEPREFIX=/tmp/pyc
P="C:\Program Files\Python312\python.exe"
# ① 冲突判据覆盖面自证（扩面后 26 件，看 [权威源覆盖自证] 行）
"$P" 05-exec/rule_conflict_scan.py --json 06-benchmark/rule_conflict_scan_r51_<日期>.json
# ② 棘轮读取面（若出现「⚠️ 读取面 username_in_skill_files」即面不完整，指标已转 unknown）
"$P" 05-exec/ratchet_gate.py
# ③ 账龄尺五面 + 台账列（--report 决定建议面能否对照）
"$P" 05-exec/r38_debt_aging.py --report <本轮报告> --json <证据件> --ledger 06-benchmark/debt_runs.jsonl
# ④ W-28：在 05-exec/schemas/r19/baseline-contracts.json 给对手取证件加 `retrieval` 必填 +
#    在 baseline_contract_scan.py 加不变式 inv_opponent_claims_have_retrieval（先写夹具看红）
# ⑤ 13 道常驻门 + 契约 + 控制符 + footer + savepoint
"$P" 05-exec/run_gates.py ; "$P" 05-exec/baseline_contract_scan.py ; "$P" 05-exec/control_char_scan.py
"$P" "/d/global_skills/A-memory-start/references/upgrade_footer_gate.py"
```

**验收口径（本轮实测）**：`r50_face_fixtures 16/16`（含 c6b 对照例）、`rule_conflict_scan 输入非空校验 PASS`（26/26 件、188 规则）、`[RATCHET:PASS]` 且读取面无告警、账龄 `OVERDUE→0`、五面中行使面全 OK（零输入面如实记 UNVERIFIED）。
