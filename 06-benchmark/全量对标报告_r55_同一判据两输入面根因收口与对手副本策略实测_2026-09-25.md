# 全量对标报告 r55 — 「同一判据读了两个输入面」的根因收口：登记不放宽判据的双向取证件，以及对手为什么根本没有 mismatch 这一类问题

日期：2026-09-25 ｜ 轮次：第 25 轮（r55）
本轮定位：**由用户指令驱动，非重复触发轮**。上一条指令给的是二选一（本机 Trae 配 hook ／ 把报错特征固化进仓库规则），我按「改动最小、长期收益最高」执行了选项②；随后你的批注选中了上一轮 footer 里那条 `A-project-handoff` 升级建议 ⇒ 本轮主体 = 把那条建议真正落地并复验。因此本轮**既不虚报"开了新维度"，也不套用"整跑一轮不降级"的重复轮声明**（X-3 只约束逐字重复触发的情形）。

> 引用规范（W-34）：可再生数值写「句柄 → 字段」，值仅作本轮快照；每节末行给取值命令。

## 1. 对比总览表（14 维存量 + 本轮新开第 15 维；只列有实测变化的行）

| # | 维度 | 对手实况（r55 gh api 实测） | 我方 r55 实况 | 差距判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖 | 五家无「判据输入面自证」层 | 常驻门 **19 道**（本轮 +2：`root_face_fixtures`、`baseline_note_fixtures`），八类面自证之外新增**登记面双向取证件**与**留账不被刷新**两锁 | 领先（不同赛道） |
| 4 | 可扩展性 | 加文件即扩展，无准入 | 新增夹具须含**反向对照**（f9：证明正例断言不是空断言）才准入 | 有意收紧 |
| 12 | 引用可解析性 | 0/5 家有"句柄"概念 | 本轮全部数字带取值命令；受管根 commit SHA 可直接 `git show` 复现 | 领先 |
| 13 | 判据可信度 | 零家声明输入面 | 修掉一处**同类根因**（未注册根被 strict 全判 ⇒ 8 条假阳性），并按"登记不放宽判据"补双向证据 | 落后→已补（D-100） |
| 14 | 债务到期治理 | 仅 spec-kit `stale.yml` | 本轮新立 W-39/40/41/42/43 五条，全部带取值命令与不接线理由 | 领先机制 |
| **15** | **单一权威源与副本同步（本轮新开）** | **0/5 家存在"需要同步的第二份技能树"**：spec-kit、pre-commit 根树无 `.agents`/`.claude*`；superpowers 只有 `.agents/plugins/marketplace.json`，anthropics/skills 只有 `.claude-plugin`，AAS 的 `.claude/commands/`（10 个 md）+ `.claude/rules/`（1 个）是**指向源的薄壳**，其 25 个 SKILL.md 全仓只有一份 | 我方 = 权威源 `D:\global_skills` + 物理副本 `C:\Users\37533\.agents\skills` + hash 门禁；本轮该门禁报 `mismatch=5`，其中 **3 个是我自己刚提交的文件**（提交未同步镜像），2 个是并会话未提交的在途件 | **对手按构造不存在此类问题**（D-101）⇒ 我方要么把同步接进提交链（W-41），要么消掉副本（W-42） |

> 取值（对手侧全部数字）：`gh api "repos/<R>/git/trees/HEAD" --jq '.tree[].path' | grep -cE '^(\.agents|\.claude|\.codex|dist|build|vendor)'`（根树）；`gh api "repos/<R>/git/trees/HEAD?recursive=1" --jq '.tree[].path' | grep -E '^(\.agents|\.claude|\.codex)/'`（嵌套清单）；AAS 的 SKILL.md 份数把上一条管道改 `grep -cE 'SKILL\.md$'`；五家 `<R>` = github/spec-kit, obra/superpowers, anthropics/skills, addyosmani/agent-skills, pre-commit/pre-commit
> 取值（我方镜像侧）：`powershell -NoProfile -ExecutionPolicy Bypass -File "C:/Users/37533/Desktop/workspace/焚诀/skill/sync/check-skill-mirror.ps1"`（无 `-Fix` 即只读）

## 2. 逐项差距说明

**D-100（P0，本轮已修）：同一个噪声判据读两个输入面，两边结论互相打脸。**
r54 我把「savepoint 第 i) 步绿」当成「项目根干净」写进 `memory/AGENTS.md` 排障表 row 1 首版 —— 那是一条**我没实测过的检查命令**（违 R236/R240）。r55 实测两个事实把它拆开：① `noise_lint` 的 `KNOWN_CORE` 里没有本项目根 ⇒ 传项目根参数时默认 strict，把 `03-audit`/`04-plan`/`05-exec`/`06-benchmark`/`memory`/`archive` 等**已跟踪治理目录全判 VIOL**；② `savepoint` 那一步调 `cmd_noise` 时**根本不带项目根参数** ⇒ 只扫四受管根，所以它那句「✅ 零散射」对项目根是一句空承诺。两个面一个是"过严"、一个是"没进面"，方向相反，任何一边单独看都像对方在撒谎。
修法按 R281 先例走**登记**（受管根 `e93896b`，R286）而不是放宽判据；复验 = 顶层 VIOL **8 → 0**，而二级扫描随即露出 3 条真项 ⇒ 证明登记只是把面认错纠正回来，牙齿还在。

> 取值：`python 05-exec/r55_rootface_fixtures.py`（f2 正例 / f4 反例 / f5 守恒 / f7 复位 / f9 对照）；单跑 `python "D:/global_skills/A-project-handoff/scripts/handoff.py" noise "C:\Users\37533\Desktop\workspace\自建skill优化"` 看末行汇总；「8」这一原值的历史出处见 `memory/AGENTS.md` 排障表 row 13（R241：旧值只加校正注，未改写）

**D-101（P1，本轮手工绕过、未落码）：提交只落权威源，镜像同步不在提交链里。**
我用 `rule_editor.py commit` 提交 R286 之后，`gates` 立刻报 `mismatch=5`，逐条对账：3 个正是 `e93896b` 里的三个文件（镜像侧仍是**提交前**字节，我用 `git show <commit>^:<path> | sha256sum` 与镜像 hash 逐一比对证实"仅陈旧、无他人内容"），另 2 个是并行会话**未提交**的源码。此时唯一的一键修法 `-Fix` 是全局单向覆盖 —— 会把别人在途工作推进镜像（铁律 8），按 R269 不能按。
本轮处置 = 只 `cp -p` 我自己那 3 个文件 + hash 复检 + 复跑只读门禁（`mismatch 5 → 2`，剩 2 归他人）；并把这套配方固化成 `memory/AGENTS.md` row 14。结构性修法立 **W-41**（commit 成功后按"本次提交文件列表"做定向同步并复检），**暂不落码**的理由也写进 W-41：改动落在门禁工具自身，且 A-memory-start 正有他人在途，避免"自改门禁 + 夹带"叠加。

> 取值：`git -C "D:/global_skills" show --stat --format="%h %s" e93896b`（本次提交面）＋ 上一条总览表的只读镜像命令（mismatch 清单）

**D-102（P2）：镜像里存在"仅在镜像"的文件 ⇒ junction 化不是免费午餐。**
本轮新开的第 15 维最省事的收敛是把物理副本换成 junction（本机 `C:\Users\37533\.qoder-cn\skills` 已是 junction，实测可用）。但只读镜像门禁同时报出 **10 个"仅在镜像存在"** 的文件（rag-eval 全族）：这些内容不在权威源，junction 之后其可见性来源就变了。对手之所以没有此类问题，是因为他们**压根不留第二份内容**（薄壳指向源）。因此 W-42 的裁定顺序必须是"先给这 10 个文件定归属，再谈消副本"，不能一步跳到 junction。

> 取值：只读镜像命令的 WARN 段（10 行）＋ `python 05-exec/r55_rootface_fixtures.py` 的 f8（本项目当前残留 VIOL 只容 R269 项与 W-39 项）

**D-103（P2·在册待裁定，未动数据）：`05-exec/mark_backup` 的名字与二级判据天然对立。**
判据按名命中 `_backup`，而这 22 份是 `r46_mark_verdict.py` 的写回滚备份、且**已跟踪入库**。我没有改名、也没有迁走凑绿（X-9），立为 W-39 三选一待裁定。另 1 条残留 `手动维护。agent` 刻意**不登记**，让它继续报红（R269 归属未明）。

> 取值：`git -C "C:/Users/37533/Desktop/workspace/自建skill优化" ls-files 05-exec/mark_backup | wc -l` ＝ 22 ＋ `python 05-exec/r55_rootface_fixtures.py` 的 f8 明细行

**D-104（P0，本轮自抓并已修）：`--update` 会把「这个基线值凭什么是它」的留账整条刷掉，且无门禁变红。**
本轮我跑 `ratchet_gate.py --update` 收口指标面，事后对 `git diff` 才发现它把 `inject_ratchet_baseline.json` 的 note 从 **496 字刷成 33 字** —— r38「真实敞口 22 而非 0（27 条 UNDATED 里 9 条实为超期）」、r40「真值直取 debt_aging 证据件，未做人工削减」、r45「W-14 新指标」三条人工核定依据一次性蒸发，而棘轮照样 `[RATCHET:PASS]`。根因是 `--update` 分支**整份重建 doc 再落盘**，`note` 与 `attributed_raises` 都不在继承名单里 ⇒ 指标面有「只降不升」的牙齿，留账面却一点牙齿都没有。
修法 = 抽 `merge_note()`（留账只增不减，人工改过的 note 整条保住不按长度切片，缩短即拒写）+ 从 `git show HEAD:` 恢复被删的三条归因 + 复跑 `--update` 证明不再刷（496 字存活）。夹具 6 例并做**变异体复验**：把落盘行改回旧行为，n4 当场红 ⇒ 断言不是空断言。

> 取值：`git -C "C:/Users/37533/Desktop/workspace/自建skill优化" diff 06-benchmark/inject_ratchet_baseline.json` 看 note 行增减 ＋ `python 05-exec/r55_note_fixtures.py`（n1-n6）

**D-106（P0，本轮自抓并已修）：登记只治好了"绝对路径"这一半，`noise .` 仍然全盘误判。**
收口前我按排障表 row 1 的写法用**相对路径**复跑了一次噪声门禁，结果同一目录报出 13 项 VIOL（绝对路径同根仅 2 项）——R286 的登记没覆盖这种调用形态。根因与 D-100 同族但成因不同：`scan_root` 只对传入根做 `normpath` 就去查 `KNOWN_CORE`，而 `normpath('.') == '.'` 是相对串，查表必然 miss ⇒ 落 strict。这恰好是我自己写进规则文件的那条"先问命令读的是什么面"的反面教材：**同一条命令的两种写法就是两个面**。
修法 = 只把**查表键**做 `abspath`（显示与扫描根仍按传入原样，改动面最小），并双向取证件：f10 相对与绝对必须给出同一个面、f11 未登记的相对根仍须判红（防止"顺手放宽"把修面变成削判据）。复验：四受管根 `violation` 全 0 且 `[GATE:noise-pass]`、镜像侧同结论、`rule_editor gates` 四门全绿。

> 取值：`cd "C:/Users/37533/Desktop/workspace/自建skill优化" && python "D:/global_skills/A-project-handoff/scripts/noise_lint.py" .` 与传绝对路径对比末行汇总；`python 05-exec/r55_rootface_fixtures.py`（f10/f11）；受管根复跑 `python "D:/global_skills/A-memory-start/references/rule_editor.py" gates`

**D-108（P0，本轮自抓并已修）：句柄的 artifact 恒取默认台账路径 ⇒ 我自己的"同轮一致性自检"反过来误报我漂移。**
h12/h13 夹具（r53 我为「报告引用了上一行」那个错位立的锁）本轮报 `[HANDLE:DRIFT] drift=['open_total']`。逐层归因后问题不在测量：`make_handles(doc)` 的 `ledger_rel` 有默认值且调用处从不传参，于是**写临时台账的夹具运行**仍去解析真台账 `06-benchmark/debt_runs.jsonl` 的末行；本轮我中途给待办卷加了 W-44，账面 91→92，夹具那一次量到 92 而真台账末行还是 91 ⇒ 判据宣布"我漂移"。与 D-100/D-106 同族：**句柄没有指向产生它的那本账**，就是第三个输入面。
修法 = 建句柄时传入本次真正写过的台账路径（`args.ledger or 默认`），使"写哪本、指哪本、比哪本"三者同源。修后 h12/h13 全绿，且语义比修前更强：现在只有「本轮该写进台账却没写（自洽拒写）」才会触发 drift —— 那正是它本该抓的情形。§5 的四行债务值同时改为**只写句柄名不抄值**（W-34 的本意，顺手消掉"每测一次就要改一次报告"的抖动）。

> 取值：`python 05-exec/r53_handles_fixtures.py`（h12/h13）；`python 05-exec/r38_debt_aging.py --json /tmp/x.json --ledger <临时台账>` 后读该 json 的 `handles.open_total.artifact` 是否等于所传台账

## 3. 改进建议清单（含优先级与状态）

| 编号 | 优先级 | 建议 | 状态 |
|---|---|---|---|
| D-100 | P0 | 未注册根被 strict 误判 ⇒ 按 R281 先例登记，并以夹具锁死"登记不放宽判据" | **本轮已执行**（受管根 `e93896b` + 11 例夹具（含 R287 的 f10/f11）+ 排障表 row 1/13 校正） |
| D-106 | P0 | 登记之后仍有一只面漏洞：**相对路径**调用噪声门禁会绕过 allowlist ⇒ 同一目录 `.` 判 13 项、绝对路径判 2 项 | **本轮已修**（受管根 `e5c785e` / R287：查表键绝对化，显示与扫描根不动；四受管根复跑 violation 全 0） |
| D-108 | P0 | 句柄 artifact 恒取默认台账路径 ⇒ 写临时台账的运行比对真台账末行，同轮一致性自检误报漂移（h12/h13 当场红） | **本轮已修**（`make_handles` 收本次台账路径；h12/h13 复绿、`--ledger` 临时台账实测 artifact 同源且 drift=None；§5 债务行改只写句柄名） |
| W-44 | P1 | R 号两个注册表共用号段，防撞号预检只扫 A-memory-start 两个文件 ⇒ 跨项目结构性漏检（同日另一项目已领 R286-R288） | 登记待裁定（不擅自扩大扫描集＝共享状态变更，呈三选一） |
| D-101 | P1 | commit 链缺定向镜像同步 | **手工绕过已复验**，结构修法登记为 W-41（暂不落码，理由在册） |
| W-40 | P1 | `savepoint` 噪声步不覆盖项目根 ⇒ 致命纪律 #17 对项目卷是空承诺 | 登记（含与 R286 的分工，防两条互相顶替） |
| D-102 / W-42 | P2 | 副本 vs junction：先定 10 个"仅在镜像"文件归属，再评估消副本 | 登记待裁定（对标实测驱动） |
| W-39 | P2 | `mark_backup` 命名与判据对立，三选一 | 登记待裁定（当前态＝保留诚实红） |
| D-103 | P2 | 我自己留下的临时件 `_tmp_r54` 与 `mark_backup` 命名对立要分开处置 | `_tmp_r54` **本轮已清**（夹具 f6/f7 自证复位）；`mark_backup` 立 W-39 待裁定，不改名不 untrack |
| D-104 | P0 | 基线留账（note / attributed_raises）被 `--update` 整条刷掉，棘轮仍判 PASS | **本轮已修**（`merge_note` + 恢复 496 字留账 + 6 例夹具 + 变异体复验） |
| D-105 | P1 | 本仓无 `.gitattributes` ⇒ `git add` 对 8 个文件报「LF will be replaced by CRLF」，被点名者含 append-only 台账 `debt_runs.jsonl`（与 W-23 混合行尾根因同族） | 登记 W-43（改 `.gitattributes` 会重写全仓规范化视图，须与并行会话错峰，本轮不动手） |

**D-107（P1·跨会话账面完整性，本轮只登记）：R 号的两个注册表共用一个号段，防撞号工具只看得见其中一个。**
本轮我在受管根领用 R286、R287 两次，`rule_editor` 编号预检均放行（fail-closed 的那道它没响）；收口核对全局记忆时才发现同日另一项目已在 `lessons.part84.md` 卷首写下「医·第十九轮 R286-R288」⇒ 同一天两个项目各自把 R286/R287 当作自己的新号。根因是口径不统一而非工具失灵：预检的登记面 `ID_REGISTRY_FILES` 只有 A-memory-start 的两个文件（注释按用户口径「扫描 SKILL.md 现有最大编号」），而设计注释 R234 又声明「R 号为全库共享序列」⇒ 跨项目维度**结构性漏检**（不报错，最难发现的那一类）。
处置 = 登记 W-44 呈三选一（分域前缀 / 扩大登记面 / 领号前人工全库 grep 并写进提交说明），**不擅自扩大扫描集**：那会改变所有会话的提交行为，属共享状态变更，须用户裁定。

> 取值：`grep -rn "R286\|R287" "D:/global_memory/lessons" | head` 与受管根 `git log --format=%s -3` 的领号记录对照；登记面定义见 `D:/global_skills/A-memory-start/references/rule_editor.py` 的 `ID_REGISTRY_FILES`

## 4. 可操作实施路径（下一轮起，按序）

1. 先做 W-40（`savepoint` 项目根进面）：它是"账面绿 vs 实际脏"的最后一处口径缺口，改动面小、可被 f8 直接复验。
2. 再做 W-41（定向镜像同步）：改 `rule_editor.py` 的 commit 尾部，**必须**带"只处理本次提交文件列表"的断言，并配反例夹具（给定他人脏文件 ⇒ 不得出现在同步列表）。
3. W-42 拆两步：先给 rag-eval 那 10 个镜像独占文件定归属（迁源或退役，走 `_trash` 不删除），再重跑只读门禁看 EXTRA 是否归零，归零才有资格谈 junction。
4. W-39 与 `手动维护。agent` 一并裁定；裁定前本项目 noise 面**保留 2 条诚实红**，不 `.gitignore` 豁免（那是用 untrack 凑绿）。

## 5. 末态快照（句柄优先，值仅本轮）

| 项 | 本轮值 | 句柄 / 取值 |
|---|---|---|
| 受检未闭环 | 句柄 `open_total`（不抄值） | `06-benchmark/debt_aging_r55_2026-09-25.json` → `evidence.open_total` |
| OVERDUE | 句柄 `overdue` | 同件 → `by_class.OVERDUE`（棘轮第 7 指标唯一取值面） |
| DECIDED | 句柄 `decided` | 同件 → `by_class.DECIDED` |
| 台账行数 | 句柄 `ledger_rows` | `06-benchmark/debt_runs.jsonl` 行数 |
| 棘轮 | PASS 9 项 | `python 05-exec/ratchet_gate.py` 末行 |
| 注入面 | 86,756 B（硬顶 65,536，超 +21,220） | 同上 `inject_union_bytes` 行（W-32 在册，未擅改硬顶） |
| 常驻门 | 19 道（本轮 +2） | `python 05-exec/run_gates.py` 汇总行「执行 19 门 / 跳过 0 门」 |
| 本项目噪声 | violation 2（R269 项 + W-39 项） | `python 05-exec/r55_rootface_fixtures.py` f8 |

> 取值（本表全部债务行）：`python 05-exec/r38_debt_aging.py --json 06-benchmark/debt_aging_r55_2026-09-25.json --ledger 06-benchmark/debt_runs.jsonl` 的「可引用句柄」行
