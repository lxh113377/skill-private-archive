# P0-C 累积语义漂移复核 · 第 1 批（07 主卷承重句）

> 日期：2026-09-24（r18）| 对标出处：mycelium-hq/ai-brain-starter `scripts/drift-detection.py`（改写频次超阈 → 人工语义复核）
> 触发：`python 05-exec/cumulative_drift_scan.py --days 30 --threshold 5` 实测本项目 `memory/07-next-steps.md` **35 次/30 天**（全仓最高，> DELEGATE-52 的 20 次量级）
> 判据依据：R241——**只加注不改写**；每条给「仍然成立 / 已失真 / 已由归属会话收口」三态结论。

## 复核方法（可复跑）

| 断言类 | 复核命令 |
|---|---|
| 技能计数/分母 | `python 焚诀/eval/verify_truth_consistency.py`（C1/C2/C10 行） |
| 受管根在途条数 | `git -C D:/global_skills status --porcelain`（计数用 `\| wc -l`） |
| 焚诀在途条数 | `git -C 焚诀 status --porcelain` |
| 判据桩健康 | `python 焚诀/eval/gate_stub_runner.py` |
| 产物在盘 | `find <路径> -type f`（**不用** `ls`，避免只看到首项） |

## 逐条 verdict

| # | 07 位置 | 原句要点 | verdict | 实测证据（2026-09-24） |
|---|---|---|---|---|
| 1 | 摘要 r13 | 「本体系 **151** SKILL.md … 全等（零实质漂移，结论维持）」 | **已失真（分母）** | C1 行 = **167**；一周 +16（+10.6%）。「零实质漂移」判据本身当时成立，但**它的取值窗口已过期** |
| 2 | 摘要 r17 / 尾款③ | 「description 基线 **151 条口径全等注册表**，双要素 98=65%」 | **已失真（两处）** | ① 该工具按设计跳过 junction，实测 **166 ≠ 注册表 167**（差 1 = `rag-eval`，`find` 判定为链接目录），"全等"一句自始不成立；② 当前 120/166 = **72.3%** |
| 3 | 摘要 r17b | 「基线校正 **103/151=68%**、无触发标记 **48** 条」 | **已失真（分母）** | 当前 120/166、无触发标记 **46**（同一工具实跑） |
| 4 | P0 条「待观察·他人在途（r7 新登记）」 | 「受管根现存 **1 条**在途」 | **已失真（时点值）** | `git -C D:/global_skills status --porcelain` = **28 条**（M）；新增在途含 `A-project-better` / `A-skill-manager` / `cloudbase__skillhub` / `github` / `sq-cleanmgr-c` 等，**按 R269 只登记不擅动** |
| 5 | P0 条（r7 收口注） | 「焚诀仓现存 36 M + 2 ??」 | **已由归属会话收口** | `git -C 焚诀 status --porcelain` = **0 条** |
| 6 | P0 条「待观察·焚诀在途债」（r7 收口注：`[GATE:stub-pass]` 14/14） | 桩全绿 | **已失真（重新变红）** | `gate_stub_runner` = **`[GATE:stub-fail]`，2 桩失败**：`stub_c16_domain` 3/4、`stub_fp15_count` 3/4（**报错原文含「注册表实测 167」**）。⇒ **本次漂移复核的独立交叉印证**：陈旧的硬编码技能数已经打到上游判据桩上，不只是本仓文档口径问题 |
| 7 | 尾款② | 「ZC 插件实物已就绪（`D:\global_memory\hooks\zcode-plugin-my-bootstrap\`）」 | **仍然成立** | `find` 实测 3 文件：`.codex-plugin/plugin.json`、`hooks/hooks.json`、`hooks/inject-bootstrap.mjs`（原句未写前缀目录，属表述粒度非失真） |
| 8 | 尾款① | 「HM CLI 入口本机损坏（uv 跳板 + ABI）」 | **仍然成立（本轮未复测）** | 本轮未重跑 hermes CLI ⇒ 标 ⚠️部分实测，维持原登记 |
| 9 | 全文 A-project-handoff 版本 | r13 句「3.51.0 全等」 | **已失真** | `grep -m1 '^version' D:/global_skills/A-project-handoff/SKILL.md` = **3.52.1**（r17b 连跳两版） |

## 本批结论

- **9 条承重句复核：仍然成立 2 / 已失真 6 / 归属方收口 1**。失真全部集中在**「当时实测但被后续引用为现状」的数字**，与 2026-09-22 用户裁定的「禁硬编码当下值、必须附取值命令」铁律同源——本批已把 5 类取值命令固化进上表，后续会话按表复跑即可，不需要重读散文。
- **最硬的一条**：#6 显示陈旧技能数已不止污染文档，**已使上游 `fp15` 计数桩实际失败**。转焚诀归属会话（红线：本项目不代改 `eval/`），处置 = 由 owner 更新桩的注册表期望值并重跑 `build_registry`。
- 下一批（P0-C-2）范围：`A-project-handoff/SKILL.md`（17 次）、`A-memory-start/SKILL.md`（11 次）、`A-project-better/SKILL.md`（10 次）——均属受管根，**只出复核结论与整改建议，改动须走 `rule_editor` 且待授权窗口**。
