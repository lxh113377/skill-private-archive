# 全量对标报告 r56 — 「声明」要算数，得先让机器能校验它：预算入契约、裁定件配生成器、代际豁免要看得见

日期：2026-09-25 ｜ 轮次：第 26 轮（r56）
本轮定位：**用户批注驱动轮，非逐字重复触发轮**。批注选中的是上一轮 footer 的三行：未闭环清单（W-27 / W-32 / W-37 下轮首推 / W-38）+ 那条挂在 A-skill-manager 的升级建议。执行顺序即按批注里的优先级：W-37 → W-38 → 落规 A-skill-manager → W-27/W-32 实测。X-3 的「禁降级」只约束逐字重复轮，本轮不套用其声明。

> 引用规范（W-34）：可再生数值写「句柄 → 字段」；每节末行给取值命令。

## 1. 对比总览表（存量 15 维 + 本轮新增第 16 维；只列有实测变化的行）

| # | 维度 | 对手实况（r56 gh api 实测） | 我方 r56 实况 | 差距判定 |
|---|---|---|---|---|
| 2 | 技术架构 | 声明散落在 workflow yaml 里，由 CI 解析 | 再生预算从代码常量迁到**被契约校验的产物**（基线件 `refresh_days`，schema v1→v2） | 机制等价化完成（D-109） |
| 4 | 可扩展性 | 加文件即扩展 | 加声明面须同时交：契约 pattern + 分代豁免 + **生成器**（本轮新铁律 W-46） | 有意收紧 |
| 8 | CI 存在性（**换口径重测**） | **自有 workflow 文件数 18 / 0 / 0 / 1 / 2**（spec-kit / superpowers / anthropics / AAS / pre-commit），而 `/actions/workflows` 总计面报 **27 / 2 / 2 / 5 / 3**，五家**全部** face_delta 非零（+9/+2/+2/+4/+1） | 无 CI（X-1 有意）；本机常驻门 **21 道** | 数字必须双路对账（D-110） |
| 9 | CI 有效性 | 21 个自有 workflow 里 **11 个**正文含校验动作（`pre-commit run`/`--check`/`validate`/`schema`/`lint`）；集中在 spec-kit（10）与 AAS（1）；**pre-commit 自己 0**（它的校验在 hook 与 pytest 里，不在 CI 声明） | 每条声明配反例夹具 + 变异体复验（本轮 b1-b9、e1-e10） | 对手"声明即被校验"，我方"声明即被反例校验" |
| 13 | 判据可信度 | 零家声明输入面 | 新增两件：**代际豁免**（新必填面不追溯判红历史证据件）与**豁免可见性**（看不见的豁免=豁免表，必须打印计数行） | 领先（且补了我方自己的洞） |
| 14 | 债务到期治理 | 仅 spec-kit `stale.yml` | W-37/W-38 同轮改判「执行完毕」并写标记；新立 W-45/W-46 两条 | 领先机制 |
| **16** | **证据可再生性（本轮新开）** | 未测（他们不做这类"自我裁定件"） | 自查发现 **2 份裁定依据件没有生成器**（`inject_face_breakdown_r52_*.json`、`w27_conflict_triage_r51_*.json`，`grep -rl` 各零命中）⇒ 它们是我方句柄体系里的死句柄；本轮补齐 3 个生成器并纳入契约 | 落后已补（D-111） |

> 取值（对手三行）：`python 05-exec/r56_workflow_face.py --json 06-benchmark/workflow_face_r56_2026-09-25.json`，读 `repos[].own_workflow_files / api_total_face / face_delta / with_validation_step`
> 取值（我方门数与契约）：`python 05-exec/run_gates.py` 汇总行 ＋ `python 05-exec/baseline_contract_scan.py --quiet`

## 2. 逐项差距说明

**D-109（P0，已闭环）：再生预算此前只是代码里的一个字典，契约看不见 ⇒ "多久必须重算"没人看守。**
W-37 落地：基线件新增必填面 `refresh_days`（9/9 与 `METRIC_NAMES` 全等），契约新增 `ratchet_refresh_days_matches`（缺项/多键/越界三向判红，合法域整数 ∈ (1,60]）。三条设计约束都是从**同类缺陷**推出来的，不是顺手写的：① `--update` **不得**改写预算面（与 r55 D-104 的"留账被刷"同一族：刷新动作不许顺带重算承诺）；② 基线缺预算时 `refresh_status()` 一律记 UNVERIFIED，**禁止回落代码常量**（回落=自留一张豁免表）；③ CLI 必须打印「预算来源」行（看不见来源就谈不上被谁看守）。夹具 10 例，含 b6 这条"禁回落"的方向反例。

> 取值：`python 05-exec/r56_budget_fixtures.py`（b0-b9）＋ `python 05-exec/ratchet_gate.py` 末两行

**D-110（P1）：`/actions/workflows.total_count` 不是"仓库自有 workflow 数"，五家全部对不上。**
本轮按 `git/trees/HEAD?recursive=1` 枚举 `.github/workflows/*.yml(yaml)` 作分母，与 API 总计面并列取数：18/0/0/1/2 对 27/2/2/5/3。r50 那次是 spec-kit 一家混进平台 `dynamic/*`；这次更彻底——**superpowers 与 anthropics/skills 的 `.github/workflows/` 目录直接 404**，自有文件为 0，而 API 各报 2（已排除 `truncated=True` 造成的假 0，实测 truncated=False、条目 298/511）。含义：凡"对手有没有 CI"这类判断，只看任一面都会得出错结论；我此前引用过的 `18/2/2/0/0`（r54 第 8 维）属 API 面，本轮改双列并报差值。

> 取值：`06-benchmark/workflow_face_r56_2026-09-25.json` 的 `repos[]`（含 `tree_truncated` 与 `unreadable` 两栏自证），重跑命令见第 1 节

**D-111（P0，本轮自抓并已修）：我自己写的"取值命令"指向了两份**没人能再生产**的文件。**
`inject_face_breakdown_r52_*.json` 与 `w27_conflict_triage_r51_*.json` 都是当时一次性内联脚本产出的，仓里 `grep -rl` 零命中生成器。W-34/W-36 只校验"件新不新"，从不问"件能不能再生" ⇒ 死句柄在两套机制下都能全绿。本轮：① 补 3 个生成器（`r56_inject_face_breakdown.py`、`r56_conflict_triage.py`、`r56_workflow_face.py`），前两个与棘轮**共用同一个枚举器**（两份算法必漂移），并各自带"逐件求和 == 判定值"的交叉核对（变异 7 字节即 `[BREAKDOWN:DRIFT]` 退出 1，实测有牙齿）；② 两类件纳入契约（新 pattern + 两条不变式）；③ 立 **W-46** 把"pattern 必须有生成器"变成下轮的机器判据（先量误报率：16 个 pattern 里现有多少本来就没生成器，清单先交出来再决定接线）。

> 取值：`python 05-exec/r56_artifact_face_fixtures.py`（e1-e10，11 例）；死句柄自查 `grep -rln "inject-face-breakdown-v1\|conflict-triage-v" 05-exec/*.py`

**D-112（P0，方法论）：给一类件新增必填面，会把同族的**历史证据件**回头判红 —— 台账早就有解法，JSON 件没有。**
新 pattern 首跑就抓出 4+2 条红，全部落在 r51/r52 那两份历史件上（缺 `by_owner`/`enumerator`/`rows` 等）。这些件不可回写（R247 台账式约束 + R241 历史不改写），也不该搬走躲判（X-9）⇒ 把台账的 `row_required_from` 代际口径推广到 JSON 件：新增 `required_from` / `invariants_from`（按 `generated_at` 划代）。两个细节：① 时间戳有 `2026-09-25 16:33` 与 `…T19:31:00` 两种形态，不统一成 `T` 分隔，字典序会把**新件**判成早于分叉点而静默豁免（比误判更坏）；② 豁免必须打印 —— 现输出「代际豁免：8 处 ｜ 涉及件 …」，e9 还专门断言"分叉点之后缺字段仍须红"，防止豁免变成万能挡箭牌。

> 取值：`python 05-exec/baseline_contract_scan.py --quiet` 看「代际豁免」行 ＋ `python 05-exec/r56_artifact_face_fixtures.py` 之 e9/e10

**W-27 / W-32 的裁定面（按批注"实测清楚，不擅改阈值"）：**
- **W-32**：注入面 86,756 B、硬顶 65,536、超 +21,220。归属拆解：**受管根 54,469 B（62.8%）+ 本项目可控 29,293 B（33.8%）+ 端上注入壳 2,994 B（3.5%）** ⇒ "真降"要么动受管根、要么只降本项目面也降不掉量级；改硬顶则属受管根 `truth_constants.inject_budget.hard_cap_bytes` 的归属会话。**本轮两者都没动**。
- **W-27**：极性冲突原始候选 **23 组**，按规则本体做分卷归一后 **13 对**（重复 10 组即放大来源），不同共用词 **9 个**，禁侧疑似"呈现形式元规则" **3 组**（token 表已写进码里可复核）。语义是否真互斥**不由我裁**——本项目红线禁止改受管根规则正文，且 R241 只允许加校正注；件里 `decision_needed` 写明三选一（真互斥改措辞 / 语境不同加成对豁免 / 元规则让位）。

> 取值：`python 05-exec/r56_inject_face_breakdown.py --json 06-benchmark/inject_face_breakdown_r56_2026-09-25.json` ＋ `python 05-exec/r56_conflict_triage.py --json 06-benchmark/w27_conflict_triage_r56_2026-09-25.json`

## 3. 改进建议清单（含优先级与状态）

| 编号 | 优先级 | 建议 | 状态 |
|---|---|---|---|
| W-37 | P1→ | 再生预算入基线件，由契约做集合全等 | **本轮执行完毕**（07 已写 r56 裁决标记） |
| W-38 | P2→ | `refresh_stale_count` 进台账成趋势列 | **本轮执行完毕**（分代点 19:15，取不到记 None） |
| 落规 | P1 | R-ENUM 增第 4 形态「枚举器须声明保质期」 | **已落受管根** `b2e3f4e`（governance.md 单文件提交 + 定向镜像同步 + 四门全绿）；SKILL.md 版本行/版本历史因并行会话占用未同步 → W-45 |
| D-109 | P0 | `--update` 与预算面解耦 + 禁静默回落 | **本轮已修**（含 b8/b9/b6 三向锁） |
| D-111 | P0 | 裁定依据件必须配生成器 | 3 个生成器已补并纳入契约；机器判据待立（W-46） |
| D-112 | P0 | 契约新增面须支持代际豁免且豁免可见 | **本轮已修**（`required_from` / `invariants_from` + 打印计数行 + e9 牙齿） |
| W-32 | P1 | 注入面超顶 +21,220 | **未动，拦在归属**：受管根占 62.8%，本项目可控面降不掉量级；改硬顶属受管根会话 |
| W-27 | P1 | 21→13 对极性冲突语义裁定 | **未动，拦在红线**：不得改受管根规则正文；`decision_needed` 已备好三选一 |
| W-45 | P2 | A-skill-manager 版本行/版本历史同步 | 登记（等并行会话让位） |
| W-46 | P1 | 「pattern 必须有生成器」做成判据（先量误报率） | 登记 |

## 4. 可操作实施路径（下一轮起，按序）

1. W-46 先做**误报率实测**：对 16 个契约 pattern 反查 05-exec 生成器，零命中清单先交出来，再决定接线与否（守"新判据先量误报率"）。
2. W-40 / W-41（r55 登记的受管根建议）：`savepoint` 项目根进面 + commit 后定向镜像同步。
3. 请用户裁 W-27（13 对逐条三选一）与 W-32（降受管根 / 改硬顶 / 保留诚实红）；两者都不该由我代裁。
4. W-39 / W-42 / W-43 与 `手动维护。agent` 一并裁定。

## 5. 末态快照（句柄优先）

| 项 | 值 | 句柄 / 取值 |
|---|---|---|
| 常驻门 | 21 道（本轮 +2） | `python 05-exec/run_gates.py` 汇总行 |
| 契约 | PASS（42 份 / 16 pattern） | `python 05-exec/baseline_contract_scan.py --quiet` |
| 棘轮 | PASS 9 项，预算声明 9/9 在基线件 | `python 05-exec/ratchet_gate.py` 末两行 |
| 台账 | `refresh_stale_count` 首列已进 | `06-benchmark/debt_runs.jsonl` 末行 |
| 债务 | 句柄 `overdue` / `open_total` | `python 05-exec/r38_debt_aging.py` 「可引用句柄」行 |
| 受管根 | 四门全绿 | `python D:/global_skills/A-memory-start/references/rule_editor.py gates` |

> 取值（本表全部）：对应命令逐行给出，本轮所有数字均可由上述三件生成器与四台机重跑复现
