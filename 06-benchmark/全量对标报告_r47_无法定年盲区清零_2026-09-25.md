# 全量对标报告 r47 — W-1 清偿：把「无法定年」的 12 条债钉上真实首现轮次

日期：2026-09-25（第 47 轮）｜触发：同一指令第十七次逐字下达 ⇒ 按 #23 整跑一轮；W-5 约束下不开新维度。
本轮是**纯还债轮**：执行挂账多轮的 **W-1（UNDATED 定年）**，并全程使用 r46 的写入器（W-18 dogfood）。

---

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖范围 | 债务台账 `UNDATED` 归零；写侧唯一入口 + 读侧完整性判据 + 9 指标看守成环 | `spec-kit` 27 workflow 全生命周期 | 覆盖面对齐，自动化面仍落后（可接受，见 X-10/X-11） |
| 2 | 技术架构与实现方式 | 定年数据来自 `git log -S <锚点>`，**不是回忆**；终局裁决由写入器落盘并读回验证 | `pre-commit` 单一配置源 | 相当 |
| 3 | 性能表现 | 账龄尺 ~3.6s（12 条各跑一次 `git log -S`）；11 道门 ~10.6s | 无可比物 | ❌未跨机实测 |
| 4 | 可扩展性 | 12 条批量定年 = 同一条命令 12 次调用，无一次性脚本；备份 8 份自动落 `05-exec/mark_backup` | — | 良好（W-18 生效） |
| **5** | **维护状态（聚焦）** | `UNDATED 12 → 0`、`DECIDED 46 → 58`、`OVERDUE 0`、`REPEAT 0`、`DEFERRED 3`；W-12 完整性 `OK` | `anthropics/skills` 1290 / `superpowers` 401 / `spec-kit` 287 / `agent-skills` 118 / `pre-commit` 25 open | 对手无「无法定年」这一层；**我方本轮起债务台账无盲区** |
| 6 | 文档完善程度 | 本报告明确限定：12 条里 **6 条定到轮号、6 条只定到日期**（首现提交主题不含 rNN），不虚报"全部定年完成" | 对手 changelog | 相当（差异在自我限定） |
| 7 | 适用场景 | 定年靠 git 取数，跨会话可复现；行号漂移风险已由写入器消除 | 对手无共享卷并发问题 | 我方独有 |

---

## 2. 逐项差距说明（全部实测）

- **D70 · 「无法定年」本身就是债务藏身处**：12 条 UNDATED 项账龄不可见 ⇒ 判据既不说它们老，也不追它们，
  等于台账上有一格"免检抽屉"。本轮按 `git log -S` 逐条取首现，12 条全部落定。
- **D71 · 定年不夸大**：6 条首现提交主题含 `rNN`（实测 r19 / r22 / r29 / r30 / r37），
  另 6 条**只能定到日期**（2026-09-23/24，提交主题无轮号）。台账里两者都写了日期，轮号缺失者写 `r?`，
  不假装全部定年成功。
- **D72 · 定年后的正确处置是终局裁决而非再挂账**：12 条按性质给三类终局 ——
  `作废` 4 条（判据/机制已落地，如 control_char_scan 已进必跑链与常驻门）、
  `保留` 6 条（留痕型登记与待用户裁定项，**登记不等于欠账**）、
  `降级（转长期看守）` 2 条（M-3 同尺分类、R19-1 残余量）。
  全部带实测取值，无一条按"看着像完成"划掉。
- **D73 · 写入器 dogfood 成功**：12 次调用全部 `[MARK:OK] … 读回验证通过`，
  自动产生 8 份备份且**不落记忆卷**；未出现一次"写到别的行"（歧义会被拒写而非静默错写）。
- **D74 · 一次调用里我先写错命令再改对**：脚本内同一 `cmd` 先按占位串构造、随后用 git 实测值重建后才执行
  —— 结果正确，但**中途那条半成品命令是本可避免的**（下轮起：先取数、后组装、再一次执行）。

---

## 3. 改进建议清单（含优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| **高** | **W-1** | 12 条 UNDATED 定年并给终局裁决 | ✅ 完成（限 6 条轮号 / 6 条日期，见 D71） |
| 高 | W-18 | 补标一律走 `r46_mark_verdict.py` | ✅ 本轮 12/12 全经工具，无手写脚本 |
| 中 | W-17 | 契约按 `ts` 分代必填（`coverage_*` 三列进取值域） | ⏳ 挂账 r49 |
| 中 | W-6 | 归属声明可机检（`UNOWNED` 态 + owner 字段） | ⏳ 挂账 r49 |
| 中 | M-1 | 写时冲突影子计数探针（历史不可测已证） | ⏳ 挂账 r44→r49（已 2 次延期，下轮按 REPEAT 强制升级） |
| 低 | L-6 | 编号命名空间收敛 | ⏳ P2 看守 |

---

## 4. 可操作实施路径（本轮实际序，全部可复现）

1. 测量：`python 05-exec/r38_debt_aging.py --json 06-benchmark/debt_aging_r47_2026-09-25.json` → `UNDATED 12`。
2. 逐条取数：`git log --format=%ad|%s --date=short -S <锚点前 60 字> -- memory/07-next-steps.md`，取**最后一行**为首现。
3. 逐条落裁决（12 次）：`python 05-exec/r46_mark_verdict.py --key <锚点> --round 47 --verdict "<终局词>｜定年：rNN 登记（git log -S 实测 <日期>）→ <定性>"`
   —— 一次写入同时完成"定年"与"终局裁决"，规避同轮二次写入被拒。
4. 复测：`UNDATED 0`、`DECIDED 58`、`OVERDUE 0`、`REPEAT 0`；`[ADJUDICATION] OK`。
5. 复跑 11 道门 + `[CONTRACT:PASS]` + `[CTRL:CLEAN]` + footer 门禁 → 提交、打 tag `r47`、savepoint。

---

## 5. 结论与不学清单

- 本轮没有新判据，只有**把判据照到的债真还掉**：台账上"看不见年龄"的那一格清零，
  且 12 条的年龄全部来自 git 取数而非我的记忆。
- **X-21（本轮立）**：禁止用「加了裁决标记」冒充「已定年」—— 定年必须有可复现取值命令（`git log -S`），
  否则 UNDATED 只是被 DECIDED 掩盖，盲区换了一层皮。
- 下一步：W-17（契约分代必填）→ W-6（归属可机检）→ M-1（下轮即 REPEAT，须实质交付探针或作废）。
