# 全量对标报告 r58 — 把「谁在跑我的门禁之前改了工作树」算成一维度：动作固定面 + 判据自身的三处假通过

日期：2026-09-26 ｜ 轮次：第 27 轮（r58）｜ 端：QD
本轮定位：**逐字重复触发轮**（指令与 r18…r57 同文）。按 `behavior_core.md#23`「用户命令绝对优先」——
重复命令 = 按原文整跑一轮，**不降格**为「只核验/只出差异」。收口方式沿本仓既定：新开一维 + 承接排队项 + 自抓同类缺陷。

> 引用规范（W-34）：可再生数值写「句柄 → 取值命令」，不抄值。本轮所有对手数字实物 =
> `06-benchmark/supply_chain_face_r58_2026-09-26.json`（生成器 `05-exec/r58_supply_chain_face.py`）。

## 0. 取证纪律声明（consulting-analysis Step 1.05 · M1-M6 逐条过）

标注口径：`✅当次实测（附命令）｜⚠️引用他处（注明取证日）｜❌未核实（不得进结论）`

| # | 判据 | 本轮结论 | 依据 |
|---|---|---|---|
| M1 | 当次实跑 | ✅ 对手四面与本项目改动面全部本轮命令取数；唯一 ⚠️ 引用他处 = 「superpowers/anthropics 无自有 workflow」复用 r56（2026-09-25）tree 面结论，本轮以 contents 404 **独立复核一致** | `r58_supply_chain_face.py` 实跑 2026-09-26 |
| M2 | 同一份集 | ✅ 报告表格与 `supply_chain_face_r58_2026-09-26.json` 同源同批，无跨件借数 | 件内 `generated_at` 与本报告同轮 |
| M3 | 口径同分母 | ⚠️ **须区分两种刻度**：聚合 `53.8%` 是**按引用数加权**（被 spec-kit 一家 484 处拉高），仓库层面是 **3/8 = 100%、1/8 = 38%、4/8 = 0%** 的两极分布。引用本维度时**不得把 53.8% 当作「一半对手做了 pin」**——那是加权假象 | 件内 `repos[].pin_ratio_pct` 与 `face_totals` 双列 |
| M4 | 有判别力 | ✅ 对手在本维度 0%~100% 满量程分布，非饱和指标 | 同上 |
| M5 | 粒度·环境同构 | ✅ ⑦ 分母自证：`usable(8) + blind(2) == 总数(10)` 恒等式落件内 `cross_check`，2 个盲区仓**逐条点名**且原因（目录 404）写明；✅ ⑨ 双通道：「无 CI」由 contents 404 与 r56 tree 面两条互不依赖通道互证 | `05-exec/r58_supply_chain_face.py::cross_check` |
| M6 | 材料追上对象 | ❌→✅ **本轮自抓**：报告写了第 17 维，但读者真正会看的常驻页 `comparison.md` 当时**尚无该维度行**（磁盘有、材料没写）。已回扫补行并同步 M3 的加权口径警示 | 补写见 `comparison.md`「动作固定面」行 |

禁令落地自查：本轮未把「门禁 PASS」当「判据有效」——三处判据自身失效（D-116）全部由人工抽验 + 反例侧抓出，而非门禁自己报的。

## 1. 对比总览表（存量 16 维 + 本轮新增第 17 维；只列有实测变化的行）

| # | 维度 | 对手实况（r58 `gh api` 实测，8/10 仓可测） | 我方 r58 实况 | 差距判定 |
|---|---|---|---|---|
| **17** | **动作固定面（供应链）**（本轮新开） | 外部 `uses:` 引用共 **1,060** 处，其中 pin 到 40 位 commit SHA **570** 处（53.8%）。**两极分化**：spec-kit 484/484、AAS 58/58、mycelium-hq 20/20 = **100%**；vercel-labs 6/16=38%、mem0 2/152=1%；ruflo 0/313、addyosmani 0/9、pre-commit 0/8 = **0%** | 改判前 0/2（`@v4`/`@v5` 浮动）；**改判后 2/2 全 pin**（取值 `python 05-exec/r58_action_pin_guard.py`） | 数量级落后（2 处 vs 对手数百处），但**性质**已对齐；差距见 D-113 |
| 17b | 依赖更新器配对 | `dependabot.yml` **3/8**（spec-kit、ruflo、mycelium-hq） | 本轮从 **0** → 1（`github-actions` 生态，weekly） | 与 pin 成对才算闭环：三家 100% pin 者里 **AAS 无更新器** = 「pin 了没人推」的反例 |
| 17c | 漏洞/密钥防护声明面 | `SECURITY.md` **5/8**；workflow 正文含 codeql/dependency-review/gitleaks/osv 者 **5/8** | 0/1（有意不做，见 X-7） | 对手面存在但**本仓无外部攻击面**（私有归档仓、无包发布、无密钥），登记为设计边界不登记为缺陷 |
| 8 | CI 存在性（复测，双路） | 自有 `.github/workflows` 文件数 **18/1/29/34/9/3/11/2**，API 总计面 **27/5/49/40/15/7/14/3** ⇒ 八家 **face_delta 全部非零**（+9/+4/+20/+6/+6/+4/+3/+1）；`superpowers`、`anthropics/skills` 的 `.github/workflows` 目录**直接 404**（自有 0 文件） | 无对外 CI 语义（X-1 有意）；本机常驻门 **23 道**（本轮 +2） | 双路对账铁律再复证（D-110 未复发，本轮 8/8 一致） |
| 4 | 可扩展性 | 加文件即扩展 | 加声明面须同时交：契约 pattern + 分代豁免 + **生成器** + **生成器可加载**（本轮新立 W-47） | 有意收紧 |
| 13 | 判据可信度 | 零家声明输入面 | 本轮自抓 **3 处判据自身失效**（D-116/D-117/D-118），全部在落盘前被反例侧暴露 | 领先（代价=每轮抓自己） |
| 14 | 债务到期治理 | 仅 spec-kit `stale.yml` | `overdue` / `open_total` 句柄化，本轮台账新增 r58 行 | 领先机制 |

> 取值（对手全部行）：`python 05-exec/r58_supply_chain_face.py --json 06-benchmark/supply_chain_face_r58_2026-09-26.json`
> → 读 `repos[].uses_total / uses_pinned_sha / pin_ratio_pct / dependabot_yml / security_md / vuln_scan_workflows / api_total_face / face_delta`
> 取值（我方）：`python 05-exec/r58_action_pin_guard.py` ＋ `python 05-exec/run_gates.py` 汇总行

## 2. 逐项差距说明

**D-113（P0，本轮已闭环）：未固定的 `checkout` 是「判据输入面劫持」入口，不是安全洁癖。**
本体系全部价值建立在「21→23 道门机器自证」上，而 CI 里决定这些门**读到什么字节**的那一步，
此前按 tag 引用。tag 在 GitHub 侧可被重新指向 ⇒ 一次上游账号失陷就让所有门禁在改写过的源码上判绿，
且回执全绿、无人起疑。这与 r49「判据输入面五面自证与三处劫持根因修」是同一族，只是入口在**仓外**。
改判要点：这不是补一个上游遗漏，而是**推翻 r31/r32 写进 `gates.yml:42-43` 的既定裁定**
（原文「本仓取 tag：动作面只读」）。原裁定在其前提下没错，但它论证的是「不涉及密钥」，
漏了「涉及判据输入面」这条更值钱的链路。处置 = 原裁定文字保留、追加改判理由（R241），
SHA 由 `gh api repos/<a>/git/ref/tags/<major>` 实测取得（不跨大版本，避免 v4→v7 行为漂移）。
配套：**pin 必须配更新器**——钉死而不推进会把「拿不到上游安全修复」固化成常态，比浮动更坏；
这条配对关系由 `action_pin_guard` 的 P2 面看守，而非靠人记得。

> 取值：`python 05-exec/r58_action_pin_fixtures.py`（层 a 9 例 + 层 b 真仓对照/变异体）

**D-114（P1，本轮已修）：常驻页把对手全名写错，抄名者必 404。**
`comparison.md` 第 119/125/148 行写 `mycelium/ai-brain-starter`，而真名是
`mycelium-hq/ai-brain-starter`（`gh api` 实连通，36★，pushed 2026-09-25T22:18Z）。
证据件里用的是**对**的名字（4 处），只有面向读者的常驻页用了错的 ⇒ 本轮我照页抄名，
第一跑就 404，差点把「该仓 workflows 目录不存在」当成实测结论写进表。
同类形态已第 N 次复现：**面向人的展示层与面向机器的证据层脱节**。修法是让展示层只能引证据层句柄。

> 取值：`gh api repos/mycelium-hq/ai-brain-starter --jq .full_name` ＋ `grep -n "mycelium" 06-benchmark/comparison.md`

**D-115（P1，本轮实测推翻前轮方案）：r57 给两条 L2 体量项开的药方（`handoff.py trim-shell`）对二者都不成立。**
`trim-shell` 的操作对象是 `memory/` 下的卷（`TRIM_ENTRY_TARGETS` = 07/05/06），
而 ① 根 `AGENTS.md` 是 `handoff.py` 从 memory/01..09 **装配出的生成壳**，其体量是
`memory/07` 的 P0 面的**函数**（volume 实测：07 主壳 119,610B，大头 `## P0 — 必须做` 70,919B），
② `TODO.md` 是仓根手工索引，压根不在其目标表内。照方抓药的结果是「命令跑通但一件事也没做」。
本轮分治：TODO.md 用专用件 `r58_todo_shell_slim.py` 收（**已落**，见下）；
根 AGENTS.md **不动**，因为它唯一的收敛路径是「逐条人工裁决 07 的 P0 活债」，
而 volume 体检与本仓红线都写明禁自动改 P0 ⇒ 登记为需老大裁定的项，不假装解决。

> 取值：`python <handoff.py> volume <项目根>`（三条发现 → 现应剩 2 条）＋ `python 05-exec/r58_todo_shell_slim.py`（dry-run）

**D-116（P0，本轮自抓并已修，方法论）：我本轮写的**第一版**判据有三处「自身失效」，全靠反例侧才没上线。**
① `r58_action_pin_fixtures.py` 的**对照组**路径少写一层 `.github` ⇒ b1 恒红。若无对照组，
我会把「守卫 11 例全过」写进报告——实际是守卫从来没被真仓输入喂过（R238「用单测掩盖接线错误」第四次实物复现）。
② `r58_py_syntax_guard.py` 首版把 `doraise=True` 传给 `compile()`（那是 `ast.parse`/`py_compile` 的参数）
⇒ **99/99 全判不可解析**，一道纯粹的假红门；反例目录恰好也「红」，若只看反例侧就会误判它工作正常。
③ `r58_generator_coverage.py` 首版把路 C 的命中排除在严格档之外（`elif WRITE_RX.search(src) and (hit_a or hit_b)`）
⇒ 补的那一路根本不进判定，白补。
三条共同点：**判据的「红」和「绿」都可能是假的，只有同时握有「应绿的绿」与「应红的红」两个实测才叫证据**。
本轮为此新增的元教训固化为 W-47。

**D-117（P0，本轮已修）：已入库的计量工具本身不可解析，而 21 道门没有一道会因为「工具加载失败」而红。**
`05-exec/evidence_resolvability_measure.py:111` 少一个 `print(` 右括号 ⇒ 该件自 2026-09-24 入库起
就**跑不起来**，它名下的 `06-benchmark/evidence_resolvability_measure.json`（13.7KB）
因此是「生成器根本加载不了」型死句柄。W-46 问「pattern 有没有生成器」，问不出这一层。
按「修同类不修单例」：不是补那一个括号（已补），而是把「`05-exec/*.py` 全部可被 `compile()` 解析」
钉成常驻门 `py_syntax_guard`（99/99 绿；反例目录红；空面红）。
同一判据在本轮还救了我两次——D-116② 与 `r58_todo_shell_slim.py` 的首版语法错都是它抓出来的。

**D-118（P1，本轮已修）：W-46 设想的「逐 pattern `grep -rl <前缀> 05-exec/*.py`」若真接线，误报率 30.6%。**
本轮把该判据**先做成测量**（`05-exec/r58_generator_coverage.py`，三路取数 + 夹具剔除 + 写盘面证据）：
宽口径命中 62 处 → 严格口径 38 处，**19 处（30.6%）会被误记为「有生成器」**——被误记者正是
夹具与只读校验器（`baseline_contract_scan.py`/`run_gates.py` 读件名自证，却不生产件）。
反向也存在：首版严格口径把 `rule_conflict_scan_*`（生成器靠 `--json <path>` 传名、源码无字面量）
与 `attention_sim_raw_*`（生成器在受管根外 = `焚诀/audit/attention_sim.py --json` + 重定向）
误判成死句柄，故补路 C（同名工具）与外部根探测。**接线结论 = HOLD**：
现存真死句柄 **1 条** —— `noise_falsepositive_*.json`（r37 的修前/修后对照件，
全库 `grep -rln "falsepositive"` 在 05-exec / 焚诀 / `D:\global_skills` 三处**均零命中**）。
它清空之前接线，等于让门禁天天为一条历史事实判红，随后必然被「放宽判据」绕开（X-9 反面）。

> 取值：`python 05-exec/r58_generator_coverage.py --json 06-benchmark/generator_coverage_r58_2026-09-26.json`
> （读 `cross_check.loose_hits / strict_hits / false_positive_pct / dead_handles`）

**句柄漂移 `[HANDLE:DRIFT] ['open_total']` 的归因（按「未归因不得写成边界声明」）：**
本轮开场 `r38_debt_aging.py` 即报 DRIFT。逐字段对账 = 台账末行 `open_total` 停在 **2026-09-25T19:47（r56）**，
而当场实测 `open_total` 已因 r57 的 3 条 VOL + 本轮新增项而变大 ⇒ **不是缺陷，是同轮一致性守卫在正常工作**：
它拒绝让报告引用一份「上一轮的账」当本轮数。收口动作 = 本轮落账一行（已随本报告执行），
判据本身不动。**结论可信度 ✅已实测**（两值均由命令当场取出，非记忆）。

## 3. 改进建议清单（含优先级与状态）

| 编号 | 优先级 | 建议 | 状态 |
|---|---|---|---|
| D-113 | P0 | CI 动作 pin 到不可变 SHA + 配 dependabot + 常驻判据（P1/P2/P3 三不变式） | **本轮执行完毕**（`gates.yml` 2/2 pin、`dependabot.yml` 新建、新门 `action_pin_guard` 桩 11/11） |
| D-114 | P1 | 展示层对手名必须可解析（错名 404） | **本轮已修**（`comparison.md` 校正注 + 生成器内禁抄名注记） |
| D-115 | P1 | 推翻 r57 的 `trim-shell` 药方，改分治：TODO.md 收 / 根 AGENTS.md 呈人工 | **TODO.md 已落**（VOL-D908D done）；**根 AGENTS.md 未动**（拦在「禁自动改 P0」，转 W-48 呈裁定） |
| D-117 | P0 | 「工具面可加载」进门禁（同类不修单例） | **本轮执行完毕**（第 23 门 `py_syntax_guard`，99/99 + 反例 + 空面三向） |
| D-118 | P1 | W-46 接线判据 | **本轮量完误报率并 HOLD**（30.6%；真死句柄 1 条待清；取值 `python 05-exec/r58_generator_coverage.py`）→ 新立 W-46 续账 |
| W-47 | P1 | 新立铁律：**任何新判据首跑必须同时交「应绿的绿」与「应红的红」**，且对照组自身须被证伪过一轮（本轮三处实物） | 登记并本轮起自适用 |
| W-48 | P1 | 根 `AGENTS.md` 29,293B 超限，唯一收敛路径 = 逐条人工裁决 `memory/07` 的 P0 活债（70,919B）→ 请老大裁 | 呈裁定 |
| W-46 | P1 | 清 `noise_falsepositive_*.json` 死句柄（补生成器或从契约摘牌）后再接线 | 续账 |
| W-45 | P2 | A-skill-manager 版本行同步 | **未动**：本轮实测 `SKILL.md` 与 `version-history.md` 仍双双 ` M`（并行会话未让位） |
| W-40/W-41 | P1 | 受管根工具行为变更（savepoint 项目根进面 / commit 后定向镜像） | 本轮实测 `A-project-handoff` 已**清洁无在途** ⇒ 前置条件成立；仍未落码（改门禁自身 + 需领 R 号，而 W-44 已证跨项目撞号是结构性漏检，缺号段安全保证时不动共享工具） |
| W-27/W-32 | P1 | 极性冲突语义裁定 / 注入面超顶归属 | **未动**，仍拦在红线与归属（受管根占大头） |
| W-39/W-42/W-44 | P1-P2 | 备份落点对立 / 镜像是否继续做物理副本 / R 号跨项目登记面 | 待裁定，本轮不动 |
| X-7 | — | `SECURITY.md` 与漏洞扫描 workflow 不做 | **有意不做并登记边界**：私有归档仓、无包发布、无密钥面；做了即制造无人消费的产物 |

## 4. 可操作实施路径（下一轮起，按序）

1. **W-48 请老大裁**：`memory/07` 的 P0 活债 70,919B 逐条裁决（可销账者迁 05/归档），这是根 `AGENTS.md` 降到 16KB 以下的唯一路径。
2. **W-46 续账**：给 `noise_falsepositive_*.json` 补生成器（或按 R241 从契约摘牌并留注），随后把「pattern 必须有生成器」正式接进 `run_gates` 判红。
3. **W-40/W-41**：前置已清洁，若某轮决定落码，须先解决 W-44 的跨项目领号安全（否则撞号概率非零）。
4. 第 17 维纵深：把「仓外输入固定」推广到**受管根 clone/镜像**面（我们的 21 道门读的是本地字节，与 CI 读 checkout 字节同构），这是本维度在本体系里的真正主场。

## 5. 末态快照（句柄优先）

| 项 | 值 | 句柄 / 取值 |
|---|---|---|
| 常驻门 | **23 道**（本轮 +2） | `python 05-exec/run_gates.py` 汇总行 |
| 契约 | 18 pattern（本轮 +2） | `python 05-exec/baseline_contract_scan.py --quiet` |
| 棘轮 | 9 项在基线内 | `python 05-exec/ratchet_gate.py` 末两行 |
| 注入面 | 见告警行（超顶项未动） | `python 05-exec/ratchet_gate.py` `inject_union_bytes` |
| 债务 | `overdue` / `open_total` / `decided` | `python 05-exec/r38_debt_aging.py` 「可引用句柄」行 |
| TODO.md 注入壳 | 迁移量与新壳字节 | `python 05-exec/r58_todo_shell_slim.py`（dry-run）＋ `volume` 体检 |
| 动作固定面 | 我方 pin 比例 / 对手 1,060 处引用 | `python 05-exec/r58_action_pin_guard.py` ＋ 供链件 |
| 受管根 | 四门状态 | `python D:/global_skills/A-memory-start/references/rule_editor.py gates` |

结论可信度：对手面与本项目改动面 ✅已实测（每条附命令）；「源头在排程侧」⚠️沿用 r21 推断，本轮未重测；
「`SECURITY.md` 对本仓无价值」⚠️为判断而非测量，已按 X-7 显式标为边界，供推翻。
