# 全量对标报告 r40 — 还债轮：延期到期追讨首次真响 + L-4 归属反转

日期：2026-09-25（第 40 轮）｜触发：同一指令第十次逐字下达 ⇒ 按 #23 整跑一轮
本轮按 r39 自己立的 **W-5 约束：不开新维度**，只做三件：W-4（延期独立棘轮）、L-4（本仓自持死条目）、M-1（可测性判定）。

---

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖范围 | 债务治理补到「到期追讨 + 延期独立计量 + 输入面自证」 | `spec-kit` issue 生命周期 27 workflow | 我方少自动流转，多**到期回判**（对手无） |
| 2 | 技术架构与实现方式 | 第 8 指标 `deferred_debt_items` 与第 7 指标共用 `_debt_class_state` 单源读取器（同等 fail-closed） | `pre-commit` 单一 config 源 | 相当；本轮顺手消除两态逻辑重复 |
| 3 | 性能表现 | 10 道门合计 ~8s；账龄尺 ~2s | — | ❌未跨机实测 |
| 4 | 可扩展性 | 新分类态 = 一处 `TAXONOMY` + 一处不变式；契约代际豁免防历史被回溯判红 | `aas` schema 版本化 | 本轮补强（D36） |
| **5** | **维护状态 → 本轮聚焦** | **延期到期后 3 条自动回判 OVERDUE**（机器逼裁决，第二次真响） | superpowers/anthropics/skills/pre-commit 四家 0 条到期自动化 | **可宣称领先这 4 家**；spec-kit 仍领先我方（自动化面） |
| 6 | 文档完善程度 | 每条裁决带**实测取值或阻塞原因**；本轮把 4 处判据缺陷写成 D31–D37 | 对手 README/CONTRIBUTING | 相当 |
| 7 | 适用场景 | 07 为跨会话唯一入口，且实测**存在并发整卷重写**（新交付记忆写纪律同族） | 对手面向公共贡献者 | 我方须用 append/patch + 读回，禁整卷 Write |

---

## 2. 逐项差距说明（全部实测）

- **D35 · 「延期」此前无人看守**：r39 把 22 条超期压到 0，同一步让 DEFERRED 从 9 涨到 23 —— 账面干净、债务换格子。
  本轮立第 8 指标。落地即咬人：本轮自己新增 `挂账至 r42` 两条 ⇒ `deferred_debt_items=25 > 基线 23` 变红。
- **D36 · 新不变式会回溯误杀历史产物**：`conflict_no_dead_inputs` 首跑把 r24 那份**如实记录了死条目**的历史件判红。
  按 R241 加「代际豁免」（无 `files_declared` 字段 ⇒ 不适用），判据只约束会写自证字段今后产物。
- **D37 · L-4 归属反转（本轮最重）**：`rule_conflict_scan.py` 实测**就在本仓 `05-exec/`**，
  而它在 07 里被标成「归属方件 L-4」挂了 4 轮 ⇒ **自家债务被错误外部化，永远不会有人做**。
  实测还发现：扫描器本身**早已透明打印** `文件 9/10 存在` —— 缺的是**契约不看这一项**，
  所以 `rule_conflict_scan_*.json` 长期带死条目仍 `[CONTRACT:PASS]`。
- **D38 · 我的 M-1 前置条件不可测**：要求「先量近 20 次真实编辑的 CONFLICT 误报率」，
  但 git **不记录提交时的脏树**（`git stash list` = 0 条，历史无从回溯）⇒ 该判据在历史上不可能被证实。
  诚实处置：**改为前瞻影子计数**（不阻塞任何写入，遵守 r25 否决），并把 M-1 从"待接线"改为"待攒样本"。
- **D39 · 我自己的夹具写出两处错**：① 首版夹具断言"扫描器静默少扫"，实测**它已透明上报**（前提错，测试对象改）；
  ② 修 `files_declared` 时先插错层级（顶层而非 `input_evidence` 内），跑 t4 才发现没进判据面。
- **D40 · 并发整卷重写**：给 07 一条待办打标记后，账龄尺仍读回未标状态，逐次 readback 才确认被覆盖
  （与本轮新到的「共享记忆写纪律」同源）。⇒ 共享记忆文件**只用 append/patch + 立即读回**，禁整卷 Write。

### 反例（判据没有放松）
- t4：`files_declared=11 / files_present=10` 的合成件被契约点名 `CONFLICT-INPUT`；
- t23：延期堆从 23 涨到 30 时第 8 指标如实跟涨；
- t25：五态之和 != `open_total` 时两态**一起**返回 None（脏证据不得单独放行）；
- 棘轮对「两项同时长大」直接 `[RATCHET:FAIL]` 并拒绝上调：`多项同时长大…一条归因不足以解释（判据不为人心情让路）`。

---

## 3. 改进建议清单（含优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| **高** | W-4 | DEFERRED 独立第 8 指标（只降不升 + 同等 fail-closed） | ✅ 落地，夹具 t20–t25 |
| **高** | L-4 | 死输入条目改指真实在役件 + 契约不变式 `conflict_no_dead_inputs` + 第 10 道门 | ✅ 落地（`文件 10/10 存在`） |
| **高** | W-8 | 修「已带 `裁决=保留` 仍报 OVERDUE」的判据不稳定（含并发重写与去重交互） | ⏳ **下轮首推 P0** |
| 中 | M-1 | 写时冲突检查改**前瞻影子计数**（攒够 20 样本再决定接线） | ✅ 结论落地（历史不可测已证） |
| 中 | W-6 | 待办「归属声明」可机检：条目若写"归属方/转办 X"，须能解析到该归属方的真实文件路径 | ⏳ 新立（由 D37 提炼） |
| 中 | W-1 | 13 条 UNDATED 定年 | ⏳ 挂账至 r42 |
| 低 | D36 后续 | 契约代际豁免机制推广到其它不变式 | ⏳ 待办 |

---

## 4. 可操作实施路径（本轮实际序 + 复现）

1. RED→GREEN：`python 05-exec/r39_debt_ledger_fixtures.py`（先 5 红 → 后 24/24 绿；含删掉我自己写的恒真测试 t24）
2. RED→GREEN：`python 05-exec/r40_scan_inputs_fixtures.py`（先 3 红 → 后 5/5 绿）
3. `python 05-exec/rule_conflict_scan.py --json 06-benchmark/rule_conflict_scan_r40_2026-09-25.json` → `文件 10/10 存在`
4. `python 05-exec/baseline_contract_scan.py --quiet` → `[CONTRACT:PASS] 17 份 / 13 pattern`
5. `python 05-exec/r38_debt_aging.py --json … --ledger 06-benchmark/debt_runs.jsonl` → 趋势台账累积 5 行
6. `python 05-exec/ratchet_gate.py` → **`[RATCHET:FAIL] 2 项`（overdue +1 / deferred +2）—— 本轮如实留红**，
   理由：上调需逐项归因，overdue 那 +1 是 D40/W-8 判据不稳定造成，不该用归因通道掩盖。
7. `python 05-exec/run_gates.py` → `PASS=10`（棘轮红由 W-8 承接，下轮修判据后自然转绿）

---

## 5. 结论与不学清单

- 本轮真正的进步不是"又造了一把尺"，而是**尺子开始咬我自己**：到期追讨把 3 条空挂项打回 OVERDUE、
  第 8 指标立刻因我的延期变红、棘轮拒绝打包归因。
- **X-14（本轮立）**：禁止把待办标成"归属方件"而不给**可解析的真实路径**——本仓已实证一次自家人债挂 4 轮。
- 下一步：W-8（判据稳定性）→ W-6（归属可机检）→ W-1（13 条定年）→ M-1 影子样本攒够再决策。
