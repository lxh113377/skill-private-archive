# P0-C 累积漂移人工复核 · 第 2 批（r19，2026-09-24）

> 范围来源：`05-exec/cumulative_drift_scan.py --days 30 --threshold 5` 的**规则/技能类**候选清单，
> 本批取 30 天改写次数最高的三个技能正文：`A-project-handoff/SKILL.md`(17) /
> `A-memory-start/SKILL.md`(11) / `A-project-better/SKILL.md`(10)。
> 判据用 mycelium-hq/ai-brain-starter `drift-detection.py` 的六态口径（禁只写「已看过」）：
> `drift_detected`（意图/守卫/ canonical 断言发生了实质位移）· `softened`（守卫被放松）·
> `refinement` · `additive` · `neutral_rephrase` · `uncertain`。
> 第 1 批（07 主卷 + 8 分卷）见 `P0-C_累积漂移复核第1批_2026-09-24.md`。

## 一、逐条判定（11 条承重句）

| # | 文件:行 | 承重句（摘要） | 取值命令（复跑） | 实测 | 判定 |
|---|---|---|---|---|---|
| 1 | handoff/SKILL.md:17 | `version: 3.52.1` | `python 焚诀/eval/verify_truth_consistency.py`（C13 行）+ `grep -m1 version .../SKILL.md` | C13 PASS；r17b 受管根 `46d9b31` | ✅ 仍然成立（`refinement`） |
| 2 | handoff/SKILL.md:76 | 「19 个子命令完整用法」枚举 | `python A-project-handoff/scripts/handoff.py --help` 取 Commands 段，与 L76 枚举做双向差集 | 声称 19 项 / 实际 19 项，`comm` 双向零差集 | ✅ 仍然成立（本批唯一「集合全等」级复核） |
| 3 | handoff/SKILL.md:73 | 「**20 条**致命纪律 + **14 条**重要规则」 | `python -c` 按 `^\d+[.)] ` 分节计数 `references/disciplines.md` | 实测 **22 条 + 15 条**（+2 / +1） | ❌ **已失真（`drift_detected`）** |
| 4 | handoff/SKILL.md:52 | 「先验 #20 四步基线」 | 同上（致命纪律编号域） | 现 22 条，#20 仍在域内且条文可读 | ✅ 仍然成立 |
| 5 | handoff/SKILL.md:63 | 回退解释器 `...binaries/python/versions/3.13.12/python.exe` | `Test-Path` 等价：`test -f` | EXISTS | ✅ 仍然成立 |
| 6 | memory-start/SKILL.md:32 + :15 | H1 与 **frontmatter description** 均写「V9.7.0 瘦身版」 | `grep -n "V9\.7\.0" .../SKILL.md`；`grep -n "^version" .../SKILL.md` | 实际 `version: 10.67.0`；相差 20 个 minor，**且该串在 description 内 = 每轮随目录注入** | ❌ **已失真（`drift_detected`）** |
| 7 | memory-start/SKILL.md:60 | G2 平台枚举 `<HM/WB/CX/TC/ZC>` | `python 05-exec/claim_truth_scan.py --json ...` | 真值 8 端，缺 `oc/qd/qw` | ❌ **已失真（`drift_detected`）** |
| 8 | memory-start/SKILL.md:131/135/139 + contract.md:10/85 | 「五端统一 / 五端生效 / 五端共享 / OC 已退役 2026-09-21」 | 同上 + `truth_constants.json` `endpoints.active` | 真值 8 端；`oc` 代号 09-23 由 **OpenCode 接管复用**（OpenClaw 本体仍退役）、`qw`/`qd` 09-24 入役 | ❌ **已失真 5 句（`drift_detected`）**；「OC 已退役」属**代号语义被复用而文本未跟**的高危形态 |
| 9 | memory-start/SKILL.md:92 | 「七步闭环统一读 workflow_seven_step.md（V1.1…）」 | `grep -m1 version 焚诀/../workflow_seven_step.md` + C11 行 | C11 PASS（规范 V1.1 存在、八端脚本齐全） | ✅ 仍然成立（`neutral_rephrase`） |
| 10 | project-better/SKILL.md:3 + :55 | 「破坏性按备份+Git 双硬前提自动批准（用户 2026-09-24 立规）」+ 版本历史称「behavior_core **#23 锚点**」 | `grep -rn "#23" D:/global_memory/core/behavior_core*.md`（输入非空：6 个 behavior_core* 在盘） | 立规句**在 A-project-better 侧成立**（本次任务即其适用场景）；但 `#23` 在 `core/` 全量 grep **0 命中** ⇒ 双层留痕只做了一半 | ⚠️ `uncertain`——**归属会话在途未收口**（该 skill 及其 3 个 references 当前为 `M`），按 R269 只登记不擅动 |
| 11 | project-better/SKILL.md:45/74 | 「Step1 默认搜 GitHub 同类（可跳过）」「V1.7.0 对标默认开」 | 本轮即为该步执行体（gh api 14 对象实时取数） | 机制真实在跑 | ✅ 仍然成立 |

**小计**：仍然成立 7 句 · 已失真 2 类共 **7 句**（含每轮注入面 6 句）· `uncertain` 1 句（在途归属）。

## 二、本批最大结构性发现（不是句子级，是门禁面级）

`C1~C28` 里没有任何一条比对「**技能/规则正文里的计数与枚举断言 == 真相源**」：

| 现有判据 | 覆盖面 | 为何漏掉本批 7 句 |
|---|---|---|
| C3 活跃端 == `platform_tiers` | 焚诀内部两件**派生件自比** | 不含技能正文 |
| C13 frontmatter 版本 == 版本历史 | 只比版本号 | 不比 H1 标题、不比 description 里的版本串 |
| C16 关键产物内容 == 真相源 | STATUS/scorecard 等**产物** | 外推不到 SKILL.md |
| C25 注入硬预算 | 只量**字节数** | 内容错但字节合规 ⇒ 恒 PASS |

⇒ 失真可以长期住在「每轮注入」的文本里而不被任何棘轮发现——这正是 DELEGATE-52 说的「20 次改写后约 25% 内容损坏」在本体系的存活通道。

**已落地的兜底（本项目自持，零红线冲突）**：`05-exec/claim_truth_scan.py`
（三族判据 + R241 历史行豁免 + R247 输入面非空硬门；首跑 **10 条候选**，其中 7 条即本批复核句，
另 3 条为 `A-get-memory`×2 与 `A-skill-manager` description×1 的「四端」表述）。
夹具：`05-exec/r19_scan_fixtures.py` 层 a 9 例 + 层 b 5 例（含「真相源不可读 ⇒ 禁止判零失真」）。

## 三、转焚诀归属会话（红线：本项目不代改 `eval/`）

| 建议 | 内容 | 已备证据 |
|---|---|---|
| **C29′** | 目录注意力税双棘轮（r18 P0-A 原样维持，未落地） | r18 `catalog_attention_tax.py`；本轮复测 45,173 字符（零漂移） |
| **C30′** | 注入区口径合一（r18 P0-B 原样维持） | r18 实测并集 69,494B > 硬顶 65,536B 而 C25 判 PASS |
| **C31′（本批新增）** | 「计数断言 == 真相源」内容级门禁：端数 / 技能数 / 枚举集合，豁免历史留痕行 | 判据与 10 条候选全在 `06-benchmark/计数断言基线_2026-09-24.md`；本仓工具可直接移植（约 200 行含三族） |

## 四、下一批（P0-C-3）范围

按同一扫描器口径续做余下 7 条规则类候选：`A-skill-manager/SKILL.md`(8) /
`memory/06-constraints.md`(10，本仓) / `global_memory/memory/07-next-steps.md`(8) /
`A-get-memory/SKILL.md`(5) / `A-memory-start/references/contract.md`(5) /
`prompts/workflow_seven_step.md`(5) / `memory/07-next-steps.md`(37，本仓主卷第 2 次全文复核)。
并建议把「失真 7 句」的整改与**派生件重建窗口**合并执行（description 一改就要重建 registry/BGE/skill_content，
r17b 已登记该窗口未开，拆成两次做会双倍成本）。
