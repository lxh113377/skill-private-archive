# 全量对标报告 r42 — 还债轮第三击：OVERDUE 反弹根因定位（结论：不是 bug，是流程缺陷）

日期：2026-09-25（第 42 轮）｜触发：同一指令第十二次逐字下达 ⇒ 整跑一轮。延续 W-5：**不开新维度**。
本轮按 `systematic-debugging` 四阶段做，**Phase 4 实现未落地**（见 §4），如实挂红。

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖 | 债务治理已有 5 态 + 趋势台账；缺 REPEAT（反无限延期）与分档宽限 | `spec-kit` issue 标签流转（label→assess→fix） | 我方多到期语义、少优先级语义 |
| 2 | 技术架构 | 账龄与棘轮单源（`_debt_class_state`）、last-wins 裁决读取（r41 修） | `pre-commit` 单配置源 | 相当 |
| 3 | 性能 | 10 道门 ~9s；账龄尺 ~2.4s（含 20 次 `git log -S`） | 对手无可比物 | 未跨机实测 |
| 4 | 可扩展性 | 新态需改 3 处（TAXONOMY / 不变式自洽 / 台账行），已被夹具锁死 | — | 本轮以测试固化 |
| 5 | **维护状态（本轮聚焦）** | `OVERDUE 20`：14 条 = 我 r39 写的 `挂账至 r41` **如期到期**；6 条 = 上轮漏裁、账龄涨过宽限 | `anthropics/skills` 1290 open 最老 344 天、`superpowers` 401 / 241 天、`agent-skills` 118、`pre-commit` 25（最老 8 年）四家 **0 到期自动化**；`spec-kit` 到期即**关闭** | 到期治理面领先 4 家；**优先级分档面落后 spec-kit 的 label 分级** |
| 6 | 文档 | 本轮把上一轮报告里「压到 1」的说法公开限定为**单次快照**，并给出复现命令 | 对手 changelog | 相当 |
| 7 | 适用场景 | 共享记忆卷存在并发整卷重写（本轮实测 07 被外部改写致行号漂移） | 对手无此约束 | 我方独有风险，已入约定 |

## 2. 逐项差距说明（实测）

- **D46 · 反弹不是 bug**：`current_round` 取 git log 最大 rNN，r41 提交后 40→41 ⇒ 15 条 `挂账至 r41` 集体到期（归因统计：目标 r41 = 14 条）。这正是 r39 设计要的效果。
- **D47 · 真正的缺口是流程**：我 r41 只裁「当期到期清单」，没裁「账龄新越线清单」⇒ r38 自立的 W-0~W-3 / L-6 共 6 条默默涨过宽限。**裁决必须覆盖全量 OVERDUE，而不是我记忆里那批。**
- **D48 · 延期可以无限滚**：同一条目改期 2 次、3 次在账面上一路 `DEFERRED → DEFERRED`，趋势线看不出恶化（r39 那批 15 条即是二期）。缺 `REPEAT` 显形。
- **D49 · 宽限期不分优先级**：统一 2 轮 ⇒ P2 长期项与 P0 同罪，噪音会稀释 P0 紧迫感（W-2 自 r38 起就在册，本轮被自己判成 OVERDUE，是活证据）。
- **D50 · 我上一轮的措辞过强**：「OVERDUE 10→1」只对那次快照成立。本轮公开限定，并按 R241 加校正注而非改写。

## 3. 改进建议清单（优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| 高 | W-11a | 新增 `REPEAT` 态：同一条改期 >=2 次单列，强制升级（执行 / 降级为长期看守 / 作废） | ⏳ 测试已写（t30/t32），实现未落地 |
| 高 | W-11b | 宽限分档 `P0:2 / P1:4 / P2:8`，值放常量表可读 | ⏳ 测试已写（t33/t34） |
| 高 | W-12 | **裁决清单完整性判据**：本轮裁决条目数必须 == 上轮 OVERDUE 数（缺一条即红），把 D47 变成机器约束 | ⏳ 新立 |
| 中 | W-9 / W-10 | 残留 1 条假红；上游节名锚定缺陷转办 | ⏳ 挂账 r44 |
| 中 | W-1 | 13 条 UNDATED 定年 | ⏳ 挂账 r44 |
| 低 | L-6 | 编号命名空间收敛（X/M/H/W 已三次撞车） | ⏳ P2 |

## 4. 可操作实施路径（含本轮未完成项）

1. Phase 1 取证：按 `挂账目标 rNN` 对 20 条 OVERDUE 归因（14 目标 r41 / 6 无标记），并用 `split(chr(10))` 与 `splitlines()` 比对行号（153 全等）→ **排除**「拆卷丢标记」「正文替换扰动」两个假说。
2. Phase 2/3：与工作样例对照（r41 到期 3 条那次为何没爆量）→ 假设「延期无限滚 + 清单不完整」，最小验证即看同一行是否含多个 `挂账至`（实测存在 3 期样本）。
3. Phase 4（**未完成**）：`python 05-exec/r39_debt_ledger_fixtures.py` → 已红 t30/t32/t33/t34（**28/32**）；实现补丁在写入前断言失败中止（heredoc 把正则反斜杠吃掉），**未产生半成品代码**，也未谎报转绿。
4. 下一步（r43 首件）：用 Edit 工具直接落 `REPEAT` + `GRACE_BY_PRIORITY`（避开 heredoc 转义），夹具转 32/32；再落 W-12 完整性判据。

## 5. 结论与不学清单

- 本轮的净收益是**把一次误判变成三条机器约束**（W-11a/W-11b/W-12），并且**允许自己空手而归**：实现没落地就写 28/32，不写 27/27。
- **X-16（本轮立）**：禁止「只裁当期到期清单」——裁决必须覆盖全量 OVERDUE；否则漏裁项下一轮会以 2 倍数量回来（本轮实测：1 → 20）。
