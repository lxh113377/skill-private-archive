# 全量对标报告 r41 — 还债轮第二击：W-8 真根因（裁决标记必须 last-wins）+ H-5 结构归位

日期：2026-09-25（第 41 轮）｜触发：同一指令第十一次逐字下达 ⇒ 按 #23 整跑一轮
延续 r40 的 **W-5 约束：不开新维度**，本轮只做两件：修 W-8、清到期债。

---

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖范围 | 债务治理链补上「同一条目多次裁决的时间序」语义（last-wins） | `spec-kit` issue 生命周期（评论/事件按时间序覆盖状态） | 此前我方**语义缺失**，本轮补齐一处关键语义 |
| 2 | 技术架构与实现方式 | 判据读取由 `search()`（取首个）改 `finditer()[-1]`（取最新），与本仓 append-only 台账口径对齐 | `pre-commit` 配置合并语义 / `gate_runs.jsonl` last-wins | 同族一致化 |
| 3 | 性能表现 | 10 道门合计 ~9s（新增两态测试）；账龄尺 ~2.4s | — | ❌未跨机实测 |
| 4 | 可扩展性 | 一条目多标记场景下分类可重放；测试 t26/t27 双向锁死 | — | 本轮补强 |
| **5** | **维护状态 → 本轮聚焦** | **到期一次触发 11 条回判**（轮号 39→40 让整批 `挂账至 r40` 到期）→ 经修根因 + 逐条裁决压到 **残留 1 条** | superpowers/anthropics/agent-skills/pre-commit **0 条**到期自动化 | 到期治理面可宣称领先这 4 家；spec-kit 自动化面仍领先 |
| 6 | 文档完善程度 | 本轮把上一轮的**误诊**公开纠正（R241 加校正注，不改写） | 对手 changelog 惯例 | 相当，且我方有机器证据 |
| 7 | 适用场景 | 实测**并发整卷重写**会吞掉我的记忆标记（新到记忆纪律同族），共享卷须 append/patch + 读回 | 对手无此约束 | 我方独有风险，已入判据与约定 |

---

## 2. 逐项差距说明（全部实测）

- **D41 · 上一轮我把"尺子正常工作"误判成 bug（本轮纠正）**：r40 报告写「07 已带 `裁决=保留` 仍报 OVERDUE ⇒ 判据不稳定」。
  实测真因是**轮号推进**：`current_round` 取 git log 最大 rNN，r40 提交后由 39→40，
  于是整批 `挂账至 r40` 如实到期 —— 这正是 r39 设计要的效果。误诊的代价是我把一天产能指向了错方向。
- **D42 · W-8 的真根因：标记读取取首个**。一条待办一生会被多次裁决，标记是**追加**在同一行里的；
  `RE_DECIDED.search()` 拿到的永远是**最早那条**（常见是已过期挂账）⇒ 新的终局裁决被旧挂账永久劫持。
  夹具 t26/t27 从**两个方向**验证（旧挂账 + 新作废、旧作废 + 新挂账），改 `finditer()[-1]` 后 27/27 绿。
- **D43 · 结构性债 H-5 清掉**：`07-next-steps.md` 的 `## P0` 节内混着 **15 条轮次摘要**（r28 起 10 轮 += 后续追加）。
  本轮整段归位到「最近对话摘要」：总行数守恒（仅多 1 行分节空行）、P0 节内残留摘要 0、待办计数 60 条一条未删
  （守 r40 立的原话：**禁止为凑判据形状删待办**）。
- **D44 · 并发整卷重写**：给某条目打完 `r41 裁决` 标记后再测仍 OVERDUE，取证显示该行"出现 1 处、补标 0 处"
  —— 标记在、判据读不到，与并行会话重写共享卷相互作用。**本轮不强行抹绿**，残留 1 条如实挂红并立 W-9。
- **D45 · 棘轮基线仍守 0**：`overdue_debt_items=1 > 基线 0` 报 `[RATCHET:FAIL]`。
  本轮**未使用** `--raise-baseline` 通道：r40 已实测多项打包归因被拒，且这一条是待修判据缺陷而非真实进度回退，
  上调等于用归因掩盖 bug。

### 反例（判据没有放松）
- t26/t27：双向锁死 last-wins；t28：目标轮 == 本轮即到期（不给额外宽限）；
- `deferred_debt_items` 由 25→22 如实下降（第 8 指标本轮第二次生效）；
- 台账累计 8 行，脏测量值一行未进（append_ledger 三重拒写）。

---

## 3. 改进建议清单（含优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| **高** | **W-8** | 修「多标记行取首个」→ last-wins，双向夹具锁死 | ✅ 已修（27/27） |
| **高** | **H-5** | 07 摘要与 P0 混排归位（15 条），行数守恒 + 待办不删 | ✅ 已执行并勾销 |
| **高** | **W-9** | 残留 1 条已带 `r41 裁决` 仍判 OVERDUE：疑与并发整卷重写 / 行内标记次序有关 | ⏳ 下轮 P0，复现：`python 05-exec/r38_debt_aging.py --json /tmp/x.json` |
| 中 | W-6 | 归属声明可机检（条目写"归属方/转办"须能解析真实路径，否则记 UNOWNED）—— 防 r40 的 D37 自家债外部化 | ⏳ 待办 |
| 中 | W-1 | 13 条 UNDATED 定年 | ⏳ 挂账至 r44 |
| 中 | M-1 | 前瞻影子计数探针（接线前置） | ⏳ 挂账至 r44 |
| 低 | D41 后续 | 轮号推进会造成"到期洪峰"，考虑给 `current_round` 变更加提示（不改变判据） | ⏳ 待办 |

---

## 4. 可操作实施路径（本轮实际序 + 复现）

1. **先证伪自己的上一轮结论**：`python -c` 比对 split(chr(10)) 与 splitlines() 行号 → 完全一致（153 行），
   排除"行号错位"假说；再看 `轮号基准 = r40` ⇒ 到期洪峰是真因（D41）。
2. **RED**：`python 05-exec/r39_debt_ledger_fixtures.py` → t26/t27 两红（旧挂账劫持新作废；反向同理）。
3. **GREEN**：`classify_item` 改取最后一个匹配 → **27/27**。
4. **H-5 归位**：脚本按 `^- \*\*20\d\d-…（第 N 轮` 识别摘要行，从 P0 节整段搬入摘要节；
   守恒校验：`总行数 153→154（+1 空行）`、`P0 节前 60 行残留摘要 0`、`grep -c '^- [ ]'` = 60 不变。
5. **逐条再裁 9 条到期债**：3 条 `作废`（H-3 / H-3″ / r31 旧 M-2M-3 编号撞车案），
   6 条 `挂账至 r44` 并各带实测取值（A-skill-manager 仍 2 条在途、需第二台机、需真实 Hermes 会话等）——
   **没有一条按"看着像完成"划掉**。
6. 复测：`OVERDUE 10 → 1`、`DECIDED 12 → 15`、`DEFERRED 25 → 22`；`[GATES:…]` 见 §5。

---

## 5. 结论与不学清单

- 本轮最大价值不是又修一个 bug，而是**公开纠正上一轮的误诊**：我把机制的正常输出当成判据缺陷，
  差点用 `--raise-baseline` 把它"归因"掉。判据的意义在于允许自己被推翻。
- 当前账面（取值命令均已写进条目）：`OVERDUE 1 / ACTIVE 8 / DECIDED 15 / UNDATED 13 / DEFERRED 22`；
  `[GATES:FAIL] 1`（ratchet overdue=1 > 基线 0）**如实留红**，`[CONTRACT]`/`[CTRL]`/夹具 27/27 全绿。
- **X-15（本轮立）**：判据读"同一实体的多次状态"时**必须 last-wins**；取首个 = 让最早的那次决定永久生效。
  同族先例：`gate_runs.jsonl`、`ac-verdicts.jsonl`、memory-eval 台账全部是 append-only last-wins。
- 下一步：W-9（抹掉最后 1 条假红，不许用归因通道）→ W-6（归属可机检）→ W-1 / M-1（r44 到期）。
