# 全量对标报告 r43 — 落地 REPEAT（反延期 treadmill）+ 分档宽限，并把 X-16 变成真裁决

日期：2026-09-25（第 43 轮）｜触发：同一指令第十三次逐字下达 ⇒ 整跑一轮；延续 W-5 不开新维度。
本轮将 r42 只写了失败测试的两条判据**落地**（改用 Edit 工具，避开 heredoc 吞反斜杠）。

## 1. 对比总览表

| # | 维度 | 我方（本轮实测） | 对标项目（对应本仓哪一段） | 判定 |
|---|---|---|---|---|
| 1 | 功能模块覆盖 | 债务态扩到 **6 态**：OVERDUE/ACTIVE/DECIDED/UNDATED/DEFERRED/**REPEAT**；宽限按 P0/P1/P2 分档 2/4/8 | `spec-kit` label 驱动流转（bug/enhancement 分级）| 分档面本轮**追平**；仍少自动流转 |
| 2 | 技术架构 | 分类器 last-wins（r41）+ 改期计数 `RE_DEFERRAL`（r43）；台账行新增 `repeat` 字段 | `pre-commit` 单配置源 | 相当 |
| 3 | 性能 | 账龄尺 ~2.6s；10 门 ~9s | — | ❌未跨机实测 |
| 4 | 可扩展性 | 新态只需 TAXONOMY 一处，自洽不变式（声明==计数）自动覆盖；夹具 **32/32** | — | 本轮验证 |
| **5** | **维护状态（聚焦）** | 首跑 REPEAT 显形 **6 条**（同一条被改期 >=2 次），分档后 OVERDUE 由 20 降至 15；逐条升级：降级为长期看守 11 条 + 首次延期 4 条 | `anthropics/skills` 1290、`superpowers` 401、`spec-kit` 287、`agent-skills` 118、`pre-commit` 25 open；仅 spec-kit 有到期自动化且**到期即关闭** | **延期显形面本轮起领先全部 5 家**（对手均无「同一事项被反复改期」的度量） |
| 6 | 文档 | 每条降级都带**实测阻塞原因**；未闭合项如实留在账面（15 条红） | 对手 issue 模板 | 相当 |
| 7 | 适用场景 | 共享记忆卷行号会因并发写漂移（本轮实测 5 条 OVERDUE 报出的行号在写入时已非未勾选行 → 跳过未强改） | 对手无此约束 | 我方独有风险，已立 W-13 |

## 2. 逐项差距说明（实测）

- **D51 · 延期 treadmill 现已显形**：`RE_DEFERRAL` 数同一行的「挂账至 rNN」出现次数，>=2 即判 `REPEAT`（t30 三期样本、t31 单期仍 DEFERRED 防一刀切）。首跑抓到 **6 条**在滚延期 —— 其中含我自己 r39 批量写的那批。
- **D52 · 统一宽限期是噪音源**：分档后同一数据 OVERDUE 20 → 15，说明 5 条本是 P2 长期项被误判（t33/t34 锁死）。
- **D53 · 裁决不能靠记忆**：按 X-16 把 15 条中已延期过的 **11 条判「降级为长期看守」**（终局态、仍被棘轮看守），首次越线 4 条给到期日 r46。**没有一条是靠「看起来做过」划掉的**。
- **D54 · 判据仍红，本轮不抹**：`overdue_debt_items=15 > 基线 0` → `[RATCHET:FAIL]`。基线 **不上调**：这 15 条里含 W-11/W-12/W-13 等 r42-r43 新立且确未做的项，属真实敞口。
- **D55 · 行号漂移**：写入阶段有 5 条报出的行号已不是未勾选行（并发/前序插入所致），脚本按守卫**跳过未强改**，并立 W-13（按标题锚点而非行号定位待办）。

## 3. 改进建议清单（优先级）

| 级别 | 编号 | 建议 | 状态 |
|---|---|---|---|
| 高 | W-11a | `REPEAT` 态（改期 >=2 显形） | ✅ 本轮落地，夹具 t30/t31/t32 绿 |
| 高 | W-11b | 宽限按优先级分档 2/4/8，值入常量表 | ✅ 本轮落地，夹具 t33/t34 绿 |
| 高 | W-12 | 裁决清单完整性：本轮裁决数须覆盖上轮全部 OVERDUE（缺即红）| ⏳ 待落地（本轮 D55 正是缺它的后果）|
| 中 | W-13 | 待办定位改**标题锚点**而非行号（防并发漂移）| ⏳ 新立 |
| 中 | W-1 | 13→12 条 UNDATED 定年 | ⏳ 挂账 r46 |
| 低 | L-6 | 编号命名空间收敛 | ⏳ P2 看守 |

## 4. 可操作实施路径（本轮实际序）

1. RED（已存在）：`python 05-exec/r39_debt_ledger_fixtures.py` → t30/t32/t33/t34 四条红（28/32）。
2. GREEN：Edit 工具落 `GRACE_BY_PRIORITY` + `RE_DEFERRAL`/`REPEAT_AFTER` + `classify_item` 分支 + 台账 `repeat` 字段 → **32/32**。
3. 复测：`python 05-exec/r38_debt_aging.py --json 06-benchmark/debt_aging_r43_2026-09-25.json --ledger 06-benchmark/debt_runs.jsonl` → `OVERDUE 20 / REPEAT 6 / DEFERRED 0`。
4. 按 X-16 逐条升级裁决 → 复测 `OVERDUE 15 / DECIDED 24 / DEFERRED 4 / REPEAT 2`。
5. 诚实性检查：`python 05-exec/ratchet_gate.py` 仍 `[RATCHET:FAIL] +15`，**未使用 `--raise-baseline`**。

## 5. 结论与不学清单

- 本轮把「延期」从可以无限滚，变成**两次即显形、显形即须升级**；这是本仓债务治理链上第一道反自我欺骗的闭环。
- **X-17（本轮立）**：禁止用「再挂一个到期日」处理 REPEAT 项 —— 必须三选一：执行 / 降级为长期看守（带指标看守）/ 作废（带实测证据）。
- 下一步：W-12（完整性判据）→ W-13（标题锚点定位）→ 把 15 条敞口继续实质压降（不是改判据）。
