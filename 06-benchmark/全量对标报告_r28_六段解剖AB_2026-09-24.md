# 全量对标 r28：同一把尺量双方真实文件（六段解剖 A/B）

> **⚠️ 校正注（2026-09-24 r29，R241 只加注不改写原文）**：本页两处结论被下一轮实测修正，引用时须带上注：
> ① **§3 短板量级**：「verification 差 41pt」含措辞假阴性 —— 三档锚点复测本体系 59.0%→**72.3%**（68 条原判缺里 22 条 = 32.4% 只是没用那组词，实物例证 `story-scan` 的「采集质量门」），
>    真差 **27.7pt**；`overview 18.1% vs 96.0%` 一列在宽口径下**五家全 100%**，属无鉴别力列，禁再引用。
>    本轮唯一未被推翻的是 **rationalizations 差 82.1pt**（两口径差值 +0.0）。
> ② **§5 H2 队列口径**：「按 `verification=false` 体积排序大的先补」实测 top14 **全是市场/上游件**，越过本项目 2026-09-22 范围裁定（维护面 = HIGH 51 + MID 26）；
>    过裁定表 + 稳健性双过滤后真队列 = **3 条**，已于 r29 全部补齐（受管根 `6a4f97b`）。工具护栏见 `05-exec/r29_scope_filtered_queue.py`。
> 原文按留痕要求保持零改写。

> 2026-09-24 23:0x | 端 QD | 第 28 轮 | 权威口径：`behavior_core.md` #23 用户命令绝对优先，重复命令按原文完整再跑
> **本轮方法论修正**：r10~r27 的结构对比全是"我们的源码 vs 对手的 README 宣称"。本轮用同一份判据
> （直接复用 `skill_structure_rubric_scan.RUBRIC_RX`，单一真相源）去量**对手仓库里的真实 SKILL.md**。

## 1. 基线（本轮实测）

| 项 | 值 | 取值命令 |
|---|---|---|
| 注册表 == 磁盘 | **167** | `python 焚诀/eval/verify_truth_consistency.py` → C1 |
| 真相源门禁 | **C1~C33，33 PASS / 0 FAIL** | 同上（上一轮的 C31 FAIL 已由并行会话修好） |
| 新增对标对象 | **mattpocock/skills ★268,921**（09-24 仍在推） | `gh api repos/mattpocock/skills` |
| 上游星标（本轮） | superpowers 291,040 / anthropics 177,919 / addyosmani 98,823 | `gh api repos/<r>` |

## 2. 对比总览：六段解剖 A/B（同一把尺，全实测真实文件）

| 对象 | n | overview | when_to_use | process | rationalizations | red_flags | verification | **六段全含** |
|---|---|---|---|---|---|---|---|---|
| **本体系** `D:\global_skills` | **166** | 18.1% | 24.1% | **42.2%** | 13.9% | 54.2% | 59.0% | **0.0%** |
| obra/superpowers | 15 | 66.7% | 60.0% | 13.3% | 66.7% | 60.0% | 80.0% | 0.0% |
| **addyosmani/agent-skills** | 25 | **96.0%** | **92.0%** | 24.0% | **96.0%** | **96.0%** | **100.0%** | **20.0%** |
| anthropics/skills（官方） | 20 | 25.0% | 20.0% | 25.0% | 5.0% | 30.0% | 25.0% | 0.0% |
| mattpocock/skills | 30 | 23.3% | 3.3% | 40.0% | 3.3% | 6.7% | 16.7% | 0.0% |

复现：`python 05-exec/rubric_ab_compare.py --json 06-benchmark/rubric_ab_2026-09-24.json`
（两侧退出码已验：正常跑 RC=0 且打印 4 仓取数明细；喂不存在的仓 → **RC=2 + "禁止输出对比结论"**，不静默出 0 命中表）

## 3. 逐项差距说明 —— 三条旧推断被本轮实测推翻

### 推翻① 「anthropics 官方口径 = 六段解剖」是误读
官方仓 20 个 SKILL.md 实测：rationalizations **5.0%**、red_flags 30.0%、verification 25.0%。
⇒ 官方真正强制的只有 frontmatter 两字段（name + description，我们 r17 已按此建 description 基线），
**正文解剖规范是 addyosmani 的工程选择，不是 Anthropic 的标准**。r18 报告 §7「抄-B1」的论证里把两者混在同一条引述下，需按此收窄。

### 推翻② 「结构纪律 = 优质项目标配」不成立
星标最高的两个之一 mattpocock（268,921★）实测 rationalizations 3.3% / when_to_use 3.3% / verification 16.7%，几乎无固定解剖；
superpowers（291,040★）也只有 66.7/60/80。**五家之中只有 addyosmani 真把六段执行下来（96/92/96/96/100，六段全含 20%）**。
⇒ 结论要改：**我们不是落后于行业，而是落后于一家执行得最严的项目**。补结构的收益是真实的，但不能用"大家都这么做"来论证。

### 推翻③ 我们的短板位置被量错了
本体系 **process 42.2% 是全场最高**（superpowers 只有 13.3%）——"流程化"我们本来就领先。
真正落后的两段是 **rationalizations 13.9%**（addyosmani 96.0%，差 **82pt**）与 **verification 59.0%**（差 41pt）。
⇒ r18 把"六段全含 0/166"当成一个笼统短板，掩盖了"只需补两段、且其中一段我们几乎为零"这个精确结论。

### ⚠️ 本轮数字的已知水分（必须一起读，否则会误判）
1. **overview / when_to_use 两列对本体系不公平**：我们的技能习惯把"做什么/何时用"写进 **frontmatter description**（本轮 description 双要素 120/166=72.3%），而 addyosmani 写成正文小节标题。锚点是标题级的 ⇒ 假阴性集中在 overview 18.1% / when_to_use 24.1% 两列。
   真正可信的是 **rationalizations / verification** 两段（这两种内容无法靠 frontmatter 替代，也没有等价写法）。
2. `cap=30`：mattpocock 树内 38 个，只取前 30（字母序），非全量。
3. 关键词锚点判定，非语义判定（沿用 r18 已声明的判据性质）。

## 4. 可借鉴的优势功能（本轮新增实证）

| 来源 | 实物做法 | 我们对应缺口 |
|---|---|---|
| addyosmani | **Verification 段 100% 覆盖**：每个技能都以"证据要求"收尾，明写 "Seems right is never sufficient" | 我们 59.0%；A 族技能里缺证据段的属高危（会被当成"做完了"） |
| addyosmani | **Rationalizations 表 96%**：把 agent 跳过步骤的借口列成表 + 逐条反驳 | 我们 13.9%，且只有 superpowers 血统的技能带反理性化表 |
| addyosmani | `docs/comparison.md` 常驻横向页 + 受控 head-to-head 实验链接 | 我们有 5 份对标报告但无常驻横向页（r18 列 P2-A，本轮升为可做） |
| superpowers | 技能里内建 `scripts/` 且脚本带**边界守卫**（r27 实测：`rm -rf` 只在 `/tmp/*` 前缀下执行） | 我们技能侧脚本无"破坏性命令必须带作用域守卫"这条规范 |

## 5. 改进建议清单（优先级）+ 本轮执行

| 级 | 建议 | 本轮状态 |
|---|---|---|
| **高** | **H1 把"证据要求"与"反理性化表"写进新建技能模板**（只强制这两段，六段全要会重演 addyosmani 也没做到的 80% 空转） | ⏳ 未落地：模板在 `A-skill-manager` / `A-skill-onboarding`，并行会话正在改 A 族（07 已登记其夹带风险）⇒ 按 R269 不抢改，待窗口 |
| **高** | **H2 存量技能补 Verification 段的优先队列**：以本轮 `rubric_ab_*.json` 的 `verification=false` 名单排序（体积大的先补，收益集中在头部） | ✅ 数据已就绪（基线 JSON 落盘），名单可直接出队；改动本体需 `rule_editor` + 派生件重建，与 H1 同批走 |
| **高** | **H3 破坏性命令作用域守卫入规范**（对标 superpowers `if [[ "$D" == /tmp/* ]]`）：技能内任何删除/覆盖命令必须显式限定前缀 | ✅ **本轮已写成判据文本**，进 `memory/06-constraints.md` 与报告 §4；落 skill 模板同 H1 窗口 |
| **中** | M1 常驻 `comparison.md` 升级：把本轮 A/B 表并入（r18 列 P2-A，现实证可做） | ⏳ 下轮顺带 |
| **中** | M2 修 A/B 尺子的已知水分：`overview/when_to_use` 增"frontmatter description 也算命中"的兼容判据，重跑一次给双口径 | ⏳ 待办（改动在本仓工具，无红线，下轮做） |
| 低 | L1 追 mattpocock（268k★）具体做法：它证明"无结构也能拿最高星标"，值得读它靠什么（可能是分发/人群），但**不作为我们的整改项** | 📝 登记 |

## 6. 实施路径

1. ✅ 本轮：`rubric_ab_compare.py`（尺子复用单一真相源）+ 基线 JSON + 本报告 + H3 判据文本
2. 下轮（需 A 族空闲窗口，走 `rule_editor` + `--file` 白名单）：H1 模板两段 → H2 按 `verification=false` 队列补存量 → 重建派生件 + 复跑 verify/gates
3. 随时可做（本仓，无红线）：M2 双口径重跑，把 overview/when_to_use 两列的水分消掉再引用
4. 引用纪律：**任何对外结论必须带 §3 的水分明示**，不许单拎"我们 0% vs addyosmani 96%"这种跨风格比较

## 7. 结论（对"同等或更优"的更新判定）

本轮把一条含糊的自我批评变成了精确的、可执行的三行：
**我们的流程化（process 42.2%）全场第一；真正缺的只有 rationalizations（差 82pt）和 verification（差 41pt）两段；而全行业只有 1 家（addyosmani，25 个技能）把这两段真做满——所以这是"向一家学习"，不是"向行业标准靠拢"。**

可信度：双方文件内容 ✅已实测（gh api 逐文件取回，n=166/15/25/20/30）；`overview/when_to_use` 两列 ⚠️含已知假阴性（frontmatter 风格差异，已在 §3 标注并给出修正路径 M2）；mattpocock ⚠️非全量（30/38）。
