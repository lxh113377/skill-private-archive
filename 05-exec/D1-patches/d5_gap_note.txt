
## R9 事故 · 每日日志空档说明（事后补记，2026-09-22）

> 本条目为**事后说明**，不是当日日志。按「历史留痕不可改写」纪律，**不补造 09-09 ~ 09-20 的当日记录**，仅在此留痕事实与影响面。

**事实（✅已实测）**：`D:\global_memory\memory\` 的日期日志从 `2026-09-08.md` 直接跳到 `2026-09-21.md` —— **09-09 ~ 09-20 共 12 天无日志**；`memory/2026-09-14.md` 实测 `Test-Path = False`。

**原因**：2026-09-21 R9 事故 —— 卸载 OpenClaw 时 `robocopy /MIR` 漏加 `/XJ`，跟随 junction 把 `D:\global_memory` 与 `D:\global_skills` 整体清空（含各自 `.git`）。

**不可恢复性**：两仓当时**均无远端**（只有本地 `.git`），故历史提交不可追；焚诀仓因有远端而完好。该事故已催生纪律 #20 升级（「有远端才叫基线」+ 勤存档），见 `A-project-handoff/references/disciplines.md` #20。

**影响面（已在各项目侧留痕）**：
- 期间完成任务的 **footer 协议落盘证据不可核验**（例：`自建skill优化` 项目 09-14 / 09-19 两轮的 `[skill清单]` / `[升级建议]` 块已不可考）；
- 期间 `lessons/` 新增的经验条目可能一并丢失（`lessons-p0.md` 为 R9 重建版）。

**处置决定（用户 2026-09-22 裁定）**：建本「事后说明」条目留痕，**不回填、不伪造**历史日志。
