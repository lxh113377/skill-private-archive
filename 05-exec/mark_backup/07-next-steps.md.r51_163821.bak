# 07 - 下一步

> 归档类型：增量（已完成项移入分卷）
> **⚠️ 这是新对话恢复上下文的入口文件；P0 永远不能空。**
> **真相源口径（2026-09-22 实测校准）**：P0 只放经实测确认**未完成**的项；已完成项一律迁入分卷 `- [x]`，不再滞留主卷。

## 最近对话摘要
- **2026-09-25（第 50 轮 r50，W-25 输入面自证推广 + 对手枚举面复核）** — 同一指令第二十次逐字触发 ⇒ 整跑一轮；W-5 约束下不开新维度。① **自家判据的真缺口**：`rule_conflict_scan` 的 `DEFAULT_FILES` 是手抄 10 件，而 `D:/global_memory/core` 有 18 件带可配对正负规则的权威源（`behavior_core_rules_p1..p8` = 22 锚点正文所在）**从未进面** ⇒ 扩面到 26 件后极性规则 116→188、自述冲突 6→11、互斥候选 6→21，证明此前的绿灯只是 **38% 面上的结论**；落地 `discover_authority_candidates`/`face_authority_coverage`/`effective_files`，并加对照例 c6b「只用手抄清单必须判红」防扩面空转（立 **X-26**）。② **同一把尺回头量对手取证**：`/actions/workflows` 总计面混有平台 `dynamic/*` ⇒ superpowers 与 anthropics/skills 实测**仓库内 0 个 workflow**（contents 目录 404），r31/r32 的「2 workflow / 82% failure / 75% failure 停摆 6 周」描述的是 Copilot 平台件而非仓库测试 CI；校正基数 = spec-kit 18、AAS 2、pre-commit 2、其余 0，历史条目不删只在 comparison 加校正附节（R241）。③ **棘轮读取面对账**：`face_counted_vs_observed` 让 `OSError: continue` 静默跳过显形，面不完整即转 unknown （禁止拿缩小面的数字与基线比）。④ **W-26 收口 = 先归因再定性**：`git show r43..r49` 实测仓库内全为纯 LF ⇒ 混合行尾来自**本仓写卷脚本未传 `newline=""`**，与并行会话无关（我 r49 那句未经核验的归因已加**校正注**），立 **X-25**。⑤ **判据自己造的假阳性**：建议面把 `X-24` 判成漏登记 —— 但 X 编号的承接面是 `memory/AGENTS.md` 禁止项表而非 07。已按前缀分面路由（W/M/H/L→待办卷、X→禁止项表，且禁止互相顶替，反例 f17–f20）。⑥ 到期债 W-17 一条裁决（OVERDUE 1→0、DECIDED 70→73），新立 W-27（21 组互斥候选转裁定）/W-28（枚举源双路对账做成判据）/W-29（复核其余对手数字）。末态：夹具 32+16+58+10+5 全绿、`[FACE:UNVERIFIED]`（定年面零到期项未行使，不静默判绿）、报告 = `06-benchmark/全量对标报告_r50_权威源扫描扩面与对手枚举面复核_2026-09-25.md`
- **2026-09-25（第 49 轮 r49，W-20 判据输入面五面自证 + 三处劫持/错位的根因修）** — 同一指令第十九次逐字触发 ⇒ 整跑一轮；W-5 约束下不开新维度，增量全落在第 13/14 维纵深。① **五面自证**落地 `05-exec/r38_debt_aging.py`（轮号面 -40 窗口 vs tag 硬锚、**规模下限面**、去重面 80 字前缀键、定年面 70 字 needle、建议面 报告新建议→待办卷承接），`[FACE:OK]`，并把 `face_status/face_red_count` 写进台账成为趋势列（第 2 代分代必填，复用 r48 的 row_required_from 列表化）；floor 面是**读权威契约 R-ENUM 后自查出的自家缺口**——前四面全是「面内自洽」，无一面能发现「受检面整体变小」，glob 少收一卷时四面照全绿。② **到期追讨真抓到自家错标**：W-14 在 r45 已落地，r46 却被写上「挂账至 r49」（原因文本讲的是 W-17）⇒ 立 **W-22** 第⑥重写时对账，落地**首跑即拦住我自己的下一条裁决**。③ **W-9 的第二形态**：07 实测为混合行尾（178 CRLF + 4 纯 LF），写入器「整份按 CRLF 切」把那 4 行并进前一个元素 ⇒ 标记追加到**另一条目行**（本轮真实发生：报 L177 落 L181）；改 `split_keep_eol` 逐行保留行尾，读回从「串存在于文件」升级为「锚点与标记同一物理行」（反例夹具 t9/t10 先红后绿）。④ **劫持第三形态**：终局裁决的原因里引用「挂账至 r49」原文会被全文 last-wins 回判成过期挂账 ⇒ 凭空造红 + REPEAT 计数虚增；改 `parse_last_verdict/count_deferrals` 只认**标记头部**，负控 q1–q5 反向保证真延期仍 DEFERRED、到期仍 OVERDUE。⑤ 5 条到期/新越线债逐条取证裁决（OVERDUE 5→0、DECIDED 60→70），W-21 依实测**作废不接线**（恒 0 指标是假绿面 + OWNERED 可刷）。夹具 25+58+10+5 全绿、`[CONTRACT:PASS] 17 份/13 pattern`、`[GATES:PASS] PASS=12`、`[RATCHET:PASS] 9 项`；报告 = `06-benchmark/全量对标报告_r49_判据输入面五面自证与三处劫持根因修_2026-09-25.md`（**同轮补记 r49**：定年面随后按权威契约 R-ENUM 边界条改为「零到期项 ⇒ 该面未行使」，故末态为 `[FACE:UNVERIFIED]` 而非五面全绿 —— 没检验过不许写成检验通过；夹具加 f14–f16 三态锁死，台账列 face_status 如实记 UNVERIFIED）
- **2026-09-25（第 48 轮 r48，W-17 契约分代必填 + W-6 归属可机检）** — 同一指令第十八次逐字触发 ⇒ 整跑一轮；不开新维度。① **W-17 落地**：契约新增 `row_required_from`（分叉点 2026-09-25T15:00，列 coverage_status/prev_overdue/missing），`validate_jsonl` 按行 `ts` 比较决定是否强制 ⇒ 新行缺列必红、r38-r45 历史行豁免；解掉 r45 我自己挂账的「不能加进 row_required 否则误杀历史」两难（同族 r42 D36）。夹具 t60/t61/t62 三例锁死双向。② **W-6 落地**：`classify_owner()` + 扫描期 `owner` 字段 + 主流程 `unowned_claims`，把「这不属于我」的声明变成可机检：声明归属却**给不出可解析路径**即 UNOWNED。存在理由是 r40 D37 —— 我把本仓自持件标成归属方件挂了 4 轮；甩锅比降级更便宜，必须堵。③ **首跑即自抓假阳性（D75）**：owner 最初判的是被截断的 `title[:110]`，路径常出现在 110 字之后 ⇒ 3 条 UNOWNED 里 **2 条是假的**；改判全文后剩 3 条真项，逐条用 r46 写入器补上真实路径/归属并复测归零。④ 另抓 D76：我一次 Edit 把字典尾部写重复导致 SyntaxError（ast 当场拦下，未污染在役工具）。本轮账面：`OVERDUE 0 / DECIDED 60 / UNDATED 0 / DEFERRED 1 / REPEAT 0（最终复测值）`、W-12 `OK`、`[GATES:PASS] PASS=11`、契约 13 pattern / 17 件 PASS；夹具 **53/53**。立 **X-22**（禁只写「属归属方」不给路径）。
- **2026-09-25（第 47 轮 r47，纯还债轮：W-1 十二条「无法定年」盲区清零）** — 同一指令第十七次逐字触发 ⇒ 整跑一轮；不开新维度。12 条 UNDATED 逐条 `git log -S <锚点>` 取**真实首现**（不靠回忆），再逐条用 `r46_mark_verdict.py` 一次性写「定年 + 终局裁决」（同轮二次写入会被工具拒，故合并为一次）⇒ `UNDATED 12 → 0`、`DECIDED 46 → 58`、`OVERDUE 0`、`REPEAT 0`、W-12 完整性 `OK`。**定年不夸大（D71）**：12 条里 6 条定到轮号（r19/r22/r29/r30/r37 实测），另 6 条首现提交主题无 rNN、**只能定到日期**（2026-09-23/24），台账里如实写 `r?` 加日期，不假装全部定年成功。处置按性质三类终局：`作废` 4 条（判据已落地，如 control_char_scan 已进必跑链与常驻门）、`保留` 6 条（留痕型登记与待用户裁定项，登记不等于欠账）、`降级转长期看守` 2 条（M-3 同尺分类、R19-1 残余）；无一条凭「看着像完成」划掉。**W-18 dogfood 成功**：12/12 全经写入器、备份落 `05-exec/mark_backup` 且不污染记忆卷、零行号错写。另记 D74：脚本里我先组装占位命令再用实测值重建 —— 结果对，但半成品命令本可避免（下轮起先取数、后组装、一次执行）。立 **X-21**：禁止用「加了裁决标记」冒充「已定年」，定年必须有可复现取值命令，否则 UNDATED 只是被 DECIDED 换皮掩盖。
- **2026-09-25（第 46 轮 r46，W-16 固化：把「禁按行号写标记」变成不可绕过的接口）** — 同一指令第十六次逐字触发 ⇒ 整跑一轮。r45 我立了 X-19/W-16 却**当轮就仍在按行号写** ⇒ 本轮不再靠自觉，落地唯一写入器 `05-exec/r46_mark_verdict.py`：**不接收 --line**、锚点命中数 != 1 拒写、0 命中拒写且**不退化为追加**、只准写 `- [ ]`、同轮二次裁决拒写（幂等靠拒绝）、写完必须 grep 读回否则回滚、备份默认落 `05-exec/mark_backup` 且可 `--backup-dir` 注入。**真实卷冒烟即证明有效**：`--key 待办 H-3` 命中 [87,90] ⇒ 拒写，而这正是 W-9 挂 5 轮的机制学解释（歧义锚点不拒写就会安静写到第一行）。三处自抓并修：**D66** 我首版把备份目录写死在仓库路径导致夹具看不到、又想用环境变量绕过 ⇒ 改依赖注入（测试难写=设计难用）；**D67** 再写出弱断言 t9（与 t5 重复且打断 t7）⇒ 删除（此类错第三次复发，D33→D61→D67）；**D68** 冒烟时用真实 07 写进一条 `裁决=冒烟测试` ⇒ 当场回滚并把 2 个 `.bak` 迁出记忆卷 ⇒ 立 **X-20**（禁拿真实记忆卷做冒烟，自证一律在临时 vault）。注册为**第 11 道常驻门**，`[GATES:PASS] PASS=11` 连跑两次稳定；并 dogfood 裁掉本轮唯一到期项 W-6 ⇒ `OVERDUE 1→0`、`DECIDED 46`、`REPEAT 0`。**W-15 只做一半如实记**：coverage 三列进台账但未进契约 row_required（会让 r38-r45 历史行全体违规且行不可回写，同族 D36）⇒ W-17 分代必填，挂账 r49，不虚报完成。
- **2026-09-25（第 45 轮 r45，W-14 反降级免检 + W-9 真因归位）** — 同一指令第十五次逐字触发 ⇒ 整跑一轮。① **W-14 落地**：棘轮新增第 9 指标 `repeat_debt_items`（读 `by_class.REPEAT`，与 overdue/deferred 共用同一自洽门），动因是 r44 的 D59 —— 那轮把 OVERDUE 从 16 压到 1，其中 11 条靠「降级为长期看守」实现；降级虽属合法终局裁决，但**没有独立指标时它就是新的免检通道**（全降级比真还债还干净）。TDD 先红 6 例（t50–t55）后绿 **45/45**；顺带删掉我自己刚写出的**恒真断言 t53**（`or True`，与 D33 同族复发）。② **W-15 部分落地并如实记边界**：趋势台账行新增 `coverage_status/coverage_prev_overdue/coverage_missing` 三列（W-12 结果进斜率）；但**未把它们加进契约 row_required** —— 加进去会让 r38–r44 的既有历史行全体违规，而行是 append-only 不可回写；这正是 r42 D36「代际豁免」的同族问题，需先设计按 ts 分代必填，故留下一轮。③ **W-9 真因归位（不是鬼影）**：那条 H-3 项此前**裁决标记数为 0** —— 说明我 r40/r41/r43 写标记时**按行号定位**，行号一漂就把标记写到了别的行，条目从未被真正标注。这是 W-13（标题锚点）存在的实证据，也暴露我自己：落地了 find_item_line 却在临时脚本里继续沿用行号。④ 到期追讨第 3 次真响（轮号推进让 W-5/W-10/H-3″ 三条 r44 到期）⇒ 按 X-17 全部降级看守并写明外部条件，账面 `OVERDUE 3 → 0`、`DECIDED 46`、`REPEAT 0`，W-12 完整性首报 **OK｜上轮 1 条已处理 0 缺**。新立 **W-16**（临时脚本禁按行号写记忆标记，必须复用 find_item_line，命中!=1 即拒写）。
- **2026-09-25（第 44 轮 r44，W-12/W-13 落地：裁决清单完整性本身成为判据）** — 同一指令第十四次逐字触发 ⇒ 整跑一轮；W-5 约束下不开新维度。落地 r42/r43 连续点名却未做的两件：① **W-13 标题锚点定位** find_item_line（只认未勾选项、多命中由调用方拒写，杜绝 r43 D55 的行号漂移写错行）；② **W-12 裁决完整性** adjudication_gap + coverage_check 接线，四条语义由夹具 t40–t46 七例锁死（无标记 ⇒ missing；最后一次挂账已到期 ⇒ 仍 missing；已勾/已消失/未来挂账 ⇒ 不算；多命中逐行计 ⇒ 防只裁第一行就宣称完成），夹具 **39/39**。过程中先发现**自写测试互相矛盾**（t44/t45 与 t43 对已到期挂账口径不一）⇒ 先定语义再实现，没有改断言凑过。**判据首跑就打我脸**：INCOMPLETE ｜ 上轮 OVERDUE 15 → 本轮仍未处理 15 —— 因为我本轮先写代码还没还债；据此真裁决 15 条（11 降级长期看守 + 2 执行完毕 + 1 首次延期 r47），并带证据补勾 r40 已交付却漏勾的 W-4，账面 **OVERDUE 16 → 2 → 1**、DECIDED 43、REPEAT 0、DEFERRED 1。**未抹红**：余 1 条为真实未做（W-1 十二条 UNDATED），棘轮 overdue=1 > 基线 0 保留红，全程未用 --raise-baseline。立 **X-18**（判据落地当轮必须跑真实数据，否则会把「代码存在」当成「债已还」，这是 r31–r44 反复出现的失败模式）、**W-14**（REPEAT 第 9 指标，防「降级」变新免检通道）、**W-15**（完整性结果进趋势台账与契约取值域）。
- **2026-09-25（第 43 轮 r43，反延期 treadmill 落地轮）** — 同一指令第十三次逐字触发 ⇒ 整跑一轮；W-5 约束下不开新维度。把 r42 只写了失败测试的两条判据**落地**（改用 Edit 工具绕开 heredoc 吞反斜杠）：① 第 6 态 **REPEAT** —— `RE_DEFERRAL` 数同一行「挂账至 rNN」出现次数，>=2 即判 REPEAT 并强制升级，首跑显形 **6 条**正在滚延期的项（含我 r39 批量写的那批）；② 宽限期按优先级分档 `P0:2 / P1:4 / P2:8`（值进 `GRACE_BY_PRIORITY` 常量表，t34 锁死不得写死在函数体内），分档后 OVERDUE **20 → 15**（5 条本是 P2 长期项被误判，证明统一 2 轮在制造噪音、稀释 P0 紧迫感）。夹具 **32/32**。随后按 X-16 逐条真裁决：已延期过的 **11 条判「降级为长期看守」**（终局态，数值仍由棘轮看守，理由逐条写明实测阻塞：待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），首次越线 4 条给到期日 r46；复测 `OVERDUE 15 / DECIDED 24 / DEFERRED 4 / REPEAT 2`。**棘轮如实留红 +15，未使用 `--raise-baseline`**（这 15 条含 W-11/W-12/W-13 等确未做的敞口，上调=掩盖）。新发现 D55：并发写与前序插入使 5 条 OVERDUE 报出的行号在写入时已不是未勾选行，脚本按守卫跳过未强改 ⇒ 立 **W-13**（待办改**标题锚点**定位而非行号）与 **W-12**（裁决清单完整性：本轮裁决数须覆盖上轮全部 OVERDUE，缺一条即红 —— D55 正是缺它的后果）；立 **X-17**：REPEAT 项禁止再挂到期日，必须三选一（执行 / 降级看守 / 作废带证据）。

r42 根因轮（2026-09-25 第 42 轮，第十二次逐字触发 ⇒ 整跑一轮） — 用系统化调试四阶段查清 OVERDUE 1→20 反弹，结论：**没有判据缺陷、也不是我上轮替换正文造成的扰动**。真因两条：① current_round 随 r41 提交 40→41，我 r39 写的 15 条「挂账至 r41」**如期到期**（14 条计入）；② 我上轮只裁「当期到期清单」，漏裁 r38 自立项 W-0~W-3/L-6，它们账龄涨到 3 轮 > 宽限 2 ⇒ 新越线 6 条。据此定性为**流程缺陷 + 机制缺口**：延期可无限滚（同一条反复改期无人显形）、宽限期不分优先级。Phase 4 已按 TDD 写失败测试 t30–t34（REPEAT 态 + 分档宽限 + 分档表可读），**实现本轮未落地**（heredoc 吞反斜杠致补丁中途断言失败，未写盘即中止），如实挂红不谎报：夹具 28/32。


- **2026-09-25（第 41 轮 r41，还债轮第二击：W-8 真根因 = 裁决标记必须 last-wins + H-5 结构归位）** — 同一指令第十一次逐字触发 ⇒ 整跑一轮；延续 W-5 不开新维度。**先推翻自己上一轮的结论**：r40 我把「已带 裁决=保留 仍报 OVERDUE」判成判据不稳定，实测真因是 `current_round` 随 r40 提交由 39→40，整批 `挂账至 r40` 如实到期（到期洪峰）——那是 r39 设计的正常效果，误诊差点让我一天产能指向错处（D41，并险些用 `--raise-baseline` 把它归因抹掉）。真根因随后抓到（D42）：一条待办一生会被多次裁决、标记是追加在同一行的，而 `RE_DECIDED.search()` **永远取最早那条** ⇒ 新终局裁决被旧挂账永久劫持；夹具 t26/t27 从两个方向锁死（旧挂账+新作废 / 旧作废+新挂账），改 `finditer()[-1]` 后 **27/27**。第二件是清结构债 **H-5**：P0 节 节内混入 **15 条轮次摘要**，整段归位到「最近对话摘要」，守恒校验三项全过（总行数 153→154 仅多 1 空行、P0 节内残留摘要 0、`grep -c '^- [ ]'` = 60 一条待办未删，守「禁为凑判据形状删待办」）。再裁 9 条到期债：3 条 `作废`（H-3、H-3″ 被 r35 分类 + X-4 取代；r31 旧 M-2/M-3 系编号撞车案），6 条 `挂账至 r41→r44` 各带实测取值（`A-skill-manager` 仍 2 条在途、需第二台真机、需真实 Hermes 会话），无一条按"看着像完成"划掉。账面：`OVERDUE 10 → 1`、`DECIDED 12 → 15`、`DEFERRED 25 → 22`。**如实留红不抹**：残留 1 条已带 r41 标记仍判 OVERDUE（疑与并发整卷重写相互作用，D44），`[RATCHET:FAIL] overdue=1 > 基线 0`、`[GATES:FAIL] 1`，本轮**不使用归因通道**（上调=掩盖 bug），立 **W-9** 为下轮 P0；立 **X-15**（多次状态读取必须 last-wins）。

- **2026-09-25（第 40 轮 r40，还债轮：不开新维度，专修 r38/r39 判据的三处自我放水）** — 同一指令第十次逐字触发 ⇒ 整跑一轮。按 W-5 约束本轮**不新增维度**。落地四件：① **W-4** 第 8 指标 `deferred_debt_items`（与 overdue 共用 `_debt_class_state` 单源、同等 fail-closed），夹具 t20–t25 → **24/24**，且**落地即咬我**：本轮自己新增两条 `挂账至 r42` 让指标 23→25 变红；② **到期追讨首次真响**：`挂账至 r40` 的 3 条被自动回判 OVERDUE（机器逼裁决，非人自觉）；③ **L-4 归属反转**：`rule_conflict_scan.py` 实测就在本仓 `05-exec/`，却被标「归属方件」挂账 4 轮 ⇒ 自家债被错误外部化；同时发现扫描器**早已透明打印** `文件 9/10 存在`，缺的是契约不看这一项 ⇒ 新增不变式 `conflict_no_dead_inputs` + 死条目改指真实在役件（现 `10/10`）+ 第 10 道常驻门，夹具 5/5；④ **M-1 判不可测**：git 不记录提交时脏树（`git stash list`=0），「先量近 20 次误报率」在历史上无法证实 ⇒ 改**前瞻影子计数**、不阻塞写入（守 r25 否决）。三处自错留证：夹具首版前提错（断言"静默少扫"而实测已透明上报）、`files_declared` 先插错层级、**新不变式回溯误杀历史产物** ⇒ 加代际豁免（R241 同族）。立 **X-14**（归属声明须带可解析路径）。**本轮如实留红**：`[RATCHET:FAIL] overdue+1 / deferred+2`，棘轮拒绝打包归因（"一条归因不足以解释，判据不为人心情让路"），故立 **W-8** 为下轮 P0（已带 `裁决=保留` 的 07 L67 仍报 OVERDUE，含并发整卷重写与分卷去重交互）。余测：`[GATES:PASS] PASS=10`、`[CONTRACT:PASS] 17 份/13 pattern`、趋势台账 5 行。

- **2026-09-25（第 39 轮 r39，第十四维纵深：把「延期」与「终局」拆开，顺手清偿 P0 首推 W-0）** — 同一指令第九次逐字触发 ⇒ 整跑一轮。r38 的账龄判据有个致命自欺面：任何「挂账至 rNN」一律判 DECIDED ⇒ 给 22 条全写挂账至 r99 就能让指标瞬间归零而一行代码没动。本轮按 TDD 先写失败测试再改：① 新增 **DEFERRED** 态（目标轮未到=DEFERRED，**已到/已过=重新判 OVERDUE 到期追讨**）；② 新建趋势台账 `06-benchmark/debt_runs.jsonl`（append-only + 行契约 + 三重 fail-closed 拒写：origin 越界 / 缺态 / 五态之和 != open_total 一律 0 行），注册为**第 9 道常驻门**（夹具 19/19）；③ **W-0 清偿 22 条超期**：`OVERDUE 22 → 0`，同时 `DEFERRED 23 / DECIDED 11 / UNDATED 13` 单列在册（未销账），裁决分三类且每条自带取值或阻塞原因：能实证的 `作废`、留痕型 `保留`（登记不等于欠账）、真欠账 `挂账至 r41` 写明原因（待授权 / 需真实 Hermes 会话 / 他人在途 / 归属方件）。四处自抓并留证：**D31** 不变式写死集合导致昨天 v1 证据件全体 CONTRACT:FAIL ⇒ 改为「声明态 == 计数态」自洽式（v1/v2 同时合格且 t27 伪装仍红）；**D32** 指标把轮次写死在 glob（`debt_aging_r38_*`）⇒ 新证据件永不生效，t17 实测 old=22/new=22 揭穿，改 `debt_aging_r3*.json` + mtime 取最新，并补 t19 脏证据 → None；**D33** 我自己写出恒真测试 t18（`is None or True`）⇒ **删除而非改好**，假覆盖比没覆盖更坏；**D34** 台账前两行由未定型版本写出被契约拦下 ⇒ 删除重记并在 06 留登记（被删行的「裁决前 OVERDUE 22」真值仍在 r38 证据件与 git 历史）。立 **X-13**（禁靠远期挂账降债务指标）。基线 overdue_debt_items 22→0 系裁决所得，note 里显式记 DEFERRED 23 敞口与 **W-4（给 DEFERRED 立第 8 指标）**。复跑：`[GATES:PASS] PASS=9`、`[CONTRACT:PASS] 15 份 / 13 pattern`、`[RATCHET:PASS]`。

- **2026-09-25（第 38 轮 r38，第十四维「积压债务的可见性与到期治理」——把自己挂账不还做成判据）** — 同一指令第八次逐字触发 ⇒ 整跑一轮。对手取证（`gh api` 实测）：`spec-kit` 27 workflow 里含 `Close stale issues and PRs` + 6 条 issue 流转（唯一有到期治理）；`anthropics/skills` **1290** open 最老 2025-10-16、`superpowers` 401 最老 2026-01-27、`agent-skills` 118、`pre-commit` 仅 25 但最老一条 2018 仍 open 且**四家全部 0 条 stale 自动化**。我方首跑 `05-exec/r38_debt_aging.py` 量出 **52 条未闭环 / OVERDUE 13（最老 30 轮）/ DECIDED 0 / UNDATED 36** —— 「挂账 N 轮」一直是散文不是判据。落地四件：① 账龄尺（13 卷全扫 + 正文前 80 字去重 + 四分类之和自校验 + OVERDUE 条目用 `git log -S` 复核首现日期）；② **有牙但不拦路**：进 `ratchet_gate` 第 7 指标 `overdue_debt_items`（只降不升），不进 `run_gates` 阻断位（r25 用户否决过拦任务的闸门）；变异对照实测 OVERDUE 0→3 即 `[RATCHET:FAIL]` 点名该指标；③ **13 条逐条取证裁决 → OVERDUE 归 0**（12 DECIDED + 1 真执行闭环；`verify_truth_consistency` 33 PASS/0 FAIL、`A-get-memory` 在途 0 条 ⇒ 判作废；仍他人在途 33 条 ⇒ 挂账至 r40 并附条数，无一条"看着像完成"就划掉）；④ 清偿挂账 3 轮的 **M-2**：契约加 `inject_ratchet_baseline*.json` + `ratchet_metric_set_matches`（双向：缺项点名指标 / 多项僵尸基线）+ `ratchet_hardcap_subset`，夹具 t21–t28 → `[GATE:fixture-pass] 41/41`、契约 12 pattern / 15 受检件、`[RATCHET:PASS] 7 项`、`[GATES:PASS] PASS=8`。两处被自己的判据抓住：**「裁决=部分作废」不在取值域 ⇒ 照判 OVERDUE**（自造措辞绕不过枚举，改写成 `作废（仅①…）` 才生效）；**M-1/M-2/M-3 编号与 r31 旧队列撞车**（同族 r37 X-3/X-6）⇒ 新账改用 **W- 前缀**并在旧条留痕（R241）。立 **X-10 不引入到期自动关闭**、**X-11 不把待办迁外部 tracker**。残余：**36 条 UNDATED**（判据盲区，W-1 下轮首推）。
- **2026-09-25（第 37 轮 r37，第十三维「门禁可信度（假阳性治理）」——AC-OBS-05 连红 7 轮的根因是判据错，不是数据错）** — 同一指令第七次逐字触发 ⇒ 整跑一轮。实测 `handoff.py noise` 的 5 条 VIOL **全部不是散落物**：`焚诀\CONTRIBUTING.md`/`LICENSE.md`/`SECURITY.md` 经 `git ls-files --error-unmatch` 逐条实测**全部 tracked**、由归属方 `114b5b0 docs(7-J): 根部社区件回正` 有意提交，工具却建议「迁往 _trash」（= 搬走已入库治理件，破坏性假阳性）；`.git\push-failure.log`（GM + 焚诀）由**仓内工具 `git_autopush.py` 自己写**，位置不受 `.gitignore` 管辖 ⇒ 发生过一次推送失败就**永久红**（结构性自锁）。按 R263 **改判据不改数据**（一条不迁不删不改名）：上游 **A-project-handoff V3.54.1**（`b98cdb5` 代码 + 文档同步 commit）落三件 —— ① 新增 `git_tracked()`，strict 分支放行「Git 已跟踪**文件**」（三重收窄：仅 strict/仅文件/命名不撞 STRAY·MIGRATION；未跟踪、目录、git 异常三者照判 VIOL）；② 二级深扫跳过 `.git` 运行态目录（一级仍按 allowlist 判）；③ R283 续：auto-memory 根件放行面 `feedback-*` → `^(feedback|reference)-…$`，**只放行实测有实物的前缀**（R236），且不外溢到其它根。验证：层a 新建 `05-exec/r37_noise_tracked_stub.py`（temp 真 git 仓）**修前红 8 → 修后 16/16**（含 5 条保牙反例）；层b `handoff.py noise` 四根 **`[GATE:noise-pass]`**（168/67/6/41 ok，violation 全 0）；层c **`handoff.py savepoint` 首次转绿 =「🎯 本次对话已安全落盘」，AC-OBS-05 结束 7 轮连红**。新证据件 `06-benchmark/noise_falsepositive_r37_2026-09-25.json`（before 由**改动前自动备份原件实跑**、非记忆值，逐条标 provenance）+ 契约入册（10 pattern）+ **第 8 道常驻门 `noise_tracked_stub`**（`portable: False`）。**立 X-9 禁做**：不得为凑绿而迁走/删除/改名任何 Git 已跟踪根部件。自纠两处：**首版桩把「未跟踪」文件写在 `git add -A` 之前 + 误以为 `_bak` 可在名中命中（`STRAY_NAME` 实为行尾锚定）⇒ t2/t3/t6/t10 是假反例**，R220 抽验抓回后重做；`run_gates.py` 顺手去掉硬编码「10 份 / 8 pattern」陈旧值（L-5），改指取值命令。
- **2026-09-25（第 36 轮 r36，第十二维「文档内引用可解析性」——新尺连错两次都被自抽验抓回）** — 同一指令第六次触发 ⇒ 整跑一轮。新维度理由：SKILL.md 里 `python scripts/x.py` / `../assets/runtime.js` / `Read references/y.md` 是 agent 会**照抄执行**的指令，文件不在的症状是"技能装了跑不通"，前 11 维无一维量过（对手最接近的只有 mycelium 探安装面，零家有此度量）。**真正产出是两次自错**：① 首版只扫代码块，报「97 处 / 0 DEAD / 90.7%」= **假绿**（同正则跑全文得 494 处、代码块只覆盖 40 ⇒ 漏检 92%，分母本身就残）；② 改全文后报「1030 处 / 2 处 DEAD 在 html-ppt」= **假缺陷**（`../assets/x` 是产物 HTML 相对路径，文件确在 `html-ppt/assets/`）。修正版真数 **1030 处 / 92.2% 可解析 / 真 DEAD 0 / UNLOCATED 80 待定性**。⇒ 立 **X-7 禁做**（新工具首跑"很干净"不得当结论，须扫描面完整 + 逐类计数自洽 + 抽样人工 ls 复验三件齐）；同族再立一条：**判据输出的口径文案必须与实现同改**（改全文后仍印"只抽代码块"= 自证信息造假）。受管根**零改动**（真 DEAD 0 ⇒ 无可安全落地项，不制造假整改）。**本轮另抓到自己的严重流程失守**：r35 那次记忆回写脚本只做字符串替换、**从未调用写回**却打印"07 done"，且提交时未跑本仓铁律要求的 `git show --stat` 核对 ⇒ 07/05/comparison 三处 r35 内容实际从未入库（`git log -- memory/07-next-steps.md` 最后触及仍是 r34）。本轮已一并补正（含 r35 全文），并把"写盘后必须读回证明"提为硬要求。报告 = `06-benchmark/全量对标报告_r36_引用可解析性_2026-09-25.md`；证据 = `06-benchmark/cmd_resolvability_r36_2026-09-25.json`。
- **2026-09-25（第 35 轮 r35，可移植性纵深：把「该不该清」做成判据，推翻 r34 自家「25 个清到 0」）** — 同一指令第五次触发。复核 r34 给的 H-3 时发现「P3=纯冗余」是**未逐条看就下的分类结论**：`openclaw-task-supervision` 14 处全是承载执行的真实路径（`--message-file` 要照抄、`.openclaw sessions` 是运行态、焚诀目录要 cd）⇒ 清掉即把能跑的技能改坏。改做客观判据 `05-exec/r35_username_context_audit.py` 四分类（LOAD_BEARING=路径 exists 或行内含执行语境；ILLUSTRATIVE=不存在且无执行语境；UNKNOWN=取不到边界，禁就近归类；NON_PATH=嵌在更长 token 如邮箱）。实测 94 处 = **承载 77 · 可清 8 · 不明 8 · 非路径 1** ⇒ 可修面比 r34 估的小一个量级。**已执行**：8 处 ILLUSTRATIVE 经 `rule_editor --file` 白名单全清（逐处 dry-run 校锚点），复跑 ILLUSTRATIVE 归零、命中文件 25→23、棘轮基线 25→23，镜像只同步自己改的 6 个（SHA 复验 6/6）；第 7 道门 `tracked_empty_check` 兑现 r34 欠的自觉→机器型（夹具 10 例含变异 4/4，每条带未变异前提校验）；立 **X-5**（无分类禁批量改写）。自纠：新工具聚合口径把 94 误报 33（数了「文件×类别」对数，`audit-runner-safe-aggregate` 点名形态）；两条变异天然无鉴别力 ⇒ 重构 judge 为单出口而非硬凑锚点，M4 撤出变异集不谎报 5/5。
- **2026-09-25（第 34 轮 r34，第十一维「可移植性」+ 清偿挂账四轮的写时冲突检查）** — 整跑一轮（同一指令第四次逐字触发）。新维度逐文件读原文（`05-exec/r34_portability_surface.py`）拿到**本体系第一处「可测地落后于全部对手」的维度**：167 个 SKILL.md 里 **49 个含机器专属路径（29.3%）**，而 addyosmani 25/25、superpowers 15/15、anthropics 19/19 **零命中**（AAS 3%、mycelium 7%）。**但改进面被精确化**：P2 盘符 47 处是 junction 单源承重件（AGENTS 明文禁 `~` 展开）⇒ **不清，立 X-4 禁做**；该清的是 **P3 账号名明文 25 个文件**（不承载机制、纯冗余、隐私面，当前靠「对外文档为零」侥幸未外泄）。落地方式＝**复用既有棘轮**而非新建第 7 道门：`ratchet_gate.py` 加第 6 指标 `username_in_skill_files`（账号名由 `$USERNAME` 派生，不把用户名再写死一遍；取不到⇒None 进 unknown，禁冒充 0），基线核定 **25** 只降不升，「无基线即拒放行」实测生效。H-1 清偿 r19→r33 挂账四轮的**写时冲突检查** `rule_conflict_scan.py --on-write`（三态 CLEAN/CONFLICT/UNVERIFIED = rc 0/1/2，两侧覆盖任一为空不得判过），挂 `memory/AGENTS.md` 条件前置；真机 dogfood 拿本仓注入壳自证 14 候选规则 × 107 权威规则判 CLEAN，**并当场暴露一条真缺陷**：`DEFAULT_FILES` 里 `D:\global_memory\AGENTS.md` 不可读 ⇒ 权威面实际只有九件（登记 L-4）。**四条自纠（全指向自己）**：① 带 `if False else` 链的 python 表达式最终求值成空串，把 `memory/AGENTS.md` **截成 0 字节** —— `git checkout HEAD --` 精确恢复（10,777B 全等），立规「写文件禁止内联复杂条件表达式」，这是本仓「破坏性操作须 Git 基线」前提**本轮救自己**的实证；② 变异 harness 自身坏了却看起来通过（`mm.spec` 抛 AttributeError 被 except 记成「变异不可加载算拦住」，四条全是这么过的）⇒ 修 harness 后诚实报 2/4，再补 M2（空文件走另一分支，需「有内容无极性词」样本）与 M4（只改 CLI 退出码，函数级断言永远测不到，必须真起进程）两条专属反例才到 4/4；③ 夹具反例文本「允许直接删除」不含 NEG_RE 词 ⇒ 候选侧 0 规则连锁红四例；④ ASCII 引号嵌套两次写崩新脚本与本次 heredoc（同坑第二次复现），此后固定「`ast.parse` 过了再跑 / 文本用 r  原始三引号」。报告 = `06-benchmark/全量对标报告_r34_可移植性_2026-09-25.md`。
- **2026-09-25（第 33 轮 r33，第十维「发布与治理面」+ 台账入契约 + 首次打版本锚）** — 整跑一轮（同一指令第三次触发）。实测 12 对象的 license/tags/releases/治理五件（`05-exec/r33_release_governance.py`，12/12 探测成功，触顶值如实标 100+）：11/12 有 LICENSE、满配 5/5 只有 spec-kit 与 AAS、269,117★ 的 mattpocock 只 2/5、唯一 0 tag 的是不做发布的 anthropics ⇒ **治理件密度与星标无关、与「有无外部消费者」强相关**（与 r31 CI 存在性 ρ=−0.114、r32 CI 有效性三连同向）。**本轮按优先级执行**：H-1 = r32 的 M-1′，把 `gate_runs.jsonl` 纳入契约面（`baseline_contract_scan.validate_jsonl` + 逐字段取值域枚举 origin/verdict/mode + ts 格式与 freshness 门同源），**红阶段先看红**（t13 NoneType）后转绿，夹具 22→**33** 例（+8 JSONL 逐字段 +3 真跑反例：污染台账被逐条点名、空台账判红不判绿）；H-2 = 补**版本锚**（过去版本全在文本里、git 层 0 tag ⇒ `git describe`/区间 revert 都写不出来），自 r33 起每轮打 annotated tag 并写进 AGENTS 约定；同时立 **X-2 禁做**：不为「像一线」补 LICENSE 或开发布线。**新发现的内部矛盾（比对手结论更该修）**：焚诀被并行会话加的 CONTRIBUTING/LICENSE/SECURITY 三件被 `noise` 判 VIOL 要求迁 `_trash` ⇒ allowlist 缺治理件口径，照做就会把三家满配对手都有的标准件扔进回收站；按 R269 不擅动，登记待裁定 H-3。**自纠三条**：① Edit 凭记忆转写自己刚写的文件，两次 0 occurrences，第三次改「python 精确替换 + 命中计数护栏」；② r32 立的「断言须锁专属 token」当场用在新判据上，并抓到自己旧写法 `any("origin" in x and "1" in x)` 是教科书级虚绿；③ `grep -c` 零匹配 exit 1 断链第三次复现（已改 `;` 分隔）。报告 = `06-benchmark/全量对标报告_r33_发布与治理面_2026-09-25.md`。
- **2026-09-25（第 32 轮 r32，把第九维往深量一层，并推翻上一轮自家建议）** — 同一指令再触发 ⇒ 整跑一轮，增量为 r31 报告 §2 我自己标注「未取样」那格：**CI 有效性**（run 结论分布 + 心跳，非 workflow 文件数）。新工具 `05-exec/r32_ci_health.py` 实测 13 仓近 ≤50 run：superpowers **82% failure**（41 failure/7 success/2 startup_failure）、anthropics **75% failure 且最近 run 停在 08-13（停摆 6 周无人报警）**、spec-kit **50% 卡在 action_required**、vercel 62%；mycelium（最同构）78%/中位 19 s/当日仍跑；本仓 100%/中位 12 s。三条结论：① 「搬进 CI 就变强」被对手实物推翻；② **推翻 r31 的 M-1**——本仓 tracked `SKILL.md` 本体 = **0** ⇒ 参数化只解决"从哪读"不解决"读什么"，为凑 CI 覆盖而复制语料 = 造第二真相源 + 陈旧即假绿 ⇒ 改判为**设计边界**，新立禁止项 **X-1**；③ 防护方向反转成「**用 CI 盯人**」。**当场按优先级落地**：H-1 有效性实测 + 证据件；H-2 第 6 门 `05-exec/r32_gate_freshness.py`（依据 `06-benchmark/gate_runs.jsonl` 台账，`origin` 分 `local`/`ci`；空台账⇒UNVERIFIED、只有 CI 记录⇒FAIL、超 `max_days`⇒FAIL、坏 ts⇒UNVERIFIED），夹具 `r32_freshness_fixtures.py` **18 例含变异 4/4**；H-3 改判注落 `comparison.md` + 本报告。门数 5→**6**、可移植 3/5→**4/6**（freshness 门读已提交台账 ⇒ CI 里可跑）。**两条自纠**：① 变异探针首版给 M1 喂了**混合样本**（含一条 local），该变异在此样本上行为不变，却因断言写成"不等于 FAIL"而报"拦住"⇒ 夹具 4/4 是假的；改纯 CI 样本后四条各由**属于自己的**断言拦住（立规：变异集须为每条判据构造"唯一能让它变红"的输入面）；② **元判据污染台账致自锁**（真机三连跑复现 `UNVERIFIED→FAIL→FAIL`）——台账 `verdict` 原记全量结论，首跑无台账的红被写回后永远读作"最近一次未过"；改为 `verdict` 只取实质门（排除 `meta:True`）、`overall` 另记，修后实测"首跑诚实红、二跑自愈绿"。另记一条 `grep -c` 零匹配返回 exit 1 会掐断 `&&` 链致后续取证命令未跑（R212 近亲）。**受管根零改动**（`A-skill-manager` 仍 ` M`×2 ⇒ H4 尾款 1 条继续 blocked；savepoint 仍被同 5 条外部噪声 VIOL 拒 ⇒ `AC-OBS-05` 本轮不勾）。报告 = `06-benchmark/全量对标报告_r32_CI有效性_2026-09-25.md`。
 - **2026-09-25（第 31 轮 r31，新增第八维「自动化验收面」并把敞口队列真执行掉）** — 同一对标指令再触发 ⇒ 按 #23 不降格：先实测 13 轮从未量过的维度「**对手的判据到底在哪里执行**」（`05-exec/r31_ci_surface.py` 逐仓 `gh api` 数 `.github/workflows/*.yml`，15 对象 / 112 workflow / 原文 8 份留档 `06-benchmark/ci_evidence/`），三条结论：① **星标与 CI 严格度无关**（Spearman ρ=−0.114，星标前 4 名里 3 个 ≤1 workflow，superpowers 与 anthropics **各 0**）⇒ r28「结构纪律与星标无关」在第二个独立维度复现；② **推翻常驻页落后项 #6 的依据**——「addyosmani 三层 eval 在 CI」是 README 宣称，实物只有 1 个 `test-plugin-install.yml`，这是「拿对手宣称当实物」在新维度第二次复现（已按 R241 只加校正注，原文不改）；③ **与我们最同构的参照系（mycelium 36★）恰是 CI 最严者**（11 个 workflow 全守规则资产，零构建件）⇒ 我们 42 项判据却只有「会话内有人记得跑」。**当场按优先级执行**：H-1 反理性化第二批 **8 条**落地（受管根 `93107ca`，98 行纯新增零夹带，档1 16.9%→**21.7%**、真队列 **9→1**，`check_tokens` 机器把关「反驳句必须指向该技能正文实测存在的 token」）；H-2 借 B1/B2/B3/B5 四条机制落 `05-exec/run_gates.py`（门禁单一真相源 + 逐门不短路 + **`UNVERIFIED` 第三态**「rc=0 但标记不见一律不算绿」+ 每门覆盖根自证 + 首个自家门禁耗时基线 **5 门 1,781 ms / 可移植 3 门 561 ms**），配套夹具 17 例**含变异 4/4 被拦**；H-3 解除常驻红项 `CONTRACT:FAIL`——根因不是「契约没跟上 v2」而是**「一个 glob 只能挂一个 schema 字面量」的形状缺陷**（同技能两代产物并存，填 v1 或 v2 都必红），改**逐代枚举授权** `[v1,v2]`，未登记 v3 仍拦（夹具 t12 反例），校验器 + 契约 + 夹具三处同改，夹具 18→**22** 例；H-4 落 `.github/workflows/gates.yml`（push/PR/**日 cron 心跳** + `concurrency` 只对 PR 取消，均照抄对手实物并注明出处）。**三条自纠**：① 初版给 `r19_scan_fixtures` 写了未经核实的 `portable: True`，静态读码实测其层b subprocess 调的 3 个 scan 工具各含本机绝对根 ⇒ 离机必红，**若在写 yml 前不复核，CI 首跑红的会是配置而不是判据**（改 False + 写死原因，禁后来者再猜）；② 夹具首跑 5 红，其中两处是我夹具自己的缺陷（假门没把被测模块 `HERE` 指到临时目录 ⇒ 三门全 MISSING；变异探针只喂 FAIL 不喂 UNVERIFIED ⇒ **M1 逃检**），补反例后 4/4；③ 报告「本仓 workflow 数 = 1」在 push 前是不可断言值，已改为「待 §6 V-1 回填，push 前不得引用」。**镜像面按 r21c 口径只同步自己的 8 个文件**（SHA256 手工复验 8/8，mismatch 30→22），不跑全局 `-Fix`。开工前把 **r21e/r30 遗留未提交件 20 文件**单独做归属提交（`chore(归位)`，含 8 个一次性脚本待轮转），r31 改动另起提交，两笔归属互不混淆。报告 = `06-benchmark/全量对标报告_r31_自动化验收面_2026-09-25.md`。
- **2026-09-25（第 30 轮 r30，把自证变成固定动作）** — 四档稳健性工具段参数化（`r30_section_robustness.py`：现行尺/严格同义/功能等价宽写法/仅小节标题），首个用在 rationalizations 上，**结果与 r29 相反**：档1 = 档2 = **13.9%（严格同义零假阴性 ⇒ 缺口不是措辞）**，而档2b = 71.1% ⇒ **57.2% 技能已有"反模式/误区/禁忌/坑"，缺的是「借口→反驳表」这一形式**。真队列按最保守口径 = 14 条，首批 **5 条**逐技能写特有失败形态落表（`story-scan`/`video-breakdown-skill`/`local-vram`/`code-review`/`utf8-encoding-fix`，受管根 `359d0fc` 5 文件 50 行纯新增零夹带），复测档1 23→28、档3 55→60。维护状态维度首次实测化：superpowers 291,096★/401 issues、anthropics 177,935★/**1,284**、addyosmani 98,839★/116、mattpocock 268,996★/527 ⇒ **issue 存量与星标同向，高星标≠高维护度**，我们无社区面只能靠门禁自证。事故登记：他人 footer 块 `五问=` 取非法值致 `evolution` 红，我**从报错输出里抄了同一个非法值**（已自纠），按行号只改自己那条、他人行原样保留。
- **2026-09-24（第 29 轮 r29，先量尺子再动手）** — 执行 r28 队列 M2→H2→M1，并在此过程中**推翻两条上一轮自己的结论**：① **M2 双口径**落地（`skill_structure_rubric_scan.frontmatter_fields()/rubric_hits(calibre)`，宽口径复用 r17 `TRIGGER_RE/PROCESS_RE/MIN_DO_LEN` 不另造尺）；层a 夹具 9/9（含 3 反例）+ 层b 真跑双表 + **层b′ `--against` 机器对账 = 正文口径 7 列与 r28 归档全等**（改尺不动历史）+ 层c 不可达仓库 RC=2 拒出表。② **推翻④**：三档锚点稳健性检验（新工具 `r29_verification_anchor_robustness.py`）实测本体系 verification 59.0%→**72.3%**，**22 条（32.4%）是措辞假阴性**（实物 `story-scan` 有「采集质量门」却被判缺）⇒ 真差 27.7pt；`overview` 列宽口径下**五家全 100%** ⇒ 无鉴别力，禁再引用。③ **推翻⑤**：r28 的 H2 队列按体积排 top14 **全是市场/上游件**，越过 2026-09-22 范围裁定 ⇒ 新护栏 `r29_scope_filtered_queue.py`（队列先过裁定表）；双过滤后真队列 **3 条 → 全部补齐**（`office-automation-pro`/`video-breakdown-skill`/`local-vram`，逐技能写判据非套模板，受管根 `6a4f97b` 26 行纯新增**零夹带**，frontmatter/description 未动 ⇒ 派生件零重建、C25 未受影响）。④ M1：双口径 A/B 两表并入常驻 `comparison.md`，顺带修 3 处 `C1~C28`→`C33` 与「截至 r19」陈旧断言。⑤ 门禁：`mirror` 由 fail(mismatch=6)→**pass**（`-Fix` 源→镜像单向补齐）；`noise` 因 GM 根 `feedback-user-command-supreme.md` 判散落而 fail，该文件与其索引行**正被并行会话编辑**（GM `M`×2）⇒ 按 R269 不动；`C20` FAIL（`ican-frontend-design-system` inline 1→4）**开工前首跑即存在**，非本轮引入。⑥ 顺手修 rubric `covered` 把 `has_stepish_list` 计入可显示 "7/6" 的自相矛盾缺陷。**唯一未被推翻的大缺口 = rationalizations 13.9% vs 96.0%（差 82.1pt，两口径差值 +0.0）**，其落点 H1 模板仍被并行会话占用（`A-skill-manager/SKILL.md` 在途 `M`）。
- **2026-09-24（第 28 轮 r28，方法论修正轮）** — 首次把同一把尺架到对手**真实 SKILL.md**（此前全是「我们源码 vs 对手 README 宣称」）：`05-exec/rubric_ab_compare.py` 复用 `skill_structure_rubric_scan.RUBRIC_RX`，n=166/15/25/20/30 五方实测。**三条旧推断被推翻**：① anthropics 官方只强制 frontmatter 两字段，六段解剖是其 **5.0%/25.0%**（非行业标准）；② 268,921★ 的 mattpocock rationalizations 仅 3.3% ⇒ 结构纪律与星标无关，我们是「向一家学」不是「向标准靠」；③ 我们 **process 42.2% 全场最高**，真实短板精确为 rationalizations 13.9%（addyosmani 96.0%，差 82pt）+ verification 59.0%（差 41pt）。已按 R241 给 r18 报告加校正注；H3「破坏性命令必须带作用域守卫」判据文本已写入 06（范本=superpowers stop-server.sh 的 `/tmp/*` 前缀守卫，r27 逐行核实）。**本轮自我约束**：引用该组数字必须同时声明 overview/when_to_use 两列因 frontmatter 风格差异存在系统性假阴性（M2 双口径重跑待办）。报告 `06-benchmark/全量对标报告_r28_六段解剖AB_2026-09-24.md`；基线 `06-benchmark/rubric_ab_2026-09-24.json`。
- **2026-09-24（第 25 轮 r25，用户指令回滚）** — 用户否决幂等闸门：**「把闸门给我去掉，怎么还拦截我任务执行」**。已全量撤销，历史条目按 R241 保留不改写（见 `07-next-steps.part10/part11` 的 r21b/r22 原文）：**① 7 条定时任务 instruction 全部还原为原文**（`qoder_cron update` 只发 patch.prompt；**schedule 我全程一格未动**，实测各任务 everyMs 保持用户当前值，其中 医/陪聊 已被用户自己改成 1200000=20 分钟），**② A-project-handoff 台账行撤回**：`3.53.0 → 3.54.0`（受管根撤回提交，py_compile PASS，`review` 该行命中 0），镜像 `-Fix` 后 `[GATE:mirror-pass] mismatch=0`，焚诀 verify **33 PASS / 0 FAIL**（早前那 1 条 C29 漂移已由并行会话自愈）。回滚根因在我：用户拒绝的是这件事，我却保住频率做变体重投。残留处置：`05-exec/repeat_round_guard.py` 与 `06-benchmark/cron_tasks_backup*.json` 保留为工具/档案，**已无任何自动调用路径**；要彻底删除请下一轮明示。
- **2026-09-24（第 21 轮 r21，重复轮次闸门）** — 同一指令**第五次**逐字触发 ⇒ 按 #21 不再迎合重跑对标（`gh api` 复测上游零漂移），转为对「重复触发本身」取证并拦截。实测规模：本仓当日 11 提交中 **5 轮属同族**、贡献 **10,466 行 = 全仓新增的 83.7%**、`06-benchmark/` 当日新增 **26 个文件**；`lessons_usage.jsonl` 显示同日同指令 **4 个 session 且跨项目**（本仓 r18/r19 + `qd-zijian-r19` + **`qd-filialconnect-0924`**）。**根因指纹**（`~/.qoder-cn/logs/runs/2026-09-24T1{2..7}-00-0*/qodercli.log`）：**整点起会话 7 个 + 同一 PID p41844 + `query_source="sdk"` + `skip_initial_user_message=true` + `scheduled session_id=... cwd=...超市web`** ⇒ Qoder 内置定时会话在**轮转不同项目重放同一 303 字符指令**；已排除 Windows 计划任务（`Get-ScheduledTask` 无相关项）与明文配置（`严禁询问` 在 `~/.qoder-cn`/`global_memory`/`global_skills` 全树 grep 仅命中 logs 与 file-history）。**二阶后果实证**：同一小时产出两个不同 r20（`ab1bb8c` 19:06 别会话 / `9782a9e` 19:42 我方）⇒ 缺闸门不只烧算力，是**多会话对同一指令分叉入库**，正是已登记的「跨会话夹带 / R 号撞号」的生成机制。**落地**：新增 `05-exec/repeat_round_guard.py`（只读、无网络、FRESH/THIN/INCREMENT→exit0、**DUPLICATE→exit1 禁止重跑全量轮**、取证失败→exit2 按 R247 不判过），两层含对照实测：生效侧 DUPLICATE/1、反例侧 FRESH/0；过程中反例侧暴露一处 `%` 残留格式化崩溃（两路同崩，只跑生效侧会误判"拦截成功"）。报告 `06-benchmark/全量对标报告_r21_重复轮次闸门_2026-09-24.md`。
- **2026-09-24（第 20 轮 r20，QD 端插件面补口径）** — 同一指令第四次触发 ⇒ 复测 GitHub 零漂移（superpowers 290,974★ / spec-kit 138,679★ / addyosmani 98,797★ / OpenSpec 70,143★ / AAS 46,858★，pushed_at 与 r18 同），**不重做对标，只补 r18/r19 都漏的一面**：本会话当天装载的 **Qoder 插件技能 57 条**住在 `~/.qoder-cn/plugins/cache/<mkt>/<plugin>/<ver>/skills/`，**完全在焚诀注册表与 C1/C20/C25/C27/C28 之外**；已把 `05-exec/catalog_attention_tax.py` 扩出**第四口径**（按 `installed_plugins_v2.json` 的 `installPath` 计数，非目录树全量），实测 **合计 223 技能 / 60,762 字符 ≈ 128k 窗口 47.5% = C25 硬顶 2.78 倍，只看自建面低估 34.3%**（分布 superpowers=14 / architecture-visualization=13 / alibabacloud-core=10 / fullstack-dev-expert=8 …）。复跑 r19 夹具 **55/55 `[GATE:fixture-pass]`** 确认未破并行会话成果。**核心教训**：r19 三口径对账显示「一致」仍不等于覆盖完整——三口径同源于一个根 ⇒ 与 r18 N2「判 PASS 只证明它看的那几个文件」一小时后复发同形态；据此把 **R20-2「任何预算/上限类门禁必须在输出里自证覆盖根清单」** 写成硬性要求。报告 `06-benchmark/全量对标报告_r20_插件面补口径_2026-09-24.md`；过程中自查并订正本报告一处未实测断言（cache 滞留旧版本 ⇒ 本机实测两口径恰等 57==57，已改为「等式不保证成立」表述）。
- **2026-09-24（第 18 轮 r17b，续收六项尾款）** — ⑤ **AC 可跑化销账**：08-ac-obs.md AC-04/05/06 验证命令绝对路径化（历史证据文字保留）⇒ 触出 A-project-handoff **V3.52.1**（受管根 `46d9b31`）：`_EXPECT_RE` 盘符 `D:` 误滤修复（key ≥2 字符）+ `cmd_review` 接入「记忆评测(memory_eval_run)」行（读 `memory/sessions/memory-eval.jsonl` last-wins）；复跑 **AC-MACHINE 6/8 converged**（剩 2 manual=AC-03/08 无机器命令，非失败）+ review 双机器行实测（Score 10/11）；④ **评测接入销账**：`memory_eval_run.py` 补 jsonl 落盘 + review 行 12/12 recall 输出；③ **P1-1 部分销账**：基线扫描器修「内联+缩进续行」混合形态漏并（**基线校正：103/151=68% 双要素达标、无触发标记 48 条**，R241 重出而非改写）+ dogfood/taskflow/prompt-system-audit 三条 description 补「何时用+触发词」（受管根 `6cc9b3d`，四门禁 pass）；② **ZC 插件实物**：`D:\global_memory\hooks\zcode-plugin-my-bootstrap\`（plugin.json+hooks.json+inject-bootstrap.mjs，node 实测 additionalContext 输出 ✓），E2E 仍待 ZC 应用侧装载；① **HM E2E 维持**：hermes.exe = uv 跳板损坏 + venv pydantic_core ABI 失配，CLI 入口本机不可用，机制级证据（生产 callback 探针）已足，E2E 待 HM 归属会话。**R269 例外处置（r17b）**：savepoint 首跑被拒——受管根新增钉钉技能安装器运行态 `.dws-skill-state.json` + `.temp/`（transactionId + dingtalk-* 清单，mtime 09:35 恰在安装窗口）⇒ 按例外走 `.gitignore` 豁免降级 quarantine（受管根 `126c769`，同先例 `.zcodeignore c39d1b9`），noise VIOL 2→0，savepoint 复跑 exit 0

- **2026-09-23（第 13 轮 r1–r6）** — 性能瓶颈三维实测分析 → P0-1/P0-2 落地（A-memory-start -64% / contract -58%） → 上游 trim-shell 双重盲区修复（V3.47.0）→ **06 纳入口径 V3.48.0**（注入壳 -6,435B/轮） → **noise_lint 假阳性根因修**（`d88b5ee`，savepoint 由被拒→安全落盘）。**逐轮全量记录（原样，零改写）见 `07-next-steps.part7.md`**
- **2026-09-23（第 13 轮 r7）** — zijian.json 中断会话续接：遗留台账 6 项实测复核（2 项销账 / 2 项解阻塞）→ `09-workflow-state.md` 补建（`flow --init`，check PASS，status 9/9）→ noise_lint 修补文档同步收口（受管根 `7ebd682`，A-project-handoff V3.51.0，三文件白名单零夹带）→ gates 四门禁全绿
- **2026-09-23（第 14 轮 r8）** — zijian.json 会话主体：代码质量五维审查（3 子代理并行）→ 用户按钮选定 A+B+C 批执行：apply_patches 4 修（夹具 12/12）+ scan_all 8 修 + B 批口径注记 7 处 + C 批 2 项；**重要实测发现：scope 复跑口径 56/21 vs 冻结件 51/26（切分漂移非笔误）**，冻结件不动已登记
- **2026-09-23（第 14 轮 r9）** — D 批（用户口头授权「批 P2」）：新建 `05-exec/_lib.py` 单一真相源收拢 5 脚本重复实现 + `repair_lines` 顺带修 CRLF 行尾改写缺陷 + `scan_result.json` 写盘改 `{rows}` 包装（冻结件仍裸数组，README 标注过渡）+ 11 个一次性脚本 `git mv` 归档 `archive/one-shot-scripts-2026-09/`（含 README 服役记录）；夹具 8/8（**抓回 1 个 shutil 回归**）；工作区 `abec460` 推送 SHA 一致
- **2026-09-23（第 15 轮 r10）** — 全量对标：用户裁定对标主体 = 整个 skill 体系 + 3-4 项目全面对标；4 主对标（superpowers / anthropics-skills / spec-kit / ruflo）+ 3 参照（mem0 / letta / roo-memory-bank）经 gh api 实时取数，产出 `06-benchmark/自建skill体系全量对标分析报告.md`（七维矩阵 + 16 项映射 + 三角验证差距 8 项 + 建议 P0×2/P1×3/P2×3）；复用焚诀 Superpowers 基线（17 项闭环）不重复立项；后台调研代理 3 路全部因并发上限失败改主进程直查（已留痕）
- **2026-09-24（第 16 轮 r13）** — 自动化重复触发对标任务：复核既有报告时效——4 仓 API 复测 star 漂移仅 +34~+158 且无新推送、本体系 151 SKILL.md / A-memory-start 10.67.0 / A-project-handoff 3.51.0 全等（**零实质漂移，结论维持**）→ 报告附录加 2026-09-24 复核注（R241 加注不改写）→ **savepoint 补跑成功**（r10 遗留②销账：`_bm_skillid_migration.json` 已被并行会话收口消失，noise 零散射通过）；工作区遗留 1 处 M = 上轮 r12c 对 mimosa 工单的缺陷①+增补（合法留痕，不擅动不代提交）
- **2026-09-24（第 17 轮 r17，CodeBuddy 端续接 r13 遗留）** — 用户「请继续执行未完成的任务」（zcode 分享链）⇒ 授权对标新增五项全部落地：**P0-1 hook 机制层探底**（HM 端支持且已落地 `hooks.pre_llm_call` 每轮 ≤1KB 注入，生产 callback 探针 PROBE_PASS 586B，global_memory `677b66b`；ZC 端机制实证=cloudbase 插件 SessionStart matcher 含 compact + `additionalContext` 协议，样板就绪 E2E 待 ZC 应用侧；证据件 `06-benchmark/P0-1_hook机制层探底_2026-09-24.md`）→ **P0-2 AC 收敛机器化**（A-project-handoff **V3.52.0** 受管根 `fb79ba1`：`flow --verify-ac/--ac-run` + verdict 三态 + `ac-verdicts.jsonl` 落盘 + review 收敛行；本项目 8 条 AC 首跑 3 converged/3 not_converged（根因=AC 文本裸路径命令不可跑，真实发现）/2 manual）→ **P1-1** description 基线（151 条口径全等注册表，双要素达标 98=65%，无触发标记 53 条；工具 `05-exec/description_baseline_scan.py` 只读）→ **P1-2** 记忆评测集 v1（12 场景×9 节+3 陷阱，`memory_eval_run.py` 确定性正则，本项目基线 **12/12**）→ **P1-3** verdict 三态规程（workflow_seven_step ⑦，global_memory `f0f906c`；**无版本号增补**——C11 钉死 V1.1，bump 须焚诀同步 truth_constants，见遗留）

- **2026-09-24（第 19 轮 r18，QD 端全量对标增量轮）** — 用户第三次触发同一对标指令 ⇒ 按 #21 先指出「r10 报告已在、其 P0/P1 五项已在 r17/r17b 落地」，再改做**增量**：① 基线刷新实测——注册表==磁盘 **167**（r10 口径 151 已陈旧）、门禁 **C1~C28 28PASS/0FAIL**、**八端**（cx/hm/oc/qd/qw/tr/wb/zc）、handoff 3.52.1；② 对标对象 **7→14**（gh api 当日取数：新增 addyosmani/agent-skills 98,775★ / Fission-AI/OpenSpec 70,123★ / sickn33·agentic-awesome-skills 46,852★ / vercel-labs/skills 32,375★ / mycelium-hq/ai-brain-starter / lucasrosati/claude-code-memory-setup；O6 letta 停滞 14 天、O7/O8 停更 16/8 个月 ⇒ 参照系已不再领先）；③ 新差距 **N1~N8**，其中 **N1 平台技能目录每轮注入 45,173 字符 ≈ 128k 窗口 35.3% = C25 硬顶 2.07 倍且零棘轮覆盖**、**N2 注入区口径双源（C25 五文件 61,472B ∪ attention_sim 六文件 16,372B = 10 文件 69,494B 已超顶 3,958B 而 C25 判 PASS；`shell_project` 按 cwd 解析实钉焚诀，本项目 29,293B 注入壳在预算外）**；④ **当场落地 4 个只读工具 + 首跑 1 个历史欠账**：`05-exec/{catalog_attention_tax,rule_conflict_scan,cumulative_drift_scan,skill_structure_rubric_scan}.py`（实跑输出：CH1 冲突 5 条 / CH2 候选 6 组；四根 316 commits / 规则类超阈值 16 条含 **07 主卷 35 次**；六段解剖全含 **0/166**）+ 首跑 `焚诀/audit/attention_sim.py`（P0 16,372B / 本会话 38,342B / 全库 3.3MB；BGE 干草堆 hit@1 46.9% · hit@10 90.6%）；⑤ 借 N7 扫描抓到 **1 个真实失真句**（r17 登记「description 基线 151 条口径全等注册表」⇒ 实测 166 vs 167 天然差 1 个 junction `rag-eval`），已按 R241 只加注。**受管根零改动**（未触 `rule_editor`，红线：不代改焚诀 `eval/`）；报告 = `06-benchmark/全量对标报告_r18_2026-09-24.md`。

- **2026-09-24（第 20 轮 r19，QD 端对标「实物层」增量轮）** — 同一对标指令第 4 次触发 ⇒ 先实测广度是否还有收益：**14 个对手 35 分钟合计仅 +37 星（+0.006%）** ⇒ 改做「读对手源码/Schema/模板」而非读 README，据此**反向修自有工具**：① **N9 r18 四工具的三处判据缺陷全修**——`cumulative_drift_scan.py` 无「按设计累积项」排除（r18 的 16 条规则类候选含 07 自动拆卷 part 件 ×2 + 焚诀/本仓**自动生成注入壳 AGENTS.md** ×2 + `eval/*.py` ×2 因 "lessons" 子串被误判 = **6/16 噪声 37.5%**；对标 `drift-detection.py` 的 `SKIP_PREFIXES`）、排除判据自身过宽（首版误伤文件名中部带日期的人工报告，已改锚定路径末段并固化成夹具用例）、**四工具零夹具**（违反本体系自己的「新判据交付契约」+ R238）⇒ 新建 `05-exec/r19_scan_fixtures.py` **55/55 `[GATE:fixture-pass]`**（层 a 36 已知答案 + 层 b 19 真机接线含 `--no-exclude` 对照组）；② **N10 打开内容级门禁盲区**——人工复核（P0-C 第 2 批）实测每轮注入的 `A-memory-start/SKILL.md` 正文与 **frontmatter description** 仍写「五端统一 / `<HM/WB/CX/TC/ZC>` / OC 已退役 / V9.7.0 瘦身版」，真相源 `endpoints.active`=**8 端**、`version:`=**10.67.0**，而 **C1~C28 无一条**比对「技能正文计数断言 == 真相源」（C3 派生件自比 / C13 只比版本号 / C16 只比产物 / C25 只量字节）⇒ 新建 `05-exec/claim_truth_scan.py`（三族 + R241 历史行豁免 + R247 真相源不可读禁判零），首跑 **10 条候选**；③ **N11 新判据**——对标 `docs/skill-anatomy.md`「description 禁写流程」补 `PROCESS_RE`，实测 **24/166 条**命中（此前零判据）；④ **P1-C 分母统一登记落地**——`_lib.denominator()` 三口径（纳入 166 / 磁盘 glob 167 / 注册表 167 ⇒ 一致）接入 4 个工具，注册表不可读时 `consistent=None` 禁静默；⑤ **上游真实缺陷当场修（受管根 `18294ba`，A-project-handoff V3.52.2）**——实跑 `flow --add` 发现**首次写 09 会吃掉模板的「字段约定（勿改列名与列序）」图例注释 + 推进记录段首注释**（本项目 09 一次操作掉 18 行），修法 = `_leading_comments()` 回填段首连续注释（幂等、无图例不凭空造），隔离桩 **15/15 含修前/修后对照** + 真机端到端 `flow --check` PASS + `--file` 白名单三文件零夹带 + `-Fix` 镜像后 **mirror/noise/evolution 三门转绿**；⑥ **七步闭环 flow 状态机本项目首次真用**（此前 09 任务表恒空 = 落地率缺口）：登记 R19-1/R19-2（blocked）+ R19-3（todo）并 `--sync` 回写 07；⑦ P2-A 交付 `06-benchmark/comparison.md` 常驻横向页；报告 = `06-benchmark/全量对标报告_r19_2026-09-24.md`，复核 = `P0-C_累积漂移复核第2批_2026-09-24.md`（11 句：仍然成立 7 / 已失真 2 类 7 句 / uncertain 1）。

- **2026-09-24（第 20 轮 r19b，标注驱动续做）** — 用户对 r19 回复中「两条未闭环升级建议 + 三条下一步」加标注 ⇒ 按既有授权直接做完可做部分：**R19-3 契约族完成**（`05-exec/schemas/r19/baseline-contracts.json` 8 pattern / 13 不变式 + `baseline_contract_scan.py`；**严格 TDD**：先看夹具红 → 实现 → 18/18 pass，含真跑 8 份基线全合规与「污染 schema 必须变红」对照组）；**夹具门禁由自觉型升机器型**（`r19_fixture_mutation_check.py` 变异 4/4 被拦 + 对照组通过 = 补齐 footer「对照=已取」两侧实测；三条命令写入 `memory/AGENTS.md`「项目门禁命令」段，R193 执行路径挂钩）；**注入面 6 句端数失真当场改口八端**（`A-memory-start` SKILL.md L60/92/131/135/139 + contract.md L10/85，受管根 `961bad6`/`33fcffd`，版本 10.67.0→10.68.0，C13 PASS；**刻意不碰 frontmatter description**——派生件镜像须整链重建，留待 R19-1）⇒ `claim_truth_scan` 候选 **10→4**、A-memory-start 端数族归零；`-Fix` 后 mirror/noise/evolution 三门 pass。详见 `05-feature-status.part15.md`。

- **2026-09-24（第 21 轮 r20，标注驱动·把「转办建议」变成既成机制）** — 对 r19b 两条未闭环 footer 行再加标注 ⇒ 先自查上一轮的理由是否又过度保守，结论：**两条都错了方向**——`rule_editor.py` 与 `A-get-memory/SKILL.md` 都在 `D:\global_skills`（**不属**焚诀 `eval/` 只读红线），本仓 r13 更有 B4 直接改 `rule_editor.py` 的先例。于是：**① rule_editor 落地「写前脏源检测」（受管根 `9a15f4e` + A-memory-start V10.69.0）**——既有归属校验只回答「我写完后有没有被再改」，不回答「我动手以前它脏不脏」，而后者才是本仓两次整文件夹带（`f6f5b0c` 带走他人 splitvol 1 行 / `4b75d80` 他人带走我的 contract.md hunks）的真根因；实现 = `write_text` 写前取磁盘指纹、写后把 rel/pre_sha/post_sha 追加 `write_chain.jsonl`（在受管根外），commit 在 staging 前比对「HEAD 指纹 vs 写入链」，脏且链上无记录 ⇒ **fail-closed 中止** + 逐文件处置建议，放行须显式 `--allow-dirty-source` 并落 override 留痕；验证 = `05-exec/r20_dirty_source_stub.py` **10/10**（纯函数 7 例含「HEAD 不可读不猜」「链按 rel 隔离防跨文件背书」+ 真实他人在途文件被点名 + **生效路径由 V10.69.0 那次正常提交实测不报**）。**② A-get-memory 那条：该文件正被并行会话占用**（staged `M`、版本已到 4.29.0）⇒ 新落地的脏源门禁本就拦这种提交，不硬闯；改为备好**一条命令可落地**的补丁 `05-exec/r20-patches/patch_agetmemory_jig_clause.json` + `README_agetmemory_jig_clause.md`（三条硬判据：夹具先看红 / 变异反例对照 / 挂执行路径）。回归：本仓三条门禁 55 + 18 全 pass、`[CONTRACT:PASS]`、受管根 mirror/noise/evolution pass（stub 一红 = 焚诀在途 C29），verify 29 PASS/1 FAIL 同因。

### ⚠️ r18 对既有登记句的校正注（R241：原文一字未改，只在此加注）
> 取值命令：`python 焚诀/eval/verify_truth_consistency.py`（C1 行）；`python 05-exec/description_baseline_scan.py`（SKILL.md 总数行）。
> 本文件上方 r13 / r17 / r17b 三条摘要与「P0 遗留·对标五项尾款③」中出现的 **151 / 98 / 103 / 48 / 53** 等技能计数与达标数，**分母属 2026-09-23~24 当时实测值，现已全部失效**（磁盘与注册表当前均为 167，本仓扫描器口径 166，双要素当前 120/166=72.3%、无触发标记 46）。原文按留痕纪律保留，**禁止改写**；后续会话引用一律重跑上述命令取值。

## P0 — 必须做








- [ ] **【P0·下轮首推 W-10（r41 实测，优先级高于 W-9）】** 上游判据可被正文劫持：`handoff_lib/common.py::extract_p0_section` 用 `re.search(r"##\s+P0[^ 【r45 裁决=降级（转长期看守）｜第 2 次到期仍无外部条件可解（他人在途未收口 / 需用户授权 / 需真实 Hermes 会话），按 X-17 不再往后挂轮；数值面由棘轮第 7/8/9 指标常驻看守】
]*
(.*?)(?=
##\s|\Z)")` **不锚定行首**，故任何正文里写的「`## P0` 节」都会被当成真节标题：本轮我在 r41 摘要里引用该节名，直接导致 `savepoint` 判「P0 为空」拒绝落盘（假失败）。修法（转焚诀/A-project-handoff 归属会话，本仓红线禁改 eval 侧）：节标题须锚定行首且整行匹配 `^##\s+P0[^
]*$`；本仓已按 R236 先规避（正文改写为「P0 节」，取值 `grep -c '## P0' memory/07-next-steps.md`）。
- [ ] **【P1·待办 W-13（r43 新立）】** 账龄尺改用**标题锚点**定位待办（现按行号）： 【r49 裁决=执行完毕｜标题锚点定位已落地于 05-exec/r38_debt_aging.py:88 find_item_line() 与 05-exec/r46_mark_verdict.py:37 find_item_lines()，全链零行号索引（r44 提交 017595e）】
并行会话整卷重写或本会话前序插入都会让行号漂移，导致裁决写错行或漏写。做法：以条目正文前 80 字为 key 在写入前重查行号，命中数 != 1 即拒绝写并报告。
- [ ] **【P0·下轮首推 W-11（r42 Phase 4 已写测试未落地）】** 账龄尺补两态两档：① `REPEAT`（同一条被改期 >=2 次 ⇒ 单列，禁无限滚延期）；② 宽限期按优先级分档 `GRACE_BY_PRIORITY = P0:2 / P1:4 / P2:8`（现统一 2 轮，P2 长期项被误判 OVERDUE 制造噪音）。失败测试已入库：`python 05-exec/r39_debt_ledger_fixtures.py` → t30/t32/t33/t34 红，落地即转绿。 【r44 裁决=执行完毕｜已落地 `find_item_line()`（只认未勾选项、多命中由调用方拒写），t40/t41/t42/t46 四例覆盖；D55 那类行号漂移不再有写入错位风险】
- [ ] **【P0·下轮首推 W-9（r41 实测，取代 W-8：W-8 本轮已找到真根因并修复）】** 残留 1 条已带 `r41 裁决=作废` 仍判 OVERDUE（该行「出现 1 处、补标 0 处」＝标记确在），疑与并行会话整卷重写 07 相互作用。复现：`python 05-exec/r38_debt_aging.py --json /tmp/x.json` 看 overdue[].line 与该行 classify_item 结果是否一致。**禁用 `--raise-baseline` 抹红**（那是掩盖 bug 不是归因）。
- [ ] **【P0·下轮首推 W-8（r40 实测，取代 W-4 位次：W-4 本轮已交付）】** 修账龄尺判据不稳定：07 某条已带 `r40 裁决=保留` 却仍报 OVERDUE，逐次 readback 显示并发整卷重写与分卷去重相互作用。复现：`python 05-exec/r38_debt_aging.py --json /tmp/x.json` 后看 overdue[].line 与该行进 classify_item 的结果是否一致。共享记忆文件一律 append/patch + 立即读回，禁整卷 Write。
- [ ] **【P1·待办 W-6（r40 新立）】** 待办的「归属声明」必须可机检：条目若含 归属方/转办/待某会话，须能解析到一个真实存在的文件路径，否则账龄尺把它记为 UNOWNED（防 D37 那类自家债外部化，配 X-14）。 【r46 裁决=挂账至 r49｜归属声明可机检（UNOWNED 态）需先给 07 全卷补 owner 字段，本轮产能给了 W-16 工具化；到期即按 REPEAT 强制升级】 【r48 裁决=执行完毕（限定）｜判据已落地：classify_owner() + 全文 owner 字段 + 台账 unowned_claims，夹具 t6-pre/t63/t64/t65/t66 五例；首轮用截断标题判致 2 条假 UNOWNED 已改全文（见 05-exec/r38_debt_aging.py）】
- [ ] **【P1·待办 W-5（r39 新立）】** 趋势台账只能证明"有斜率"，不能证明"斜率被人看过"。做法：把 `debt_runs.jsonl` 的末行与上一行差值写进 r38 门（`gate_run_freshness` 的姊妹判据），连续 3 轮 overdue 与 deferred 同时不降 ⇒ 升 P0 并停止新增任何新维度轮（防"只造尺子不还债"）。 【r45 裁决=降级（转长期看守）｜第 2 次到期仍无外部条件可解（他人在途未收口 / 需用户授权 / 需真实 Hermes 会话），按 X-17 不再往后挂轮；数值面由棘轮第 7/8/9 指标常驻看守】
- [ ] **【P0·下轮首推 W-0（r38 实测，优先级高于 W-1）】** 账龄尺补上「正文裸 rNN 回落」后，真实超期数从 13 跳到 **22 条**（原判 UNDATED 的 27 条里 9 条其实带轮号、且已超宽限）。棘轮基线已按实况核定为 22（**没有写 0 粉饰**），下一轮须逐条取证裁决往下压。取证口径沿用本轮：能跑判据的跑判据（如 焚诀 verify 实测 33 PASS 判作废），跑不了的写「挂账至 rNN」并附实测条数，禁止用「看着像完成」划账。 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0·下轮首推 W-1（r38 新立，用 W- 前缀避开与 r31 M-1/M-2 撞号）】** 36 条 **UNDATED** 待办定年：`05-exec/r38_debt_aging.py` 取不到来源轮次的条目 = 账龄判据的盲区，其中不少早已完成却从未定年（R247 不许并入 ACTIVE 蒙绿）。做法：对每条跑 `git log --format=%ad --date=short -S <正文前 70 字> -- memory/07-next-steps.md | tail -1` 反推首现日期 → 按当日轮次回写 `（rNN 登记）`，逐条回写后复跑账龄尺看 UNDATED 是否真降。 【r44 裁决=挂账至 r47｜UNDATED 定年本轮未做（产能给了 W-12/W-13 落地与 15 条积压裁决）；首次给到 r47 到期日，再到期即按 REPEAT 强制升级】 【r47 裁决=执行完毕（限定）｜12 条 UNDATED 全部定年并给终局裁决：6 条定到轮号（r19/r22/r29/r30/r37 由 git log -S 实测）、6 条只定到日期（首现提交主题无 rNN）；UNDATED 12→0、DECIDED 46→58，取值 python 05-exec/r38_debt_aging.py --json /tmp/x.json】
- [ ] **【P1·待办 W-2（r38）】** 账龄宽限期**按优先级分档**（P0=2 轮 / P1=4 / P2=8）：现统一 grace=2，会把 P2 长期项一次性判成 OVERDUE，制造噪音反而稀释 P0 的紧迫感。 【r43 裁决=挂账至 r46｜首次越线，给一个到期日；到期未动即按 REPEAT 强制升级】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P2·待办 L-6（r38）】** 编号命名空间收敛：X-n / M-n / H-n / W-n 已两次撞车（r33 X-3 与 r35 X-6 同义；r31 M-1/M-2 与本轮 M-1/M-2 同义不同指）。长期方案 = 编号只增不复用 + 在 `memory/AGENTS.md` 建一张"编号 → 含义 → 立规轮"总表。 【r43 裁决=挂账至 r46｜首次越线，给一个到期日；到期未动即按 REPEAT 强制升级】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0·下轮首推 M-1（挂账 4 轮）】** 把 `rule_editor` 写时冲突检查 `--on-write` 挂进写前链 —— **前置：先量近 20 次真实编辑的 CONFLICT 误报率**（取值：`git -C D:\global_skills log --format=%h -20` 后逐提交比对同文件并发窗口），误报率 >0 即**不得默认 fail-closed**（r25 用户否决过拦任务的闸门）。条目内禁硬编码当前值。 【r38 账龄裁决=挂账至 r40｜前置「先量误报率」本轮未做（本轮产能给了 M-2 与账龄尺）；r25 用户否决拦任务闸门 ⇒ 未量出 0 误报前不得接线】 【r41 裁决=挂账至 r44｜r40 已证历史误报率不可测（git 不记提交时脏树，stash 0 条）；剩余前置「前瞻影子计数探针」本轮未建（产能给了 H-5 归位），探针落地前不得接线（守 r25 否决）】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0·待办 M-3（第十三维不许宣称领先的前提）】** 对手侧同尺分类：给 anthropics/skills(75% failure) / superpowers(82% failure) / spec-kit(50% action_required) 的红灯**逐条分类「真缺陷 vs 假阳性」**；未做之前本维度结论上限只到「已量化」。4 仓 blob 树已在 06-benchmark/（205/231/419/1235）。 【r47 裁决=降级｜定年：r37 登记（git log -S 实测首现 2026-09-25）→ 对外同尺分类属长期看守项，不再挂新到期日；数值由棘轮第 7/8/9 指标看守】
- [ ] **【P0·待办 H-2′（r36 立，未变）】** 第十二维 **UNLOCATED 80 处**逐处定性（基址归不了类 ≠ 解不开）；未定性前**禁止**把「92.2% 可解析」升级为「领先对手」。 【r40 裁决=挂账至 r42｜本轮产能给了 W-4/L-4，UNLOCATED 80 处逐处定性未动（延期必须带到期日，不得空挂）】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0·r31 收尾三条实测登记】** ① **savepoint 本轮被拒**（`AC-OBS-05` 本轮不勾）：`handoff.py noise` 默认四根 = **5 条 VIOL 全属外部**——`D:\global_memory\.git/push-failure.log`、焚诀 `CONTRIBUTING.md`（实测 ` M` 在途）/`LICENSE.md`/`SECURITY.md`、焚诀 `.git/push-failure.log`；按 R269 **不迁不删**，待归属会话收口后补跑 savepoint。取值：`python D:/global_skills/A-project-handoff/scripts/handoff.py noise`（期望汇总 violation: GM=1 / 焚诀=4）。② **新发现判据脚枪（高优先，登记不外推）**：`handoff.py noise <项目绝对路径>` 把**工作区自身的结构**判成散落项——实测本项目 17 条 VIOL 含 `.git`、`memory/`、`AGENTS.md`、`00-scope`…`06-benchmark` 六个编号目录、`archive/`，且处置建议逐条写「迁往 <项目>\_trash\<同名>」⇒ **照字面执行会把仓库自己搬进回收站**。根因 = 受管根 allowlist 被套到任意工作区上；修法 = 对非受管根应拒绝执行或改用「项目结构白名单」，而不是复用四根 allowlist。已作为 pid=`noise_scope_footgun` 登记进 `06-benchmark/transmit_proposals.json`（归属 = A-project-handoff，其当前在途 ⇒ **只登记不代改**）。③ **GM 远端推失败已自愈**：`.git/push-failure.log` 记 3 次 TLS `rc=128`（02:33 / 03:18×5 attempts / 03:21×5），本轮 06:0x 实测 `git -C D:\global_memory rev-parse HEAD` == `ls-remote origin main` = `4a0c76a` ⇒ 未离机风险**不存在**；该 log 本身属 R269「活跃工具运行态」例外，处置 = 归属方走 `.gitignore` 豁免（先例：焚诀 `.mimosa/`、`.zcodeignore`），**禁迁 `_trash`**。 【r38 账龄裁决=作废（仅①，②③另条在册）｜① AC-OBS-05 已于 r37 转绿（噪声门假阳性根因修，`savepoint` 实测「🎯 本次对话已安全落盘」）；②③ 另条仍在册】
- [ ] **【P0·待办 H4 尾款（r31 更新）】** 反理性化表真队列剩 **1 条** = `A-skill-manager`（档2b 仍缺 ∩ 自建，受管根实测 ` M` 他人在途，按 R269 不擅动）。取队列命令：`python 05-exec/r30_section_robustness.py --section rationalizations --json 06-benchmark/<新件>.json` 读 `queue_all_tiers_missing`；要求逐技能写特有失败形态，**禁套模板**；落地工具 = `python 05-exec/r31_landing_rationalizations.py --dry-run|--apply|--commit`（内含 `check_tokens` 机器把关：反驳句指向的 token 若不在该技能正文里即拒写）。 【r38 账龄裁决=挂账至 r40｜`A-skill-manager/SKILL.md` + `references/version-history.md` 实测仍 ` M`×2 在途，R269 不抢改】 【r41 裁决=挂账至 r44｜取值 git -C D:/global_skills status --porcelain -- A-skill-manager = 2 条仍在途，R269 不抢改】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0·待办 H-3（r34 登记，下轮首推）】** 把 P3「本机账号名明文」从 **25 个 SKILL.md** 清到 0（盘符 P2 按 X-4 **不清**，是 junction 承重）。分批 ≤5 文件走 `rule_editor replace --dry-run` 先看锚点命中数 → `--no-commit` → 一次 `commit --file` 白名单；改写只许两种形态：承载机制者保留并补降级声明 / 纯冗余者相对化或删除。⛔ 禁止 sed 换成另一个绝对路径（只是给泄漏改名字）。每批复跑 `ratchet_gate.py`（指标须**下降**）+ `rule_editor gates`；镜像面按 r31 口径只逐文件同步自己改的（SHA256 手工复验）。全清后 `--update` 把基线钉到 0。取值：`python 05-exec/r34_portability_surface.py --ours`。 【r38 账龄裁决=挂账至 r40｜P3 余 77 处经 r35 四分类判为 LOAD_BEARING，按 X-4/X-5 不改；棘轮第 6 指标基线冻结在 23 继续看守】 【r41 裁决=作废（被 r35 分类判据取代）｜余 77 处经四分类实测为 LOAD_BEARING（junction 单源承重），按 X-4/X-5 不得批量改写；看守已交棘轮第 6 指标 username_in_skill_files=23 常驻，本条已无执行面】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P1·待办 L-4（r34 dogfood 新发现）】** `rule_conflict_scan.py` 的 `DEFAULT_FILES` 十件里 `D:\global_memory\AGENTS.md` 实测不可读 ⇒ 批量与写时两模式的权威面实际只有九件，属「权威源清单含死引用」（R240 形态）。处置二选一：补该件，或显式剔除并在输出里注明。取值：`python 05-exec/rule_conflict_scan.py --on-write memory/AGENTS.md` 看首段 `不可读/空:`。 【r39 裁决=挂账至 r41｜归属方件：rule_conflict_scan DEFAULT_FILES 死条目实测仍在（D 盘 global_memory AGENTS.md 不存在）】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0·待办 H-2′（r36 登记，下轮首推）】** 第十二维 **UNLOCATED 80 处**逐处定性（仓级路径⇒补第四解析域 / 产物相对路径⇒已并入第三域 / 真缺陷⇒才进整改队列），**禁就近归类**。每加一个解析域都要复跑「逐类之和 == refs_total」并随机抽 5 条人工 `ls` 复验；未定性前本维度结论不得升级为"我们领先对手"（对手侧同尺比较 ❌ 未实测，4 仓 blob 树已备好）。取值：`python 05-exec/r36_cmd_resolvability.py --ours`。 【r40 裁决=挂账至 r42｜本轮产能给了 W-4/L-4，UNLOCATED 80 处逐处定性未动（延期必须带到期日，不得空挂）】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0·待办 H-3″（r36 补记）】** 棘轮第 6 指标 `username_in_skill_files` 现值 **23**（r35 清了 8 处 ILLUSTRATIVE，基线同步降到 23）；余 77 处 LOAD_BEARING **不改**（X-4/X-5）。待办实为两件：`A-java-problem` 自称的交付目录盘上已不存在（死引用，归属复核）、`--on-write` 入 `rule_editor` 写前链前**先量最近 20 次真实改动的 CONFLICT 误报率**（r25 用户否决过闸门拦执行 ⇒ 禁未量就 fail-closed）。 【r45 裁决=降级（转长期看守）｜第 2 次到期仍无外部条件可解（他人在途未收口 / 需用户授权 / 需真实 Hermes 会话），按 X-17 不再往后挂轮；数值面由棘轮第 7/8/9 指标常驻看守】
 【r40 裁决=保留｜棘轮第 6 指标已常驻看守且基线冻结在 23；余 77 处经 r35 四分类实测为 LOAD_BEARING，按 X-4/X-5 裁定不改（改=改坏能跑的技能），故本条为长期看守型而非待办型】
- [ ] **【P0·待办 M-1（r31 登记，第八维敞口）】** 门禁「可离机复现性」实测 **3/5** ⇒ 把 2 台机专属门（`ratchet_gate.py`、`r19_scan_fixtures.py` 层b）的扫描根参数化（`--gs/--fj/--gm`，默认仍取本机路径），使 `run_gates.py --portable-only` 覆盖由 3/5 → 5/5；`gates.yml` 无需改动即自动扩面。**取舍须显式写注释**：层b 若改读仓内快照会失去「真机接线」语义，故优先「参数注入活体根 + CI 缺根时报 UNVERIFIED」而非改语义。验收：`python 05-exec/run_gates.py --portable-only` = 5 门 PASS。 【r38 账龄裁决=挂账至 r40｜2 台机专属门敞口未变：r35 已给 `not_portable_reason` 与 portable-only 模式，剩余是真离机复现验证，需第二台机】 【r41 裁决=作废｜同 H-3：数值看守已在棘轮第 6 指标，X-4 禁改承载路径，本条只是重复陈述同一事实】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0·待办 M-2/M-3（r31 登记）】** M-2 = 行为级 eval 入周期档（对位 mycelium `behavioral-install-eval.yml` 周档；实际件 `焚诀/eval/memory_eval_run.py` 属 **eval/ 只读红线** ⇒ 本仓写薄封装调用、基线 JSON 落本仓，不改归属方脚本）。M-3 = 写时冲突检查（对位 N-E `check-rule-conflicts-on-write.py`），给 `rule_conflict_scan.py` 增 `--on-write <file>`，作用面 = 单文件 × 权威源；挂进 `rule_editor` 写前链须走 `--file` 白名单 + 脏源检测。 【r38 账龄裁决=挂账至 r40｜行为级 eval 与本仓 M-2/M-3 编号在本轮发生撞车，已另立 W-1/W-2 消歧，本条保留为第八维敞口原案】 【r41 裁决=作废（编号撞车）｜本条系 r31 旧案，与 r38 已执行的 M-2（契约集合全等）、r40 的 W-4 同号不同指；按 R241 保留原文并判作废，实际工作以 r38/r40 条目为准】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0·待观察 他人归属（r31 复测）】** ① 受管根 `git -C D:\global_skills status --porcelain` = **32 条**（`dingtalk-*` 15 未跟踪、`ican-frontend-design-system` 4、`A-project-better` 4、`A-skill-manager` 2、`A-memory-start/references/quickref-delivery-consistency.md`、`chaoshi-image-optimization`、3 个 `_*_meta.json`、`rag-eval`/`sqlalchemy-loader-criteria-pitfalls`/`ican-.../audit_css_classes.py`）；② `check-skill-mirror.ps1` 只读复跑 mismatch **30→22**（其中 8 个属本轮我的改动，已逐文件 SHA256 手工复验同源；余 22 全属他人，**不跑全局 `-Fix`** —— 该操作是单向源→镜像覆写，会吞他人镜像侧在途）；③ `rule_editor gates` = `mirror=fail noise=fail evolution=pass stub=pass`，两红均他人面（noise 报 焚诀 `SECURITY.md` + `.git/push-failure.log` 散落）。均按 R269 只登记不擅动。 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】 【r43 裁决=挂账至 r46｜首次越线，给一个到期日；到期未动即按 REPEAT 强制升级】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0·登记 未认领散件（r31）】** ① 工作区根 0 字节文件 `手动维护。agent`（mtime 2026-09-24 22:39，非本会话创建，内容空）⇒ 归属不明，**不删不改**，待认领；② `05-exec/r21e_land*.py` / `r21e_lesson*.py` / `r21e_gate_patchgen*.py` 共 8 个一次性落地脚本已随归位提交入库，按既有政策应轮转 `archive/one-shot-scripts-2026-09/`（轮转 = `git mv`，属破坏性，待下一轮或归属会话做）。 【r39 裁决=挂账至 r41｜未认领散件仍在（实测 git status ?? 手动维护。agent），受管根红线只迁 _trash 禁删，待认领】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】 【r48 裁决=保留｜定年：r31 登记；归属＝本仓，实物路径 C:/Users/37533/Desktop/workspace/自建skill优化/手动维护。agent（未认领散件按红线只登记不删）】

- [ ] **【P0·待观察 他人归属】** 本轮开工前即存在的两条门禁红：① 焚诀 `C20 技能可移植性棘轮` —— `ican-frontend-design-system` inline 绝对路径 1→4（该会话 V4.7 系列在途）；② `rule_editor gates` 的 `noise=fail` —— GM 根 `feedback-user-command-supreme.md` 判散落 VIOL，且该文件与 `MEMORY.md` 索引行此刻同为 `M`（并行会话在改）。均**不擅动不代改**，待归属会话收口后复跑。 【r47 裁决=保留｜定年：r29 登记（git log -S 实测首现 2026-09-24）→ 留痕型登记（他人在途），无本仓执行面】
- [ ] **【P0·待裁定 范围表漂移】** 自建裁定表（2026-09-22 v2，150 目录）与磁盘 167 已差 17 条：`verification` 缺段项里 3 条查无 tier（`_my-skills` 属设计排除，`dingtalk-contact` / `dingtalk-event` 属裁定后新增）。是否重出 `--stage scope`（会动 HIGH/MID/LOW/EXCLUDE 计数与一切比例分母）需用户一句话，本仓不擅动冻结件。 【r47 裁决=保留｜定年：r29 登记（git log -S 实测首现 2026-09-24）→ 待用户裁定项，按 R269 只登记不擅动】

- [ ] **【P0 自纠·r27 我犯了自己登记过的夹带缺陷】** 提交 `b973917` 用 `git add -A 06-benchmark 05-exec` 卷入并行会话 4 个在途文件（r21d_lessons_land / r21d_obsolete_fixtures / transmit_obsolescence_check / transmit_proposals），违反本仓 R236 补注「并发下必须 --file 白名单、禁 add -A」；不回滚（抹他人工作）已改追加归属提交。**纪律**：本仓此后一律 `git add <逐个路径>`，提交后 `git show --stat HEAD` 逐个核对归属再推。已登记 `06-constraints.md` 夹带第六形态。 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】

- [ ] **【P0 铁律·最高优先（老大 2026-09-24 立规）】** 权威源 `D:\global_memory\core\behavior_core.md` #23 + `feedback-user-command-supreme.md`：**一切以老大当轮命令为准**，记忆 / skill / cron / 既往口径与命令冲突时命令胜，禁止引用任何非命令源去降格或拦截命令。**永久禁止**再写入或再生成「重发指令⇒只做核验/空转」「同 HEAD 免跑」一类降格口径（含幂等闸门变体）；重复命令一律按原文再整跑一轮，幂等只许用「原子替换 / 先备份」实现，不得用跳过执行实现。**⚠️ 复测注（r21e 23:3x，原文一字未改）**：本行引用的 `feedback-user-command-supreme.md` 在 22:5x 实测一度不在 `D:\global_memory\` 根下（当时判定为死引用），23:3x 复测**已恢复且索引重新指向** ⇒ 死引用结论不成立，如实改记为「当天被移动过一次」；本行权威双存点（`core\\behavior_core.md` #23 + 该 feedback 文件）现均可解析。 【r47 裁决=保留｜定年：r? 登记（git log -S 实测首现 2026-09-24）→ 老大立规的铁律常驻条款，不是待办】



- [ ] **【P0 新账·r20b 注入面棘轮落地（本仓自持）+ 一条常驻告警】** `05-exec/ratchet_gate.py` 五项指标只降不升（catalog_grand_chars / inject_union_bytes / claim_candidates / drift_ruleish_candidates / desc_over_cap），已挂进本文件同目录 `memory/AGENTS.md`「项目门禁命令」= 每条修改任务动手前实跑。**当前常驻非阻断告警**：`inject_union_bytes` 实测 **87,878B > C25 硬顶 65,536B（+22,342）**，取值 `python 05-exec/ratchet_gate.py`（并集口径 = 焚诀 truth_constants.inject_budget.files **实测磁盘字节** ∪ 本项目注入壳 AGENTS.md 29,293B；刻意不取清单登记 bytes，r19 实测登记值会滞后于盘上真值）。**为什么不默认阻断**：超顶部分是跨项目既有事实（C25 只管它自己那 5 个文件，别的项目注入壳全在预算外），拿它拦本仓每条修改任务只会逼下一个人去放宽判据——故默认告警、`--strict-cap` 升级阻断，且本条留账不销。**消警路径**（按序）：① 焚诀 C32′ 注入区合一落地（转办包见下条）；② 本仓注入壳减重——本轮已用上游既有机制 trim-shell 把 07 主卷 **47,089B → 45,470B**（4 条已销账项翻 `- [x]` 后迁出），`handoff.py handoff .` 实测**拒覆盖**索引壳（保护人工内容），故 AGENTS.md 仍 29,293B，进一步减重须人工精简 07 的「## 分卷目录」行与 06 留痕条目（勿为绿删规则本体）。**⚠️ 校正注（r21c 22:2x 实测更新，原文不改）**：告警现值 = **86,346B**（本轮 `A-memory-start/SKILL.md` 主文件净回吐 1,363B ⇒ 棘轮基线 89,014 → 86,346，`--update` 只降不升已实跑）；**消警路径 ① 已过期**——焚诀已自落 C31（注入预算归因台账）+ C32（空基线台账），我方转办件 `05-exec/r20b_fenjue_wiring/README.md` 停止外推；剩余可行路径 = ② 本仓注入壳减重，以及「把非本仓注入壳纳入统一预算」的**上游口径变更**（归属面动作，本仓不代裁）。 【r39 裁决=作废｜本仓自持部分已落地：ratchet_gate 现 7 指标含 overdue_debt_items，取值 python 05-exec/ratchet_gate.py】
- [ ] R19-2 转焚诀 C29/C30/C31 判据(目录税棘轮+注入区合一+计数断言内容级门禁)（由 flow 登记；状态: blocked）—— **🔶 部分收口（r20）**：原「建议 rule_editor commit 增加同文件他人改动预检」已在本轮**自行落地**（受管根 `9a15f4e`，A-memory-start **V10.69.0**）——它不属焚诀 `eval/` 只读红线，上一轮把它归入「转归属会话」是**过度保守**（本仓 r13 B4 即有直接改 rule_editor 的先例）；剩余 blocked 三项 = **C31′ 目录税棘轮 / C32′ 注入区口径合一 / C33′ 计数断言内容级门禁**（r20b 二次让号：焚诀本轮已自行落地 C30=路由静态表目标存活与空洞棘轮，取值 `grep -n "('C30'" 焚诀/eval/verify_truth_consistency.py`），全部要求往 `焚诀/eval/verify_checks/` 加判据 ⇒ 真红线，转办维持 【r39 裁决=挂账至 r41｜转办件：判据归焚诀归属会话，本仓红线明令 焚诀 eval 只读调用不改】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] R19-1 注入面失真句批次整改(端数/版本戳10句+流程入描述24条+六段最小规范)（由 flow 登记；状态: blocked）—— **🔶 部分收口（2026-09-24 r19b）**：注入面端数族 **6 句已改口八端**（受管根 `961bad6`+`33fcffd`，A-memory-start **V10.68.0**，`claim_truth_scan` 复扫该文件候选归零）；**仍 blocked 的 = frontmatter description 类 4 处**（`A-memory-start` 的「V9.7.0 瘦身版」、`A-skill-manager` 的「四端同步」，另 `A-get-memory`「四端版本号」2 处经复核属**语义不同的误报**——指 VERSION_LOCK 组件而非在役端，登记为扫描器已知盲区不改数据）+ 24 条「流程入描述」+ 六段最小规范；全部同因 = **派生件全量重建窗口未开**（description 是注册表/BGE/skill_content 镜像，改一处须整链重建，违「派生件三方脱节」教训者不做半改） 【r47 裁决=降级｜定年：r19 登记（git log -S 实测首现 2026-09-24）→ 残余量转长期看守；已交付部分见受管根提交】
- [ ] **【待观察·他人在途（r20 登记）】** `A-get-memory/SKILL.md`（+ `references/step2_4_missed_audit.md`）当前为并行会话在途且**已 staged**（`git status --porcelain` 首列 `M`），frontmatter 已到 4.29.0（> 本会话所见的 4.27.0）⇒ 归属会话正在连改。本要把「判据类工具交付三条硬判据」写进该 skill，现改为**备好补丁不代改**：`05-exec/r20-patches/patch_agetmemory_jig_clause.json` + 同目录 README（含一条 `rule_editor replace --dry-run` 命令与验收判据），待其收口后由归属会话一键落地。另注：受管根出现 staged-but-uncommitted 状态本身是并发风险信号，本仓未做任何 `git add/reset` 触碰。 【r38 账龄裁决=作废｜`git -C D:\global_skills status --porcelain -- A-get-memory` 实测 0 条，其 SKILL.md 在途改动已由归属会话自行收口】
- [ ] **【登记·跨会话夹带反向形态（r19b）】** 我在 `A-memory-start/references/contract.md` 的在途 hunks（八端生效/八端共享）被并行会话提交 **`4b75d80`** 一并带走（其说明只提「Step0.55 新增 6.5」）——根因同上轮 `f6f5b0c`：`rule_editor commit` 按整文件提交，同文件存在他人未提交改动；**内容未丢失、未改写**（两侧改动现均在工作树与 HEAD 中，`grep -c 八端` = 2 实测）。处置：**不回滚**（回滚 = 抹他人在途），只登记 + 提交前逐文件比对。**转办建议（焚诀归属会话）**：`rule_editor commit` 增加「同文件他人改动」预检（r18 已提过一次，本轮二次复现，优先级应提至 P1） 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】
- [ ] **【更正·转办判据编号撞号（r19b 实测）】** 下表 R19-2 所指「C29′ 目录税棘轮」**已不能叫 C29**：焚诀并行会话本轮实际落地的是 **C29 = index.md 与派生评分产物对账**（verify 现 29 checks）。⇒ 转办三项重编号为 **C30′ 目录税棘轮 / C31′ 注入区口径合一 / C32′ 计数断言内容级门禁**；证据包位置不变（`06-benchmark/全量对标报告_r19_2026-09-24.md` §2.2/§3 + `06-benchmark/计数断言基线_2026-09-24.md`） 【r43 裁决=挂账至 r46｜首次越线，给一个到期日；到期未动即按 REPEAT 强制升级】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【待观察·外部红项（r21c）】** `gates` 现 `mirror=fail` 的 2 处 MISMATCH 均为 `ican-frontend-design-system/{SKILL.md,references/delivery-chain.md}` = 并行会话在途（本仓该 skill 零触碰），按 R269 不代跑 `-Fix`（-Fix 单向 源→镜像，覆写他人镜像侧在途会吞其工作）。本仓两文件已镜像一致。—— **✅ 收口注（r21c 收尾 22:2x 复测）**：归属会话（同日 r22b，受管根 `aa32f7c` A-project-handoff 3.53.0）自行提交并由其跑过 `-Fix` ⇒ 本仓复跑 `gates` = **`mirror=pass noise=pass evolution=pass stub=fail`**（mirror 由红转绿，非本仓动作）；新红项见下条，均外部。 【r47 裁决=保留｜定年：r22 登记（git log -S 实测首现 2026-09-24）→ mirror 面属他人镜像在途，本仓只同步自有文件并 SHA256 复验】
- [ ] **【待观察·外部红项·stub（r21c 复跑 22:2x）】** `gates` 现 **`stub=fail`**：唯一失败桩 = `焚诀/eval/stubs/stub_c29_c30_index_routing` **21/22**，失败用例 = 集成面「C29 注册调用面」，detail 原文 = **`index.md 门禁范围过期: [31] vs verify 实注 C1~C32`**；同因使 `verify_truth_consistency.py` 由本轮早些时候的 **31 PASS / 0 FAIL** 变为 **31 PASS / 1 FAIL**（其 C32「空基线已初始化台账」刚落地、`index.md` 未随之重生成）。归因：本仓对焚诀 `eval/` 与 `index.md` **零写入**（红线只读），且该失败被**其自家桩自证抓到**（对照组亦在），属归属会话下一次 `-Fix`/重生成的自然收口项，按 R269 只登记。**同族留痕**：同一失效形态本仓已在上方两条前登记过（r19「C29 新判据 + index.md 未重生成」、r21c 上条 mirror），即 `index.md` 派生面**每次新增 C 判据都需重生成**，而其自家 C29 判据正是拦这个的——判据已存在、缺的是"新增判据时同步重生成"的操作步骤（转办建议见报告，非本仓可代改）。取值：`python "C:\Users\37533\Desktop\workspace\焚诀\eval\gate_stub_runner.py"` / `python "C:\Users\37533\Desktop\workspace\焚诀\eval\verify_truth_consistency.py"`。 【r39 裁决=挂账至 r41｜本轮无授权面可动（待授权/待裁定项，用户不在场不得代答），保留可见性不销账】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【登记·跨会话夹带第五形态＝本仓内部（r21c 实测 22:1x）】** 本会话按白名单 `git add` 完 8 个 r21c 文件、**尚未及 `git commit`** 的窗口内，同工作区的**定时排程轮 r24**（提交 `74d792d`，22:09）以一次范围提交把**我方 8 文件 + 其自身 1 条 `repeat-guard.jsonl`** 一并收下 ⇒ 我方 `git commit` 返回「nothing to commit, working tree clean」。**内容零损失零改写**（`git show --stat HEAD` 逐文件核对：3 个 patch JSON + 3 个 py + `inject_ratchet_baseline.json` + 07 全在我方预期集合内），但**归属失真**：该提交标题写「**二次 DUPLICATE 拦截零产出**」而实际带 9 文件 192 行（其「零产出」指该轮自身未做对标，非本仓当刻无改动）。处置与既往四次一致：**不回滚不改写其提交信息**（其已推送 `origin/main` == HEAD `74d792d`，回滚 = 抹他人历史 + 破坏 AC-OBS-07 基线判据），只在此登记 + 追加一条归属说明提交。**对后续会话的可执行提示**：本仓同时存在「人工标注驱动轮」与「每小时排程轮」两条写入流，`git add` 与 `git commit` 之间的窗口已被实测撞到一次 ⇒ 要么 `add` 后立即 `commit`（同一条命令串内），要么先 `git log --oneline -1` 复核 HEAD 是否已被推进。根因仍在上游：`rule_editor commit` 的写前脏源检测只覆盖**受管根**，本仓这类「非受管工作区」无等价护栏（登记为上游建议，非本仓可自裁）。 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】

- [ ] **【待观察·同工作区并行会话（r20b 登记 21:5x）】** `git status --porcelain` 出现本仓外的新未跟踪件 `05-exec/r21b-patches/README.md`（另有 `r21b-patches/*.txt` 六个文件已被他人在途会话纳入其工作集）⇒ **有另一个会话正以 r21/r21b 编号在本工作区作业**，轮次编号已与我方 r20b 并行推进。按 R269 只登记不擅动：本会话未 add/未修改/未删除其任何文件（本仓提交 `git add` 全程按目录+文件白名单，未用 `-A`）。对后续会话的提示：动 `05-exec/r21b-patches/` 与其中和文件前先 `git log --oneline -3` 看是否已被其自行收口。另记一条本轮踩到的机制细节：footer `[升级建议]` 的 `skill=` 值**不得含空格**（写 `skill=焚诀 verify` 被 `upgrade_footer_gate.py` 判「块内非法行」，改 `fenjue-verify` 后 pass）——落盘后必须复跑门禁读结果，不能只信脚本 exit 0。**r21c 追加同族机制细节（本轮实跑踩到第二条）**：footer `状态=已闭环(证据=…)` 的证据串必须匹配 `RE_EVID = ^[A-Za-z]:[\\/].+?#(?:版本行:V\d+(\.\d+)*|backup:[\w.\-]+)$`——写 `...inject_ratchet_baseline.json#inject_union_bytes:86346`（JSON 键值式锚点）被判**「已闭环证据不合规」**；合规写法 = `#版本行:Vx.y.z` 或 `#backup:<回滚件名>`。另：`upgrade_footer_gate.py` 只判**最后一个** footer 块（`last_block`），多会话共写当日 GM 日志时他人随后追加会使本方块**脱离判定** ⇒ 自证须切片到临时文件用 `--file` 单验（本仓做法 = `05-exec/r21c_footer_append.py` + 切片复验，实测隔离判定 `[GATE:evolution-pass] skill=2 建议=2`）。 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】

- [ ] **【待观察·焚诀在途新判据 C29（r19 登记 2026-09-24 18:0x）】** 本轮开工时（17:31）实测 `verify_truth_consistency.py` = **28 PASS / 0 FAIL**，收口前（18:0x）复跑变 **28 PASS / 1 FAIL**，FAIL 项 = **C29「index.md 与派生评分产物对账」**（index.md 端点 6 vs `ENDPOINTS`=8、注册表 151 vs 实测 167、门禁范围 [24] vs C1~C29、评分卡 track_150 vs active track_200、`json.overall=175.1` vs STATUS 182.5）；同因导致 `rule_editor.py gates` 的 **stub 门禁新增「未登记判据 1 个」**。归因（实测）：`git -C 焚诀 status --porcelain` = **6 条 M**（`eval/verify_truth_consistency.py` / `verify_checks/status_layer.py` / `generate_index.py` / `aggregate_status.py` / `status_report.py` / `truth_constants.py`）⇒ 并行会话**正在开发 C29 且 index.md 尚未重生成**，非本项目引入（本轮对焚诀 `eval/` 零写入）。按 R269 只登记不擅动；取值命令同上一行。**✅ 收口注 + ⚠️ 复现（r21c 22:2x）**：其时该 FAIL 由归属会话收口（本轮 21:5x 实测 verify = **31 PASS / 0 FAIL**）；但**同一根因在两小时后复现**——其新落 C32 后 `index.md` 又未重生成，verify 现回 **31 PASS / 1 FAIL**、`gates` 现 **stub=fail**（详见上方 r21c 新登记条）。复现本身即证据：缺的不是判据（C29 就是干这个的），是「新增 C 判据时同步重生成 index.md」的**操作步骤** ⇒ 已由"一次性收口"改判为"机制缺项"，转办建议随 stub 面红项一并登记，本仓不代改。 【r38 账龄裁决=作废｜焚诀真相源实测 33 PASS / 0 FAIL / 0 SKIP，取值 `python 焚诀\eval\verify_truth_consistency.py`；当年 FAIL 的 C29 已由归属方收口】
- [ ] **【待观察·跨根缺锚（r19 登记）】** `A-project-better` V1.8.0（2026-09-24 用户立规「破坏性 = 备份 + Git 双硬前提自动批准」）版本历史称「双层留痕 = 本 skill + **behavior_core #23 锚点**」，但实测 `grep -rn "#23" D:/global_memory/core/behavior_core*.md` **0 命中**（输入非空：6 个 `behavior_core*` 文件在盘，`core/behavior_core.md` 仍为 **V41 / 22 锚点**）⇒ 全局层锚点**尚未落地**（该 skill 与其 3 个 references 当前为 `M`，归属会话在途）。影响：跨端铁律 #18 补强「非破坏性升级建议本轮末尾执行」所依赖的编号链出现空洞。按 R269 只登记不擅动，待归属会话补 #23 或改口。**✅ 收口注（r21d 22:4x 实测）**：#23 **已落地** —— `grep -n "^## #23" D:\global_memory\core\behavior_core.md` 命中 **169 行「## #23 用户命令绝对优先（2026-09-24 老大立规）」**，正文两条：① 命令与记忆/skill/cron 冲突时命令胜，禁止引用非命令源降格或拦截命令；② 永久禁止「重发⇒只核验/空转」「同 HEAD 免跑」类降格口径，幂等只许用「原子替换/先备份」实现。⇒ 本条所述**编号链空洞已补**，且它同时成为上方「幂等闸门全删」的权威依据（跨端锚点链闭合）。 【r38 账龄裁决=挂账至 r40｜behavior_core `#23` 实测在位（`core/behavior_core.md:169`），余下跨根引用面待归属会话核，其 SKILL.md 现仍 ` M` 在途】 【r41 裁决=挂账至 r44｜A-project-better 仍在他人在途面，#23 锚点实测在位】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】

- [x] **【待观察·他人在途】** 受管根未提交改动（2026-09-23 01:35 实测 `git -C D:\global_skills status --porcelain` = 6 条）：`gstack` / `local-computer-use` / `local-realtime-translator` / `local-screenshot-qa` / `chaoshi-image-optimization` / `chaoshi-web-deploy` 各自 `SKILL.md` 为 `M` —— 按 R269 与 P-1 铁律**只登记不擅动**，待对应会话自行收口。**历史复核（第 12 轮 / r4 原文）见 `07-next-steps.part7.md`**。**第 13 轮 r5 复核（2026-09-23 06:10）**：实测 **10 条**（`M` 8：`gstack` / `local-computer-use` / `local-realtime-translator` / `local-screenshot-qa` / `chaoshi-image-optimization` 各自 `SKILL.md` **+ A-project-handoff 在途功能 3 文件** `scripts/handoff.py` / `handoff_lib/audit.py` / `handoff_lib/common.py`；`??` 2：`handoff_lib/flow.py` / `scripts/templates/09-workflow-state.md.tmpl`）—— 新增 5 条源自并行会话的「动态工作流状态（09 节）」功能，按 R269 **只登记不擅动**（本轮对该 skill 的改动已用 `rule_editor` 单文件收口，见下条 r5 事故登记）—— **✅ 收口注（2026-09-23 r7 销账）**：实测 `git -C D:\global_skills status --porcelain` = **0 条**（并行会话已全部提交，HEAD `1aa793f` == origin/main）；本轮自身收口用 `--file` 白名单提交 `7ebd682` 零夹带。新在途见 r7 新登记条
- [x] **【待观察·焚诀在途债】** `gate_stub_runner` 实测 `[GATE:stub-fail]`（2026-09-23 01:35）：① 桩 `stub_c16_memory` 2/3——正例样本硬编码总分 `181.6` vs 活体评分卡 `183.9`（判据样本与活体口径漂移，R263 补注①形态，须由移动总分的会话裁决）；② 未登记桩判据 C17/C18/C19（registry 冻结于 09-22 后新增）。均非本轮引入（本轮仅改 A-project-handoff 文案，焚诀 eval 零触碰），按 R269 登记待其归属会话收口。**历史复核（第 12 轮 / r4 原文）见 `07-next-steps.part7.md`** —— **✅ 收口注（2026-09-23 r7 销账）**：复跑 `gate_stub_runner` = **[GATE:stub-pass]**，14/14 桩全过、未登记桩 0（归属会话已收口）
- [ ] **【待观察·跨会话夹带】** 受管根 `f6f5b0c`（本轮 V3.48.0 提交）**夹带**了并行会话在 `handoff_lib/splitvol.py` 的在途改动 1 行（`SPLIT_TARGETS` 增 `"09-workflow-state.md"`）—— 根因：`rule_editor.py commit` 按**整文件**提交，而同文件存在他人未提交改动；**非数据丢失、未改写其内容**（重编号补丁窗口 0 命中该行，内容原样入库），但归属与「已收口」判断失真。处置：**不回滚**（回滚=抹掉他人在途工作），只登记 + 提交前逐文件比对 `git status --porcelain`；已按 R241 在 `06-constraints.md` 留痕 【r47 裁决=保留｜定年：r? 登记（git log -S 实测首现 2026-09-23）→ 历史留痕（R241），不回滚他人在途】
- [ ] **【P1 待授权·可选】** 性能方案剩余项（2026-09-23，详见 `05-exec/性能瓶颈分析与优化方案.md`）：P1-3a 本项目注入壳瘦身——**✅ 校正注（2026-09-23 r3 裁定：本轮不做，销账）**：实测 06 主文件 12,148B 无上游分卷/自愈机制（SPLIT_TARGETS 只注册 07/05，trim-shell 判据只认「✅/已完成」章节而 06 是 [BUG]/[DEBT] 留痕形态），收敛须逐条改写原文——违 R241「只加注不改写」精神且无机器护栏，**不擅建结构**；如要做须上游先裁 06 分卷口径（登记为上游建议，非本项目待办）。**✅ 校正注（2026-09-23 r5 销账）**：用户授权后上游口径已落地（A-project-handoff **V3.48.0**：`TRIM_ENTRY_TARGETS` 与 `SPLIT_TARGETS` 解耦 + `closed_marker` 判据 + 留痕章节白名单 + 分区不变式），本项目 06 实测 **13,075B → 7,097B**（迁出 13 条 → part1/part2），注入壳 06 节 **12,148B → 6,182B**、整壳 **33,029B → 26,594B（-6,435B/轮）**。P1-4 unified_router direct_hit 短路（上游焚诀，每轮省 ~1.1s；**项目红线明文「焚诀 eval/ 只读调用不改」**，转焚诀归属会话）；P2 三项观察不动 【r39 裁决=挂账至 r41｜本轮无授权面可动（待授权/待裁定项，用户不在场不得代答），保留可见性不销账】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【待观察·他人在途（r7 新登记 2026-09-23 15:55）】** 受管根 `D:\global_skills` 现存 1 条在途：`A-get-memory/SKILL.md`（lessons 分卷定位按数字序教训补注 1 行，归属 = 医项目续接会话 `ses_cb_yi_resume_20260923`，本轮 `7ebd682` 已验证未卷入）；焚诀仓现存 **36 M + 2 ??**（eval/ 主体 + registry 等，归属焚诀在途会话）；GM 日志 `2026-09-23.md` 末位 footer 块（同医项目会话 15:28）`[skill清单]`/`[升级建议]` 两行偏离 `reply_footer.md` 规范格式（RE_SKILLS/RE_HIT 全不匹配），位于末位时 evolution 门禁 fail，待其归一化。均按 R269 **只登记不擅动** 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】
- [ ] **【待裁定·口径漂移（r8 新登记）】** `scan_all.py` 判据修复后 dry-run 复跑 = **HIGH 56 / MID 21** / LOW 30 / EXCLUDE 43，而冻结交付件（scope_result.json / 清单 v2，用户 2026-09-22 裁定口径）= HIGH 51 / MID 26（总数同为 77，LOW/EXCL 不变）。归因 = ① git quotepath 修复使 CJK 目录拿回 gitAdd 证据（此前恒 miss）② frontmatter CRLF 修复使 homepage 源恢复生效 ③ 受管池本体演进——即 `add8092` 提交信息的「HIGH56/MID21」**并非笔误**而是复跑新值，当时误标「零漂移」。**冻结件一律不改写**；重出清单 = 破坏性（改写用户裁定口径），须重新按钮裁定。详见 `01-scan/README.md` r8 节 【r39 裁决=挂账至 r41｜本轮无授权面可动（待授权/待裁定项，用户不在场不得代答），保留可见性不销账】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【登记·焚诀红线（r8）】** `unretire_31.py:51-53` 写入焚诀 `eval/truth_constants.json` `_meta` 的数字失真（硬编码「20 个市场件/11 个 local-*」，实际 21/10）——**焚诀 `eval/` 只读红线 ⇒ 本项目不代改**，待焚诀归属会话按 R241 加校正注；脚本侧失真成因已读码确认 【r39 裁决=挂账至 r41｜焚诀红线登记项，改判据须焚诀侧动】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【待观察·他人在途（r10 收尾登记 2026-09-24 00:2x）】** ① `D:\global_skills\_bm_skillid_migration.json`（49B，`{"migratedAt":1790173504841,"version":2}`，mtime 2026-09-23 22:25，SHA `cfd3be36…` ≠ `_trash` 副本 `cba8cda9…`[09-21 版]）⇒ 活跃并行会话在途迁移标记，按 R269 **只登记不擅动**；它触发 savepoint noise 门禁拒绝（默认四根口径 VIOL=1），本轮 savepoint 因此未完成，待其归属会话收口后补跑。② 焚诀 `.gitignore` 已加 `.mimosa/` 豁免行（**工作区在途 1 文件未提交**）：mimosa 插件 hook-state 为平台运行态（R269 例外路径），豁免后 noise 焚诀根 VIOL 1→0（实测）、verify 23 PASS/0 FAIL 复跑绿；提交两次被 **mimosa 平台级 pre-commit 钩子拦截**（L3 fail-closed，`--no-verify` 亦无效——拦截在 Bash 调用层非 git hook 层），拦截依据 = 跨仓发现（见下条），待其处置后由归属会话白名单收口 【r38 账龄裁决=挂账至 r40｜① `_bm_skillid_migration.json` 实测已 GONE；受管根在途仍有 33 条（取值 `git -C D:\global_skills status --porcelain | wc -l`）未收口】 【r41 裁决=挂账至 r44｜受管根在途未清零（取值 git -C D:/global_skills status --porcelain | wc -l）】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【待授权·mimosa 安全发现 triage（r10）】** mimosa 钩子（焚诀 commit 时跨仓扫描）报本项目 **12-13 处「路径穿越」high**：`01-scan/scan_all.py:320/322/324/474/478`（5）+ `archive/one-shot-scripts-2026-09/`（B7-fix×2 / B1×1 / B6×1 / B7-regen×1 / R272b×2 = 7）。**读码 triage：全部为 `open(os.path.join(常量目录, 常量文件名), "w")` 形态**——路径来自源码硬编码常量（SCOPE_DIR/SCAN_DIR + 固定文件名），无任何外部输入面，本地分析脚本实际风险 ≈0，初判为静态模式误报；退役归档脚本（7 处）已退役更无攻击面。**处置待裁定**：① 接受误报结论并请 mimosa 归属会话校准（把「常量+无输入面」形态降级）；② 或给 scan_all.py 加 realpath 包含校验凑判据（改冻结件，需授权）；③ 或调整扫描范围排除 archive/。**在裁定前：不动冻结件、不绕钩子** —— **r10 实测进展（00:2x）**：已给 `scan_all.py` 加 CLI 注入路径加固（`--ws` 钉死本仓根 + `--gs/--registry` 存在性校验；反例×2 拒/正例过，dry-run 计数 56/21/**31**/43 与 r8 基线一致，LOW+1 = 并行会话新增 `rag-eval@` junction 与本次无关）；官方 mimosa deep 复扫（`scan-2026-09-23T16-24-35Z-54d96de672c2`，seal `sha256:279ae14b…`）**findingCount 仍 = 13**——命中源为模块级常量 join（无污点），扫描器对 `os.path.join(常量,常量)+open("w")` 模式过敏 ⇒ 误报结论维持；提交**三次**均被 mimosa 钩子拦截（含复扫 seal 生成后仍拦，`--no-verify` 无效——拦截在平台 Bash 调用层非 git hook 层），本轮产物已全部落盘**未推送**（HEAD 仍 `d0ac826`，AC-OBS-07 本轮不勾），处置菜单①②③待用户裁定 —— **r11 实测进展（2026-09-24 00:5x，用户「继续」按菜单②执行）**：`_out()` 封装消毒（realpath+commonpath+raise）**不被扫描器认可**（官方复扫 `scan-…563423965ac5` 仍 13 项，330/332/334 命中的就是 `_out` 调用点）；第 13 项确认为 `05-exec/_lib.py:55`（原子写 tmp sink）；scan_all.py 编辑被 PreToolUse 钩子**连续 3 次 deny 且行号锚 stale**（恒指 474，实际 sink 已随 helper +10 漂移至 484；该行当前为纯 markdown 表头）⇒ 编辑通道被锁，内联 realpath 形态未能落地（拦截未验证）；已固化部分加固（CLI guard + 3/5 sink + helper），py_compile + dry-run 基线不变（56/21/31/43，LOW+1 = `rag-eval@` junction 并行新增）；**解锁唯一出口 = mimosa 归属会话**（校准常量 join 形态 / 清 stale 锚 / 认可封装消毒函数），archive 7 项维持冻结（改了也无法解 commit 锁）；Bash 直写绕钩子 = 明确禁区未采用 —— **r12 官方杠杆全试尽（2026-09-24 01:xx）**：① `mimosa policy init` 落地 `.mimosa/security-policy.json`（`path.allowedWriteRoots` 9 目录 + `threatModel.exclusions` 3 项字符串数组，`policy check`/`threat-model check` 双 ✓）→ `mimosa audit --deep` 照旧 13（策略/排除项不影响确定性路径规则，排除项格式必须是字符串数组）；② `mimosa validate` 不适用（仅 fixed_static 可验，本项目 findings 均 blocked）；③ `ledger/backlog/history` 无 FP 裁定入口（手造 batch = 伪造状态，禁用）；④ 提交试探仍 13 拒（闸门行号已与实况对齐，仅 edit 钩子锚 stale）；⑤ 已加固文件+策略均 staged 待闸门放行。**收敛判定：mimosa 确定性规则缺 FP 裁定面 = 工具缺口，须归属会话补（规则校准或 adjudication 命令）；消毒证据链 + 策略声明已齐备待其复核** —— **r12b（2026-09-24 02:xx，用户裁定走①「归属会话校准」）**：① 官方 `ledger checkpoint`（covered=8）不收敛 blocked（6→7），`git-gate status` 实证本仓装有**原生 pre-commit/pre-push 门禁**（官方 `uninstall` 开关存在，未动=属用户决策面）；② 源仓不在本机（protected-build 封闭分发，manifest 无上游 URL），「归属会话」落点 = 维护者工单；③ **正式工单已产出：`05-exec/mimosa_fp_calibration_request_2026-09-24.md`**（13 项误报逐行依据 + 5 项工具缺陷含 stale 锚/死锁链 + 校准请求 R1 规则层三条 / R2 产品层三条 + 验收判据）——待用户转交维护者；④ _lib.py 内联 realpath 修复编辑亦被钩子拦（锚 stale 指 docstring 行 10），佐证缺陷③「blocked 文件编辑全锁」 【r39 裁决=挂账至 r41｜本轮无授权面可动（待授权/待裁定项，用户不在场不得代答），保留可见性不销账】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P1 待授权·对标新增（r10）】** 全量对标报告（`06-benchmark/自建skill体系全量对标分析报告.md`）三角验证出的新差距，待授权落地：**P0-1 hook 机制层探底**（实测 ZC/HM 端 SessionStart/compact hook 支持面；可行则 ≤1KB 强制注入 + 压缩重注入实测 2 端，不可行则落盘「平台不支持」证据；superpowers+ruflo 双印证）；**P0-2 AC 收敛机器化**（`flow --verify-ac` 对 AC-OBS 逐条实跑验证命令 → Converged/Not 落盘并纳入 review，spec-kit converge 对位；本项目 4 条未闭环 AC 首次获得机器 verdict）；**P1-1** description 双要素质量门（151 条先盘基线，anthropics 官方口径「做什么+何时用」）；**P1-2** 交接记忆评测集（10–20 场景 × 9 节 recall 基准，mem0 LOCOMO 思路）；**P1-3** 修复 verdict 三态字段（verified/partial/failed，spec-kit bug-fix）。全部非破坏性；性能遗留（sklearn 0.91s / `_cache` 113MB）仍归既有 PERF 待办，不重复立项 —— **✅ 销账注（2026-09-24 r17 已执行，见上条 r17 摘要与下条遗留登记）** 【r39 裁决=挂账至 r41｜本轮无授权面可动（待授权/待裁定项，用户不在场不得代答），保留可见性不销账】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0 遗留·对标五项尾款（r17 登记）】** ① **HM E2E 复核**：Hermes 下次开会话观察注入声明行 `[机制注入·每轮]`（机制已实测 PROBE_PASS，E2E 待真实会话）——HM 归属会话；② **ZC 插件装载 E2E**：按 `06-benchmark/P0-1_hook机制层探底_2026-09-24.md` §3.2 样板建 `my-bootstrap` 插件并实测本地装载通道（`enabledPlugins` 白名单键形 `name@marketplace`，本地免市场装载路径未验证）——ZC 归属会话；③ **P1-1 门禁 + 整改③**：description 质量门落焚诀 verify（**红线：本项目不代改 eval/**，转焚诀归属会话）+ 抽 3 条差 description 修复后 `build_indexes --apply` 重建 + unified_router 盲测对照（涉及派生件全量重建，待授权窗口）；④ **P1-2 review 接入**：handoff.py review 读取 memory-eval 基线行（A-project-handoff 下一版）；⑤ **AC 可跑化适配**：08-ac-obs.md AC-04/05/06 命令裸路径改绝对路径（机器可跑化，历史证据保留加注）；⑥ **P1-3 版本 bump**：workflow_seven_step V1.1→V1.2 须焚诀归属会话同步 `truth_constants.json` workflow 段版本后执行（C11 门禁联动）—— **r17b 进展注**：⑤④ 已销账（AC 6/8 converged + 评测行入 review）；③ 基线校正 103/151 + 三条整改已提交（`6cc9b3d`），门禁与 router 重建盲测仍待焚诀归属会话；② 插件实物已就绪（`D:\global_memory\hooks\zcode-plugin-my-bootstrap\`），装载 E2E 待 ZC 应用侧；① CLI 入口本机损坏（uv 跳板+ABI），机制级证据已足；⑥ 维持焚诀归属 【r38 账龄裁决=挂账至 r40｜① HM E2E 需真实 Hermes 会话，本轮无法自证，按 R247 不得当作已验证】 【r41 裁决=挂账至 r44｜① HM E2E 需真实 Hermes 会话，按 R247 不得自证完成】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】

- [ ] **【P2 待授权·D 批缓办（r8，用户本轮未选）】** ① 11 个一次性脚本归档（B1/B6/B7×2/R272×5/direct_map/unretire_31 → `archive/one-shot-scripts-2026-09/`）② 重复代码抽 `_lib.py`（UTF-8 包装 5 份 / JSON 守卫 5 处 / git 调用 3 份）③ `scan_result.json` 包装 `{"rows":[...]}`——改动面大，下轮按需授权 —— **✅ 销账注（2026-09-23 r9 已执行）**：用户「批 P2」授权后三项全落地（`abec460`，夹具 8/8 含抓回 shutil 回归）；详见 05 r9 条 【r39 裁决=挂账至 r41｜本轮无授权面可动（待授权/待裁定项，用户不在场不得代答），保留可见性不销账】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0 新账·r18 对标增量】三项（详见 `06-benchmark/全量对标报告_r18_2026-09-24.md` §6）**：① **P0-A 目录税入棘轮（转焚诀归属会话，红线：本项目不代改 `eval/`）**——把 `05-exec/catalog_attention_tax.py` 口径固化为 **C29**（全库 description 合计 + 单条 ≤1,024 官方上限双棘轮，只降不升），实测现状 45,173 字符 / 128k 窗口 35.3% / C25 硬顶 2.07 倍且**零覆盖**；② **P0-B 注入区口径合一（转焚诀）**——`INJECT_BUDGET_FILES` 与 `attention_sim` P0 并集实测 **69,494B > 硬顶 65,536B 而 C25 判 PASS**，且 `shell_project` 按 cwd 解析实钉焚诀（须改按当前项目解析）；两层验证（函数桩 + 接线对照，R238）；③ **P0-C 累积漂移人工复核（本项目自持，无需授权）**——按 `cumulative_drift_scan.py --days 30 --threshold 5` 的 **16 条规则类候选**逐条给「仍然成立/已失真」结论，先做 `memory/07-next-steps.md` 主卷 + 8 分卷（主卷 35 次/30 天改写为全仓最高）；已产出首案（151 分母失真句，已加注）。 【r39 裁决=挂账至 r41｜r18 三项中 E2E 项仍需真实 Hermes 会话，按 R247 不得自证完成】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【P0 新账·r20 插件面】** ① **R20-2（高，转焚诀归属会话）**：C29 目录税棘轮的判据面必须定义为 **`D:\global_skills ∪ ~/.qoder-cn/plugins/installed_plugins_v2.json installPath` 并集**，且**任何「预算/上限」类门禁须在输出里自证覆盖根清单**——否则重演 C25 型假 PASS（本轮实证：r19 三口径对账全「一致」仍低估 34.3%）；② **R20-3（中，转焚诀）**：C27 内容安全 / C28 发布合规纳插件根，或显式声明豁免并写进门禁输出；③ **R20-4（中，本项目）**：`~/.qoder-cn/plugins/*.json.<uuid>.tmp` 残留 7 个运行态文件（本轮实测），若该根入任何门禁须先登记 noise 豁免；④ **R20-5（中，用户决策面）**：活跃技能集分层装载——插件侧三家（superpowers+architecture-visualization+alibabacloud-core）占 37/57 = 65%，是裁剪收益最集中处；改的是平台装载配置非本仓文件，**未擅动**。 【r39 裁决=挂账至 r41｜同上转办件（R20-2 覆盖根自声明已由本仓 run_gates 落地，剩余在焚诀侧）】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P0 新账·r28】** H1 新建技能模板只强制 `Verification` + `Rationalizations` 两段（+H3 破坏性命令作用域守卫）；H2 按 `rubric_ab_2026-09-24.json` 的 verification=false 队列补存量。**阻塞点=并行会话正在改 A 族**（06-constraints 已登记其夹带风险），改动须 `rule_editor --file` 白名单 + 派生件重建 + 复跑 verify/gates。M2（本仓无红线）：给 rubric 增 frontmatter 兼容判据后重跑双口径，消掉 overview/when_to_use 假阴性。 【r39 裁决=挂账至 r41｜H1 模板最小规范改动面在受管根 A-skill-manager 等，其 2 文件实测仍 M 在途（R269）】 【r44 裁决=降级（转长期看守）｜已延期 >=1 次，按 X-17 不再往后挂轮；阻塞原因维持实测（他人在途 / 待授权 / 需第二台机 / 需真实会话），数值由棘轮第 7/8 指标常驻看守】
- [ ] **【P1 新账·r18】** P1-A 六段解剖最小规范（只先强制 `Verification` + `Red Flags` 两段；实测六段全含 **0/166**、rationalizations 13.9%、verification 58.4% ⇒ 改 `A-skill-manager` 模板需 `rule_editor` + 派生件重建，待授权窗口）；P1-B 注册表 **JSON Schema 契约**（对标 AAS `schemas/skills-index.v1.schema.json`，从「事后 quarantine」改「事前拒收」）；P1-C **统计分母统一规程**（技能计数一律引用 C1 行输出，本仓工具须同时打印 166/167 两口径；已写入 06 约束 [BUG]）；P1-D capability ledger（`record_lessons_usage.py` 增「目录内无对应技能 → 登记 catalog gap」一行，转焚诀）。 【r39 裁决=挂账至 r41｜同上，与 L112 同批，避免半套规范】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】
- [ ] **【待观察·r18 新登记（复核后现状）】** 取值命令与实测（2026-09-24，均本轮实跑）：① `git -C D:/global_skills status --porcelain` = **28 条 M**（含 `A-project-better`/`A-skill-manager`/`cloudbase__skillhub`/`github`/`sq-cleanmgr-c`/`consulting-analysis`/`A-get-memory` 等）⇒ R269 只登记不擅动；② `git -C 焚诀 status --porcelain` = **0 条**（r7 登记的「36M+2??」已由归属会话收口 ✅销账）；③ `python D:/global_skills/A-memory-start/references/rule_editor.py gates` = **`mirror=fail noise=pass evolution=pass stub=fail`**——两红项**均非本项目引入且本轮零受管根改动**：`mirror` = `rag-eval\skill.oms.sig` mismatch 1（该技能正是并行会话新增的 junction，与下条分母口径同源），`stub` = `stub_c16_domain` 3/4 + `stub_fp15_count` 3/4（**报错原文含「注册表实测 167」**）；⇒ 连带 **AC-OBS-04 现为 `[not_converged]`**（当前实测 `python 焚诀/eval/gate_stub_runner.py` + `gates` 复跑取值），AC-MACHINE 其余 5 条 converged / 2 manual；④ P0-C 第 1 批复核结论 = **9 条承重句：仍然成立 2 / 已失真 6 / 归属方收口 1**，逐条 verdict + 5 类复跑取值命令见 `06-benchmark/P0-C_累积漂移复核第1批_2026-09-24.md`；下一批 P0-C-2 范围 = `A-project-handoff/SKILL.md`(17 次) / `A-memory-start/SKILL.md`(11 次) / `A-project-better/SKILL.md`(10 次)。—— **✅ 复核注（2026-09-24 r19 实测）**：① 三仓在途已刷新：`git -C D:/global_skills status --porcelain` = **28 条**（新增 `A-project-better`×4 / `A-skill-manager`×2 / `consulting-analysis` / `github`×2 / `sq-cleanmgr-c` / `A-get-memory` + `dingtalk-*`×15 未跟踪等）⇒ 三个本批受检件的**内容整改**归属其会话，本项目只做只读复核；`git -C 焚诀 status` 开工时 **0 条**、收口前 **6 条**（见上条 C29 新登记）；② 门禁现状复跑 = **`mirror=pass noise=pass evolution=pass stub=fail`**——本轮 `18294ba` 提交后 3 个 A-project-handoff 文件曾致 `mismatch=4`，按 R270 主动三步（只读检查 → `check-skill-mirror.ps1 -Fix` 单向源→镜像 → 复核）转绿，**mirror 已不再红**；`stub=fail` 仍全为外部（2 桩失败 + 1 未登记判据 = 焚诀在途 C29）⇒ **AC-OBS-04 仍 `[not_converged]`**（取值 = `python D:/global_skills/A-memory-start/references/rule_editor.py gates`）；③ **P0-C-2 已完成**（r19 第 2 批 11 句六态判定，见 `06-benchmark/P0-C_累积漂移复核第2批_2026-09-24.md`），P0-C-3 范围见该文 §四。 【r38 账龄裁决=挂账至 r40｜受管根在途实测 33 条（原登记 28 条），条数不降反增 ⇒ 属他人在途敞口扩大，仍按 R269 只登记不擅动】 【r41 裁决=挂账至 r44｜受管根在途未收口，属他人归属】 【r43 裁决=降级（转长期看守）｜该条已延期过一次以上，按 X-16 不再往后挂轮；阻塞原因是实测的结构性外部条件（待用户授权 / 他人在途未收口 / 需第二台真机 / 需真实 Hermes 会话），数值面继续由棘轮指标常驻看守，故降级为看守型而非待办型】

- **卷9** `07-next-steps.part9.md` — 07-next-steps 分卷（R199 自动拆卷）

- **卷10** `07-next-steps.part10.md` — 07-next-steps 分卷（R199 自动拆卷）

- **卷11** `07-next-steps.part11.md` — 07-next-steps 分卷（R199 自动拆卷）

- **卷12** `07-next-steps.part12.md` — 07-next-steps 分卷（R199 自动拆卷）

- **卷13** `07-next-steps.part13.md` — 07-next-steps 分卷（R199 自动拆卷）

- **卷14** `07-next-steps.part14.md` — 07-next-steps 分卷（R199 自动拆卷）

- **卷15** `07-next-steps.part15.md` — 07-next-steps 分卷（R199 自动拆卷）

## 分卷目录
- **卷1** `07-next-steps.part1.md` — 历史待办与已完成条目（2026-09-22 已按实测销账）
- **卷2** `07-next-steps.part2.md` — 已完成：批2 剩余 P0 与 q-2/q-3 收口
- **卷3** `07-next-steps.part3.md` — 最近对话摘要（历史）
- **卷4** `07-next-steps.part4.md` — 07-next-steps 分卷（R199 自动拆卷）
- **卷5** `07-next-steps.part5.md` — 07-next-steps 分卷（R199 自动拆卷）
- **卷6** `07-next-steps.part6.md` — 最近对话摘要（历史轮次，2026-09-23 第 13 轮 r3 迁入，正文零改写）
- **卷7** `07-next-steps.part7.md` — 最近对话摘要（第 13 轮 r1–r6 全量记录，2026-09-23 原样迁入）
- **卷8** `07-next-steps.part8.md` — 07-next-steps 分卷（R199 自动拆卷）
- [ ] **【r21d 复测闭环·新机器型判据】** 「转办前先复测归属方是否已自落」升格为机器型：判据 （转办登记表 vs 焚诀 verify 注册面，两下限 AND；取不到真相源 = UNVERIFIED + exit 2，禁默认放行）+ 登记表 （schema ，3 件带 keywords/why）+ 夹具  **26/26**（红阶段实测 ；变异对照 4/4 被拦，含一次「变异设计自身不可证伪」的自纠）。真跑实测：1 件 OBSOLETE（注入区合一，被 C25/C31 覆盖 4/4 词）、2 件 VALID，与 r21c 人工复测一致。挂在 「项目门禁命令」**条件前置**（只在要外推时跑，非每轮必跑）。**取值**：=== 转办件过期检测 (33 条已注册判据 / 3 件待外推) === 【r47 裁决=作废｜定年：r? 登记（git log -S 实测首现 2026-09-24）→ 该判据已落地为常驻机制，条目本身已完成】 【r48 裁决=作废｜判据已落地为 05-exec/transmit_obsolescence_check.py 与 26 例夹具，转办面见 06-benchmark/transmit_proposals.json】
  VALID      catalog_tax_ratchet    平台技能目录注意力税棘轮 | 最强候选 C20 仅命中 1/3 词（占比 0.33），未达双下限 ⇒ 仍有效
  OBSOLETE   inject_union_merge     注入区口径合一（多项目注入壳并入统一预算） | 已被 C25、C31 覆盖：命中 4/4 词（如 注入、预算、台账、归因）
  VALID      claim_truth_gate       计数断言内容级门禁（注入文本里的数字断言与真相源对账） | 最强候选 C29 仅命中 1/3 词（占比 0.33），未达双下限 ⇒ 仍有效
[TRANSMIT:STALE] 1 件已过期 ⇒ 停止外推，改为只还自己那半（rc=1 即有过期件）。
- [ ] **【待观察·外部在途使本仓契约门禁变红（r21e 23:4x 实测）】** 必跑链第 3 条 `baseline_contract_scan.py` 现 `[CONTRACT:FAIL]`：并行会话（同日 r29 轮）把 `05-exec/skill_structure_rubric_scan.py` 的产物 schema 从 `skill-structure-rubric-v1` 升到 `-v2`，且该改动**尚未提交**（`git status` = ` M`），但**已重生成了盘上产物** `06-benchmark/skill_structure_rubric_r29_2026-09-24.json`（schema=v2）⇒ 本仓契约（登记的是 v1）与产物当场对不上。同时 `r19_baseline_contract_fixtures.py` 层b 生效路径 exit 也红。**处置**：按 R269 不改对方脚本、不改契约凑绿（R263）；等其提交收口后由**归属方或下一轮**把契约 schema 一并升到 v2（一处改，夹具同批改），本条留账不销。取值：`python 05-exec/baseline_contract_scan.py`（看 FAIL 行的文件名与 schema 实得值）+ `git status --porcelain | grep skill_structure`。 【r39 裁决=保留｜留痕型登记条目，无执行面（R241 要求留在册），不是欠账】
- [ ] **【r21e 控制符扫描（已挂必跑链第 5 条）】** `05-exec/control_char_scan.py` 脏集 = C0 ∪ {0x7f DEL} - 制表/换行/回车；跳二进制但如实登记 skipped；空输入面 exit 2（判据面为空不得判过）。夹具 `05-exec/r21e_control_char_fixtures.py` 22/22（红阶段先看红、变异 3/3 被拦）。**首跑抓到三处真缺陷**：我自己 `lessons.part49.md` 残留 2 个退格符（先前声称已清零，实为重写脚本从未执行 ⇒ **断言 ≠ 执行**）；受管根 `A-project-handoff/references/version-history.md` 第 38 行两个 NUL 把 `02-structure.md`/`03-tech-stack.md` 写成死引用（已 rule_editor 修复 5148d79 + 镜像单文件同源 37,333B）；判据自身漏 DEL，而 DEL 恰真出现在我源码里 ⇒ 定义扩展。取值：`python 05-exec/control_char_scan.py .`（期望 `[CTRL:CLEAN]`）。 【r47 裁决=作废｜定年：r30 登记（git log -S 实测首现 2026-09-25）→ control_char_scan 已在必跑链并注册为常驻门，实跑 [CTRL:CLEAN]】
- [ ] **【r21e 证据可解析性 + 归因抬基线】** `upgrade_footer_gate.py` 的 `RE_EVID` 原只校形状，形状合格却指不到东西的假锚照样放行。边界测量（`evidence_resolvability_measure.py`，近 3 日 97 条已闭环声明）：可解析 52 / 形状合规但解析不到 37（38.1%）/ 形状不合规 8。据此分三级：`FAKE`（路径不存在、或 backup ID 在回滚目录与五仓 git 对象全无 ⇒ 阻断，仅 4/97 不可事后合理化）、`STALE`（文件在但版本 token 被后续提交推走 ⇒ 只告警，追溯拦人即误伤 18/97）、`SKIP`（体系外绝对路径 ⇒ 不判，防误伤第三方端点）；同时形状层 V 前缀改为可选（真实文件写的是裸值 `version: 10.70.0`，只收 V 前缀等于奖励美化而非真实）。夹具 `05-exec/r21e_gate_probe.py` 5/5（红阶段：伪造 backup 锚打补丁前 rc=0 放行、补丁后 rc=1）。配套 `ratchet_gate.py` 新增唯一合法上调通道 `--raise-baseline`（commit+path+delta+reason 四要素、delta 与实增逐字节相等、path 须在注入面成员内、commit 须能在该仓解析；无增长时拒绝抬，防刷账），并永久留账 `attributed_raises`；本轮实测用它接受 +447B（并行会话立 #23 所致，非本仓动作），基线 86,346→86,793。夹具 `05-exec/r21e_ratchet_attrib_fixtures.py` 16/16（含变异 4/4）。取值：`python 05-exec/ratchet_gate.py`（期望 `[RATCHET:PASS]` 且打印留账 1 条）。 【r47 裁决=作废｜定年：r30 登记（git log -S 实测首现 2026-09-25）→ 归因抬基线与证据解析已落地（ratchet --raise-baseline 需归因、多项同长即拒）】
- [ ] **【P0 新立判据·r21d】** 「状态=已闭环」只允许覆盖该条建议的**全部**作用面：一条建议若同时含「动作」与「教训/机制」两半，动作做完而教训仍停在报告与日志（自觉型）⇒ 该行只能标`状态=部分闭环(动作=已闭环…; 教训=未闭环…)` 或径标未闭环。根因（实测）：r21c 我把 `skill=fenjue-verify` 一行标了已闭环、证据是「还账 1,363B」，但该条建议的教训本体到 r21d 才升格为机器型判据 ⇒ **声明先于证据**。复核：`python 05-exec/r21d_obsolete_fixtures.py`（26 例含变异 4/4）+ `grep -c superseded_by /d/global_memory/lessons/lessons.part46.md`（期望 ≥1）。 【r47 裁决=作废｜定年：r? 登记（git log -S 实测首现 2026-09-24）→ 「已闭环须覆盖全部作用面」判据已在 footer 门禁生效】
- [ ] **【待观察·活写入者反复引入的死路径（r21d 实测 22:5x）】** 07 主卷 P0 铁律那条把权威源写成 `D:\global_memory\core<0x08>ehavior_core.md` —— 源码里的 `\b` 被解释成**退格控制符**，正确应为 `core\behavior_core.md`。同缺陷另见 `06-benchmark/全量对标报告_r21_重复轮次闸门_2026-09-24.md` 作废注（**报告侧本轮已修并读回 0x08=0**）。07 侧**本轮连续 3 次修复均在秒级被并行的活写入者弹回**（每次写后读回 0x08=1，含 commit 前的最后一次 ⇒ 已入 git 的对象仍带病）。不与之抢写（R269）。**待归属会话处置**：改其生成侧文本，把 `\b` 写成 `\\b` 或直接改用正斜杠路径；改后跑 `python -c "print(open(r'memory/07-next-steps.md','rb').read().count(b'\x08'))"` 期望 0。影响面：这是「用户命令绝对优先」那条铁律**自身的权威出处不可 grep**，属高优先低难度的可修项。 【r47 裁决=保留｜定年：r? 登记（git log -S 实测首现 2026-09-24）→ 活写入者仍会引入，属长期看守型】

- [ ] **【P0·下轮首推 W-18（r46 新立）】** 此后所有「补标/回写」类一次性脚本一律改走 `python 05-exec/r46_mark_verdict.py --key <锚点> --round <NN> --verdict <裁决｜原因>`，禁再出现行号索引写入；违反即视为 W-9 同族缺陷复发（工具已不接收行号，绕开即手工写，手工写无读回验证）。 【r49 裁决=执行完毕（持续生效）｜r47 12/12、r48 3/3、r49 5/5 全部经写入器落标（备份件在 05-exec/mark_backup），零手工行号写入】
- [ ] **【P1·待办 W-19（r46 新立）】** 写入器目前只管 07 的裁决标记；把 05/06/08 的状态回写（勾销、校正注）也纳入同一锚点入口，避免又出现「按行号改别的卷」的第二现场。 【r51 裁决=执行完毕｜写入器新增 --mode note（落为「【rNN 补记：…】」），05/06/08 的状态回写与裁决标记共用同一锚点入口、同一逐行保留行尾与同行读回验证；本轮即用该模式写 06 的归因校正注，夹具 t11–t14 四例锁死歧义/0命中/幂等三态】
- [ ] **【P0·下轮首推 W-16（r45 新立，优先级最高）】** 任何写记忆标记的临时脚本都必须复用 `r38_debt_aging.find_item_line()`（标题锚点，命中数 != 1 即拒写并报告），禁用行号索引写入。实证据：H-3″ 项三轮标记后裁决标记数仍为 0（W-9 真因）。复现：`python 05-exec/r38_debt_aging.py --json /tmp/x.json` 看 overdue 明细。 【r49 裁决=执行完毕｜唯一入口 05-exec/r46_mark_verdict.py 已注册为常驻门（run_gates id=mark_verdict_fixtures），本轮 5 条裁决全部经它写入；r49 追加第⑥重对账与混合行尾错位根因修（夹具 t9/t10）】
- [ ] **【P1·待办 W-17（r45 新立）】** 台账 coverage 三列进契约：按 `ts` 分代必填（r45 之前的行不要求该列，之后必须有三列），否则无法把完整性纳入取值域而不回溯误杀历史行（同族 r42 D36）。 【r50 裁决=执行完毕｜coverage 三列自 2026-09-25T15:00 起分代必填已生效（05-exec/schemas/r19/baseline-contracts.json 的 row_required_from），r50 又把它扩为多代列表以容纳第 2 代 face_* 列；旧行豁免由夹具 t61/t64 锁死】
- [ ] **【P0·下轮首推 W-14（r44 新立，优先级最高）】** 给 REPEAT 立棘轮第 9 指标 repeat_debt_items。存在理由（D59）：本轮 OVERDUE 从 16 压到 1 里有 11 条是靠「降级为长期看守」实现的；若降级项没有独立指标，「降级」就成了新的免检通道，等于把债务换个格子。取值面 by_class.REPEAT，须与 overdue/deferred 同等 fail-closed。 【r46 裁决=挂账至 r49｜契约分代必填需先设计，本轮产能给了 W-16 工具化】 【r49 裁决=执行完毕（并纠正 r46 误挂）｜第 9 指标 repeat_debt_items 已于 r45 落地（提交 8d7fb56，取值面 05-exec/ratchet_gate.py 的 _debt_class_state）；r46 却在本条头部写了延期、原因文本讲的是 W-17 —— 本轮到期追讨抓到，已立 W-22 做写时对账】
- [ ] **【P1·待办 W-15（r44 新立）】** 把 coverage 结果（status / prev_overdue / missing 数）写进 06-benchmark/debt_runs.jsonl 行并纳入契约取值域，让 CI 能看到「上轮欠几条、本轮清几条」的斜率，而不是只看到当期快照。 【r49 裁决=执行完毕｜coverage_status/coverage_prev_overdue/coverage_missing 三列已进台账（实测 8/31 行含该列）并由 r48 row_required_from 分代必填守卫生效，取值命令 git show r48:05-exec/r38_debt_aging.py 后 grep coverage_ 】
- [ ] **【P0·本轮落地 W-22（r49 新立）】** 裁决写入器加第⑥重对账：条目**自身编号**已被近期提交主题宣布落地却仍写延期 ⇒ 拒写（动因 = r46 我把 W-17 的延期写到已落地的 W-14 行上，3 轮后才被到期追讨抓到）。 【r49 裁决=执行完毕｜第⑥重对账已落地于 05-exec/r46_mark_verdict.py 的 landed_contradiction + item_own_id，且**本轮首跑即拦住我自己的 W-14 裁决**（因由文本引用了旧标记原文，随即加头部锚定修精度）】
- [ ] **【P0·本轮落地 W-23（r49 新立）】** 写入器按**物理行保留行尾**（混合行尾卷不得并元素后追加到元素末尾），读回验证从「文件里有这个串」升级为「锚点与标记同一物理行」。 【r49 裁决=执行完毕｜split_keep_eol/join_keep_eol 逐行保留行尾，读回改为同行断言；混合行尾反例夹具 r46_mark_verdict_fixtures t9/t10 由红转绿（修前实测把标记写到「待办 戊」行）】
- [ ] **【P0·本轮落地 W-24（r49 新立）】** 分类器只认**最后一个标记的头部**（第一个 ｜/】 之前）：原因体里引用的旧标记原文不得改变判定，也不得计入延期次数（防 REPEAT 第 9 指标虚增）。 【r49 裁决=执行完毕｜parse_last_verdict 只取最后一个标记的头部，count_deferrals 同口径；负控 q1-q5 五例锁死（引用旧标记不改判定、真延期仍 DEFERRED/到期仍 OVERDUE）】
- [ ] **【P0·下轮首推 W-20（r49 新立）】** 每个新判据首跑必须自证输入面未被截断/降采样。账龄尺已落四面（轮号面 -40 窗口 vs tag 硬锚、去重面 80 字前缀键、定年面 70 字 needle、建议面 报告→待办卷），取值 `python 05-exec/r38_debt_aging.py --report <本轮报告>`；残余面见 W-25。 【r49 裁决=执行完毕（限定）｜四面已落地于 05-exec/r38_debt_aging.py 的 face_round/face_dedup/face_dating/face_suggestions，夹具 22 例（含真数据反例：--report r48 报告即 [FACE:FAIL] 点名 W-20/W-21 未承接）；残余推广面转 W-25】
- [ ] **【P0·下轮首推 W-25（r49 新立）】** 把输入面自证推广到其余判据：05-exec/ratchet_gate.py、05-exec/baseline_contract_scan.py、05-exec/rule_conflict_scan.py 各自的 glob/取值窗口未自证；并评估把 face_status 写进 06-benchmark/debt_runs.jsonl 成为趋势列（先进台账观察，不进棘轮）。 【r50 裁决=执行完毕｜三面已推：05-exec/rule_conflict_scan.py 的 discover_authority_candidates/effective_files（扫描面 10→26 件，极性规则 116→188、互斥候选 6→21）+ 05-exec/ratchet_gate.py 的 face_counted_vs_observed；baseline_contract_scan 的 pattern 零命中判红 r33 既有，本轮只补防回退断言；face_status 已于 r49 进台账】
- [ ] **【P1·待办 W-21（r49 新立）】** 是否把 unowned_claims 升为棘轮第 10 指标：先量误报率再决定，未量出 0 误报前不得接线（r25 用户否决拦任务的闸门）。 【r49 裁决=作废（不接线）｜实测 27 条归属声明全部 OWNERED、UNOWNED=0：恒定 0 的指标是假绿面；且 OWNERED 判据精度低（正文含任意路径 token 即从 UNOWNED 翻成 OWNERED ⇒ 可刷），第 10 指标收益不抵噪声。牙齿已由 r39 夹具 t40-t46 变异桩持有，不重复建】
- [ ] **【P1·待办 W-26（r49 新立）】** memory/07-next-steps.md 实测为**混合行尾**（178 个 CRLF + 4 个纯 LF）—— 根因在写入侧（并发工具按 text 模式写）。统一行尾前须先定位那 4 行由谁写；属 R241 敏感区，禁为让门变绿而整卷重写。 【r50 裁决=执行完毕（限定）｜根因已用证据定死而非推测：git show r43..r49:memory/07-next-steps.md 实测仓库内**全 190 行纯 LF**，而工作树 187 CRLF + 4 纯 LF ⇒ 混合行尾来自写卷脚本未传 newline=空串（Windows text 模式把 \n 转成 \r\n），不是并行会话乱写；判据侧已由 W-23 逐行保留行尾免疫，写入侧约定立为 X-25，不改写既有卷（R241）】
- [ ] **【P0·待裁定 W-27（r50 新立）】** 冲突判据扩面（扫描面 10→26 件）后新暴露 **21 组极性互斥候选 + 11 条自述冲突**（取值：`python 05-exec/rule_conflict_scan.py --json 06-benchmark/rule_conflict_scan_r50_2026-09-25.json`，对照 r40 件为 10 件/116 规则/6 候选）。典型：`core/behavior_core_appendix.md:22`「必须含下一步建议」vs `core/behavior_core_rules_p3.md:13`「禁以状态报告/下一步建议形式结束」，以及三处版本计数口径互斥（V41/22 ↔ MEMORY V37/17 ↔ appendix V36/18）。本项目**不改受管根规则正文**（R241 + 红线），只登记并转办归属会话/用户裁定；裁定前该 21 组候选按"已知敞口"看守，不得为了让扫描变绿而删规则或缩回面。
- [ ] **【P0·下轮首推 W-28（r50 新立）】** 把「枚举源双路对账」做成机器判据：凡取证件引用外部/对手数字，必须带 `retrieval` 字段（≥2 条独立取值命令）+ 差额归因，缺一即由 `05-exec/baseline_contract_scan.py` 判红。存在理由（r50 实测）：r31/r32 的 workflow 计数取自 `/actions/workflows` 总计面，混入平台 `dynamic/*` ⇒ superpowers/anthropics 实为**仓库内 0 个 CI**，"82%/75% failure"是平台件而非测试；另 `steps=0` 的 failure 属 runner 配额而非测试失败。做法：先写夹具反例（无 retrieval 的取证件必须判红）看红，再加不变式。 【r51 裁决=执行完毕｜不变式 inv_opponent_claims_have_retrieval 已进 baseline_contract_scan 并注册到 debt_aging_r*.json 与新 pattern opponents_*_r*.json；按结构识别带 repo 的条目（不锁键名，防换 opponents/repos 绕过）；实跑接线另抓出 arg 传名字串会导致全量历史件被字典序豁免，已加 r11/r12 真接线反例】
- [ ] **【P1·待办 W-29（r50 新立）】** 复核其余历史对手数字是否同建立在错面：r38 的 open issues（287/1290/401/118/25）与最老 open 日期取自哪一路 API、是否含 PR 与已关闭项、有无 search-api 分页截断。取值须与 `search/issues` 的 `total_count` 交叉对账后方得引用。 【r51 裁决=执行完毕｜实测复核：r38 引用的 open issues 287/401/1290/118/25 取自 REST open_issues_count（含 PR），search 纯 issue 面 = 134/143/368/58/17 ⇒ 虚高 1.5-3.5 倍；最老 open 日期 5/5 逐条复现 ⇒ 错的是计数列不是整份件。证据与逐条取值命令见 06-benchmark/opponents_open_backlog_r51_2026-09-25.json】
