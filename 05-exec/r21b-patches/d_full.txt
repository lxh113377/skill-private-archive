> V10.70.0 (2026-09-24): **新增 ROUTER 行「同一指令当日重复触发 ⇒ 先过重复轮次闸门」（R285，自建skill优化 r21/r21b 实测治理）**——根因：Qoder 内置 7 个本地自动化任务（6 个 everyMs=3600000 + 焚诀 7200000，全 enabled、全 bypassPermissions）以**同一条 303 字符对标指令**逐字重放；实测本仓当日 11 提交中 5 轮属该族、贡献 **10,466 行 = 全仓新增 83.7%**，同日同指令跨 **4 个 session 且分布在不同项目**（本仓×2 / 医 / 孝心联），同一小时并出两个内容不同的 r20（`ab1bb8c` 与 `9782a9e`）⇒ 「缺重复识别」不只烧算力，而是**多会话对同一指令分叉入库**，正是本仓已登记「跨会话夹带 / R 号撞号」两条缺陷的生成机制。判据面盲区同族印证：r19 三口径对账全显示「一致」仍低估 34.3%（三口径同源于单根）。做法：SKILL.md ROUTER 表增一行，把「先跑闸门」变成路由动作而非提醒；退出码三分（0 放行 / 1 判定拒绝 / 2 取证失败按 R247 不得当放行），**故意区分「拒绝」与「崩溃」码**——实测踩点：闸门首版 `--pretty` 串残留 `%` 格式化致 `ValueError` 且两条路径同返 exit=1，只测生效侧会把崩溃误判为拦截成功，故反例侧必须同测并核对输出正文判定行。工具实物与两层含对照记录 = `自建skill优化/05-exec/repeat_round_guard.py`、`06-benchmark/全量对标报告_r21_重复轮次闸门_2026-09-24.md`；源头侧仅更新本项目那条任务（`36e379f3`，revision 7→8，只换 instruction，schedule/模型/权限/outputMode 全保留原值），备份与 restore_command 见 `06-benchmark/cron_tasks_backup_2026-09-24.json`；其余 6 个任务属其它项目决策面，**未擅动**。同步 frontmatter 10.69.0 → 10.70.0。
