# 第 3 轮执行报告（2026-09-22）— 项目四维体检 + 非破坏性整改

> 执行口径（用户 2026-09-22 按钮选定）：用 **A-project-better** 体检本项目 → 产出建议清单 → **非破坏性项自动执行**，破坏性项单列待确认。
> 结论可信度口径：`✅已实测 / ⚠️部分实测 / ❌未实测`（项目 `memory/AGENTS.md` 铁律）。
> 本轮**未修改** `D:\global_skills` 一个字节。

---

## 一、现状感知（四源采集 + 门禁实跑取证）

### 1.1 四源

| 优先级 | 源 | 关键实测 | 可信度 |
|---|---|---|---|
| 1 | 对话上下文 | 用户目标 = 项目方向体检；未口述迷茫点（迷茫点为隐含：三处状态口径不一） | ✅ |
| 2 | 项目记忆 | `07` 主卷 P0 4 条 / 分卷另有 11 条未完成；`01-goal` 六阶段目标 **0 项勾选**；`03`/`04` 空模板；`08` 仅注释示例 | ✅ |
| 3 | 代码结构 | 交付物 00-scope~05-exec 六阶段齐备；**最新写入 09-19 04:14（停滞 3 天）**；`01-scan/overlap_raw.txt` 283KB 中间产物滞留 | ✅ |
| 4 | git 历史 | 工作区**仅 1 次提交** `49ff0d6`（09-21 baseline，A-project-handoff #20 自动 git 化）；工作树 clean | ✅ |

### 1.2 门禁即证据（实跑输出摘录）

| 门禁 | 命令 | 实测结论 | 可信度 |
|---|---|---|---|
| 记忆健康度 | `handoff.py status` | `Completeness: 6/8`；07 P0 可交接（**未完成 15 项：主卷 4 / 分卷 11**）；各卷均 ≤4KB | ✅ |
| 记忆完整性 | `handoff.py review` | **Score 5/9（56%）**，4 warnings（02/03/04 未 sync + 05 无功能状态） | ✅ |
| 三门禁 | `rule_editor.py gates` | `mirror=pass noise=pass evolution=pass` | ✅ |
| 真相源 | 焚诀 `verify_truth_consistency.py` | **15 PASS / 0 FAIL / 0 SKIP** | ✅ |
| 版本控制基线（#20） | `git remote -v` + `ls-remote` | `origin` = `github.com/lxh113377/skill-private-archive`；远端 `main` = `49ff0d6` **== 本地 HEAD** → **有远端，基线达标** | ✅ |

### 1.3 四源冲突标注

1. **07 P0 分裂**：主卷只显 4 条，分卷藏 11 条 → 新对话按入口（主卷）读**看不到那 11 条**。
2. **skill 数口径四数不一**：项目记忆记「磁盘 169 == 注册表 169」(09-19)；本轮焚诀 verify **C1 实测 120**；`D:\global_skills` 顶层目录实测 **152**；全盘 `SKILL.md` 实测 **205** ⇒ 现行 P0「按当前 169 条复核」**基数不可用**。
3. **门禁与磁盘互相矛盾**：见 B 维度 ① ② ③。
4. **GM 每日日志断档**：`D:\global_memory\memory\` 日期文件从 `2026-09-08.md` 直接跳到 `2026-09-21.md` → **09-09~09-20 共 12 天空档**（R9 事故清空未回填）；`memory/2026-09-14.md` `Test-Path=False` ⇒ 本项目 09-14/09-19 两轮的 footer 落盘证据**已不可核验**。

---

## 二、四维诊断

### A 功能方向

- 六阶段中 **0-4 已完成、阶段5 只有 1 份建议文档且未执行**；阶段5 四项验收里三门禁已绿，`direct_map` 误命中与注册表失真未动。
- **最致命缺口：没有单一状态总表**。`TODO.md`（自称「跨会话续接唯一入口」）、`memory/07-next-steps.md`（handoff 法定唯一入口，致命纪律 #1）、`memory/05-feature-status.md` 三处各记一份状态 —— 已实测漂移。
- **过期待办堆积**：`07-next-steps.part1.md` 的 `## 已完成` 标题下塞着 **7 条 `- [ ]` 未完成项**，其中 ≥5 条实测早已完成：
  | 待办 | 实测依据 |
  |---|---|
  | 批量替换 `Desktop\焚诀` → `workspace\焚诀` | commit `1b6fe6d`（11 文件 34 处） |
  | 修正权威平台口径行（HM 回在役） | commit `5e89bb6` / `42760fc` + 焚诀 `f145f7f` |
  | A族 P0 整改（handoff 拆卷 / A-get-memory 死链 / 版本漂移 / 平台名） | commit `f8bc12f` / `5bcb550` / 第6轮平台名清理 |
  | 阶段2 按族分批全量精读 102 个 | `02-review/` 6 份族审计卡（7 批） |
  | 裁定 LOW 灰区 33 个归属 | 用户 2026-09-14 裁定 = **排除** |
  | `A-memory-start` Step 0.6 补 PowerShell `&` | commit `c3dba47` |
  | 确认两个 fenjue 件退役前无反向引用 | 二者已退役 `716ae69`，6 处活跃引用已清 4/6、2 处判为历史留痕 |
  唯一仍开口的 2 条：`收尾 A-get-memory 反哺`（⚠️ 09-14 GM 日志已随事故丢失，无法核验）、`阶段1 补跑 attention_sim/content_snr/negative_tag_audit`（❌ 未做）。

### B 技术债 / 遗留

- **门禁判据 3 处缺陷（✅实测，本轮新发现，属 R220/R247「判据可信度」族）**：
  1. **假通过**：`review` 输出 `08-ac-obs.md: 3 个验收标准，格式正确`，而磁盘上那 3 条**全在 `<!-- 示例： ... -->` 注释内** —— `handoff_lib/savepoint.py:632` 用 `re.findall(r"AC-OBS-\d+", content)` **全文匹配、不剔除 HTML 注释** ⇒ 模板示例被当成真实验收标准。
  2. **假阴性**：`review` 输出 `05-feature-status.md: 没有记录任何功能状态`，而磁盘主文件实有 **10 条「✅ 已完成」条目** —— 同文件 `:594` 正则 `^-\s*(\[x\]|\[ \]|[✅🚧📋])` 只认 `- ✅` 形态，**不认「`## ✅ 已完成` 章节标题 + 普通 `- 描述` 列表项」这一主流写法**。
  3. **口径自相矛盾**：`status` 判 `02-structure.md ✅ filled (auto-synced)`，`review` 同日判 `02-structure.md: 尚未运行 sync，目录树为空` —— 同一文件两个子命令结论相反（`status` 只数非空行）。
- 记忆层技术债：`01-goal` 六阶段目标 0 勾选；`03`/`04` 空模板；`06` 已知Bug / 技术债 / 红线 / 性能四节**全空**；`08` 零条真实 AC。
- 中间产物滞留：`01-scan/overlap_raw.txt`（282,977 B）留在交付目录；`05-exec/` 19 个 patch JSON **无索引**（本轮已补）。
- **`.rule_backup` 实测 32 个 `.bak`**，全部未跟踪（污染 `git status`）：且**全库备份集中落在 `D:\global_skills\A-memory-start\references\.rule_backup\`**（内含 `A-project-handoff.*.bak`）⇒ 备份与目标 skill 分离，回滚定位成本高；同日同文件份数超 R198.7 阈值（>5 即轮转）。
- 工作区 git 仅 1 次提交 ⇒ **无回滚粒度**；`archive/` 实测为空目录 ⇒ 归档机制从未执行（致命纪律 #4「archive = 安全网」形同虚设）。

### C 架构质量

- ✅ 目录与六阶段一一对齐（`00-scope` → `05-exec`），边界基本清晰。
- ⚠️ **三重同义源**：`TODO.md` / `memory/07-next-steps.md` / `memory/05-feature-status.md` 同时承载「下一步」；根 `AGENTS.md`（平台自动生成）又与 `memory/01/05/07` 高度重复。
- ⚠️ `04-plan` 与 `05-exec` 边界模糊：`剩余待办分类清单.md` 名为计划却含执行结论。
- ⚠️ 无可用归档层（`archive/` 空）。

### D 文档 / 交付

- ✅ 六份报告齐备；`.gitignore`（含 `.codebuddy/`）+ `.aiexclude` 到位；版本控制基线达标（含远端 + SHA 一致）。
- ⚠️ **无工作区入口文档**：新人/新会话不知道先读哪个；`TODO.md` 自称「唯一入口」与 handoff 法定入口冲突。
- ⚠️ 门禁复跑命令散落在历史报告里，无集中说明（本轮补入根 `README.md`）。
- ⚠️ `handoff review` **56%（5/9）** 是当前客观交付就绪度。

---

## 三、建议清单（10 条）

| # | 优先级 | 维度 | 建议 | 二元验收判据 | 执行属性 | 预估 |
|---|---|---|---|---|---|---|
| 1 | P0 | A/B | 按实测校准 `07-next-steps`：主卷 P0 重写 + `part1` 过期待办销账（≥5 条已实测完成） | `part1` 的 `## 已完成` 节下 `- [ ]` 计数 = 0；主卷 P0 每条都有实测依据 | **非破坏性** | 0.5h |
| 2 | P0 | B | 清失效阻塞：`05-feature-status.part1` 的「LOW 灰区 33 待裁定 / HM 方案待定 / q-2 待做」均已闭环 → 移出阻塞节 | 该文件 `33 个` / `待用户裁定` 0 命中 | **非破坏性** | 0.2h |
| 3 | P0 | D | 补记忆状态层：`01-goal` 按实测勾选六阶段 + 补已完成目标；`06` 补已知 Bug/技术债；`08` 补 ≥6 条**真实** AC | `handoff review` warnings 4 → ≤1；`08` 非注释 AC ≥6 | **非破坏性** | 0.8h |
| 4 | P1 | C | 消三重同义源：`TODO.md` 去「唯一入口」定位 → 改「阶段状态总表（索引）」并指向 `memory/07` | `TODO.md` 首行含指针；`唯一入口` 在 `TODO.md` 0 命中 | **非破坏性** | 0.3h |
| 5 | P1 | D | 补 `02` 模块说明 + `03` 技术栈 + `04` 核心文件地图（已跑 `handoff.py sync`，余下手工回填） | 三文件无占位串；`review` 的 3 条 sync 警告归零 | **非破坏性** | 0.5h |
| 6 | P1 | B | 建 `05-exec/README.md`：19 个 patch JSON → 对应 P0 编号 / 目标文件 / 结果 commit | 索引覆盖 19/19 patch 文件 | **非破坏性** | 0.4h |
| 7 | P1 | D | 建工作区根 `README.md`：读法入口 + 六阶段产物地图 + 门禁复跑命令 | `README.md` 存在且含 `rule_editor.py gates` 原文 | **非破坏性** | 0.4h |
| 8 | P2 | B | 修 `handoff.py` 3 处判据缺陷（AC 未剔注释 / 05 正则形态 / `status`≠`review` 口径） | 3 处各带**两层对照**：注释示例不再计入、真实 AC 仍识别；`## ✅ 已完成` 写法判 ✅；两子命令同结论 | **破坏性**（改 skill 源码） | 2h |
| 9 | P2 | A | 阶段5 剩余：`direct_map` 误命中修正 + 注册表 `user_created` 失真复核（**基数须先裁定**：169 / 120 / 152 / 205 四数不一） | 焚诀 `verify` 全绿；误直连用例消失（`优化自建skill` 不再直连 `vp-perspective-audit`） | **破坏性**（改焚诀派生件） | 1.5h |
| 10 | P2 | C | 归档与轮转：`.rule_backup` 32 个 `.bak` 按 R198.7 轮转（迁 `_trash`，禁删）+ `01-scan/overlap_raw.txt` 迁 `archive/` | 同日同文件 `.bak` ≤5；`overlap_raw.txt` 不在交付目录且在 `archive/` 内 | **破坏性**（移动文件） | 0.3h |

---

## 四、本轮已执行（非破坏性 7 项）与验收判据实测

| # | 项 | 验收判据 | 实测结果 |
|---|---|---|---|
| 1 | `07-next-steps` 主卷 P0 重写 + `part1` 销账 | `part1` 的 `## 已完成` 节下 `- [ ]` 计数 = 0 | ✅ 实测 **0 条**（原 7 条：5 条按磁盘/Git 销账、1 条上提主卷、1 条「09-14 反哺」不可核验已如实标注 ⚠️） |
| 2 | `05-feature-status.part1` 清失效阻塞 | 文件内 `33 个` / `待用户裁定` 0 命中 | ✅ 实测：4 项阻塞全部移入「已解除（历史留痕）」并按 R241 加校正注 |
| 3 | 记忆状态层回填（01/02/03/04/06/08） | `handoff review` warnings 4 → ≤1 | ⚠️ **部分达成**：4 → **2**（未达 ≤1）；剩 2 条**均非内容缺失**，而是上游判据缺陷所致（见 B 维度 ①②）；分数 5/9（56%）→ **7/9（78%）** |
| 4 | `TODO.md` 去「唯一入口」定位 | `唯一入口` 在 `TODO.md` 0 命中；首行含指针 | ✅ 实测：标题改「阶段状态总表（索引，**非入口**）」，首行写明入口 = `memory/07-next-steps.md` |
| 5 | `02`/`03`/`04` 回填 | 三文件无占位串；`review` 的 3 条 sync 警告归零 | ✅ 实测：`handoff.py sync` 填充 02 目录树；03/04 手工回填；三条 sync 警告**全部消失** |
| 6 | `05-exec/README.md` 补丁索引 | 覆盖 19/19 patch | ✅ 实测：`patch_*.json` 18 + `A5_patch.json` 1 = **19/19** |
| 7 | 根 `README.md` 入口文档 | 含 `rule_editor.py gates` 原文 | ✅ 实测：含读法顺序 / 六阶段产物地图 / 门禁命令块 |

新增文件：`README.md`、`05-exec/README.md`、`05-exec/第3轮执行报告.md`。
**未改动**：`D:\global_skills`（0 字节）、`00-scope/`~`04-plan/` 既有交付物、`01-scan/overlap_raw.txt`。

## 五、收口门禁复跑（实测输出）

| 门禁 | 复跑结果 | 对比整改前 |
|---|---|---|
| `handoff.py review` | **7/9（78%）**，2 warnings | 5/9（56%），4 warnings ↑ |
| `handoff.py split --check` | 07 与 05 全部卷均 ≤4KB ✅ | 同 |
| `handoff.py savepoint` | ✅ **exit 0「本次对话已安全落盘」**（01:20:13 之后复跑） | 中途曾 1 次被拒（`[GATE:noise-fail]`）；根因与解除见第五-附节 |
| `handoff.py coldstart --check` | ✅ `COLD_START_DECL` 与分卷逐字快照一致（6 行 diff 全等） | 同 |
| `handoff.py sanitize` | ✅ 无保留名 / 无空目录 / 无过期副本 | 同 |
| 焚诀 `verify_truth_consistency.py` | ✅ **15 PASS / 0 FAIL / 0 SKIP** | 同（未改焚诀） |
| `rule_editor.py gates`（mirror/noise/evolution） | ✅ `mirror=pass noise=pass evolution=pass` | 同（未改受管根） |

> 说明：`savepoint` 最终 **exit 0 通过**；但中途有 1 次被拒，其根因与解除过程完整记录在第五-附节（**不因最终通过而抹掉中途失败**）。`memory/08-ac-obs.md` 的 `AC-OBS-06/07/08` 仍保持未勾选（原因逐条标注在文件内）。

### 五-附：`savepoint` 中途被拒 → 已解除（含两条教训，✅已实测）

**时间线（多源互证）**

| 时刻 | 事件 | 证据 |
|---|---|---|
| 01:15:58 | `焚诀\.codebuddy\` 出现（内含 `memory\2026-09-22.md`，1038B）—— **运行中 IDE 的会话数据** | `Get-Item` 实测 `CreationTime` |
| ~01:17 | 本项目 `savepoint` **被拒**：`[GATE:noise-fail]` → `VIOL … 焚诀\.codebuddy` | savepoint 实跑输出 |
| 01:20:13 | **并行会话**补 `焚诀\.gitignore:9` 的 `.codebuddy/`（同 `.workbuddy/`，属环境元数据） | 焚诀 commit `1f354e9` |
| 01:20 后 | `rule_editor.py gates` → `noise=pass`；`handoff.py noise` → `[GATE:noise-pass]`（四根 `violation=0`） | 两入口实跑输出 |
| 01:20 后 | 本项目 `savepoint` **复跑 exit 0「本次对话已安全落盘」** | savepoint 实跑输出 |

**教训 ①（反直觉，跨项目通用）**：`焚诀\.codebuddy\` 是**活跃工具目录**，按致命纪律 #17 **字面处置「迁 `_trash`」会破坏运行中的 IDE 会话状态**。`noise_lint` 的既有正确出口是「**被 `.gitignore` 命中 → 降级 `quarantine`**」⇒ 正确修法是**补 gitignore 豁免**，不是迁文件。

**教训 ②（R-CURRENT 时间戳盲区，方法论级）**：我先后两次实跑同一门禁得到**相反结论**（`handoff noise` fail → `gates` pass），一度误判为「门禁自相矛盾」。**真实根因是两个并行会话在同一对象上先后改动**（01:17 失败 / 01:20 修复），并非判据缺陷。⇒ 判据：**「同项两入口结论不一致」时，先查对象与仓库的 mtime / commit 时间，再谈判据缺陷。** 该误判已从 `memory/06-constraints.md` 撤回，**不计入 D1 的 3 处判据缺陷**。

## 六、待确认（破坏性，未执行）

| # | 项 | 为什么算破坏性 | 需要你决策什么 |
|---|---|---|---|
| D1 | 修 `handoff.py` 3 处门禁判据缺陷 | 改 `D:\global_skills` 受管根内 skill 源码，须走 `rule_editor.py` + 复跑三门禁 | 是否授权改上游 skill 源码（会同步镜像端） |
| D2 | 阶段5 两项（`direct_map` / 注册表失真） | 改焚诀派生件（`direct_map.json` / 注册表 JSON / BGE 索引），须 `build_indexes.py --apply` + verify | 先裁定 skill 数口径（169 / 120 / 152 / 205），再定复核范围 |
| D3 | `.rule_backup` 轮转 + 中间产物归档 | 移动受管根内文件（迁 `_trash`，禁删）+ 移动工作区交付目录内文件 | 是否授权；轮转目标是否用 `D:\global_memory_archive\_trash\backup-rotation\` |
| D4 | 本轮 7 项改动的 `git commit` + `push` | git 提交属破坏性操作（写入仓库历史 / 远端） | 是否提交并推送（建议分 2 个主题提交） |
| D5 | GM 每日日志 09-09~09-20 空档 12 天 | 涉及基础设施仓内容补记，且**禁止改写历史留痕**（R241） | 是否回填（只能新建「事后说明」条目，不得伪造当日日志） |


## 七、附：`焚诀\.codebuddy` 归属说明（⚠️不能确证，不作单方归因）

- 本会话确实执行过 `Set-Location 'C:\Users\37533\Desktop\workspace\焚诀'` 跑 `verify_truth_consistency.py`（**cwd 曾为焚诀**），故本会话**可能是写入方之一**。
- 但该目录性质为「**运行中 IDE 的会话数据**」（当日日志 mtime 分钟级），且同一时段另有并行会话在焚诀作业（并已就此提交 `1f354e9`）⇒ **无法单方归因**。本报告**不**把它记为本轮引入的散落项。
- 已吸收的教训：跨受管根跑脚本改用 `Push-Location` + `Pop-Location`（本会话后段已改；实测 `Get-Location` 已复位回本项目工作区）。

---

## 八、D1–D5 执行结果（用户 2026-09-22 全部授权后）

### D1 修 `handoff.py` 3 处门禁判据缺陷 —— ✅ 完成
**受管根 commit `7cc7d4d`**；`A-project-handoff` **V3.41.5 → 3.42.0**（frontmatter + 版本历史分卷同步）。

| 缺陷 | 修法 | 验证 |
|---|---|---|
| 假通过：AC 计数不剔 HTML 注释 | `savepoint.py` 计数前调 `strip_html_comments()` | 层a：注释-only 输入 AC 数 3 → **1** |
| 假阴性：05 只认 `- ✅` 标记式 | 新增 `has_feature_entries()`，兼容「`## ✅ 已完成` + 普通 `- 描述`」 | 层a：章节式 **True** / 只有标题无条目 **False** |
| 口径矛盾：`status` 与 `review` 对 02 结论相反 | `status` 结构类**先判 `is_sync_placeholder()` 再累加计数** | 层b：探针项目 `status` 现报 `❌ empty / needs sync`，与 `review` **同结论** |

三个判定函数**下沉 `common.py` 作单一真相源**（`is_sync_placeholder` / `has_feature_entries` / `strip_html_comments`），`savepoint`（review）与 `projcmds`（status）共用，根绝双处漂移。

验证（R238 两层含对照）：
- **层 a 隔离桩 11/11 通过**（含旧行为对照：未剥离时 AC=3，即假通过来源本身）
- **层 b 集成对照**：合成项目「有真实内容」→ 05/08 **双 PASS**；合成项目「注释-only」→ 08 **仍报** `没有定义 AC-OBS 验收标准`（**防线未被放松**）
- 收口：本项目 `review` **5/9（56%）→ 9/9（100%）**；三门禁 `mirror=pass noise=pass evolution=pass`；焚诀 `verify` **15 PASS / 0 FAIL**（C13 版本线对齐）

### D2 阶段5 两项 —— ✅ 完成
**焚诀 commit `bf5e284`**（已推送；`rev-parse HEAD` == `ls-remote origin master`，SHA 核对一致）。

**D2① 直连表误命中修正 + 死目标清理**

- 复现：`为优化我的所有自建skill及其所搭载的工作流提出建议` → 原直连 `vp-perspective-audit`；根因 = pattern 里的**无锚点裸 `提出建议`**（任何含该词的查询都被劫持）
- 修法：收窄为需「项目/主线」锚点方可直连（`(项目|主线).*提出建议`；`不足` 类锚点保留）
- **系统性复扫（不止修个案）**：全表 270 条 → **7 个死目标 / 11 条死直连**：`skill-install`×3、`deep-research-pro`×2、`tencent-cos-skill__skillhub`×2、`skill-creator`、`multi-search-engine`、`fenjue-advisor-scoring`、`fenjue-cc-audit-cycle`
  - **重指**：`skill-install`·`skill-creator` → `A-skill-manager`；`deep-research-pro` → `consulting-analysis`；两个 fenjue 件 → `fenjue-memory-audit`
  - **删除**（无在役继任者，回落常规管线）：`tencent-cos-skill__skillhub`×2、`deep-research-pro` 的「搜一下」、`multi-search-engine`
- 验证：死目标复扫 **0 条**；回归套件 **ALL PASS**（T2 直连 **47/47**、T3 top1 死件→`A-skill-manager`、T4 **33/34 (97.1%)**、T5 **35/35 (100%)**）；定向双向验证 —— 正例（项目不足→`vp-perspective-audit`、装个技能→`A-skill-manager`、深度研究→`consulting-analysis`）**通过**，反例（原误命中查询 `direct_hit=false`）**通过**
- 连带修：`test_router_regression.py` + `test_queries.json` 中 **6 处**指向已退役 skill 的过期期望（另删 1 条无在役继任者的用例）

**D2② 注册表 `user_created` 失真复核（口径已裁定）**

口径裁定（✅实测）：**注册表口径 = `焚诀/skill/registry/unified-skills-index.json` 的 120 条**（同口径由焚诀 `verify` C1/C2/C10 三方守）；记忆里的「169」为 R9 事故前旧值。

实测（明细见 `05-exec/user_created_audit.json`，脚本 `05-exec/user_created_audit.py`）：

| 项 | 实测结果 |
|---|---|
| 注册表条数 | **120**（`user_created=true` **27** / `false` 93） |
| A 类：注册表有但磁盘无 SKILL.md | **0**（无死件残留） |
| B 类：`true` 但带市场元数据 | **0** |
| C 类：`false` 且无市场元数据 | **85** —— ⚠️ **不可直接判为失真**：反证 `canvas-design`（Anthropic 市场件、无 `_meta.json`）同落此类 ⇒「无市场元数据」是**必要非充分**条件 |
| `source` 与 `user_created` 不一致 | 4 条，其中 **2 条为真脏数据**：`testing` 的 `source` 为空串、`oc-dispatch-exec-guard` 用非标准值 `user`（另 2 条 `skillhub` 属正常市场来源标注） |

**根因（✅已实测，读 `build_registry.py:80-82`）**：`user_created` 由各 SKILL.md frontmatter 的 `user_created:` 字段决定，**缺省 false** ⇒ 93 条 `false` 的真实含义是「**未标注**」而非「标错」。

**结论**：09-14 审计报的「42 条 `user_created` 失真（市场件被标自建）」在当前 120 条口径下**已不复现**（那批失真条目正是 R9 前 169 条时代的市场件，现多已不在注册表）。**建议不机械批量改**（判据不可靠 + 改动需 `build_registry` + `build_indexes --apply` + verify 全链）；仅登记 2 条真脏数据为 P2。

**附带发现（重要，已量化）**：

| 口径 | 数 | 说明 |
|---|---|---|
| 注册表 | **120** | 唯一对账口径 |
| 磁盘含 SKILL.md | **151** | 仅磁盘有 **31**：11 个 `local-*`（灰区排除件）+ 20 个新装市场件（brainstorming / figma / slides / obsidian-* / notion-cli / theme-factory 等） |
| 仅注册表有 | **0** | 无死件残留 ✅ |
| 阶段0 自建清单（HIGH+MID） | 90 → 仅 **60** 仍在册 | **30 个已从磁盘消失**（如 `data-layer-consistency-fix` / `skill-hitrate-full-audit` / `fenjue-routing-health-check`；`local-vram` 磁盘在但未入册）⇒ 项目记忆「自建 ≈90」基数**已过期** |

### D3 归档与轮转 —— ✅ 完成

- `.rule_backup`（`D:\global_skills\A-memory-start\references\.rule_backup`）按 R198.7 逐「文件 × 日期」分组实测：**仅 1 组超阈值** —— `A-memory-start.SKILL.md` @ 2026-09-19 共 **10** 份 → 保留最近 5 份，**迁移（非删除）** 5 份至 `D:\global_memory_archive\_trash\backup-rotation\A-memory-start\2026-09-19\`；备份总数 **40 → 35**
- `01-scan/overlap_raw.txt`（282,977B 中间产物）→ **`archive/overlap_raw.txt`**；其后 `handoff.py sync` 刷新 `02-structure.md` 目录树
- 其余组均 ≤5 份/（文件·日），按纪律不触发轮转

### D5 GM 每日日志空档留痕 —— ✅ 完成

- 按「历史留痕不可改写」纪律**不补造当日日志**，改为在 `D:\global_memory\memory\2026-09-22.md` 追加「**R9 事故 · 每日日志空档说明（事后补记）**」：事实（09-09~09-20 共 12 天无日志；`2026-09-14.md` 实测 `Test-Path=False`）/ 原因（R9 `robocopy /MIR` 漏 `/XJ` 跟随 junction 清空两仓含各自 `.git`）/ 影响面（期间 footer 落盘证据与 lessons 增量不可核验）/ 处置决定
- 门禁：`upgrade_footer_gate.py` → `[GATE:evolution-pass]`（追加后仍取合法块）

### D4 工作区提交与推送 —— 见第九节

---

### D4 工作区提交与推送 —— ✅ 完成

按主题拆 **3 个提交**（逐路径白名单暂存，**禁 `git add -A`**，符合「并发会话防护」纪律）：

| commit | 主题 | 内容 |
|---|---|---|
| `5c00034` | `docs(memory)` | 记忆层按实测回填 + 三重状态源收敛（07 为 P0 唯一真相源、TODO 降为阶段状态总表）—— 14 文件 |
| `b1801f7` | `docs(exec)` | 阶段5 执行证据（D1 补丁与索引 / 直连表死目标扫描 / `user_created` 复核脚本 / 第3轮报告）+ `overlap_raw.txt` 删除 —— 16 文件 |
| `c55d2ea` | `chore(archive)` | 归档阶段1 中间产物 `overlap_raw.txt`（283KB）至 `archive/` |

**#20 基线核对（✅已实测）**：`git rev-parse HEAD` = `c55d2ea…` == `git ls-remote origin main` = `c55d2ea…`（远端私有归档仓）；`git status --porcelain` **为空**。

### D1–D5 总收口门禁（✅已实测）

| 门禁 | 结果 |
|---|---|
| 本项目 `handoff review` | **9/9（100%）**（起点 5/9 = 56%） |
| 本项目 `handoff savepoint` | **exit 0「本次对话已安全落盘」** |
| 本项目 `handoff split --check` | 07 / 05 全部卷 ≤4KB ✅ |
| `rule_editor.py gates` | `mirror=pass noise=pass evolution=pass` |
| 焚诀 `verify_truth_consistency.py` | **15 PASS / 0 FAIL / 0 SKIP** |
| 焚诀 `test_router_regression.py` | **ALL PASS**（T2 直连 47/47 / T4 97.1% / T5 100%） |
| 死目标复扫 | **0**（修复前 7 目标 / 11 条） |

---

## 十、用户追加授权项执行结果（2026-09-22 第 5 批）

### ① 阶段0 清单按新基数重出 —— ✅ 完成

- **前置修复（工具已失效）**：`01-scan/scan_all.py` —— 阶段0/1 的「唯一可复跑工具」**实跑崩溃**：第二源 `platform-oc.json` 随 OC 退役（2026-09-21）被删除 → `FileNotFoundError`。适配：口径回落为「**在役端 `platform-*.json` 的 `user_created_skills` 并集**」（语义等价），并在产物头部注入来源/基数/覆盖说明
- **历史留痕（R241）**：v1 三件（2026-09-14，169 条时代）**原样归档**至 `archive/scope-v1-169-2026-09-14/`，不覆写、不加改
- **重出结果（✅已实测）**：基数 = 注册表 **151** 条（= 磁盘实测，焚诀 verify C1 全等）

| 置信度 | v1（169 时代） | **v2（151 基数）** |
|---|---|---|
| HIGH | 68 | **51** |
| MID | 34 | **26** |
| LOW（灰区） | 33 | **30** |
| EXCLUDE | 34 | **43** |
| 自建合计（HIGH+MID） | 102 → 后修正 90 | **77** |
| 失真（命名域自建但 `reg=false`） | 42 | **41** |

- **对记忆的校正**：项目记忆里「自建 ≈90 / 基数 169」**全部作废**，此后统一引用 **v2 清单 + 注册表 151 条**
- 覆盖说明：脚本统计磁盘 150 个目录（`SKIP_DIRS` 排除 `_my-skills` 保护标记 + `hooks` 基建等）与注册表 151 差 1，已在产物头部显式写明

### ② 31 个「仅磁盘有」的 skill 入册 —— ✅ 完成

- **关键认知修正**：这 31 个**不是"漏注册"**，而是被 `truth_constants.RETIRED_SKILLS`（R198.6 fail-closed「禁止批量入库还原退役件」）**有意排除** —— 实测 **31/31 全部命中黑名单**
- 摘除黑名单 **100 → 69**：21 个市场件（2026-09-21 OC 卸载时按「插件源技能」登记退役，其后经市场**重装回磁盘**）+ 10 个 `local-*`（灰区裁定只管「维护范围」，用户裁定仍入册）
- 重建链：`build_registry.py` → 注册表 **120 → 151** → `build_indexes.py --apply`（BGE `(151,512)` / TF-IDF `(151,5000)`、25 个派生件、守恒 PASS）
- **暴露并修好 2 处派生件脱节（纪律 #18 族）**：
  | 门禁 | 失败原因 | 修法 |
  |---|---|---|
  | C6 | GM `cross_platform_map.json` 的 note 仍引用仓库版 V1.21 | 同步为 **V1.22**（并写明本次变更） |
  | C16 | `STATUS.md` / `STATUS.part1.md` **内容过期**（写 120 skills，mtime 却新鲜 —— C14 只验 mtime 的盲区） | **用生成器 `aggregate_status.py` 重出**（不手改数字） |
- **收口（✅已实测）**：焚诀 `verify` **16 PASS / 0 FAIL / 0 SKIP**；路由回归 **ALL PASS**（T2 47/47 / T4 97.1% / T5 100%）；双仓提交且 SHA 一致 —— 焚诀 `28daf6e`（master）/ GM `575ca99`（main）

### ④ A-project-handoff 工作流升级（删除 → 回收站）—— 见第十一节

---

## 十一、④ A-project-handoff 工作流升级：删除 → 回收站 —— ✅ 完成（受管根 commit `49789bb`）

**用户口径（2026-09-22 按钮三选）**：① 实现形态 = **两者结合**（系统回收站优先，失败/无 GUI 回落 `_trash`，两条路都写 manifest）；② 适用范围 = **升级为致命纪律**（约束所有 agent 手删行为）。

### 现状诊断（为什么需要）
「删除」此前有**三条互不衔接**的路径：

| # | 路径 | 问题 |
|---|---|---|
| 1 | 散落项按致命纪律 #17 迁 `_trash` | 不永久删，但**无台账、只能手工复原**，且无统一入口 |
| 2 | `audit forget` 里 `os.remove(src)` | 源文件仅剩被归档那一条时**硬删**，不可逆 |
| 3 | agent 手工 `Remove-Item` / `rm` | 无台账、无复原路径、无纪律约束 |

### 实现
- **新增模块** `handoff_lib/recycle.py`：`recycle()` / `restore()` / `read_manifest()` / `anchor_for()` / `recycle_bin_available()`
  - **路径优先级**：① **系统回收站**（Windows Shell `SHFileOperationW` + `FOF_ALLOWUNDO`，资源管理器右键即可「还原」）→ ② 回落 `<anchor>/_trash/<时间戳>/`（`shutil.move`，同盘可逆）
  - **防静默永久删除（本版最容易漏的坑）**：`FOF_ALLOWUNDO` 在**无回收站的卷**上会被忽略而**直接永久删除** → 送回收站前先探测「盘符根下的 `$Recycle.Bin`」，不存在则**显式走 `_trash`**，不赌运气
  - **台账**：`<anchor>/_trash/recycle_manifest.jsonl`（id / 时间 / 原路径 / 去向 / method / 原因 / 回落原因），两路径都写；`<anchor>` 缺省就近向上找含 `memory/` 或 `.git` 的目录
- **新增 CLI**（`handoff.py` 薄 facade 独立分支）：`recycle <path> [--anchor] [--reason] [--dry-run]` / `--list` 查台账 / `--restore <anchor> <id|原路径>` 复原（系统回收站条目返回资源管理器还原指引，脚本不代执行）
- **新增致命纪律 #21**（`references/disciplines.md`）：任何删除动作（含 agent 手工 `Remove-Item`/`rm`、脚本 `os.remove`/`shutil.rmtree`）**必须先入回收站**，禁止直接永久删除；**唯一例外** = 用户显式要求「彻底删除」（须在汇报写明「不可回滚 + 已按用户明示执行」）
- **连带修**：`audit forget` 的 `os.remove(src)` → 改走回收站（**先写台账再删，失败即中止不静默**）
- 文档同步：`SKILL.md` 决策树新增 `recycle` 行 + 分卷索引 17→18 子命令 + facade 计数与 USAGE；`references/commands.md` 新增 **§18**；版本 **V3.42.0 → V3.43.0**

### 验证（R238 两层含对照，✅已实测：**18/18 通过**）

| 层 | 用例 | 结果 |
|---|---|---|
| a 隔离桩 | A1 `anchor_for` 就近命中含 `.git` 的根 | PASS |
| a | A2 **反例**：路径不存在 → `ok=False` 且**台账不增长**（不静默成功） | PASS |
| a | A3 回落分支（patch 掉回收站可用性）→ `method='trash'`、原路径消失、dest 存在、台账 +1 | PASS |
| a | A4 `restore` → 原路径回来且 **SHA256 与删除前逐位一致**（哈希级判据，非「文件回来了」） | PASS |
| a | A5 **反例**：同一 id 二次 `restore` → `ok=False`（备份已不在 `_trash`） | PASS |
| b 真机集成 | B1 走真实 CLI 子进程 → 退出码 0、`ok=True`、原路径消失、台账条数符合预期、**实际 `method=recycle_bin`**（确实进了系统回收站） | PASS |

> ⚠️ **判据自身也要过边界样本（本轮自曝）**：B1 首版 **3 个 FAIL**，查明是**我的测试判据 bug**（① CLI 是多段输出，只取首行 `{` → `json.loads("{")` 失败 → 误判 `ok=None`；② 台账预期漏算「复原也会记账」）—— 修判据后全绿。这是 R263 的现场版：**报「不一致」时先证判据，再动代码**。

### ④ 收口门禁
- `rule_editor.py gates` → `mirror=pass noise=pass evolution=pass`
- `handoff.py coldstart --check D:\global_skills` → `COLD_START_DECL` 与分卷逐字快照一致（**6 行逐行 diff 全等**）
- 焚诀 `verify` → **16 PASS / 0 FAIL**（C13 版本线 V3.43.0 已对齐）
- 受管根 commit `49789bb`（7 文件，含新模块）

### ⚠️ 本轮新发现（工具缺陷，已登记待办）
**`rule_editor.py commit --fix-mirror` 本轮未生效**：commit 成功但镜像端 `missing=1 mismatch=6` → `gates` 随即报 `mirror=fail`；**需手工**跑 `check-skill-mirror.ps1 -Fix` 才恢复 `[GATE:mirror-pass]`（已被 ⑥ 收口实测捕获）。该缺陷与「改完必须镜像零漂移」纪律直接冲突，应修 `rule_editor` 的自动补齐链。

---

## 十二、第 6 批：修 `--fix-mirror` + 范围裁定维持 + 31 件入库体检（2026-09-22）

### 1. 修 `rule_editor.py commit --fix-mirror` —— ✅ 完成（受管根 `7d8a8e1`，`A-memory-start` **V10.62.0 → V10.63.0**）

**根因（✅已实测，非推测）**：该 flag 的修复**只挂在 `_do_commit` 的失败分支**（「hook 判为镜像拦截 → `-Fix` → 重试」），而**三仓 `.git/hooks` 实测只有 `post-commit`（自动 push）、没有 `pre-commit`**：

```
实测命令：Get-ChildItem <repo>/.git/hooks -File | Where-Object Name -notmatch '\.sample$'
结果：D:\global_skills → post-commit(324B) ｜ 焚诀 → post-commit(324B) ｜ D:\global_memory → post-commit(324B)
      （三仓 pre-commit 均不存在 / 仅 *.sample）
```

⇒ `_do_commit` **永远 rc=0** ⇒ 该分支是**死代码**；`04-plan/工作流专项建议.md` §4「mirror / noise / evolution 已挂 `hooks/pre-commit`」属**设计意图而非既成事实**。

**修法**：`--fix-mirror` 改为**提交前主动三步** —— ① 只读检查（`check-skill-mirror.ps1` **不带** `-Fix`；漂移即 `[GATE:mirror-fail]` + exit 1）② 漂移则 `-Fix` ③ **复核**，复核仍失败则**显式告警**（不静默）。新增 `_mirror_check_only()`：脚本不存在时返回 True 且告警（不把「检查不可用」误报成「镜像漂移」，R247 精神）。原 hook 失败重试分支保留以兼容将来装回 hook。

**验证（两层含对照，✅已实测）**：

| 路径 | 操作 | 结果 |
|---|---|---|
| **反例基线** | 应用 R270 补丁后、未做任何修补 | 镜像实测 `missing=0 mismatch=3` + `[GATE:mirror-fail]` |
| **生效路径** | 同一 `commit --fix-mirror` | `[mirror] 检测到镜像漂移，自动执行 -Fix ...` → `[mirror] 镜像已补齐（复核通过）` → `[git] committed` → **`gates` = `mirror=pass noise=pass evolution=pass`（零手工操作）** |

**落盘 R270**（`SKILL.md`「现状核验 / 历史留痕速查」区**区内追加**，区数 **6 不变**；落盘后 **282 行 / 64,000B**）—— 通用判据：**任何「提交后修复」型开关，都必须同时有「提交前主动检查」路径**；**自查一行** = `Test-Path <repo>/.git/hooks/pre-commit`（False ⇒ 所有基于 hook 拦截的自动修复都是死代码）。

### 2. 范围裁定：v2 清单 LOW 30 / EXCLUDE 43 **维持不变** —— ✅ 已记录

用户 2026-09-22 裁定「维持」→ 自建口径定格 **HIGH 51 + MID 26 = 77 条**（基数 = 注册表 151 条），已写入 `memory/01-goal.md`「范围裁定」节 + `memory/06-constraints.md`「范围 / 口径裁定」节；「自建 ≈90 / 基数 169」**正式作废**。

### 3. 31 件纳入版本化 —— ⏸ 已授权，**入库粒度待定**（见第十三节）

**入库前体检（✅已实测）**：

| 分桶 | 体积 | 性质 |
|---|---|---|
| `local-*/bin/`（openvino `dll/pyd/lib/exp`） | **29.57MB** | 第三方运行时**预编译二进制**（可重下） |
| `local-*/wheels/`（`*.whl`） | **9.77MB** | 第三方**离线包缓存** |
| `local-*/tests/`（`qgs2.mp4` / `9.png` 等） | **7.48MB** | 测试素材 |
| `local-*/nltk_data/`（`*.zip`） | **4.96MB** | 第三方数据包 |
| `local-tts/assets/ref/`（`*.wav`） | **3.60MB** | 参考音频资产 |
| `__pycache__` / `*.pyc` | **0.31MB** | 编译产物（按纪律本不该入库） |
| **合计** | **57.63MB** | 纯源（`.py/.md/.json/.ps1`）仅约 **1.9MB** |

风险：**git 历史不可瘦身** —— 44MB 依赖与产物一旦入库即永久留在历史里；且 `__pycache__/*.pyc` 与项目自身散落项纪律冲突。体检同时确认**无 `node_modules`、无嵌套 `.git`**。

