- V3.54.1 (2026-09-25): **R283** **noise_lint 两处「破坏性假阳性」修复（自建skill优化 r37；该项目 savepoint 连红 7 轮的根因）**——① strict 模式把**归属方有意提交的治理件**判 VIOL：`焚诀\CONTRIBUTING.md`/`LICENSE.md`/`SECURITY.md` 实测全部 `git ls-files` 命中、由 `114b5b0 docs(7-J): 根部社区件回正` 有意入库，却因未登记 allowlist 被判违规，且工具给出的处置是「迁往 `_trash`」= 建议把已入库治理件搬走（R263 禁「改数据凑判据」，此处错在判据）；② 二级深扫把 `.git` 当项目内容面：`GM/.git/push-failure.log` 与 `焚诀/.git/push-failure.log` 是**仓内工具 `git_autopush.py` 自己的运行态日志**，位置天然不受 `.gitignore` 管辖 ⇒ 只要发生过一次推送失败本门就**永久红**（结构性自锁），且工具例外条款（登记 .gitignore 降级 quarantine）对它无效。**修法（三重收窄，牙齿不松）**：新增 `git_tracked(root,name)`（异常/非仓库一律 False = fail-closed，R247），仅在 strict 分支 + **仅文件**（目录仍须登记 `dirs`，保住「防随手建目录」）+ 命名不撞 `STRAY_NAME`/`MIGRATION_NAME`（该判在前方先跑）三条件齐才放行；二级扫描对 `name == ".git"` 跳过深扫，一级仍按 allowlist 判。**验证**：层a 隔离桩 `05-exec/r37_noise_tracked_stub.py`（本仓，temp git 仓真提交）**13/13**，含 5 条保牙反例（未跟踪随手件仍 VIOL / 已跟踪但 `notes_old` 命名散落仍 VIOL / 已跟踪**目录**不在清单仍 VIOL / `scripts/*.bak_20260815` 二级仍抓 / 非仓库根 `git_tracked` 为 False）；修前红 8 项、修后全绿，另含两个真实受管根断言。**层b 真机**：`handoff.py noise` 四根复跑 **`[GATE:noise-pass]`**（global_skills 168ok/0viol、global_memory 65/0、archive 6/0、焚诀 41/0）—— 该门自 V3.51.0 之后首次对全部受管根零违规。桩自身两处修桩留痕：首版把「未跟踪」文件写在 `git add -A` 之前、且误以为 `_bak` 可在名中命中（`STRAY_NAME` 实为行尾锚定），写出过 2 条**假反例**，R220 抽验抓回后重做。

- V3.54.0 (2026-09-24): **R274**