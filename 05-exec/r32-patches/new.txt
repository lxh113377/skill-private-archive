  ② **变异/反例对照**：至少一条「把判据改坏，夹具必须变红」的实证（参考 `05-exec/r19_fixture_mutation_check.py`：未变异对照组 PASS + 4 项判据变异全部 rc=1）。
     **② 的有效性前置（2026-09-25 自建skill优化 r32 实测补，勿删）**：每条变异必须绑定**只有该变异能使其变红**的专属输入面——探针样本形态与目标变异**一一对应**。否则「全部被拦」可以是假的：本轮 M1（`local = [r for r in rows if r.get("origin")=="local"]` 改成 `local = rows`）在**混合样本** `[ci, ci, local]` 上行为不变，却因断言写成「结果 != FAIL」而被报成"拦住"，夹具一度 4/4 虚绿。
     **自证法（一行命令级）**：逐条删掉该变异对应的那条断言后重跑，若其余断言**仍全红**，说明该变异根本没被覆盖 ⇒ 补专属反例（本轮 = 纯 `[ci, ci]` 样本）。同族形态：`lessons.part9.part1.md`「防回潮扫描命中自身字面量致自指假命中」。