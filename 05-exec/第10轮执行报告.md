# 第 10 轮执行报告（2026-09-22）— A-project-better Step 0 首次实跑 + 三方向并集执行

> 执行口径（用户 2026-09-22 按钮选定）：**① 确认 Step 0 遗留台账 ② 全执行（破坏性逐项回按钮确认）③ 优先范围 =「记忆层滞后回写 + 阶段1 扫描补跑 + 注册表与平台名收尾」三项并集**。
> 结论可信度口径：`✅已实测 / ⚠️部分实测 / ❌未实测`。
> 本轮**已改动**：本项目工作区（记忆层 / README / TODO / `01-scan/`）+ `01-scan/scan_all.py`；**未改动** `D:\global_skills` 与焚诀一个字节。

---

## 一、Step 0 — 遗留待办盘点（v1.3.0 新门禁首次实跑）

采集源：`memory/07-next-steps.md`(+part1/2) / `TODO.md` / `memory/05-feature-status.md`(+part1~5) / `memory/06-constraints.md` / 上次 A-project-better 产出的「待确认（未执行）」区（第 3 轮报告 §六）/ git 工作区实测。逐条实测核验（R240）。

| # | 来源 | 待办（压缩） | 声明状态 | 实测状态 | 判定 |
|---|---|---|---|---|---|
| 1 | `07-next-steps.md:16` | 阶段1 补跑三脚本 + 通配符死链检测 | 未完成 | 三脚本 3/3 存在于 `焚诀\audit\`，从未跑过；`scan_all.py` 已可用 | **仍有效** |
| 2a | `07-next-steps.md:17` | 遗留弃用平台名 ① | 未完成 | `openclaw-task-supervision:114` = 「OC/WB/CC/TC/HM/CX」 | **仍有效** |
| 2b | 同上 | 遗留弃用平台名 ② | 未完成 | `wps-knowledgebase:219` = 上游 CLI 客户端清单（注册表 `source=community`） | **待判定** |
| 2c | 同上 | 遗留弃用平台名 ③ | 未完成 | `data-layer-consistency-fix` 磁盘不存在 | **已失效** |
| 3 | `07-next-steps.md:19` | 注册表 2 条真脏数据 | 未完成 | `testing` source 空串已不复现；`oc-dispatch-exec-guard` `source='user'` 仍在（+ `install_state.mv=''`） | **部分仍有效** |
| 4 | `06-constraints.md:9-11` | 3 条 [BUG] 未修复 | 未修复 | D1 已修（受管根 `7cc7d4d` V3.42.0） | **已完成（文档滞后）** |
| 5 | `06-constraints.md:12` | `.rule_backup` 集中 + 32 未跟踪 | 未修复 | 实测 51 个；无一组 >5；`git status` 零条 | **部分仍有效** |
| 6 | `06-constraints.md:19-28` | 6 条 [DEBT] | 未修复 | 逐条实测全部闭环 | **已完成（文档滞后）** |
| 7 | `TODO.md:20` + 第3轮 §六 D7 | D7 受管根 39 条脏项 | 待确认 | `git status` 零条；part3 记 `172eb88` | **已完成** |
| 8 | `05-feature-status.part1.md:7` | 🚧 阶段5 两项未做 | 进行中 | `bf5e284` 死目标 7→0；`user_created_audit` A/B 类均 0 | **已完成（文档滞后）** |
| 9 | `05-feature-status.part1.md:12` | ⛔ skill 数口径未裁定 | 阻塞 | 已裁定 = 注册表 151（verify C1 全等） | **已完成（文档滞后）** |
| 10 | 工作区 `git status` | `05-exec/第9批_*.md` 未跟踪 | — | `??` 未提交 | **仍有效** |

**三态**：仍有效 5（#1/#2a/#3部分/#5部分/#10）｜待判定 1（#2b）｜已完成文档滞后 5（#4/#6/#7/#8/#9）｜已失效 1（#2c）。用户已确认此归类。

### Step 0 关键发现（机制性根因，已写入 footer 升级建议）

**`A-project-handoff` 判据盲区**：`status` 报 **8/8 filled**、`review` 报 **9/9（100%）** 的**同时**，`06-constraints.md` 主卷仍有 3 条已修缺陷写作「未修复」、6 条已闭环技术债仍列，`05-feature-status.part1.md` 仍列 2 项已完成的 🚧 与 1 项已裁定的 ⛔。
⇒ `status`/`review` 只判「文件是否被填充」，**不做「主卷声明 vs 分卷已完成项」交叉校验** —— 健康度评分对**失效声明完全免疫**（R220 假通过族新形态）。这是本项目**记忆复发性滞后**的机制性根因。

---

## 二、Step 1 现状感知（四源采集）

| 优先级 | 源 | 关键实测 | 可信度 |
|---|---|---|---|
| 1 | 对话上下文 | 用户目标 = 项目方向优化；口径 = 全执行 + 三方向并集；无额外约束口述 | ✅ |
| 2 | 项目记忆 | `status` **8/8 filled**；`review` **9/9（100%）**；07 P0 未完成 **3 项**（主卷 3 / 分卷 0）；各卷 ≤4KB | ✅ |
| 3 | 代码结构 | 交付物 00-scope~05-exec 齐备；最新交付写入 **09-22 02:38** 后停滞（本轮前 14:13 才有新文件）；`archive/` 实测 4 文件 | ✅ |
| 4 | git 历史 | 工作区 **14 次提交**，HEAD `632d183`；`D:\global_skills` `status` **零条**；焚诀 `verify` **16 PASS / 0 FAIL** | ✅ |

**四源冲突标注**：① 主卷记忆声明 vs 分卷已完成项 —— **大面积冲突**（即 Step 0 发现）；② README 写的 `review 7/9` / `verify 15 PASS` vs 当场实测 `9/9` / `16 PASS` —— **时间点不同**（R241 加注处理，非判据矛盾）。

---

## 三、Step 2 四维诊断

```
【诊断摘要】
A 功能方向: 六阶段 0-5 全部完成（本轮据实测销账阶段1/阶段5）；核心价值缺口已从"做不完"转为"记忆与实况脱节"
           —— status 8/8 + review 9/9 与主卷 9 条失效声明并存（06-constraints.md / 05-feature-status.part1.md）
B 技术债:   ① 焚诀 NEGATIVE_TAG_MAP 残留 7 ghost 键 + 7 ghost 引用，健康率 72.5%（negative_tag_audit.py 实测 FAIL）
           ② scan_all.py 两处判据缺陷：通配符引用整体漏检 + CC 检测静默失效（`\bCC\b` 配子串匹配）
           ③ fenjue_measure.py:168 引用不存在的 behavior_core.md → D1 少算 P0 文件
           ④ 注册表 oc-dispatch-exec-guard source='user'（非标准值）+ install_state.mv=''
           ⑤ .rule_backup 集中落点（51 个，无超阈值组）
C 架构质量: 目录与六阶段一一对齐；但 01-scan 三脚本产物此前从未落盘（本轮补齐并新建 README 索引）；
           content_snr 报 M3「lessons.part1 声明 P0 必须记住 8 条、实际 0 条」= 索引声明失真
D 文档交付: README 基线过期（已加校正注）；`05-exec/第9批_*.md` 未提交；
           startup 文档散落（本轮已集中到 01-scan/README.md 的复跑命令块）
冲突标注: 主卷声明 vs 分卷已完成项（大面积）；README 数值 vs 当场实测（时间点差异，已加注）
遗留台账对账: 仍有效 5 条 → #1 落 B/D、#2a 落 B、#3 落 B、#5 落 B、#10 落 D；已失效 #2c 不再排期
```

---

## 四、Step 3 建议清单（10 条）

| # | 优先级 | 维度 | 建议 | 验收判据（二元） | 执行属性 | 台账对账 | 本轮状态 |
|---|---|---|---|---|---|---|---|
| 1 | P0 | B/D | 记忆层滞后回写：`06-constraints` 3 条已修 [BUG] 转已修复 + 6 条已闭环 [DEBT] 销账；`05-feature-status.part1` 的 2 项 🚧 / 1 项 ⛔ 销账；README / TODO 基线更新 | `06-constraints.md` 中「待确认 D1」「待确认 D3」0 命中且 [DEBT] 行均带「已闭环」；`05-feature-status.part1` 的 ⛔ 段标注「已清零」 | 非破坏性 | 接续遗留 #4 #6 #8 #9 | ✅ 已执行 |
| 2 | P0 | B/D | 阶段1 补齐：三脚本实跑 + 通配符死链检测入编 + 两处判据缺陷修复 | `01-scan/` 含 3 份新输出 + `scan_report.md` 含「通配符死链候选」列；`scan_all.py --stage scan --apply` exit 0 | 非破坏性 | 接续遗留 #1 | ✅ 已执行 |
| 3 | P0 | B | 焚诀 `NEGATIVE_TAG_MAP` 清 7 ghost 键 + 7 ghost 引用 | `negative_tag_audit.py` 健康率 **100%** 且 GHOST 计数 = 0；`test_router_regression.py` ALL PASS | **破坏性** | 新增 | ⏳ 待确认 |
| 4 | P1 | B | 修 `openclaw-task-supervision:114` 六端口径 → 在役端 | 该行改为 `OC/WB/TR/CX/HM`；`scan_report` 中该 skill 的 `CC`/`QW` 归零（历史留痕行除外） | **破坏性** | 接续遗留 #2a | ⏳ 待确认 |
| 5 | P1 | B | 修 `fenjue_measure.py:168` 的 `behavior_core.md` → `core/behavior_core.md` | `attention_sim` 的「上游口径差异」告警 0 命中；评分重跑后 D1 分值变化可解释 | **破坏性** | 新增 | ⏳ 待确认 |
| 6 | P1 | B | `.rule_backup` 集中落点治理（备份与目标 skill 分离） | 备份落在目标 skill 同级或受管根外；回滚 1 步可定位 | **破坏性** | 接续遗留 #5 | ⏳ 待确认 |
| 7 | P1 | D | 提交工作区未跟踪文档 + 本轮改动 | `git status --porcelain` 为空；#20 基线 SHA 一致 | **破坏性(git)** | 接续遗留 #10 | ⏳ 待确认 |
| 8 | P2 | B | 注册表归一化：`oc-dispatch-exec-guard` `source='user'`→`user-created`、`install_state.mv=''`→`None` | 注册表 `source` 值 ∈ 合法枚举；`mv` 空串计数 = 0；`verify` 全绿 | **破坏性** | 接续遗留 #3 | ⏳ 待确认 |
| 9 | P2 | B | `content_snr` M3/M4 失真处置（`lessons.part1` 声明 8 条 P0 实际 0；degradation-test FAIL=4） | M3/M4 判定转 1.00，信噪比 4.7 → 6.0 | **破坏性** | 新增 | ⏳ 待确认 |
| 10 | P2 | B | `A-project-handoff` review 增「主卷声明 vs 分卷已完成项」交叉校验告警 | 合成项目（主卷声明已闭环但未加注）能报出 warning；已加注项目 0 告警 | **破坏性** | 新增 | ⏳ 待确认 |

---

## 五、本轮已执行（非破坏性 2 项）与验收判据实测

| 建议 | 验收判据 | 实测结果 |
|---|---|---|
| #1 记忆层滞后回写 | `06-constraints.md` 「待确认 D1/D3」0 命中；[DEBT] 行均带「已闭环」 | ✅ 实测：4 条 [BUG] 全部加注（3 已修复 / 1 部分解决）、8 条 [DEBT] 全部「已闭环」；新增 1 条 [BUG]（review 判据盲区）+ 2 条 [DEBT]（ghost 负标签 / fenjue_measure 路径） |
| #1（续） | `05-feature-status.part1.md` ⛔ 段标注「已清零」 | ✅ 实测：🚧 2 项 → 划销并注明证据；⛔ 段改「已清零」+ 裁定 151；📋 段三脚本状态改「已执行完毕」 |
| #1（续） | README / TODO 基线更新 | ✅ 实测：README 加校正注（review 7/9 → **9/9**、verify 15 → **16 PASS**、HEAD `632d183`）；TODO 阶段改「阶段0-5 全部完成」+ 阶段1 行 ✅ + D7 标已闭环 |
| #1（续） | `05-feature-status.md` 主卷 v1 数字加注 | ✅ 实测：阶段0 「自建 102」加校正注 → v2 **77**；阶段1 补「三脚本已跑」实测结论行 |
| #2 阶段1 补齐 | `01-scan/` 含 3 份新输出 + 报告含通配符列 | ✅ 实测：`attention_sim_20260922.txt`(3256B) / `content_snr_20260922.txt`(413B) / `negative_tag_audit_20260922.txt`(851B)；`scan_report.md` 新增「通配符死链候选」列 |
| #2（续） | `scan_all.py --stage scan --apply` exit 0 | ✅ 实测：`[stat] 超4KB 58 \| 无触发词 59 \| 有死链候选 3 \| 有通配符死链候选 2 \| 有弃用平台名 17`；`py_compile` OK |
| #2（续） | 判据缺陷修复有效性 | ✅ 实测（两层）：通配符 —— 修前 `wf_*.ps1` **0 命中**，修后纳入 `dead_wildcards`；CC —— 修前 12 skill 命中（CC 恒 0），修后 **17**（`openclaw-task-supervision` 等被正确标出） |

**新增文件**：`01-scan/README.md`、`01-scan/attention_sim_20260922.txt`、`01-scan/content_snr_20260922.txt`、`01-scan/negative_tag_audit_20260922.txt`、`05-exec/第10轮执行报告.md`
**归档**：`archive/scan-v1-2026-09-14/`（v1 的 `scan_report.md` + `scan_result.json`，R241 原样留痕）
**未改动**：`D:\global_skills`（0 字节）、焚诀（0 字节）、`00-scope/`~`04-plan/` 既有交付物

### 五-附：修 `scan_all.py` 判据缺陷的对照证据（R238 两层）

| 缺陷 | 反例（修前） | 生效路径（修后） |
|---|---|---|
| 通配符引用漏检 | 死链正则字符类 `[A-Za-z0-9_\\\-./一-鿿]+` 不含 `*` 且要求以扩展名收尾 ⇒ `` `scripts/wf_*.ps1` `` 永不进入检测 | 新增 `glob.glob(GS/<skill>/<pat>)` 分支 ⇒ `A-get-memory` 的 `wf_*.ps1` 被标出（且与已知「实测不存在」一致） |
| CC 检测静默失效 | `d in raw`（子串）配 `DEPRECATED_PLATFORMS` 中的正则项 `r"\bCC\b"` ⇒ 字面量永不命中，命中数恒为 12 | 改 `re.search(d, raw)` ⇒ 命中 **17**，`openclaw-task-supervision:114` 的 CC 被正确标出 |

> ⚠️ **误报标注（不许默认为真缺陷）**：`prompt-system-audit` 的 `scripts/*.ps1` 是**文档用法示例**（非真实引用）；CC 命中的 17 个中**多数为正当历史留痕**（如「CC 已弃用 2026-09-08」），扫描只负责暴露，改写须逐条判定。

---

## 六、待确认（破坏性，未执行）

| # | 项 | 为什么算破坏性 | 需要你决策什么 |
|---|---|---|---|
| B1 | 焚诀 `NEGATIVE_TAG_MAP` 清 7 ghost 键 + 7 ghost 引用（建议 #3） | 改焚诀源码（`eval/tag_layer.py`），须 `build_indexes --apply` + `verify` + 路由回归 | 是否授权清理（清理后 7 个已退役 skill 的负标签守卫一并消失，需确认无在役 skill 需要这些 key） |
| B2 | `openclaw-task-supervision:114` 六端口径改写（建议 #4） | 改 `D:\global_skills` 受管根 skill 源码，须走 `rule_editor.py` + 复跑三门禁 | 是否授权；`wps-knowledgebase:219`（注册表 `source=community`）一并裁定 = **改** 还是 **保留原文** |
| B3 | `fenjue_measure.py:168` 路径修复（建议 #5） | 改焚诀评分器源码，会影响 D1 分值口径 | 是否授权（修后需重跑评分卡，D1 分值会变） |
| B4 | `.rule_backup` 集中落点治理（建议 #6） | 移动受管根内备份文件（须先备份 + 迁 `_trash` 禁删） | 是否授权；落点选「按 skill 就近」还是「受管根外统一目录」 |
| B5 | 工作区 git 提交与推送（建议 #7） | 写仓库历史 + 远端 | 是否提交（建议分 2 主题：`docs(memory)` 记忆回写 + `feat(scan)` 阶段1 补齐） |
| B6 | 注册表归一化（建议 #8） | 改焚诀注册表派生件，须 `build_registry` + `build_indexes --apply` + `verify` | 是否授权改 2 个字段（`source` / `install_state.mv`） |
| B7 | `content_snr` M3/M4 处置（建议 #9） | 涉及 `lessons.part1` 索引声明与 degradation-test 数据层 | 是否授权；M3 是「索引声明 8 条实际 0 条」→ 改索引还是补内容 |
| B8 | `A-project-handoff` review 增交叉校验（建议 #10） | 改受管根 skill 判据源码，影响所有项目的 `review` 输出与分数 | 是否授权（新增 warning 会让部分项目的 review 分数下降 —— 属**暴露真问题**而非回归） |

---

## 七、收口门禁复跑（实测）

| 门禁 | 结果 |
|---|---|
| `handoff.py status` | **8/8 sections filled** |
| `handoff.py review` | **9/9（100%）** |
| `handoff.py split --check` | 07 / 05 全部卷 ≤4KB ✅ |
| `rule_editor.py gates` | `mirror=pass noise=pass evolution=pass` |
| 焚诀 `verify_truth_consistency.py` | **16 PASS / 0 FAIL / 0 SKIP** |
| `upgrade_footer_gate.py` | `[GATE:evolution-pass]` |

> 说明：本轮**未改动**受管根与焚诀，故三门禁与 `verify` 系「基线复跑」而非「修复后验证」；`scan_all.py` 属项目工作区脚本，不进三门禁范围。

---

## 八、台账编号对账（Step 6）

| 台账 # | 终态 | 说明 |
|---|---|---|
| #1 | ✅ 已完成（2026-09-22 第 10 轮） | 三脚本实跑 + 通配符检测入编 + 两处判据修复 |
| #2a | ⏳ 顺延（进待确认 B2） | `openclaw-task-supervision:114` 需受管根授权 |
| #2b | ⏳ 顺延（进待确认 B2） | 待你裁定「改 or 保留原文」 |
| #2c | ❌ 已失效 | `data-layer-consistency-fix` 磁盘不存在，不再排期 |
| #3 | ⏳ 顺延（进待确认 B6） | `testing` 已自然消除；`oc-dispatch-exec-guard` 2 字段待授权 |
| #4 | ✅ 已完成 | D1 修复已回写 `06-constraints.md` |
| #5 | ⏳ 部分顺延（进待确认 B4） | 「未跟踪」已解决；「集中落点」未改 |
| #6 | ✅ 已完成 | 8 条 [DEBT] 全部加「已闭环」 |
| #7 | ✅ 已完成 | D7 已闭环，`git status` 零条 |
| #8 | ✅ 已完成 | 阶段5 两项销账 |
| #9 | ✅ 已完成 | 口径裁定 151 + 阻塞清零 |
| #10 | ⏳ 顺延（进待确认 B5） | 待 git 提交 |

**顺延项**（供下一轮 Step 0 采集源 #4 直接续接）：B1–B8 共 8 条，编号与上文一致。

---

## 九、破坏性项执行结果（用户 2026-09-22 全部授权 B1–B8）

| # | 项 | 结果 | 证据（✅已实测） |
|---|---|---|---|
| B1 | 清焚诀 7 个 ghost 负标签 | ✅ 完成 | `audit/negative_constraints.json` 51 → 44 条（删 7 条 + 剔 17 处 ghost 引用，涉 8 条）；`negative_tag_audit.py` 健康率 **72.5% → 100%**、GHOST 条目/引用均 **0**、`RESULT: PASS`；焚诀 `f7c538c` |
| B2 | 六端口径 + wps 客户端清单 | ✅ 完成 | `openclaw-task-supervision:114` → 「4 active platforms (WB/TR/CX/HM)」、`:185` 5→4 端；`wps-knowledgebase:219` → 「本机在役端：WorkBuddy / TRAE / Codex / Hermes；Cursor、Claude Code、Kimi 本机未安装」；版本 1.3.0→1.3.1；受管根两次 commit（均 `--fix-mirror` 自动补齐镜像） |
| B3 | 修失效路径 | ✅ 完成 | `fenjue_measure.py` L347/L387 改 `core/behavior_core.md`（D12 由 20 → **18**/25，属**真实暴露**而非回归）；同步修正 `attention_sim.py` 的误导性 note（原称「D1 少算」不实 —— D1 的 P0 集合本就用修正路径）；`上游口径差异` 告警复跑 **0 命中**；焚诀 `f7c538c` |
| B4 | `.rule_backup` 落点治理 | ✅ 完成 | `rule_editor.py` 新增 `RULE_BACKUP_EXTERNAL = D:\global_memory_archive\_trash\rule_backup`（外置不可用时回落旧落点）+ `undo` 兼容旧落点；**56 份 `.bak` 全部迁出受管根**（`src after = 0`）；功能实测：新 replace 的备份落在新落点、`undo` 从新落点成功回滚；受管根 commit `7375e40`（52 文件，显式路径白名单） |
| B5 | 工作区 git 提交与推送 | ✅ 完成 | 见第十节 |
| B6 | 注册表 2 字段归一化 | ✅ 完成 | `source` `user`→`user-created`、`install_state.mv` 空串→`null`（索引 + cpm 双源）；全库复扫：非法 source **0**、空串 install_state **0**；磁盘真相源同步补 frontmatter `user_created: true` / `source: user-created`（治本）；备份落 `global_memory_archive\_trash\registry-normalize-20260922\` |
| B7 | content_snr M3/M4 失真 | ✅ 完成 | **信噪比 4.7 → 6.0 / 6**（M3 0.66→1.00、M4 0.50→1.00）。根因链：`ican-frontend-design-system` 注册表域为**旧数字前缀域 `09-dev-tools`**（早已软删除）→ skill_content 多出第 14 个域 → degradation-test 1c/3b FAIL；`domain_map.ps1` 仍是 2026-08-08 的 **160 条**旧快照 → 3a/3d FAIL。修法：域重分类 → `design`（`classify_domain` 实测）+ 补齐 2 条残缺条目 + `build_indexes --apply`（`09-dev-tools.json` 消失，**13 域 151 条**）+ `domain_map.ps1` 按 13 域重生成（`Get-DomainMapStats().total == 151`）+ `degradation-test.ps1`/`auto-fix.ps1` 排除 `index_manifest.json` + `lessons.part1.md` P0 计数 8→4（R9 重建版实况）+ `content_snr.py` 计数正则改 `^##\s+\S`。**`degradation-test` 13/17 → 17/17 PASS**。焚诀 `f7c538c` + GM 提交 |
| B8 | review 增交叉校验 | ⚠️ **未执行（并发冲突，按 R269 未擅动）** | `handoff_lib/projcmds.py`（+79 行）与 `savepoint.py`（+16 行）**已被并行会话改过且未提交**（内容为 gitignore 派生目录树排除 / 子目录技术栈兜底检测，与本项不重叠但**同文件**）；同文件编辑会被 `git add` 一并带走（R236 补注已实证）⇒ 按纪律不擅动，改列待确认 |
| B9 | `_bak` 归属 | ⚠️ **待你裁定** | 见下方证据链 |

### 九-附：B9 `D:\global_memory\_bak` 归属证据链（✅已实测）

| 证据 | 实测值 |
|---|---|
| 目录/内容 | `D:\global_memory\_bak\07nextsteps_20260922_142621\`，18 文件 = GM 的 `07-next-steps.md` + `part1..part16.md`（**专有备份**，非通用备份器产物） |
| 时间 | CreationTime / LastWriteTime = **2026-09-22 14:26:21**（本轮会话进行中） |
| 同刻他仓改动 | `git -C D:\global_memory status` 显示 `memory/07-next-steps.md` + `part{2,5,8,16}` **同时处于已修改未提交** |
| 生产脚本 | 全仓检索**未找到**任何脚本会写 `_bak/<name>_<ts>`（`_bak` 仅作为「系统目录 / 排除项」出现在各处）⇒ 判为**人工 / agent 手工快照** |
| 结论 | **并行会话在改 GM 的 07-next-steps 之前做的手工快照**，属**在途产物**；按 R269（先查 mtime 与提交序、不擅动他人在途文件）本轮**未迁移** |
| 影响 | 本项目 `handoff.py savepoint` 被拒（`[GATE:noise-fail]`）；`rule_editor.py gates` 亦报 `noise=fail` |

### 九-附2：本轮新发现（登记待办，不阻塞）

1. **`build_registry.py` 无「已有条目字段刷新」路径**（L263-295 只处理 missing）⇒ 存量条目脏值只能一次性归一化；建议增 `--refresh` 或按 frontmatter 全量回填字段。
2. **`build_registry.py` 的 `compatible_platforms` 缺省值仍含已退役 `oc`/`tc`**（L88）—— 与在役端 `WB/TR/CX/HM` 不一致。
3. **旧数字前缀域残留生成器**：`skill/tools/_optimize_registry.js` / `.ps1` 的兜底 `return '09-dev-tools'`；`skill/watcher/detect-changes.ps1:213` 与 `skill/sync/sync-skills-bridge.ps1:198` 同样硬编码 `09-dev-tools` ⇒ 下次跑这些工具会**再生**已退役域。
4. **受管根存在并行会话在途改动**（`handoff_lib/projcmds.py`、`savepoint.py`）⇒ 任何触碰 `handoff_lib` 的改动须先协调（B8 因此挂起）。
5. **`skill_content/*.json.prev` 长期未跟踪**（本轮被 `git add -- skill_content` 顺带纳入 GM 提交）⇒ 建议在 GM `.gitignore` 显式登记，或让生成器把它写出生效目录。

---

## 十、B5 工作区提交（本轮）

分 2 主题提交（`docs(memory)` 记忆层回写 / `feat(scan)` 阶段1 补齐与本报告），逐路径白名单暂存，**禁 `git add -A`**；提交后核对 #20 基线（`HEAD == origin/main`）。

---

## 十一、第 11 批：B8 改道 + B9 观察 + 5 条新发现处置（用户 2026-09-22 按钮选定）

### 11.1 B8 改道（用户裁定：不动上游，改写本项目铁律）—— ✅ 完成

在 `memory/AGENTS.md` 新增 2 条铁律（零冲突方案）：
1. **销账必须逐条对照分卷 `partN` 的已完成条目** —— 明写「`status` 8/8 与 `review` 9/9 均只判『文件是否被填充』⇒ **分数 100% ≠ 记忆与实况一致**」，禁止仅凭分数或主卷单方声明下结论。
2. **环境脏项从外部引入时只登记不擅动**（R269）—— 禁止为让 `savepoint` 通过而擅自迁移他人在途文件。

### 11.2 B9（用户裁定：暂不动，登记待观察）—— ✅ 已登记，且**外部自解**

- 登记入 `07-next-steps` P0（带完整归属证据链）。
- ⚠️ **实测反转**：本轮收口复跑 `gates` 时 `noise` 已自动转 **pass** —— `Test-Path D:\global_memory\_bak` = **False**（该目录已不存在），即**并行会话自行清理**。⇒ 本项目 `handoff.py savepoint` 复跑 **exit 0「本次对话已安全落盘」**。**B9 风险已消除，无需我方处置。**

### 11.3 5 条新发现处置 —— ✅ 4 项完成 / 1 项仅登记

| # | 新发现 | 处置 | 证据（✅已实测） |
|---|---|---|---|
| ① | `build_registry.py` 无「已有条目字段刷新」路径 | ✅ **已补** | 新增 `refresh_fields()` + `--refresh` CLI（**安全边界**：只刷新 frontmatter **显式声明**的 `version`/`source`/`user_created`，`compatible_platforms` 仅在缺失时补；**不动** `domain`/`install_state`/`display_name`/`name`）。实跑 `--refresh --dry-run` → 变更 **38 处**（37 个 `version` 陈旧值 + 1 个 `source`）；落盘后注册表 151 条无缺字段 |
| ② | `compatible_platforms` 缺省含已退役 `oc`/`tc` | ✅ **已修** | `build_registry.py:88` → `['wb','tr','cx','hm']`；`sync-skills-bridge.ps1` / `_fix_registry.py` 同步改在役四端 |
| ③ | 旧数字前缀域硬编码生成器 | ✅ **已清（实测不止 3 处，共 6 处）** | `watcher/detect-changes.ps1:213-221`、`sync/sync-skills-bridge.ps1:193-204`、`tools/_optimize_registry.ps1:66-81`、`tools/_optimize_registry.js:20-44`、`tools/_add_oc_builtins.js:22-43`、`tools/_fix_registry.py:79` → 全部改为 `general_utils` + 注释指向唯一权威 `eval/build_registry.py → domain_classifier`。语法自检：3 个 `.ps1` `Parser::ParseFile` **OK** / 2 个 `.js` `node --check` **OK** / `.py` `py_compile` **OK**。复扫：仅剩本轮新增的说明性注释 |
| ④ | 受管根并行会话在途改动 | ⚠️ **仅登记（无法处理）** | `handoff_lib/projcmds.py` / `savepoint.py` 仍为他人未提交态；已记入 `07-next-steps` 供协调 |
| ⑤ | `skill_content/*.json.prev` 未跟踪 | ✅ **已修** | GM `.gitignore` 新增 `skill_content/*.json.prev`（注释说明它是 `load_runtime_domains()` 的过渡期兜底源，**须留磁盘但不必入库**）+ `git rm --cached` 回归未跟踪态——**13 份 `.prev` 仍在磁盘**（`Get-ChildItem` 实测 13），`check-ignore` 命中 `.gitignore:22` |

> ⚠️ **过程中的自伤与修复（R271 族，留痕）**：`git rm --cached -r skill_content` 一次**取消了整个目录的暂存**（31 文件），而非仅 `.prev`。修法 = `git add -- skill_content`（此时 `.prev` 已被忽略）→ 复核恢复为「18 个正式产物 `M` + 13 个 `.prev` `D`」。教训：**`git rm --cached -r <dir>` 是目录级操作，不能用来精确摘除目录内某类文件**。

### 11.4 收口门禁（第 11 批后全量复跑）

| 门禁 | 结果 | 对比本轮起点 |
|---|---|---|
| `rule_editor.py gates` | **`mirror=pass noise=pass evolution=pass`**（三绿） | 起点 `noise=fail`（外部 `_bak`） |
| 焚诀 `verify_truth_consistency.py` | **16 PASS / 0 FAIL / 0 SKIP** | 同 |
| 焚诀 `degradation-test.ps1` | **17/17 PASS** | 起点 13/17 |
| 焚诀 `scorecard.py` | **overall 181.9 / 200 · verdict PASS · band 优秀** | 起点 **168.4 · FAIL · 良好** ⇒ **+13.5 分并转 PASS** |
| 本项目 `handoff.py status / review` | **8/8 filled / 9/9（100%）** | 同 |
| 本项目 `handoff.py savepoint` | **exit 0「本次对话已安全落盘」** | 起点被拒（外部 `_bak`） |
| 本项目 `split --check` | 07 / 05 全部卷 ≤4KB ✅ | 同 |

**提交落点（第 11 批）**：焚诀 `42a076a`｜GM（`.prev` 边界 + 生成物重出）｜工作区（见第十二节）

---

## 十二、第 11 批工作区提交

`docs(memory)`（AGENTS.md 铁律 2 条 + 07-next-steps B8/B9 状态）与 `docs(report)`（本报告 §十一~§十二）分主题提交，逐路径白名单，禁 `git add -A`。
