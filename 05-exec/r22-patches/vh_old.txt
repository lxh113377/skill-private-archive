- V3.52.2 (2026-09-24): **flow 重写章节必须保留段首图例注释（自建skill优化 r19 实跑暴露）**——`handoff.py flow <path> --add` 首次写 `memory/09-workflow-state.md` 时，`_write_task_table` → `_replace_section` 以**替换式**重写「## 任务表」整段正文，把模板自带的「字段约定（机器解析，**勿改列名与列序**）」HTML 注释图例整块删除；`_append_log` 另因 `strip_html_comments` 抹掉「## 推进记录」段首注释（实测本项目 09 一次 `--add` + `--sync` 掉 18 行注释，`git diff` 留证）。该图例是防误改契约的唯一说明，丢失后跨会话读者无从知道列序约束。修法：新增 `LEADING_COMMENT_RE` + `_leading_comments()`（只取段首**连续**注释块，正文中间的注释语义仍由调用方决定），`_replace_section` 与 `_append_log` 两处重写均回填；无图例的存量文件不凭空造注释。验证：本项目侧隔离桩 `05-exec/r19_flow_legend_stub.py` **15/15**（含**修前/修后对照**——修前 g1 图例两项 FAIL、修后全绿；另有表头不重复 / 解析不参与注释内示例行 / 幂等重复重写只留一份 / 缺章节须返回 None 不得静默造段 五组护栏）+ 层 b 真机端到端（本项目 09 `git checkout HEAD --` 还原图例后重跑 `--add/--block/--sync`，两处注释均在且 `flow --check` PASS、台账「无变化 3」幂等）+ `py_compile`。行为零变更面：任务表渲染 / 解析 / 依赖校验 / 批次落卷 / 台账回写逻辑一字未改。同步 frontmatter 3.52.1 → 3.52.2。