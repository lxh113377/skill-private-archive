- V3.54.0 (2026-09-24): **R273 撤回（用户指令：去掉闸门，不得拦截任务执行）**——移除 V3.53.0 加入的 `_repeat_guard_row()` 与 `cmd_review` 内的「重复轮次闸门」台账行，`review` 恢复为不含该行的输出；`memory/sessions/repeat-guard.jsonl` 读侧逻辑一并撤除（文件本身作为历史台账保留不删）。根因归责在指令侧不在代码侧：定时任务 instruction 曾被子会话加入幂等闸门自拦条款，与用户明确裁定冲突，已同时把 7 条任务 instruction 全部还原原文（schedule 未动）。验证：py_compile PASS；`grep -c '_repeat_guard_row' savepoint.py` = 0；review 实跑输出无该行；三门禁复跑见当日日志。
- V3.53.0 (2026-09-24): **R273** **review 接入「重复轮次闸门」台账行（r22 落地，A-project-handoff 不反向依赖单仓路径）**——`cmd_review` 新增只读转述行：读 `memory/sessions/repeat-guard.jsonl` 末条可解析行，输出`重复轮次闸门: <verdict> — 当日同族轮次/同族新增行/同日 session 数`，DUPLICATE/INCREMENT 或 forked 归 warnings、FRESH/THIN 归 passed，与既有 AC 收敛行、记忆评测行同处一个报告函数（同族防噪音口径：无台账→静默 None）。设计取舍：原判据写「savepoint 增加预检行」，层b 接线实测证明落点应为 review（函数正确、接线错误的实物复现，属既有「验证须含接线层」族的又一次实测命中）；共享 skill 只转述各仓自己落的台账，不硬编码任何项目仓的脚本路径。验证：`05-exec/r22_repeat_guard_stub.py` 层a 8 例（无台账 None / DUPLICATE→warn 含轮次与分叉文案 / 混合行 last-wins / FRESH→ok / 缺 verdict→None）+ 层b 本仓真跑 review 命中 1 行，10/10。