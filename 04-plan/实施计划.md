# 自建 skill 优化 — 逐项实施计划

> 生成：2026-09-14（阶段4）| 输入：`03-audit/自建skill优化审计报告.md`、`02-review/` 七份审计卡
> **本文件是计划，不是执行记录。确认后才动手改 `D:\global_skills`。**

---

## 0. 全局执行约束（每批每适用）

| 约束 | 内容 |
|---|---|
| 唯一修改入口 | `python D:\global_skills\A-memory-start\references\rule_editor.py`（自动备份 + git 提交 + 镜像门禁）**禁止 `write_file` 整体重写规则文件** |
| 同文件多次编辑 | `--no-commit` 多次 → `rule_editor.py commit --file A --file B --desc "..."` 一次收口 |
| 提交后三门禁 | ① `check-skill-mirror.ps1` → `[GATE:mirror-pass]`（missing=0/mismatch=0）<br>② `handoff.py noise` → `[GATE:noise-pass]` 零散射<br>③ `upgrade_footer_gate.py` → `[GATE:evolution-pass]` |
| 镜像不一致 | 提交被拦 → `rule_editor.py commit ... --fix-mirror` 自动同步后重试 |
| 派生件 | 涉及注册表/数据层改动 → `python eval/build_registry.py --apply` 重建 → `git add` 派生件后再 commit（防 pre-commit 假 FAIL） |
| 回滚 | `rule_editor.py undo --file <相对路径>`；批量失败可 `git -C D:\global_skills log --oneline -5` 定位后 `git revert <sha>` |
| 停手条件 | 任一验证命令非 0 退出 → 立即停手，不进入下一项 |

**通用验证命令（每批收尾必跑）**
```powershell
# ① 镜像
powershell -NoProfile -ExecutionPolicy Bypass -File C:\Users\37533\Desktop\workspace\焚诀\skill\sync\check-skill-mirror.ps1
# ② 噪声散射
& "C:\Program Files\Python312\python.exe" "C:\Users\37533\.workbuddy\skills\A-project-handoff\scripts\handoff.py" noise
# ③ 协议自检（一键跑三门禁并汇总标记）
& "C:\Program Files\Python312\python.exe" D:\global_skills\A-memory-start\references\rule_editor.py gates
# ④ 扫描复测（确认指标下降）
& "C:\Program Files\Python312\python.exe" "c:\Users\37533\Desktop\workspace\自建skill优化\01-scan\scan_all.py" --stage scan --apply
```

---

## 批次 1：路径与平台口径（最高杠杆，覆盖 20+ 处 P0）

| # | 目标文件 | 改动内容 | 验证命令 | 回滚 |
|---|---|---|---|---|
| 1.1 | 全库 `.md`（约 13 处） | `C:\Users\37533\Desktop\焚诀\` → `C:\Users\37533\Desktop\workspace\焚诀\`（含 `C:/Users/.../项目/焚诀` 变体） | `Get-ChildItem D:\global_skills -Recurse -Include *.md \| Select-String -Pattern 'Desktop.{0,12}焚诀' -List \| Where-Object { $_ -notmatch 'workspace' }` → 应为空 | `undo --file` 逐个；或 `git -C D:\global_skills checkout -- .` |
| 1.2 | `data-layer-consistency-fix`(:88/155/279/404) | 同上（本项与 1.1 同源，1.1 完成后复测应自动消失） | `Test-Path 'C:\Users\37533\Desktop\workspace\焚诀\skill\registry\unified-skills-index.json'` → True | undo |
| 1.3 | `fenjue-advisor-scoring`、`fenjue-cc-audit-cycle` | **不修**，走退役（见批5）。退役前先确认无反向引用 | `Select-String -Path D:\global_skills\*\*.md -Pattern 'fenjue-advisor-scoring\|fenjue-cc-audit-cycle' -List` → 列出引用方 | — |
| 1.4 | `cross-platform-agent-sync`（CC 12+ 处、QW 6 处） | CC→TR/OC，QW→删除；删 :461 的 `A-memory-align` 死引用 | `Select-String -Path 'D:\global_skills\cross-platform-agent-sync\SKILL.md' -Pattern '\bCC\b\|QW\|A-memory-align'` → 无命中 | undo |
| 1.5 | `windows-cli-utf8-wrapper`(:11/208/233/238-247) | CC 主示例改 OC/WB/TR/CX | `Select-String -Path '...SKILL.md' -Pattern 'Claude Code'` → 无命中 | undo |
| 1.6 | `discover-agent-cli`(:23/60/87/172)、`workflow-preflight-check`(:149/222/239) | CLI 映射表删 CC 行 | 同上模式 | undo |
| 1.7 | `oc-dispatch-exec-guard`(:43-45)、`vp-perspective-audit`(:53-59) | CC 段删除/替换 | 同上 | undo |

**批次验收**：`scan_all.py --stage scan` 的「弃用平台名命中」从 25 降到 ≤6（fenjue 两件退役后归零）。

---

## 批次 2：单点死链修复（18 项，照抄即失败类）

| # | 目标文件 | 改动 | 验证命令 | 回滚 |
|---|---|---|---|---|
| 2.1 | `chaoshi-image-optimization`(:13/50/85) | `ffmpeg-8.1.1-full_build` → 动态探测（`Get-Command ffmpeg` 或 WinGet 路径枚举） | `& ffmpeg -version` 且脚本 dry-run 通过 | undo |
| 2.2 | `fenjue-routing-health-check`(:227/245-247) | `VERSION_LOCK.part2/3/4` → 动态枚举 `VERSION_LOCK.part*.md` | `Get-ChildItem D:\global_memory\meta\VERSION_LOCK.part*.md` → 与脚本枚举一致，且 CHECK-3 返回条数 >0（**防假阴性**） | undo |
| 2.3 | `c-cleanup`(:307-308) | 删除两个不存在脚本的引用，或补写脚本 | `Test-Path D:\global_skills\c-cleanup\scripts\scan_nvidia_cache.ps1` → True（二选一后一致） | undo |
| 2.4 | `wps-knowledgebase`(:75/424/430/456) | 删 4 处不存在引用或补建 `references/workflow.md`+`scripts/run.js` | `Get-ChildItem D:\global_skills\wps-knowledgebase -Recurse -File` 与引用一致 | undo |
| 2.5 | `first-principles-decomposer`(:68-71) | 删 4 个不存在联动 skill，或标注"待建" | `Select-String 'inversion-strategist\|second-order-consequences\|pre-mortem-analyst\|six-thinking-hats'` → 无断言式引用 | undo |
| 2.6 | `hook-analyzer-skill`(:36)、`report-generator-skill`(:121-124) | 上游 `video-breakdown-skill` 不存在 → 补建或两件一并退役 | `Test-Path D:\global_skills\video-breakdown-skill` → 与文档口径一致 | undo |
| 2.7 | `A-get-memory`(:173/175/213/214/441) | 删 `scripts/trace_view.py`、`scripts/wf_*.ps1` 引用，改指 `D:\global_memory\prompts\workflow_seven_step.md` | `Test-Path D:\global_skills\A-get-memory\scripts\trace_view.py` → False 且文档无该串 | undo |
| 2.8 | `openclaw-fenjue-weekly`(:73) | 收尾门禁指向改为存在的脚本（或并入 task-supervision 后整件退役） | `Test-Path D:\global_skills\sync-global-memory` → False → 引用已改 | undo |
| 2.9 | `openclaw-task-supervision`(:569-591) | 删 Qoder 兜底通道，改 OC/TR 通道 | `Select-String 'qoder_start_task'` → 无命中 | undo |
| 2.10 | `cross-platform-agent-sync`(:461) | 删 `A-memory-align` | 见 1.4 | undo |
| 2.11 | `deep-research-pro`、`multi-search-engine`、`skill-install` | **退役**（市场件 + 核心依赖不存在） | 退役后 `Select-String -Path D:\global_skills\*\*.md -Pattern '<name>' -List` 无引用 | 移入 `_trash/` 保留可恢复 |
| 2.12 | `story-setup`(:135) | 删 `generate-codex-agents.py`/`sync-opencode.py` 引用或补写 | `Get-ChildItem D:\global_skills\story-setup\scripts` 与引用一致 | undo |
| 2.13 | `wechat-voice-transcription`(:62) | 修正事实：`local-asr` **存在**，非已删除 | `Test-Path D:\global_skills\local-asr` → True，文档表述一致 | undo |
| 2.14 | `shell-encoding-pitfalls`(:2) | `name:` 改为与目录名一致 | `(Get-Content ...)[1] -match '^name: shell-encoding-pitfalls$'` → True | undo |
| 2.15 | `prompt-system-audit`(:220-238) | V8/V9 的 `common_prompts.md` 定位真实真相源或标注废弃 | `Test-Path <新真相源>` → True | undo |

**批次验收**：`scan_all.py --stage scan` 的「死链候选」从 4 降到 0；人工按上表逐条 `Test-Path` 复核。

---

## 批次 3：治理规则自违（恢复规则可信度，**建议优先于批2 剩余项**）

| # | 目标 | 改动 | 验证 | 回滚 |
|---|---|---|---|---|
| 3.1 | `A-project-handoff`（84,818B） | 新建 `references/`，18 条致命纪律 + 14 子命令拆 partN，主文件留 ≤4KB 指针 | `(Get-Item D:\global_skills\A-project-handoff\SKILL.md).Length -le 4096` | undo + 删新 references |
| 3.2 | `bigfile-split`（15,360B） | **先自拆到 ≤4KB**，再谈治理他人 | `(Get-Item ...bigfile-split\SKILL.md).Length -le 4096` | undo |
| 3.3 | `fenjue-advisor-scoring`(:265) | 自身 25.17KB 违反自定 0 分规则 → 退役或先拆卷 | 退役后引用清零 / 或体积 ≤4096 | undo |
| 3.4 | `cross-platform-agent-sync`(:264) | 自身 19.59KB 违反自定分卷上限 → 拆卷 | 体积 ≤4096 | undo |
| 3.5 | `A-project-handoff`(:17) | `version: 3.22.0` → `3.35.0`；删 :678 重复行；重排 :698-706 | `Select-String '^version:'` 与版本历史末行一致 | undo |
| 3.6 | `A-memory-start`(:17) | `version: 10.27.0` → `10.28.0`；删 :190/:191 重复编号 | 三处（frontmatter/正文/历史）一致 | undo |

---

## 批次 4：平台口径与元数据一致性

| # | 目标 | 改动 | 验证 | 回滚 |
|---|---|---|---|---|
| 4.1 | `A-memory-start`(:134/153/157) | **「HM 已卸载 2026-08-08」→ 改为在役端**（用户 2026-09-14 确认 HM 已回归） | `Select-String 'HM 已卸载'` → 无命中；`:60` 门禁模板枚举改为含 HM | undo |
| 4.2 | `A-memory-start`(:60) | 门禁模板 G2 `<OC/WB/CC/TC/HM/CX>` → `<OC/WB/TR/CX/HM>` | 与 :134 口径自洽（同文件不再自相矛盾） | undo |
| 4.3 | `A-get-memory`(:255/395/398/422) | Hermes 分支**保留**（HM 回归）；:422「WB/CC/TRAE」→「WB/TR/CX」 | 与权威口径一致 | undo |
| 4.4 | 全库版本一致性 | 修 `ican-frontend-design-system`(1.0.0 vs V4)、`fenjue-memory-audit`(1.7.0 vs V1.5.4)、`first-principles-decomposer`(1.0.2/1.0.0/1.0) | 逐 skill：frontmatter version == 版本历史末行 | undo |
| 4.5 | 缺 `version` 字段者 | `ican-deploy`、`cross-platform-skill-sync`、`skills-security-check`、`shell-encoding-pitfalls`、`utf8-encoding-fix`、`wechat-voice-transcription`、`report-generator-skill`、`hook-analyzer-skill`、`agent-browser`、`electron`、`shadcn`、`skill-install`、`taskflow`、`taskflow-inbox-triage` 补 version | `scan_all.py --stage scan` 的「有 version」覆盖率提升 | undo |
| 4.6 | 元数据失真 | 6/10 local 的 `meta.json` description 退化为 `"|"` → 补真实描述 | `ConvertFrom-Json` 后 description 长度 >10 | undo |

---

## 批次 5：合并与退役（103 → 约 85）

| # | 动作 | 涉及 | 前置 | 验证 |
|---|---|---|---|---|
| 5.1 | **退役** | `fenjue-advisor-scoring`、`fenjue-cc-audit-cycle`（用户确认不再使用）、`cloudbase-webapp-deploy-debug`、`deep-research-pro`、`multi-search-engine`、`skill-install` | 引用清零（1.3/2.11） | 移入 `_trash/`，不删除；`sync_health.json` 刷新 |
| 5.2 | **合并** | 审计族 15 → 8 顶层 + 3 references（命中率链 5→2、注册表外科 3→1、指令+数据层 2→1） | 批2 死链修完 | `Get-ChildItem D:\global_skills` 计数下降；`unified_router` 回归 top1 仍正确 |
| 5.3 | **合并** | `story-long-scan`+`story-short-scan` → `story-scan --length=long\|short` | 补齐 short-scan 缺失的采集质量门 | 两平台清单均保留；`选题决策.md` 下游契约统一 |
| 5.4 | **不合并（记录决策）** | `story-long-write`↔`short-write`(0.911)、`long-analyze`↔`short-analyze`(0.853) | — | 在两件顶部加"为何不合并"注释，防后续重复提议 |
| 5.5 | **抽共享运行时** | `_shared/local-runtime`（platform.exe 376KB×9、server-dog.py 19.45KB×8 等） | local 族无 P0 | 10 个 local 引用生效；`_shared` 加进 `.gitignore` 白名单防 noise 误报 |
| 5.6 | **合并 2 组** | `img2img`+`txt2img`→`local-image-gen`；`vram`→并入 `computer-use` | — | 触发词并集覆盖；无双命中 |
| 5.7 | **合并** | `shell-encoding-pitfalls`+`utf8-encoding-fix` → 1 个 | 4.5 补 version | BOM 修法只留一份 |
| 5.8 | **留一弃一** | 留 `c-cleanup` / 弃 `sq-cleanmgr-c`（迁 cleanup.ps1 + report_template.html；补 2 条死链脚本） | 2.3 | c-cleanup 获得脚本级门禁；sq 触发词并入 |
| 5.9 | **降级** | `taskflow-inbox-triage` → `taskflow/references/inbox-triage.md` | — | 父件 L149 指针改指 references |
| 5.10 | **合并** | 4 个 Notion 件 → 1 个占位件（Notion MCP 未配置） | 用户确认是否接入 MCP | 占位件注明"MCP 接入后启用" |
| 5.11 | **新增** | `story` 路由 skill（~2KB，声明完整流水线） | — | story-cover 补流程衔接段挂回 |

---

## 执行顺序建议

**批3 → 批1 → 批2 → 批4 → 批5**

理由：批3 修的是"治理规则自违"，先做完，后面推 4KB 分卷（批5 的合并大多伴随拆卷）才有说服力；批1 杠杆最大但风险最低（纯路径/名称替换）；批5 合并涉及文件移动，放最后。

---

## 风险与防回归

1. **批量替换风险**：1.1 的全库路径替换必须先 `--dry-run` 预览（`rule_editor.py replace --patch p.json --dry-run`），确认命中数与预期一致再 `--apply`
2. **退役不可逆**：全部移入 `_trash/` 而非删除（呼应 A-project-handoff 致命纪律 #17）
3. **合并后路由回归**：每批合并后跑 `unified_router.py --json` 对典型查询回归，确认 top1 未漂移
4. **并发会话**：新增规则号前先查占用 `git -C D:\global_skills log --all --oneline --grep "Rxxx"`
5. **派生件假 FAIL**：改完先 `git add` 派生件再 commit（R198.6）
