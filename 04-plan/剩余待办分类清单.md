# 剩余待办分类清单（2026-09-14）

> 用途：把实施计划剩余项按「能否自动完成」分派工单
> 依据：`TODO.md` 阶段4 进度表 + `03-audit/` 分级 + `02-review/` 各批精读（均本会话实测）
> 已完成基线：批3（版本号/bigfile-split/cross-platform-agent-sync 拆卷）+ 批1（路径 34 处）+ 6 项死链 + 9 条升级建议 + 预检修复 R234

---

## A 类：机械可自动（不依赖决策，我可连续几轮做完）

| # | 项 | 具体动作 | 证据 | 预估 |
|---|---|---|---|---|
| A1 | `A-get-memory` 5 处死链 | `scripts/trace_view.py`(:173/175/214/440)、`scripts/wf_*.ps1`(:213/441) → 改指 `D:\global_memory\prompts\workflow_seven_step.md` | 精读实测：`scripts/` 下仅 `sync_skill_router.py` | 1 轮 |
| A2 | `openclaw-task-supervision` Qoder 通道 | 删 :569-591 的 `qoder_start_task`/`qoder_get_task_detail` 兜底链路 | Qoder 2026-08-01 卸载 | 1 轮 |
| A3 | 14 个 skill 补 `version:` 字段 | `ican-deploy`/`cross-platform-skill-sync`/`skills-security-check`/`shell-encoding-pitfalls`/`utf8-encoding-fix`/`wechat-voice-transcription`/`report-generator-skill`/`hook-analyzer-skill`/`agent-browser`/`electron`/`shadcn`/`skill-install`/`taskflow`/`taskflow-inbox-triage`（起始 1.0.0） | 阶段1 扫描 frontmatter 维度 | 1-2 轮 |
| A4 | 6 个 local `meta.json` description 退化 | `asr`/`computer-use`/`img2img`/`translator`/`tts`/`txt2img` 的 `description` 为字面量 `"|"` → 补真实描述 | local族精读实测（`meta.json:4`） | 1 轮 |
| A5 | `openclaw-dual-gate-quality-audit` description 收窄 | 无触发词、无边界声明（`vp-perspective-audit` 同类问题，已示范改法） | 审计族精读 | 1 轮 |

**A 类合计约 5 项 / 5-6 轮**

---

## B 类：需你决策（我不擅自动手）

### B1 高风险改动（技术风险，需你知情）

| # | 项 | 风险点 | 需你回答 |
|---|---|---|---|
| B1-1 | **`A-project-handoff` 84KB 拆卷** | 含 `COLD_START_DECL` 锚点块，**必须留在主文件**否则 `coldstart --check` 失败；需专门保护逻辑 | 是否授权按"保护锚点+按 H2 切卷"方案执行？可先出 dry-run 方案给你看 |
| B1-2 | `chaoshi-image-optimization` ffmpeg 路径 | 不是删引用，要改成**动态探测**（脚本行为变更） | 改为 `Get-Command ffmpeg` 动态探测，可否？ |
| B1-3 | `fenjue-routing-health-check` VERSION_LOCK.part2-4 | 要改成**动态枚举 partN**；原逻辑会产出"假阴性"（不报错但 0 条） | 是否授权改成 `Get-ChildItem VERSION_LOCK.part*.md` 枚举？ |

### B2 退役决策（涉及移除 skill）

| # | 候选 | 审计依据 | 需你回答 |
|---|---|---|---|
| B2-1 | `cloudbase-webapp-deploy-debug` | 全篇教已停运的 tcb；已被 `chaoshi-web-deploy §7` 完整取代 | 退役（移 `_trash/`）？ |
| B2-2 | `deep-research-pro` | 核心脚本 `ddg-search` 全库不存在；市场件 | 退役？ |
| B2-3 | `multi-search-engine` | ClawHub 市场件（download_count 53055）；版本三源分裂 | 退役？ |
| B2-4 | `skill-install` | 安装目标 `~/.codebuddy/skills/` 不存在且非四端面 | 退役 or 改指 `D:\global_skills`？ |
| B2-5 | `fenjue-advisor-scoring`、`fenjue-cc-audit-cycle` | 你已确认日常不再使用；核心依赖已弃用的 CC | 归档（`_trash/`）还是保留只改口径？ |

### B3 合并决策（涉及重构）

| # | 项 | 精读结论 | 需你回答 |
|---|---|---|---|
| B3-1 | **审计族 15 → 8 顶层 + 3 references** | 5 组真重复、3 处门禁口径冲突 | 是否执行？建议先做"簇1 命中率链 5→2"试点 |
| B3-2 | `story-long-scan` + `story-short-scan` → `story-scan --length=` | scan 对相似度 0.845，差异纯平台参数 | 执行？ |
| B3-3 | local 族：抽 `_shared/local-runtime` + 合并 2 组 | 9 份重复脚手架（`platform.exe` 376KB×9 等） | 执行？（需动 10 个 skill 的文件结构） |
| B3-4 | **4 个 Notion 件（30.8KB 空转）** | 依赖的 Notion MCP **未配置**，从未跑通 | 合并为 1 个占位件？还是直接退役？还是去配 MCP？ |
| B3-5 | `taskflow-inbox-triage` → `taskflow/references/` | 父子示例关系，66 行重复率 >90%，自述"非生产" | 执行？ |
| B3-6 | `hook-analyzer-skill` + `report-generator-skill` | 共同上游 `video-breakdown-skill` 不存在 → 两件输入契约悬空 | 补建上游？还是两件一并退役？ |

### B4 大规模清理（需你定口径）

| # | 项 | 分类 | 需你回答 |
|---|---|---|---|
| B4-1 | CC 平台名剩余 **90 处** | ①路径映射表（`cross-platform-agent-sync` 4 处 `.claude\` 路径）→ **删整条**<br>②平台清单（`cross-platform-skill-sync`:3/14/15、`skill-drift-surgery` 8 处、`discover-agent-cli` 5 处等）→ 改枚举<br>③历史案例（`data-layer-consistency-fix:237`）→ **保留与否？** | ②按 `OC/WB/TR/CX` 改（已知）；①③需你确认 |
| B4-2 | 权威注册表同步 | `D:\global_memory\cross_platform_map.json` + `startup_per_framework.md` 仍是旧四端口径（含 CC） | 是否纳入本次统一？（属 `D:\global_memory` 仓库，需 PowerShell 通道 + 门禁） |

### B5 非本审计范围但需你处理

| # | 项 | 说明 |
|---|---|---|
| B5-1 | **工作区约 55 个他人未提交改动** | `cloudbase__skillhub/*`(40+)、`A-java-problem/*`(4) 等，非我造成。跑 `git -C D:\global_skills status --short` 确认归属后决定提交或丢弃 |
| B5-2 | `.rule_backup/` 248 个 .bak 堆积 | `A-memory-start/references/.rule_backup/`，同日同文件 8 份，违反自家 R198.7（>5 即轮转）→ 是否按规则轮转到 `global_memory_archive/_trash/backup-rotation/`？ |
| B5-3 | 灰区 30 个 skill 归属 | `github`/`gsap`/`obsidian-*`/`spike` 等，阶段0 判 LOW 灰区 → 是否纳入后续维护？ |

---

## 建议推进顺序

1. **先做 A 类**（5 项，我自主完成，无风险）
2. **B1 三项**（技术风险类，我可先出 dry-run 方案给你过目）
3. **B2 退役**（你给一句话即可执行）
4. **B3 合并**（建议逐个试点，不要一次全上）
5. **B4/B5**（需你定口径或亲自处理）

> 注：本清单与 `TODO.md` 重复时，以 `TODO.md`（跨会话续接入口）为准。
