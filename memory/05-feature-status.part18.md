# 05-feature-status 分卷 18 — 已完成：2026-09-24 第 21 轮 b 段（r20b 棘轮落地）

- **A-get-memory 条款落地闭环**：上一轮以「该文件被并行会话 staged 占用」为由不代改；本轮复测该文件已归还（`git -C D:/global_skills status --porcelain | grep -c A-get-memory` = 0），于是把「判据类工具交付三条硬判据」（① 夹具先写先看红 ② 变异/反例对照 ③ 挂执行路径，否则按 Step 2.7「下一会话不做任何事还生效吗」判未闭环）经 `rule_editor` 落进 Step 2.7 末尾，同步 frontmatter 4.29.0→**4.30.0** 与版本历史条目（受管根 `b8966da`，dry-run 3/3 命中、写后读回验证、镜像 `-Fix` 后 **mirror/noise/evolution/stub 四门禁首次全绿**）。
- **R19-2 转办项二次让号**：实测焚诀本轮已自行落地 `C30 = 路由静态表目标存活与覆盖空洞棘轮`（其 `205ed81` 同时占掉 C29），故本仓三项建议改号 **C31′/C32′/C33′**，并把「转办」从散文证据升级为**可接线指南**：`05-exec/r20b_fenjue_wiring/README.md`（判据逻辑 = 本仓已测的 `ratchet_gate.py` / `claim_truth_scan.py`，含 registry 注册行、truth_constants 口径、stub 登记要求与 C25 双源修法；不复制未经其运行时验证的代码）。
- **本仓自持注入面棘轮落地（把转办变成自己动手）**：`05-exec/ratchet_gate.py` 五项指标只降不升 + `--update` 拒绝抬基线；`05-exec/r20b_ratchet_fixtures.py` **21/21**（层 a 7 例纯函数 + 层 b 14 例真机，含「超棘轮必须拦 / 收紧必须允许 / 抬高必须拒绝 / 超顶按 --strict-cap 升级 / 指标算不得即失败」）。首跑即抓到两条真实信号：注入区并集 **87,878B 超 C25 硬顶 65,536B（+22,342）**（r18 报的 69,494B 系登记值滞后，实测口径更大）与 `catalog_grand_chars` 45,250（较 r18 +77，来自今日他端新增技能）。判定分两级（超棘轮/无基线阻断，超硬顶默认告警），理由与消警路径登记在 07 P0；命令已并入 `memory/AGENTS.md` 项目门禁命令第 4 条。
- **台账卫生**：把 4 条已带「✅ 收口注（销账/已补建/已收口）」却仍写 `- [ ]` 的 r7 旧项翻成 `- [x]`⇒ `trim-shell` 立刻迁出（07 主卷 47,089B → 45,470B）。这是 P-1 铁律「status/review 分数不校验主卷与分卷交叉一致」的那类欠账：分数 100% 而台账仍挂已闭环项。**注意** `handoff.py handoff .` 会保护索引壳不覆盖，故每轮注入的 AGENTS.md 体积不随 07 缩，进一步减重须人工精简 07 分卷目录行（不做删规则本体换绿的形态）。
